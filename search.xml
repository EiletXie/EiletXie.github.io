<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>184-部门最高工资</title>
    <url>/2019/08/01/184-%E9%83%A8%E9%97%A8%E6%9C%80%E9%AB%98%E5%B7%A5%E8%B5%84/</url>
    <content><![CDATA[<h1 id="184-部门工资最高的员工"><a href="#184-部门工资最高的员工" class="headerlink" title="184-部门工资最高的员工"></a><a href="https://leetcode-cn.com/problems/department-highest-salary/"><strong>184-部门工资最高的员工</strong></a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>Employee 表包含所有员工信息，每个员工有其对应的 Id, salary 和 department Id。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> +----+-------+--------+--------------+</span><br><span class="line"></span><br><span class="line">| Id | Name  | Salary | DepartmentId |</span><br><span class="line"></span><br><span class="line">+----+-------+--------+--------------+</span><br><span class="line"></span><br><span class="line">| 1  | Joe   | 70000  | 1            |</span><br><span class="line"></span><br><span class="line">| 2  | Henry | 80000  | 2            |</span><br><span class="line"></span><br><span class="line">| 3  | Sam   | 60000  | 2            |</span><br><span class="line"></span><br><span class="line">| 4  | Max   | 90000  | 1            |</span><br><span class="line"></span><br><span class="line">+----+-------+--------+--------------+</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>Department 表包含公司所有部门的信息。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+----+----------+</span><br><span class="line"></span><br><span class="line">| Id | Name     |</span><br><span class="line"></span><br><span class="line">+----+----------+</span><br><span class="line"></span><br><span class="line">| 1  | IT       |</span><br><span class="line"></span><br><span class="line">| 2  | Sales    |</span><br><span class="line"></span><br><span class="line">+----+----------+</span><br></pre></td></tr></table></figure>


<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a><strong>解题思路：</strong></h2><p>主要在于如何查出每个部门最高工资，然后匹配人</p>
<p><strong>考查点： GROUP BY  、 JOIN 、IN</strong></p>
<p>对 IN的用法，原来可以多个字段匹配</p>
<h2 id="代码："><a href="#代码：" class="headerlink" title="代码："></a><strong>代码：</strong></h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">	b.NAME <span class="keyword">AS</span> department,</span><br><span class="line">	a.NAME <span class="keyword">AS</span> employee,</span><br><span class="line">	a.salary </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">	employee a</span><br><span class="line">	<span class="keyword">JOIN</span> department b <span class="keyword">ON</span> a.departmentid <span class="operator">=</span> b.id </span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">	( a.salary, a.departmentid )</span><br><span class="line">	<span class="keyword">IN</span> </span><br><span class="line">	 ( <span class="keyword">SELECT</span> <span class="built_in">max</span>( salary ), departmentid </span><br><span class="line">		 <span class="keyword">FROM</span> employee c </span><br><span class="line">		 <span class="keyword">JOIN</span> department d <span class="keyword">ON</span> c.departmentid <span class="operator">=</span> d.id</span><br><span class="line">		 <span class="keyword">GROUP</span> <span class="keyword">BY</span> departmentid )</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title>130-被围绕的区域</title>
    <url>/2019/08/05/130-%E8%A2%AB%E5%9B%B4%E7%BB%95%E7%9A%84%E5%8C%BA%E5%9F%9F/</url>
    <content><![CDATA[<h1 id="130-被围绕的区域"><a href="#130-被围绕的区域" class="headerlink" title="130. 被围绕的区域"></a><a href="https://leetcode-cn.com/problems/surrounded-regions/">130. 被围绕的区域</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个二维的矩阵，包含 ‘X’ 和 ‘O’（字母 O）。</p>
<p>找到所有被 ‘X’ 围绕的区域，并将这些区域里所有的 ‘O’ 用 ‘X’ 填充。</p>
<p><strong>示例:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">X X X X</span><br><span class="line">X O O X</span><br><span class="line">X X O X</span><br><span class="line">X O X X</span><br></pre></td></tr></table></figure>

<p>运行你的函数后，矩阵变为：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">X X X X</span><br><span class="line">X X X X</span><br><span class="line">X X X X</span><br><span class="line">X O X X</span><br></pre></td></tr></table></figure>

<p><strong>解释:</strong></p>
<p>被围绕的区间不会存在于边界上，换句话说，任何边界上的 ‘O’ 都不会被填充为 ‘X’。 任何不在边界上，或不与边界上的 ‘O’ 相连的 ‘O’ 最终都会被填充为 ‘X’。如果两个元素在水平或垂直方向相邻，则称它们是“相连”的。</p>
<span id="more"></span>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>1、我们将边缘的O点记录下来，将这些O点同意进行深度优先遍历，</p>
<p>2、与边缘O点上下左右方向相关联的所有O坐标都记录下来，</p>
<p>3、最后将未被记录的O边覆盖成X</p>
<p>可以使用DFS的递归与非递归的做法。</p>
<p>​         涉及图论区间相连，判断区间数和是否连接的都可以用并查集来做，这里注意的地方是 并查集的内部一维数组设置为 给定数组size + 1，这个多出来的一作为虚拟头结点 dummyNode,它用来作为所有边缘O点的父节点，从而可以压缩路径，让最后的统计判断中查询与边缘O点相连接的O坐标是哪些变的容易起来。</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><h3 id="第一二种解法：DFS的递归与非递归"><a href="#第一二种解法：DFS的递归与非递归" class="headerlink" title="第一二种解法：DFS的递归与非递归"></a>第一二种解法：DFS的递归与非递归</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.Queue;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javafx.util.Pair;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SurrendArea130</span> &#123;</span><br><span class="line">   <span class="comment">// DFS非递归，用数组记录O点</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">solve</span><span class="params">(<span class="type">char</span>[][] board)</span> &#123;</span><br><span class="line">		<span class="comment">// 思路： 记录最边缘的O点，遍历O点 这种做法时空复杂度 m*n</span></span><br><span class="line">		<span class="keyword">if</span> (board == <span class="literal">null</span> || board.length &lt;= <span class="number">2</span>)</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> board[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">ylen</span> <span class="operator">=</span> board.length;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这种pair对象的写法，是我看到一些官方题解中使用过,但是总觉得没有 自己设置的private class Node这种用的舒服</span></span><br><span class="line">		List&lt;Pair&lt;Integer, Integer&gt;&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="comment">// 记录边缘的O点</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; xlen; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[<span class="number">0</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				list.add(<span class="keyword">new</span> <span class="title class_">Pair</span>&lt;Integer, Integer&gt;(<span class="number">0</span>, i));</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[ylen - <span class="number">1</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				list.add(<span class="keyword">new</span> <span class="title class_">Pair</span>&lt;Integer, Integer&gt;(ylen - <span class="number">1</span>, i));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; ylen - <span class="number">1</span>; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[i][<span class="number">0</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				list.add(<span class="keyword">new</span> <span class="title class_">Pair</span>&lt;Integer, Integer&gt;(i, <span class="number">0</span>));</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[i][xlen - <span class="number">1</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				list.add(<span class="keyword">new</span> <span class="title class_">Pair</span>&lt;Integer, Integer&gt;(i, xlen - <span class="number">1</span>));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (Pair&lt;Integer, Integer&gt; pair : list) &#123;</span><br><span class="line">			Queue&lt;Pair&lt;Integer, Integer&gt;&gt; q = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">			<span class="keyword">if</span> (board[pair.getKey()][pair.getValue()] != <span class="string">&#x27;Y&#x27;</span>) &#123;</span><br><span class="line">				q.add(pair);</span><br><span class="line">				<span class="keyword">while</span> (!q.isEmpty()) &#123;</span><br><span class="line">					<span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> q.peek().getKey();</span><br><span class="line">					<span class="type">int</span> <span class="variable">y</span> <span class="operator">=</span> q.peek().getValue();</span><br><span class="line">					q.remove();</span><br><span class="line">					board[x][y] = <span class="string">&#x27;Y&#x27;</span>;</span><br><span class="line"></span><br><span class="line">					<span class="keyword">if</span> (y + <span class="number">1</span> &lt; xlen &amp;&amp; board[x][y + <span class="number">1</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">						q.add(<span class="keyword">new</span> <span class="title class_">Pair</span>(x, y + <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">					<span class="keyword">if</span> (x + <span class="number">1</span> &lt; ylen &amp;&amp; board[x + <span class="number">1</span>][y] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">						q.add(<span class="keyword">new</span> <span class="title class_">Pair</span>(x + <span class="number">1</span>, y));</span><br><span class="line"></span><br><span class="line">					<span class="keyword">if</span> (y - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; board[x][y - <span class="number">1</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">						q.add(<span class="keyword">new</span> <span class="title class_">Pair</span>(x, y - <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">					<span class="keyword">if</span> (x - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; board[x - <span class="number">1</span>][y] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">						q.add(<span class="keyword">new</span> <span class="title class_">Pair</span>(x - <span class="number">1</span>, y));</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; xlen; j++) &#123;</span><br><span class="line">				<span class="comment">// 如果是Y证明是可与边缘O连接的O</span></span><br><span class="line">				<span class="keyword">if</span> (board[i][j] == <span class="string">&#x27;Y&#x27;</span>)</span><br><span class="line">					board[i][j] = <span class="string">&#x27;O&#x27;</span>;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (board[i][j] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">					board[i][j] = <span class="string">&#x27;X&#x27;</span>;</span><br><span class="line">				System.out.print(board[i][j]);</span><br><span class="line">			&#125;</span><br><span class="line">			System.out.println();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 第一种做法，超时了，第一是用到了pair对象存储消耗资源，第二种做法用DFS递归要快点</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">solve2</span><span class="params">(<span class="type">char</span>[][] board)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (board == <span class="literal">null</span> || board.length &lt;= <span class="number">2</span>)</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> board[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">ylen</span> <span class="operator">=</span> board.length;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这种在遍历的时候就深度优先时直接递归的好处在于将连着的边界O都一起处理了,所以速度较快，测试用例执行只要4ms</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; xlen; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[<span class="number">0</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, <span class="number">0</span>, i);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[ylen - <span class="number">1</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, ylen - <span class="number">1</span>, i);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; ylen - <span class="number">1</span>; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[i][<span class="number">0</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, i, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[i][xlen - <span class="number">1</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, i, xlen - <span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; xlen; j++) &#123;</span><br><span class="line">				<span class="comment">// 如果是Y证明是可与边缘O连接的O</span></span><br><span class="line">				<span class="keyword">if</span> (board[i][j] == <span class="string">&#x27;Y&#x27;</span>)</span><br><span class="line">					board[i][j] = <span class="string">&#x27;O&#x27;</span>;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (board[i][j] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">					board[i][j] = <span class="string">&#x27;X&#x27;</span>;</span><br><span class="line">				System.out.print(board[i][j]);</span><br><span class="line">			&#125;</span><br><span class="line">			System.out.println();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 深度递归</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">dfs</span><span class="params">(<span class="type">char</span>[][] board, <span class="type">int</span> i, <span class="type">int</span> j)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (!(i &lt; board.length &amp;&amp; i &gt;= <span class="number">0</span> &amp;&amp; j &gt;= <span class="number">0</span> &amp;&amp; j &lt; board[<span class="number">0</span>].length))</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">if</span>(board[i][j] == <span class="string">&#x27;X&#x27;</span> || board[i][j] == <span class="string">&#x27;Y&#x27;</span>)</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">        		</span><br><span class="line">		</span><br><span class="line"></span><br><span class="line">		board[i][j] = <span class="string">&#x27;Y&#x27;</span>;</span><br><span class="line">		dfs(board, i - <span class="number">1</span>, j);</span><br><span class="line">		dfs(board, i + <span class="number">1</span>, j);</span><br><span class="line">		dfs(board, i, j + <span class="number">1</span>);</span><br><span class="line">		dfs(board, i, j - <span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">char</span>[][] board = &#123;&#123;<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>&#125;,&#123;<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>&#125;,&#123;<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>&#125;,&#123;<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>,<span class="string">&#x27;O&#x27;</span>,<span class="string">&#x27;X&#x27;</span>&#125;&#125;;</span><br><span class="line">		<span class="keyword">new</span> <span class="title class_">SurrendArea130</span>().solve2(board);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第三种解法：并查集"><a href="#第三种解法：并查集" class="headerlink" title="第三种解法：并查集"></a>第三种解法：并查集</h3><blockquote>
<p>&#x2F;&#x2F; 第 3 种解法：<br>只要是图论的区块覆盖或者是 取区块数的基本都是可以用并查集来做的。那么这题用并查集怎么做呢？</p>
<ul>
<li>其实是可以做的，我们记录了边缘的O的坐标，也是检查它的上下左右连接的O点，</li>
<li>然后 union合并。合并的时候顺带赋值操作</li>
<li>注意：这里可以设置 并查集时 它的大小为 原数组大小 + 1,</li>
<li>这个 首节点 dummyNode 作为所有的边缘O的父节点,这样便于统一进行查找与边缘O相连接的节点</li>
<li>然后我们遍历数组判断是不是和dummyNode连接，这样数据结构比深度遍历要繁琐一点，相比直接数组形式的深度递归要慢一点</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">  </span><br><span class="line">		<span class="keyword">private</span> <span class="keyword">class</span> <span class="title class_">UnionFind</span> &#123;</span><br><span class="line">		<span class="keyword">public</span> <span class="type">int</span>[] parent;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="title function_">UnionFind</span><span class="params">(<span class="type">int</span> size)</span> &#123;</span><br><span class="line">			parent = <span class="keyword">new</span> <span class="title class_">int</span>[size];</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; size; i++) &#123;</span><br><span class="line">				parent[i] = i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">find</span><span class="params">(<span class="type">int</span> p)</span> &#123;</span><br><span class="line">			<span class="keyword">if</span> (p != parent[p])</span><br><span class="line">				p = find(parent[p]);</span><br><span class="line">			<span class="keyword">return</span> p;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这里q为父节点</span></span><br><span class="line">		<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">union</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> q)</span> &#123;</span><br><span class="line">			p = find(p);</span><br><span class="line">			q = find(q);</span><br><span class="line">			<span class="keyword">if</span> (p == q)</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			parent[p] = q;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isConnnected</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> q)</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> find(p) == find(q);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">solve</span><span class="params">(<span class="type">char</span>[][] board)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (board == <span class="literal">null</span> || board.length &lt;= <span class="number">2</span>)</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> board[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">ylen</span> <span class="operator">=</span> board.length;</span><br><span class="line"></span><br><span class="line">		<span class="type">UnionFind</span> <span class="variable">uf</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UnionFind</span>(xlen * ylen + <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这种在遍历的时候就深度优先时直接递归的好处在于将边界的一个O点处理的时候把连着的边界O都一起处理了,所以速度较快，测试用例执行只要4ms</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; xlen; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[<span class="number">0</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, <span class="number">0</span>, i, uf);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[ylen - <span class="number">1</span>][i] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, ylen - <span class="number">1</span>, i, uf);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; ylen - <span class="number">1</span>; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (board[i][<span class="number">0</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, i, <span class="number">0</span>, uf);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (board[i][xlen - <span class="number">1</span>] == <span class="string">&#x27;O&#x27;</span>)</span><br><span class="line">				dfs(board, i, xlen - <span class="number">1</span>, uf);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; xlen; j++) &#123;</span><br><span class="line">				<span class="comment">// 如果是Y证明是可与边缘O连接的O,首节点作为虚拟头结点</span></span><br><span class="line">				<span class="keyword">if</span> (uf.isConnnected(i * xlen + j + <span class="number">1</span>, <span class="number">0</span>))</span><br><span class="line">					board[i][j] = <span class="string">&#x27;O&#x27;</span>;</span><br><span class="line">				<span class="keyword">else</span></span><br><span class="line">					board[i][j] = <span class="string">&#x27;X&#x27;</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 深度递归</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">dfs</span><span class="params">(<span class="type">char</span>[][] board, <span class="type">int</span> i, <span class="type">int</span> j, UnionFind uf)</span> &#123;</span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> board[<span class="number">0</span>].length;</span><br><span class="line">		<span class="keyword">if</span> (!(i &lt; board.length &amp;&amp; i &gt;= <span class="number">0</span> &amp;&amp; j &gt;= <span class="number">0</span> &amp;&amp; j &lt; xlen))</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (board[i][j] == <span class="string">&#x27;X&#x27;</span> || uf.isConnnected(i * xlen + j + <span class="number">1</span>, <span class="number">0</span>))</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		uf.union(i * xlen + j + <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">		dfs(board, i - <span class="number">1</span>, j, uf);</span><br><span class="line">		dfs(board, i + <span class="number">1</span>, j, uf);</span><br><span class="line">		dfs(board, i, j + <span class="number">1</span>, uf);</span><br><span class="line">		dfs(board, i, j - <span class="number">1</span>, uf);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>深度优先搜索</tag>
        <tag>并查集</tag>
      </tags>
  </entry>
  <entry>
    <title>406-根据身高重建队列</title>
    <url>/2019/08/02/406-%E6%A0%B9%E6%8D%AE%E8%BA%AB%E9%AB%98%E9%87%8D%E5%BB%BA%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<h1 id="406-根据身高重建队列"><a href="#406-根据身高重建队列" class="headerlink" title="406. 根据身高重建队列"></a><a href="https://leetcode-cn.com/problems/queue-reconstruction-by-height/">406. 根据身高重建队列</a></h1><p>假设有打乱顺序的一群人站成一个队列。 每个人由一个整数对(h, k)表示，其中h是这个人的身高，k是排在这个人前面且身高大于或等于h的人数。 编写一个算法来重建这个队列。</p>
<p><strong>注意：</strong></p>
<p>总人数少于1100人。</p>
<p><strong>示例</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入:</span><br><span class="line">[[<span class="number">7</span>,<span class="number">0</span>], [<span class="number">4</span>,<span class="number">4</span>], [<span class="number">7</span>,<span class="number">1</span>], [<span class="number">5</span>,<span class="number">0</span>], [<span class="number">6</span>,<span class="number">1</span>], [<span class="number">5</span>,<span class="number">2</span>]]</span><br><span class="line">输出:</span><br><span class="line">[[<span class="number">5</span>,<span class="number">0</span>], [<span class="number">7</span>,<span class="number">0</span>], [<span class="number">5</span>,<span class="number">2</span>], [<span class="number">6</span>,<span class="number">1</span>], [<span class="number">4</span>,<span class="number">4</span>], [<span class="number">7</span>,<span class="number">1</span>]]</span><br></pre></td></tr></table></figure>
<span id="more"></span>

<h2 id="解题思路"><a href="#解题思路" class="headerlink" title="解题思路"></a>解题思路</h2><p>将该队列 按照身高降序，k升序排序</p>
<p>k升序排序是显而易见的，但是身高为什么要降序呢？</p>
<p>这样做就方便从左到右遍历插入排序了</p>
<p>因为只要按照索引插入，前面的身高肯定比当前索引的元素高</p>
<h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ReconstructQueue406</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="type">int</span>[][] reconstructQueue(<span class="type">int</span>[][] people) &#123;</span><br><span class="line">		<span class="keyword">if</span> (people.length == <span class="number">0</span> || people == <span class="literal">null</span>)</span><br><span class="line">			<span class="keyword">return</span> people;</span><br><span class="line">		<span class="comment">// 按照身高降序，k升序排序</span></span><br><span class="line">		Arrays.sort(people, <span class="keyword">new</span> <span class="title class_">Comparator</span>&lt;<span class="type">int</span>[]&gt;() &#123;</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compare</span><span class="params">(<span class="type">int</span>[] o1, <span class="type">int</span>[] o2)</span> &#123;</span><br><span class="line">				<span class="keyword">if</span> (o1[<span class="number">0</span>] == o2[<span class="number">0</span>])</span><br><span class="line">					<span class="keyword">return</span> o1[<span class="number">1</span>] - o2[<span class="number">1</span>];</span><br><span class="line">				<span class="keyword">return</span> o2[<span class="number">0</span>] - o1[<span class="number">0</span>];</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;);</span><br><span class="line"></span><br><span class="line">		ArrayList&lt;<span class="type">int</span>[]&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> <span class="number">0</span>; s &lt; people.length; s++) &#123;</span><br><span class="line">			list.add(people[s][<span class="number">1</span>], people[s]);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> list.toArray(<span class="keyword">new</span> <span class="title class_">int</span>[people.length][]);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="type">int</span>[][] swapPeople(<span class="type">int</span>[][] people, <span class="type">int</span> i, <span class="type">int</span> j) &#123;</span><br><span class="line">		<span class="type">int</span> <span class="variable">p0</span> <span class="operator">=</span> people[i][<span class="number">0</span>];</span><br><span class="line">		people[i][<span class="number">0</span>] = people[j][<span class="number">0</span>];</span><br><span class="line">		people[j][<span class="number">0</span>] = p0;</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span> <span class="variable">p1</span> <span class="operator">=</span> people[i][<span class="number">1</span>];</span><br><span class="line">		people[i][<span class="number">1</span>] = people[j][<span class="number">1</span>];</span><br><span class="line">		people[j][<span class="number">1</span>] = p1;</span><br><span class="line">		<span class="keyword">return</span> people;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">int</span>[][] people = &#123; &#123; <span class="number">7</span>, <span class="number">0</span> &#125;, &#123; <span class="number">4</span>, <span class="number">4</span> &#125;, &#123; <span class="number">7</span>, <span class="number">1</span> &#125;, &#123; <span class="number">5</span>, <span class="number">0</span> &#125;, &#123; <span class="number">6</span>, <span class="number">1</span> &#125;, &#123; <span class="number">5</span>, <span class="number">2</span> &#125; &#125;;</span><br><span class="line">		<span class="type">ReconstructQueue406</span> <span class="variable">rq</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReconstructQueue406</span>();</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span>[][] sort = rq.reconstructQueue(people);</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">s</span> <span class="operator">=</span> <span class="number">0</span>; s &lt; sort.length; s++) &#123;</span><br><span class="line">			System.out.print(<span class="string">&quot;[&quot;</span> + sort[s][<span class="number">0</span>] + <span class="string">&quot; &quot;</span> + sort[s][<span class="number">1</span>] + <span class="string">&quot;] ,&quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>贪心算法</tag>
      </tags>
  </entry>
  <entry>
    <title>2019年度总结</title>
    <url>/2020/01/08/2019%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p> &nbsp;&nbsp;毕业到如今，已经过去将近一年半的时间了，刚开始的半年懵懵懂懂，后续一年自己有了目标和想法，每年做一次年度总结来回顾这一年来是否达到自己的期望。</p>
<img src="/2020/01/08/2019%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/header.jpg" class="" title="header">

<span id="more"></span>
<br/>



<h2 id="聊聊19年自己的变化"><a href="#聊聊19年自己的变化" class="headerlink" title="聊聊19年自己的变化"></a><strong>聊聊19年自己的变化</strong></h2><p> &nbsp;&nbsp;2019年对我而言，并没有发生特别大的变化，依旧是每天上班8小时，下班学习3小时，娱乐运动2小时的生活，但说没有什么改变那是假的，对个人最大变化的是决定自己要进互联网大厂开始的。</p>
<p> &nbsp;&nbsp;18年的时候，虽然也是在学习，但不知道自己要走哪个方向，大都是时候除了看编程思想和一些不错的技术博客，还学了点微信小程序。而19年有了较大的不同，目标发生了变化，19年初我对标招聘网站大厂的高级Java开发工程师的招聘要求，发现一条都符合不了，发现自己对于知识点从未有过深究，路漫漫其修远兮，我开始制定年度个人计划，针对知识点开始认真学习起来。      </p>
<p> &nbsp;&nbsp;不管是什么互联网公司，对 多线程，IO，JVM,Spring，分布式组件，缓存redis,MQ中间件，数据库 Mysql，Linux，数据结构算法，设计模式都有一定的要求。</p>
<p>我就先说说自己这一年对于这些知识点的学习把。</p>
<p> &nbsp;&nbsp;对于Linux的学习，我主要是看视频总结笔记，然后开始操作生产上的linux环境，同时买了自己的个人服务器开始瞎搞，个人服务器倒腾了中间件，redis，中了几次病毒，经常端口占用啥的，整崩了几次之后就熟练了起来。。。。 当然对于文本的一些指令操作还是不太熟悉的，对于底层的设计原理也是一知半解，这一块仍然需要补充，算是熟悉Linux把。</p>
<p> &nbsp;&nbsp;对于IO这个东西，真是又爱又恨，这玩意扩展 NIO，框架 Netty，并且传统公司这块工作中很少涉及，我学了基本很快就忘记，只能说马马虎虎，百度一下能自己写把，掌握程度只能 说了解到熟悉之间。</p>
<img src="/2020/01/08/2019%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/t1.jpg" class="" title="t1">



<p> &nbsp;&nbsp;Spring家族就得认真对待了，起初1年前是只知道简单的使用，对于底层原理一概不知。  后来就买了一本 Spring in action4 来学习，对于 为了解耦的 依赖注入 DI ，将对象使用权翻转的依赖倒置 IOC。 通过将各类bean注册到容器中供业务使用，为了记录容器中的bean的行为，如何优雅的进行切入式编程而进行的AOP，使用代理模式，基于本地的接口代理和Cglib的类对象代理等等其中的细节多而全。 尝试着分析过IOC的源码，分析完后一个字，晕，个人认为是掌握了Spring，但说精通的话，远远谈不上，啥时候能自己写一个简单的spring了便算是精通了。</p>
<p> &nbsp;&nbsp;随后 针对 springcloud组件，dubbo，mysql进行了一系列的学习，写了一个简单的订单和商品服务调用，算是对分布式这块有了一个整体的了解并会基本的开发和使用了。个人觉得分布式，难的是针对业务进行拆分，这个拆分的度，以及如何保证服务间调度的可用性是比较麻烦的事情，其他的都是术的级别，百度一下，倒腾倒腾都没什么问题，而这个接口拆分的度我暂时无法掌握，没有这个机会，对了，忘记说设计模式了，之前我年初的时候简单的学了几种设计模式，但基本过目便忘，即使记得一点也不会应用到实际的业务中来，真正让我了解的方式，是我再学习Spring，学习dubbo，学习 Hystrix等组件的时候，通过理解它们如何设计的，才渐渐明白使用了什么设计模式， 为什么使用这种设计模式，为什么这种设计模式就最好，象hystrix中的命令模式，观察者模式，spring中的代理模式等。 只有你真正带入的去理解如何解决某个业务场景的问题，你才能够明白某种设计模式使用的意义。</p>
<p>  &nbsp;&nbsp;所以我觉得设计模式这个东西，不能操之过急，慢慢学其他的好的轮子，你总会接触并理解一些好的设计模式。</p>
<p>  &nbsp;&nbsp;对于数据结构与算法的学习是让我感受到快乐的，这一年让我感受到快乐的时候可算不上很多，虽然我有时候也会自娱自乐，yy的傻笑，例如我现在写总结写着写着莫名其妙的笑了，这也是一种能力，哈哈，我其实很喜欢我这一点，人生太苦，时不时的傻乐呵一下，挺好的。回归主题， 算法这个东西，是持之以恒，你不能马上学得会，你也不会马上忘掉。 我的学习之路是  起初先 看了一些简单的博客，然后去leetcode上刷题，刚开始很艰难，一天刷个一题都懵懵懂懂的，后面在慕课网上学了波波老师的 数据结构视频课程，哎，别说，对常用的数据结构是理解了不少，然后接着去刷题，刚开始就拿着热题100道刷，那叫一个难呦，并且想给自己一巴掌的是，我竟然没有按照一种结构或一种算法的刷，导致我的进步不大，后面喜欢一次刷一种类型的题型，然后自己总结一下，尽量多用几种不同的写法来解答。这样感觉自己每过几天对一种题型就有了一定的理解，前前后后刷了200多道就不太怕这方面的招聘条件了，有趣的事，有一段时间我脖子很不舒服，干啥都没心思，偏偏可以刷题，每刷完一道感觉很快乐， 这么看来我内心还是很喜欢数据结构与算法这一块的。</p>
<p> &nbsp;&nbsp;多线程这个东西，建议大家早点学，它对于你理解框架，理解Java相关源码的写法都有一定的帮助，我通过视频、技术博客、书的方式对常用的多线程类，和多线程的写法有一定的了解，并且思考了一下对自己负责业务系统做了简单改造，其实就是用了几个Future异步执行来提升速度，传统公司的项目还是很少需要用到锁这块的内容的，大部分情况注意事务一致性就行了，20年初的目标就是将并发核心类的源码都解析一遍写成博客。</p>
<p> &nbsp;&nbsp;说到博客，自己学习知识点的时候，会了解产生的原因和背后的设计，起初以为自己了解了，后面过个半个月回来问自己会了没有的时候，发现自己说不出来，这才意识到眼高手低的严重性，回看我CSDN写的，大多数是杂乱无章的，便自己用hexo+github搭建了一个个人博客，遇到一些不太懂的，就逼自己写篇博客，放心，写着写着你就懂了，写的太烂太随意你也不好意思放到上面去。</p>
<h3 id="19年表现不好的地方。"><a href="#19年表现不好的地方。" class="headerlink" title="19年表现不好的地方。"></a><strong>19年表现不好的地方。</strong></h3><p> &nbsp;&nbsp;看一看，我好像这一年内学了好多东西，但实际上我对今年的我还是算不上满意的，我按照年初给自己定的计划，我早出去像个大佬一样的面试个好几回了，但是我现在还是像个笨坨坨一样的，畏畏缩缩打算明年再出去面试，这是没底气的表现，看来自己还是有点心虚的。19年，对每日，每周，每月我都会按照时间的推移更新 这段时间要完成的目标。但是我完成的不咋地，大部分情况下都没有完成过，一是因为自己有时候给自己定的过高的要求。二是 自己的欲望还没有强盛到让我自己熬夜学习。三是因为焦虑，对，对比许多优秀的同龄人我自己还是太弱了，对了还有抽烟，我的观点是抽一点没事，抽多了不好，但是下半年的几个月我很是焦虑，戒了半年的烟便抽了起来，并且量渐渐变大，所以还是要变得更强，另外是要管控自己。</p>
<p>   还有一个大毛病，那就是我19年胖了16斤，从一个小帅变成了一个小胖子，这个和老东家的伙食和自己这张嘴以及我女友好吃都有很大的联系。我其实是不喜欢吃零食的，但是我女友经常想吃个啥，买了不吃或者只吃一点，我秉着不浪费的原则就吃了，用她的话说就是养了一头好猪。。。另外就是运动了，其实我还是一周大概会运动个2次的，但是由于我坐姿不端正和长时间对着电脑的原因，我脖子和肩膀在下半年经常不舒服，其实是有解决的办法的，那就是每周跑个3次步，但是下班就有点小累了，一想到自己要一个人跑，跑完还要马上洗澡去学习（每天3小时呀），还没时间快乐的玩一把游戏就经常选择了放弃。 这一点很不好，因为我如果坚持每天运动1小时，我全天的工作学习效率大概能提升 30%，甚至更高，这一点在新的一年我已经想到解决办法了，那就是找一个运动达人的同事，和他打好关系，让他带我运动，两个人就来劲了，不是吗？想想就有点兴奋呢。</p>
<img src="/2020/01/08/2019%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/t2.gif" class="" title="t2">

<h3 id="19年表现不错的地方"><a href="#19年表现不错的地方" class="headerlink" title="19年表现不错的地方"></a><strong>19年表现不错的地方</strong></h3><p> &nbsp;&nbsp;好的，怼了自己这么久给自己说两句好话，自己的自律能力好像越来越强了，并且随着我的持续学习，我变得越来越自信，越来越敢想一些事情，大学的时候喜欢看玄幻小说，很浪费时间并且消磨精力，这一年我不允许自己看，开了个微信读书会员，基本上看了看历史书，明朝那些事儿，万历十五年，红楼梦都大概的看来看，还看了 余华的活着，许三观卖血记，黄小波的一只特立独行的猪，以及针对我自己不足的地方看的数，情商方面，职场规划的，以及大名鼎鼎的一个聪明的投资者，都大概的看了看，你要说我明白了啥大道理把，好像没有，但是我年末焦虑的时候回去看玄幻小说就感觉没以前那个味道了，半小时都看不进，觉得剧情太不合理了，就不看了，所以我说说看了这些书有什么用，我觉得的应该是自己这个笨脑袋会想一想问题，做事的时候稍微会考虑一下有什么意义了，所以还是要多看书，看好书哈哈。</p>
<h2 id="2020年对自己的期望"><a href="#2020年对自己的期望" class="headerlink" title="2020年对自己的期望"></a><strong>2020年对自己的期望</strong></h2><p>​          与其说是期望，不如说是要求，要求自己要达成的目标，这些目标大部分是对19年自己不足的地方的补充。</p>
<h3 id="3月份之前的期望"><a href="#3月份之前的期望" class="headerlink" title="3月份之前的期望"></a><strong>3月份之前的期望</strong></h3><ul>
<li>完成 JUC核心类的解析，上传到个人博客。</li>
<li>将周志明的 深入理解虚拟机 再仔细好好的看一遍</li>
<li>照着github上的商城系统自己倒腾一个出来，造个轮子</li>
<li>准备好简历，面试个几次积累经验</li>
</ul>
<h3 id="整年的期望"><a href="#整年的期望" class="headerlink" title="整年的期望"></a><strong>整年的期望</strong></h3><ul>
<li>打出点名气来，多和大佬聊聊，比划比划，不要怕丢人，丢着丢着你就是大佬了。</li>
<li>自己选一个方向，造一个轮子出来，连一个轮子都拿不出手，丢人。。。</li>
<li>2020年我想整整Linux和Mysql的底层设计，我对他们的模型和通信还是挺感兴趣的</li>
<li>2020年我想整整网络协议这一块的东西</li>
<li>2020年我一定进大厂看看他们的微服务是怎么根据业务划分，他们是怎么实现高可用的，纸上得来终觉浅呀。</li>
<li>2020年我要瘦到135斤，恢复我帅气的模样，话不多说，今天晚上就跑起来</li>
<li>2020年希望家人安康，希望自己能够对自己狠一点。</li>
</ul>
<img src="/2020/01/08/2019%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/t3.jpg" class="" title="t3">]]></content>
      <tags>
        <tag>年度总结</tag>
      </tags>
  </entry>
  <entry>
    <title>200-岛屿数量</title>
    <url>/2019/08/05/200-%E5%B2%9B%E5%B1%BF%E6%95%B0%E9%87%8F/</url>
    <content><![CDATA[<h1 id="200-岛屿数量"><a href="#200-岛屿数量" class="headerlink" title="200. 岛屿数量"></a><a href="https://leetcode-cn.com/problems/number-of-islands/">200. 岛屿数量</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个由 ‘1’（陆地）和 ‘0’（水）组成的的二维网格，计算岛屿的数量。一个岛被水包围，并且它是通过水平方向或垂直方向上相邻的陆地连接而成的。你可以假设网格的四个边均被水包围。</p>
<p><strong>示例 1:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入:</span><br><span class="line"><span class="number">11110</span></span><br><span class="line"><span class="number">11010</span></span><br><span class="line"><span class="number">11000</span></span><br><span class="line"><span class="number">00000</span></span><br><span class="line"></span><br><span class="line">输出: <span class="number">1</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p><strong>示例 2:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入:</span><br><span class="line"><span class="number">11000</span></span><br><span class="line"><span class="number">11000</span></span><br><span class="line"><span class="number">00100</span></span><br><span class="line"><span class="number">00011</span></span><br><span class="line"></span><br><span class="line">输出: <span class="number">3</span></span><br></pre></td></tr></table></figure>



<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>该题与695 题取最大的连通面积差不多，只是这个求的是岛屿个数</p>
<p>我们采用广度遍历搜索的思想，还是将二维数组进行循环遍历</p>
<p>本题考点在于 遍历数组时如何深度遍历，也就是如何将所有的指定规则相连的值统计下来。</p>
<p>我们先将 二维数组 grid 进行遍历</p>
<p>如果 gird[ i ] [ j ] 的值为0，continue</p>
<p>如果 gird[ i ] [ j ] 的值为‘1’，将其加入队列中，检测其上下左右方向是否存在1，存在则添加进队列。</p>
<p>注意：遍历连接岛屿时，应将其值设置为‘0’，防止多次遍历查询</p>
<p>第二种解法： 将队列换成递归，在递归中将值设为‘0’，递归完让岛屿数量加+1，也是可以的，终止条件就是 值的坐标大于 x,y长度的边界或者值为‘0’</p>
<p>第三种解法：并查集，这种第一时间是没有想到的，我们遍历二维数组，将值为 ‘1’的作为父节点，查询连接的子节点，将其值设置为父节点的值，注意压缩路径，减少检索时间。</p>
<p>3种解法的时空复杂度 都是 O(m*n) 级别;</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><h3 id="第一种：队列、广度优先搜索"><a href="#第一种：队列、广度优先搜索" class="headerlink" title="第一种：队列、广度优先搜索"></a>第一种：队列、广度优先搜索</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">		<span class="type">int</span> x;</span><br><span class="line">		<span class="type">int</span> y;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="title function_">Node</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> &#123;</span><br><span class="line">			<span class="built_in">this</span>.x = x;</span><br><span class="line">			<span class="built_in">this</span>.y = y;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">numIslands</span><span class="params">(<span class="type">char</span>[][] grid)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (grid == <span class="literal">null</span> || grid.length == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">		<span class="type">char</span>[][] hasVisit = grid;</span><br><span class="line">		<span class="type">int</span> <span class="variable">wlen</span> <span class="operator">=</span> grid[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">hlen</span> <span class="operator">=</span> grid.length;</span><br><span class="line">		Queue&lt;Node&gt; q = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">		<span class="type">int</span> <span class="variable">arealen</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; hlen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; wlen; j++) &#123;</span><br><span class="line">				<span class="keyword">if</span> (hasVisit[i][j] == <span class="string">&#x27;1&#x27;</span>) &#123;</span><br><span class="line">					q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(i, j));</span><br><span class="line">					<span class="keyword">while</span> (!q.isEmpty()) &#123;</span><br><span class="line">						<span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> q.peek().x;</span><br><span class="line">						<span class="type">int</span> <span class="variable">y</span> <span class="operator">=</span> q.peek().y;</span><br><span class="line">						q.remove();</span><br><span class="line">						<span class="keyword">if</span> (hasVisit[x][y] == <span class="string">&#x27;1&#x27;</span>) &#123;</span><br><span class="line">							hasVisit[x][y] = <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">							<span class="comment">// 深度遍历的规则 上下左右都要考虑到</span></span><br><span class="line">							<span class="keyword">if</span> (y + <span class="number">1</span> &lt; wlen &amp;&amp; hasVisit[x][y + <span class="number">1</span>] == <span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">								q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x, y + <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">							<span class="keyword">if</span> (x + <span class="number">1</span> &lt; hlen &amp;&amp; hasVisit[x + <span class="number">1</span>][y] == <span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">								q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x + <span class="number">1</span>, y));</span><br><span class="line"></span><br><span class="line">							<span class="keyword">if</span> (y - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; hasVisit[x][y - <span class="number">1</span>] == <span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">								q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x, y - <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">							<span class="keyword">if</span> (x - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; hasVisit[x - <span class="number">1</span>][y] == <span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">								q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x - <span class="number">1</span>, y));</span><br><span class="line"></span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">					<span class="comment">// 已将一个节点的所有连接遍历完</span></span><br><span class="line">					arealen++;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> arealen;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第二种：-递归、深度优先搜索"><a href="#第二种：-递归、深度优先搜索" class="headerlink" title="第二种： 递归、深度优先搜索"></a>第二种： 递归、深度优先搜索</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">  <span class="keyword">void</span> <span class="title function_">dfs</span><span class="params">(<span class="type">char</span>[][] grid, <span class="type">int</span> r, <span class="type">int</span> c)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">nr</span> <span class="operator">=</span> grid.length;</span><br><span class="line">    <span class="type">int</span> <span class="variable">nc</span> <span class="operator">=</span> grid[<span class="number">0</span>].length;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (r &lt; <span class="number">0</span> || c &lt; <span class="number">0</span> || r &gt;= nr || c &gt;= nc || grid[r][c] == <span class="string">&#x27;0&#x27;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">grid[r][c] = <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">dfs(grid, r - <span class="number">1</span>, c);</span><br><span class="line">dfs(grid, r + <span class="number">1</span>, c);</span><br><span class="line">dfs(grid, r, c - <span class="number">1</span>);</span><br><span class="line">dfs(grid, r, c + <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">numIslands</span><span class="params">(<span class="type">char</span>[][] grid)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (grid == <span class="literal">null</span> || grid.length == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="variable">nr</span> <span class="operator">=</span> grid.length;</span><br><span class="line"><span class="type">int</span> <span class="variable">nc</span> <span class="operator">=</span> grid[<span class="number">0</span>].length;</span><br><span class="line"><span class="type">int</span> <span class="variable">num_islands</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">r</span> <span class="operator">=</span> <span class="number">0</span>; r &lt; nr; ++r) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> <span class="number">0</span>; c &lt; nc; ++c) &#123;</span><br><span class="line">    <span class="keyword">if</span> (grid[r][c] == <span class="string">&#x27;1&#x27;</span>) &#123;</span><br><span class="line">      ++num_islands;</span><br><span class="line">      dfs(grid, r, c);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> num_islands;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第三种解法：-并查集"><a href="#第三种解法：-并查集" class="headerlink" title="第三种解法： 并查集"></a>第三种解法： 并查集</h3><p>并查集做法其实与深度遍历差不多，我们只是把并查集的这种数据结构替换了原来的以数组的结构，以及统计时的不一样罢了，大家可以尝试一下。我这里也有相关并查集的介绍和一些并查集的题目。</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>深度优先搜索</tag>
        <tag>并查集</tag>
        <tag>广度优先搜索</tag>
      </tags>
  </entry>
  <entry>
    <title>241-为运算表达式设计优先级</title>
    <url>/2019/08/20/241-%E4%B8%BA%E8%BF%90%E7%AE%97%E8%A1%A8%E8%BE%BE%E5%BC%8F%E8%AE%BE%E8%AE%A1%E4%BC%98%E5%85%88%E7%BA%A7/</url>
    <content><![CDATA[<h1 id="241-为运算表达式设计优先级"><a href="#241-为运算表达式设计优先级" class="headerlink" title="241. 为运算表达式设计优先级"></a><a href="https://leetcode-cn.com/problems/different-ways-to-add-parentheses/">241. 为运算表达式设计优先级</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个含有数字和运算符的字符串，为表达式添加括号，改变其运算优先级以求出不同的结果。你需要给出所有可能的组合的结果。有效的运算符号包含 +, - 以及 * 。</p>
<p><strong>示例 1:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: <span class="string">&quot;2-1-1&quot;</span></span><br><span class="line">输出: [<span class="number">0</span>, <span class="number">2</span>]</span><br><span class="line">解释: </span><br><span class="line">((<span class="number">2</span><span class="number">-1</span>)<span class="number">-1</span>) = <span class="number">0</span> </span><br><span class="line">(<span class="number">2</span>-(<span class="number">1</span><span class="number">-1</span>)) = <span class="number">2</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p><strong>示例 2:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: <span class="string">&quot;2*3-4*5&quot;</span></span><br><span class="line">输出: [<span class="number">-34</span>, <span class="number">-14</span>, <span class="number">-10</span>, <span class="number">-10</span>, <span class="number">10</span>]</span><br><span class="line">解释: </span><br><span class="line">(<span class="number">2</span>*(<span class="number">3</span>-(<span class="number">4</span>*<span class="number">5</span>))) = <span class="number">-34</span> </span><br><span class="line">((<span class="number">2</span>*<span class="number">3</span>)-(<span class="number">4</span>*<span class="number">5</span>)) = <span class="number">-14</span> </span><br><span class="line">((<span class="number">2</span>*(<span class="number">3</span><span class="number">-4</span>))*<span class="number">5</span>) = <span class="number">-10</span> </span><br><span class="line">(<span class="number">2</span>*((<span class="number">3</span><span class="number">-4</span>)*<span class="number">5</span>)) = <span class="number">-10</span> </span><br><span class="line">(((<span class="number">2</span>*<span class="number">3</span>)<span class="number">-4</span>)*<span class="number">5</span>) = <span class="number">10</span></span><br></pre></td></tr></table></figure>



<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>该题中 运算符号是切分 左右运算结果再进行最后一次结合。我们可以根据符号集合进行情况判断，由于我们需要 左右两边的各种情况，那么肯定是要递归的，同时我们需要左右两边的结果返回。也就是 最后的结果是 左右两侧的结果，两侧的结果又当做一个整体进行左右切分，直到左右无法拆分位置。</p>
<p>那么这就是分治算法来做这个题目了。</p>
<p>这里有两种做法，第一种是直接遍历字符串，遇到运算符号进行切分。</p>
<p>第二种是 用split将字符串，分为数据集和运算符集合。然后通过start,end下标控制两个集合大小作为切分。</p>
<p>个人感觉第一种逻辑更加的直观，就只写第一种了。</p>
<h2 id="个人感悟："><a href="#个人感悟：" class="headerlink" title="个人感悟："></a>个人感悟：</h2><p>分治算法是需要在现有的数据基础进行切分计算，同时要求返回各切分的数据集进行最后结合处理，之前递归搜索的题目做多了，老是想着传入一个数据集，在方法中封装处理，这种是不对的，这种思想就是深入递归了，是不需要返回切分子数据。</p>
<p>总而言之：分治算法就是 一个大问题切分成几个小问题，让后将小问题当做整体不断切分，直到无法切分后，返回数据，小问题的解组合成大问题的解，从而解决最初的大问题。</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DiffWaysToCompute241</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, List&lt;Integer&gt;&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">	 </span><br><span class="line">    <span class="keyword">public</span> List&lt;Integer&gt; <span class="title function_">diffWaysToCompute</span><span class="params">(String input)</span> &#123;</span><br><span class="line">    	<span class="keyword">return</span> divideSlove(input);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">private</span> List&lt;Integer&gt; <span class="title function_">divideSlove</span><span class="params">(String input)</span> &#123;</span><br><span class="line">		<span class="comment">// 避免重复遍历的判断</span></span><br><span class="line">		<span class="keyword">if</span>(map.containsKey(input))</span><br><span class="line">			<span class="keyword">return</span> map.get(input);</span><br><span class="line">		</span><br><span class="line">		List&lt;Integer&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="keyword">if</span>(!input.contains(<span class="string">&quot;+&quot;</span>) &amp;&amp; !input.contains(<span class="string">&quot;-&quot;</span>) &amp;&amp; !input.contains(<span class="string">&quot;*&quot;</span>))&#123;</span><br><span class="line">		    list.add(Integer.valueOf(input));</span><br><span class="line">			<span class="keyword">return</span> list;</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; input.length(); i++) &#123;</span><br><span class="line">			<span class="type">char</span> <span class="variable">c</span> <span class="operator">=</span> input.charAt(i);</span><br><span class="line">			<span class="keyword">if</span>(c == <span class="string">&#x27;+&#x27;</span> || c == <span class="string">&#x27;-&#x27;</span> || c == <span class="string">&#x27;*&#x27;</span>)&#123;</span><br><span class="line">				<span class="keyword">for</span> (Integer left : divideSlove(input.substring(<span class="number">0</span>,i))) &#123;</span><br><span class="line">					<span class="keyword">for</span> (Integer right : divideSlove(input.substring(i+<span class="number">1</span>,input.length()))) &#123;</span><br><span class="line">						<span class="keyword">if</span>(c == <span class="string">&#x27;+&#x27;</span>)</span><br><span class="line">							list.add(left + right);</span><br><span class="line">						<span class="keyword">else</span> <span class="keyword">if</span>(c == <span class="string">&#x27;-&#x27;</span>)</span><br><span class="line">							list.add(left - right);</span><br><span class="line">						<span class="keyword">else</span></span><br><span class="line">							list.add(left * right);</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		map.put(input, list);</span><br><span class="line">		<span class="keyword">return</span> list;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>分治算法</tag>
      </tags>
  </entry>
  <entry>
    <title>279-完全平方数</title>
    <url>/2019/08/02/279-%E5%AE%8C%E5%85%A8%E5%B9%B3%E6%96%B9%E6%95%B0/</url>
    <content><![CDATA[<h1 id="279-完全平方数"><a href="#279-完全平方数" class="headerlink" title="279. 完全平方数"></a><a href="https://leetcode-cn.com/problems/perfect-squares/">279. 完全平方数</a></h1><p>给定正整数 n，找到若干个完全平方数（比如 1, 4, 9, 16, …）使得它们的和等于 n。你需要让组成和的完全平方数的个数最少。</p>
<p><strong>示例 1:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: n = <span class="number">12</span></span><br><span class="line">输出: <span class="number">3</span> </span><br><span class="line">解释: <span class="number">12</span> = <span class="number">4</span> + <span class="number">4</span> + <span class="number">4.</span></span><br></pre></td></tr></table></figure>

<p><strong>示例 2:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: n = <span class="number">13</span></span><br><span class="line">输出: <span class="number">2</span></span><br><span class="line">解释: <span class="number">13</span> = <span class="number">4</span> + <span class="number">9.</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>

<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>当每一次都可以判断出多种情况，有多次的时候就适合用DFS-广度优先遍历<br>我们将它第一个平方数可能出现的情况做分析 只要 i * i &lt; n 就行<br>再在此基础上进行二次可能出现的平方数分析<br> 注意：为了节省遍历的时间，曾经 n - 以前出现的平方数 这个值出现过，则在此出现这样的数时直接忽略。</p>
<h2 id="举个栗子："><a href="#举个栗子：" class="headerlink" title="举个栗子："></a>举个栗子：</h2><p>​      </p>
<p>​          <img src="/2019/08/02/279-%E5%AE%8C%E5%85%A8%E5%B9%B3%E6%96%B9%E6%95%B0/b1.jpg" class="" title="计算流程图"></p>
<h2 id="代码："><a href="#代码：" class="headerlink" title="代码："></a>代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.Queue;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NumSquares</span> &#123;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">		<span class="type">int</span> val;</span><br><span class="line">		<span class="type">int</span> step;</span><br><span class="line"></span><br><span class="line">```</span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">Node</span><span class="params">(<span class="type">int</span> val, <span class="type">int</span> step)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.val = val;</span><br><span class="line">		<span class="built_in">this</span>.step = step;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">numSquares</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">	Queue&lt;Node&gt; queue = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">	queue.add(<span class="keyword">new</span> <span class="title class_">Node</span>(n, <span class="number">1</span>));</span><br><span class="line">	<span class="type">boolean</span> record[] = <span class="keyword">new</span> <span class="title class_">boolean</span>[n];</span><br><span class="line">	<span class="keyword">while</span> (!queue.isEmpty()) &#123;</span><br><span class="line">		<span class="type">int</span> <span class="variable">val</span> <span class="operator">=</span> queue.peek().val;</span><br><span class="line">		<span class="type">int</span> <span class="variable">step</span> <span class="operator">=</span> queue.peek().step;</span><br><span class="line">		queue.remove();</span><br><span class="line">		<span class="comment">// 每一层的广度遍历</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>;; i++) &#123;</span><br><span class="line">			<span class="type">int</span> <span class="variable">nextVal</span> <span class="operator">=</span> val - i * i;</span><br><span class="line">			<span class="comment">// 说明已到最大平方数</span></span><br><span class="line">			<span class="keyword">if</span> (nextVal &lt; <span class="number">0</span>)</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 由于是广度遍历，所以当遍历到0时，肯定是最短路径</span></span><br><span class="line">			<span class="keyword">if</span>(nextVal == <span class="number">0</span>)</span><br><span class="line">				<span class="keyword">return</span> step;</span><br><span class="line">			</span><br><span class="line">			<span class="comment">// 当再次出现时没有必要加入，因为在该节点的路径长度肯定不小于第一次出现的长</span></span><br><span class="line">			<span class="keyword">if</span>(!record[nextVal])&#123;</span><br><span class="line">			   queue.add(<span class="keyword">new</span> <span class="title class_">Node</span>(nextVal,step + <span class="number">1</span>));</span><br><span class="line">			   record[nextVal] = <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">	System.out.println(<span class="keyword">new</span> <span class="title class_">NumSquares</span>().numSquares(<span class="number">13</span>));</span><br><span class="line">&#125;</span><br><span class="line">```</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>广度优先搜索</tag>
      </tags>
  </entry>
  <entry>
    <title>435. 无重叠区间</title>
    <url>/2019/08/02/435-%E6%97%A0%E9%87%8D%E5%8F%A0%E5%8C%BA%E9%97%B4/</url>
    <content><![CDATA[<h1 id="435-无重叠区间"><a href="#435-无重叠区间" class="headerlink" title="435. 无重叠区间"></a><a href="https://leetcode-cn.com/problems/non-overlapping-intervals/">435. 无重叠区间</a></h1><p>给定一个区间的集合，找到需要移除区间的最小数量，使剩余区间互不重叠。</p>
<p><strong>注意:</strong></p>
<p>可以认为区间的终点总是大于它的起点。</p>
<p>区间 [1,2] 和 [2,3] 的边界相互“接触”，但没有相互重叠。</p>
<p><strong>示例 1:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: [ [<span class="number">1</span>,<span class="number">2</span>], [<span class="number">2</span>,<span class="number">3</span>], [<span class="number">3</span>,<span class="number">4</span>], [<span class="number">1</span>,<span class="number">3</span>] ]  输出: <span class="number">1</span> </span><br><span class="line"></span><br><span class="line">解释: 移除 [<span class="number">1</span>,<span class="number">3</span>] 后，剩下的区间没有重叠。</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p><strong>示例 2:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: [ [<span class="number">1</span>,<span class="number">2</span>], [<span class="number">1</span>,<span class="number">2</span>], [<span class="number">1</span>,<span class="number">2</span>] ]  输出: <span class="number">2</span> </span><br><span class="line"></span><br><span class="line">解释: 你需要移除两个 [<span class="number">1</span>,<span class="number">2</span>] 来使剩下的区间没有重叠。</span><br></pre></td></tr></table></figure>



<p><strong>示例 3:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">输入: [ [<span class="number">1</span>,<span class="number">2</span>], [<span class="number">2</span>,<span class="number">3</span>] ]  输出: <span class="number">0</span> </span><br><span class="line"></span><br><span class="line">解释: 你不需要移除任何区间，因为它们已经是无重叠的了。</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="解题思路"><a href="#解题思路" class="headerlink" title="解题思路"></a>解题思路</h2><p>使用贪心策略，将区间的tail进行升序排序，这样我们保证tail小的与下一个tail大的发送重叠时，</p>
<p>我们移除的是大tail的区间，留下tail小的空间，从而保证和下一个tail大的区间进行比较时重叠的可能性更小</p>
<h2 id="解题代码"><a href="#解题代码" class="headerlink" title="解题代码"></a>解题代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">   	<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">eraseOverlapIntervals</span><span class="params">(<span class="type">int</span>[][] intervals)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span>(intervals.length == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">		<span class="comment">// 按照每个小数组的tail大小进行排序</span></span><br><span class="line">		Arrays.sort(intervals,<span class="keyword">new</span> <span class="title class_">Comparator</span>&lt;<span class="type">int</span>[]&gt;()&#123;</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compare</span><span class="params">(<span class="type">int</span>[] o1, <span class="type">int</span>[] o2)</span> &#123;</span><br><span class="line">				<span class="keyword">return</span> o1[<span class="number">1</span>] - o2[<span class="number">1</span>];</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;);</span><br><span class="line">		</span><br><span class="line">	    <span class="comment">// 排序后intervals[0]肯定保留</span></span><br><span class="line">		<span class="type">int</span> <span class="variable">num</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">		<span class="type">int</span> <span class="variable">prevTail</span> <span class="operator">=</span> intervals[<span class="number">0</span>][<span class="number">1</span>];</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; intervals.length; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span>(prevTail &gt; intervals[i][<span class="number">0</span>])</span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			<span class="comment">// 当不重叠时进行移位，如果重叠比较的还是之前的那个tail小的区间</span></span><br><span class="line">			prevTail = intervals[i][<span class="number">1</span>];</span><br><span class="line">			num++;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> intervals.length - num;</span><br><span class="line">	&#125;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>贪心算法</tag>
      </tags>
  </entry>
  <entry>
    <title>417-太平洋大西洋水流问题</title>
    <url>/2019/08/05/417-%E5%A4%AA%E5%B9%B3%E6%B4%8B%E5%A4%A7%E8%A5%BF%E6%B4%8B%E6%B0%B4%E6%B5%81%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="417-太平洋大西洋水流问题"><a href="#417-太平洋大西洋水流问题" class="headerlink" title="417. 太平洋大西洋水流问题"></a><a href="https://leetcode-cn.com/problems/pacific-atlantic-water-flow/">417. 太平洋大西洋水流问题</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个 m x n 的非负整数矩阵来表示一片大陆上各个单元格的高度。“太平洋”处于大陆的左边界和上边界，而“大西洋”处于大陆的右边界和下边界。</p>
<p>规定水流只能按照上、下、左、右四个方向流动，且只能从高到低或者在同等高度上流动。</p>
<p>请找出那些水流既可以流动到“太平洋”，又能流动到“大西洋”的陆地单元的坐标。</p>
<p><strong>提示：</strong></p>
<p>输出坐标的顺序不重要<br>m 和 n 都小于150</p>
<p><strong>示例：</strong></p>
<p>给定下面的 5x5 矩阵:</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">太平洋 ~   ~   ~   ~   ~ </span><br><span class="line">     ~  <span class="number">1</span>   <span class="number">2</span>   <span class="number">2</span>   <span class="number">3</span>  (<span class="number">5</span>) *</span><br><span class="line">     ~  <span class="number">3</span>   <span class="number">2</span>   <span class="number">3</span>  (<span class="number">4</span>) (<span class="number">4</span>) *</span><br><span class="line">     ~  <span class="number">2</span>   <span class="number">4</span>  (<span class="number">5</span>)  <span class="number">3</span>   <span class="number">1</span>  *</span><br><span class="line">     ~ (<span class="number">6</span>) (<span class="number">7</span>)  <span class="number">1</span>   <span class="number">4</span>   <span class="number">5</span>  *</span><br><span class="line">     ~ (<span class="number">5</span>)  <span class="number">1</span>   <span class="number">1</span>   <span class="number">2</span>   <span class="number">4</span>  *</span><br><span class="line">        *   *   *   *   * 大西洋</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p><strong>返回:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[[<span class="number">0</span>, <span class="number">4</span>], [<span class="number">1</span>, <span class="number">3</span>], [<span class="number">1</span>, <span class="number">4</span>], [<span class="number">2</span>, <span class="number">2</span>], [<span class="number">3</span>, <span class="number">0</span>], [<span class="number">3</span>, <span class="number">1</span>], [<span class="number">4</span>, <span class="number">0</span>]] (上图中带括号的单元).</span><br></pre></td></tr></table></figure>



<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><h3 id="个人思路："><a href="#个人思路：" class="headerlink" title="个人思路："></a>个人思路：</h3><p>首先 二维数组中 指定下标的 元素要满足 流向的路径是递减的（包含相等）到太平洋和大西洋。</p>
<p>当时觉得挺简单的，将二维数组遍历，然后将下标<strong>深度优先搜索</strong>进行一种大西洋的递归，一种太平洋的递归，然后发现结果不对，没有考虑到4条边界上的元素，它们是特殊一点的，添加相应的规则后，发现二维数量一旦大一点就栈溢出了。这个时候就想到了第一是递归的时候没有保存相应的数组元素的判断，导致大量的重复判断，第二是一旦涉及要保存已遍历这种情况时就发现考虑不过来了。。。</p>
<h3 id="正确思路："><a href="#正确思路：" class="headerlink" title="正确思路："></a>正确思路：</h3><p>要反过来思考，其实是<strong>求太平洋和大西洋从边界开始能够反向规则到达的所有区域的交集</strong>，我们建立两个boolean数组，将边界能到达的区域在递归的时候存储被递归元素，这样我们需要计算和考虑的情况大大较少。</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PacificAtlantic417</span> &#123;</span><br><span class="line">	<span class="keyword">public</span> List&lt;List&lt;Integer&gt;&gt; <span class="title function_">pacificAtlantic</span><span class="params">(<span class="type">int</span>[][] matrix)</span> &#123;</span><br><span class="line">		List&lt;List&lt;Integer&gt;&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="comment">// 要反方向思考，能够流到指定的边，也就是说 边缘的元素能够按照指定的规则到达某些区域，这样我们只要比较太平洋和大西洋都能达到的区域就好了</span></span><br><span class="line">		<span class="keyword">if</span> (matrix == <span class="literal">null</span> || matrix.length &lt; <span class="number">1</span>)</span><br><span class="line">			<span class="keyword">return</span> list;</span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> matrix[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">ylen</span> <span class="operator">=</span> matrix.length;</span><br><span class="line">		<span class="type">boolean</span> leftVisit[][] = <span class="keyword">new</span> <span class="title class_">boolean</span>[ylen][xlen];</span><br><span class="line">		<span class="type">boolean</span> rightVisit[][] = <span class="keyword">new</span> <span class="title class_">boolean</span>[ylen][xlen];</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; xlen; i++) &#123;</span><br><span class="line">			dfs(matrix,<span class="number">0</span>,i,matrix[<span class="number">0</span>][i],leftVisit);</span><br><span class="line">			dfs(matrix,ylen - <span class="number">1</span>,i,matrix[ylen - <span class="number">1</span>][i],rightVisit);</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			dfs(matrix,i,<span class="number">0</span>,matrix[i][<span class="number">0</span>],leftVisit);</span><br><span class="line">			dfs(matrix,i,xlen - <span class="number">1</span>,matrix[i][xlen - <span class="number">1</span>],rightVisit);</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; xlen; j++) &#123;</span><br><span class="line">				<span class="keyword">if</span> (leftVisit[i][j] &amp;&amp; rightVisit[i][j]) &#123;</span><br><span class="line">					List&lt;Integer&gt; index = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;Integer&gt;();</span><br><span class="line">					index.add(i);</span><br><span class="line">					index.add(j);</span><br><span class="line">					list.add(index);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> list;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">dfs</span><span class="params">(<span class="type">int</span>[][] matrix, <span class="type">int</span> i, <span class="type">int</span> j, <span class="type">int</span> lastValue, <span class="type">boolean</span>[][] visit)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (i &lt; <span class="number">0</span> || j &lt; <span class="number">0</span> || i &gt;= matrix.length || j &gt;= matrix[<span class="number">0</span>].length || matrix[i][j] &lt; lastValue || visit[i][j])</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		visit[i][j] = <span class="literal">true</span>;</span><br><span class="line">		dfs(matrix, i + <span class="number">1</span>, j, matrix[i][j], visit);</span><br><span class="line">		dfs(matrix, i, j + <span class="number">1</span>, matrix[i][j], visit);</span><br><span class="line">		dfs(matrix, i - <span class="number">1</span>, j, matrix[i][j], visit);</span><br><span class="line">		dfs(matrix, i, j - <span class="number">1</span>, matrix[i][j], visit);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">int</span>[][] matrix = &#123;&#125;;</span><br><span class="line">		List&lt;List&lt;Integer&gt;&gt; list = <span class="keyword">new</span> <span class="title class_">PacificAtlantic417</span>().pacificAtlantic(matrix);</span><br><span class="line">		<span class="keyword">for</span> (List&lt;Integer&gt; index : list) &#123;</span><br><span class="line">			System.out.print(<span class="string">&quot;[&quot;</span> + index.get(<span class="number">0</span>) + <span class="string">&quot;,&quot;</span> + index.get(<span class="number">1</span>) + <span class="string">&quot;],  &quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="个人感悟："><a href="#个人感悟：" class="headerlink" title="个人感悟："></a>个人感悟：</h2><p>BFS、DFS的题目做多了后感受较多的就是，绝大多数是从边界考虑规则，从边界点进行递归延伸，同时使用递归，这种解题思路清晰，且速度较快。</p>
<p>不要想着从整块区域进行探索，考虑整块区域往往只是最后的统计而已。</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>深度优先搜索</tag>
      </tags>
  </entry>
  <entry>
    <title>695-岛屿的最大面积</title>
    <url>/2019/08/05/695-%E5%B2%9B%E5%B1%BF%E7%9A%84%E6%9C%80%E5%A4%A7%E9%9D%A2%E7%A7%AF/</url>
    <content><![CDATA[<h1 id="695-岛屿的最大面积"><a href="#695-岛屿的最大面积" class="headerlink" title="695. 岛屿的最大面积"></a><a href="https://leetcode-cn.com/problems/max-area-of-island/">695. 岛屿的最大面积</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个包含了一些 0 和 1的非空二维数组 grid , 一个 岛屿 是由四个方向 (水平或垂直) 的 1 (代表土地) 构成的组合。你可以假设二维矩阵的四个边缘都被水包围着。</p>
<p>找到给定的二维数组中最大的岛屿面积。(如果没有岛屿，则返回面积为0。)</p>
<p><strong>示例 1:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[[<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>],</span><br><span class="line"> [<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>]]</span><br></pre></td></tr></table></figure>

<p>对于上面这个给定矩阵应返回 6。注意答案不应该是11，因为岛屿只能包含水平或垂直的四个方向的‘1’。</p>
<span id="more"></span>


<p><strong>示例 2:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[[<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>]]</span><br></pre></td></tr></table></figure>

<p>对于上面这个给定的矩阵, 返回 0。</p>
<p><strong>注意</strong>: 给定的矩阵grid 的长度和宽度都不超过 50。</p>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>本题考点在于 遍历数组时如何深度遍历，也就是如何将所有的指定规则相连的值统计下来。</p>
<p>我们先将 二维数组 grid 进行遍历</p>
<p>如果 gird[ i ] [ j ] 的值为0，continue</p>
<p>如果 gird[ i ] [ j ] 的值为1，将其加入队列中，检测其上下左右方向是否存在1，存在则添加进队列。</p>
<p>注意：深度遍历连接岛屿时，应将其值设置为0，防止多次遍历查询</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MaxAreaOfIsland695</span> &#123;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">		<span class="type">int</span> x;</span><br><span class="line">		<span class="type">int</span> y;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">Node</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.x = x;</span><br><span class="line">		<span class="built_in">this</span>.y = y;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">maxAreaOfIsland</span><span class="params">(<span class="type">int</span>[][] grid)</span> &#123;</span><br><span class="line">	<span class="keyword">if</span> (grid == <span class="literal">null</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">	<span class="type">int</span>[][] hasVisit = grid;</span><br><span class="line">	<span class="type">int</span> <span class="variable">wlen</span> <span class="operator">=</span> grid[<span class="number">0</span>].length;</span><br><span class="line">	<span class="type">int</span> <span class="variable">hlen</span> <span class="operator">=</span> grid.length;</span><br><span class="line">	<span class="type">int</span> <span class="variable">max</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">	Queue&lt;Node&gt; q = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">	<span class="type">int</span> <span class="variable">arealen</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; hlen; i++) &#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; wlen; j++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (hasVisit[i][j] == <span class="number">1</span>) &#123;</span><br><span class="line">				q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(i, j));</span><br><span class="line">				<span class="keyword">while</span> (!q.isEmpty()) &#123;</span><br><span class="line">					<span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> q.peek().x;</span><br><span class="line">					<span class="type">int</span> <span class="variable">y</span> <span class="operator">=</span> q.peek().y;</span><br><span class="line">					q.remove();</span><br><span class="line">					<span class="keyword">if</span> (hasVisit[x][y] == <span class="number">1</span>) &#123;</span><br><span class="line">                        <span class="comment">//遍历完设为0，防止再次遍历</span></span><br><span class="line">						hasVisit[x][y] = <span class="number">0</span>;</span><br><span class="line">						<span class="comment">// 深度遍历的规则 上下左右都要考虑到</span></span><br><span class="line">						max++;</span><br><span class="line">						<span class="keyword">if</span> (y + <span class="number">1</span> &lt; wlen &amp;&amp; hasVisit[x][y + <span class="number">1</span>] == <span class="number">1</span>)</span><br><span class="line">							q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x, y + <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">						<span class="keyword">if</span> (x + <span class="number">1</span> &lt; hlen &amp;&amp; hasVisit[x + <span class="number">1</span>][y] == <span class="number">1</span>)</span><br><span class="line">							q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x + <span class="number">1</span>, y));</span><br><span class="line"></span><br><span class="line">						<span class="keyword">if</span> (y - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; hasVisit[x][y - <span class="number">1</span>] == <span class="number">1</span>)</span><br><span class="line">							q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x, y - <span class="number">1</span>));</span><br><span class="line"></span><br><span class="line">						<span class="keyword">if</span> (x - <span class="number">1</span> &gt;= <span class="number">0</span> &amp;&amp; hasVisit[x - <span class="number">1</span>][y] == <span class="number">1</span>)</span><br><span class="line">							q.add(<span class="keyword">new</span> <span class="title class_">Node</span>(x - <span class="number">1</span>, y));</span><br><span class="line"></span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">if</span> (arealen &lt;= max)</span><br><span class="line">					arealen = max;</span><br><span class="line">				max = <span class="number">0</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> arealen;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">	<span class="type">MaxAreaOfIsland695</span> <span class="variable">m</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MaxAreaOfIsland695</span>();</span><br><span class="line">	<span class="type">int</span>[][] grid = &#123; &#123; <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span> &#125;, &#123; <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span> &#125;,</span><br><span class="line">			&#123; <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span> &#125;, &#123; <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span> &#125;,</span><br><span class="line">			&#123; <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span> &#125;, &#123; <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span> &#125;,</span><br><span class="line">			&#123; <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span> &#125;, &#123; <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span> &#125; &#125;;</span><br><span class="line">	System.out.println(<span class="string">&quot;arealen = &quot;</span> + m.maxAreaOfIsland(grid));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>深度优先搜索</tag>
      </tags>
  </entry>
  <entry>
    <title>79-单词搜索</title>
    <url>/2019/08/13/79-%E5%8D%95%E8%AF%8D%E6%90%9C%E7%B4%A2/</url>
    <content><![CDATA[<h1 id="79-单词搜索"><a href="#79-单词搜索" class="headerlink" title="79. 单词搜索"></a><a href="https://leetcode-cn.com/problems/word-search/">79. 单词搜索</a></h1><h2 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h2><p>给定一个二维网格和一个单词，找出该单词是否存在于网格中。</p>
<p>单词必须按照字母顺序，通过相邻的单元格内的字母构成，其中“相邻”单元格是那些水平相邻或垂直相邻的单元格。同一个单元格内的字母不允许被重复使用。</p>
<p><strong>示例:</strong></p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">board =</span><br><span class="line">[</span><br><span class="line">  [<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;B&#x27;</span>,<span class="string">&#x27;C&#x27;</span>,<span class="string">&#x27;E&#x27;</span>],</span><br><span class="line">  [<span class="string">&#x27;S&#x27;</span>,<span class="string">&#x27;F&#x27;</span>,<span class="string">&#x27;C&#x27;</span>,<span class="string">&#x27;S&#x27;</span>],</span><br><span class="line">  [<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;D&#x27;</span>,<span class="string">&#x27;E&#x27;</span>,<span class="string">&#x27;E&#x27;</span>]</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">给定 word = <span class="string">&quot;ABCCED&quot;</span>, 返回 <span class="literal">true</span>.</span><br><span class="line">给定 word = <span class="string">&quot;SEE&quot;</span>, 返回 <span class="literal">true</span>.</span><br><span class="line">给定 word = <span class="string">&quot;ABCB&quot;</span>, 返回 <span class="literal">false</span>.</span><br></pre></td></tr></table></figure>


<span id="more"></span>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><p>由于是路径判断的题目类型，需要顺序推进，那么肯定是要深度遍历DFS的做法了。</p>
<p>在设计的时候，我们知道已经遍历了的坐标需要标记，进行剪枝操作。并且同一单元格内的字母不允许重复使用。然而如果这个支路遍历了发现这个字母不相等，但之前的字母可能在另外一条支路的遍历延伸中又转回来了。</p>
<p>例如：在该 3 * 4 的数组中寻找一个字符串  <strong>ABCESEEEFS</strong></p>
<img src="/2019/08/13/79-%E5%8D%95%E8%AF%8D%E6%90%9C%E7%B4%A2/b1.jpg" class="" title="示例">

<p>这就需要用到回溯思想了，如果一条节点临近的多个节点都可以访问，也就是分支时，判断后续是否可以完全跑通，跑不通我们应该将hasVisit数组的堵塞的支路的坐标对其复原。</p>
<h2 id="解题代码："><a href="#解题代码：" class="headerlink" title="解题代码："></a>解题代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WordSearch79</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">exist</span><span class="params">(<span class="type">char</span>[][] board, String word)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (word == <span class="literal">null</span> || board == <span class="literal">null</span> || word.length() &lt; <span class="number">1</span> || board.length &lt; <span class="number">1</span>)</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">		<span class="type">int</span> <span class="variable">xlen</span> <span class="operator">=</span> board[<span class="number">0</span>].length;</span><br><span class="line">		<span class="type">int</span> <span class="variable">ylen</span> <span class="operator">=</span> board.length;</span><br><span class="line">		<span class="comment">// 一个二维数组，用一个数组进行记录</span></span><br><span class="line">		<span class="comment">// 将首字母在board中出现的坐标进行记录</span></span><br><span class="line">		<span class="type">char</span> <span class="variable">firstChar</span> <span class="operator">=</span> word.charAt(<span class="number">0</span>);</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; ylen; i++) &#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; xlen; j++) &#123;</span><br><span class="line">				<span class="keyword">if</span> (board[i][j] == firstChar) &#123;</span><br><span class="line">					<span class="type">boolean</span>[][] hasVisit = <span class="keyword">new</span> <span class="title class_">boolean</span>[ylen][xlen];</span><br><span class="line">					<span class="keyword">if</span> (backtrack(i, j, board, hasVisit, word, <span class="number">0</span>))</span><br><span class="line">						<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">backtrack</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">char</span>[][] board, <span class="type">boolean</span>[][] hasVisit, String word, <span class="type">int</span> i)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (i == word.length())</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (x &lt; <span class="number">0</span> || y &lt; <span class="number">0</span> || x &gt;= board.length || y &gt;= board[<span class="number">0</span>].length || board[x][y] != word.charAt(i)</span><br><span class="line">				|| hasVisit[x][y])</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 如何解决当分叉遍历后，一条遍历多个节点后需要回到其中的另一个子节点，而这个子节点已经当做遍历过的这种情况</span></span><br><span class="line">		hasVisit[x][y] = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 判断后续该路径是否可以走通，如果不可以还原 hasVisit,不要影响其他分支的遍历</span></span><br><span class="line">		<span class="type">boolean</span> <span class="variable">flag</span> <span class="operator">=</span> backtrack(x + <span class="number">1</span>, y, board, hasVisit, word, i + <span class="number">1</span>)</span><br><span class="line">				|| backtrack(x - <span class="number">1</span>, y, board, hasVisit, word, i + <span class="number">1</span>)</span><br><span class="line">				|| backtrack(x, y + <span class="number">1</span>, board, hasVisit, word, i + <span class="number">1</span>)</span><br><span class="line">				|| backtrack(x, y - <span class="number">1</span>, board, hasVisit, word, i + <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (!flag)</span><br><span class="line">			hasVisit[x][y] = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> flag;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">char</span>[][] board = &#123; &#123; <span class="string">&#x27;A&#x27;</span>, <span class="string">&#x27;B&#x27;</span>, <span class="string">&#x27;C&#x27;</span>, <span class="string">&#x27;E&#x27;</span> &#125;, &#123; <span class="string">&#x27;S&#x27;</span>, <span class="string">&#x27;F&#x27;</span>, <span class="string">&#x27;E&#x27;</span>, <span class="string">&#x27;S&#x27;</span> &#125;, &#123; <span class="string">&#x27;A&#x27;</span>, <span class="string">&#x27;D&#x27;</span>, <span class="string">&#x27;E&#x27;</span>, <span class="string">&#x27;E&#x27;</span> &#125; &#125;;</span><br><span class="line">		<span class="type">String</span> <span class="variable">word</span> <span class="operator">=</span> <span class="string">&quot;SEEEE&quot;</span>;</span><br><span class="line">		<span class="type">WordSearch79</span> <span class="variable">demo</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">WordSearch79</span>();</span><br><span class="line">		System.out.println(demo.exist(board, word));</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>深度优先搜索</tag>
        <tag>回溯算法</tag>
      </tags>
  </entry>
  <entry>
    <title>ActiveMQ配置jdbc持久化出现的问题</title>
    <url>/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="ActiveMQ配置jdbc持久化出现的问题"><a href="#ActiveMQ配置jdbc持久化出现的问题" class="headerlink" title="ActiveMQ配置jdbc持久化出现的问题"></a>ActiveMQ配置jdbc持久化出现的问题</h1><p>当我配置activemq.xml，重启时发现无进程运行，又不报错</p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/start.jpg" class="">

<p>查看日志 在 &#x2F;data&#x2F;activemq.log下</p>
<span id="more"></span>

<p><strong>tail -f activemq.log</strong></p>
<p>发现报错误了</p>
<p> <strong>java.lang.IllegalStateException: BeanFactory not initialized or already closed - call ‘refresh’ before accessing beans via the ApplicationContext</strong></p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/log_error.jpg" class="">

<p>网上的主流解决方式：</p>
<p>1.确认计算机主机名名称没有下划线；</p>
<p>2.如果是win7，停止ICS(运行–&gt;services.msc找到Internet Connection Sharing (ICS)服务,改成手动启动或禁用)</p>
<p>重新启动activeMQ即可。</p>
<p>但我发现我的windows主机名是英文加数字没有什么问题， ICS服务也是禁用的。</p>
<p>难道是 centos7的主机名问题？</p>
<p>我将自己的主机名改成了纯英文名 eiletxie</p>
<p>发现</p>
<p><strong>这里说明：</strong></p>
<p>如果有要虚拟机重启几次才可以开启activemq的情况是需要修改主机名的，我这里的不是这种原因。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[root@centos7 ~]$ hostnamectl set-hostname eiletxie          # 使用这个命令会立即生效且重启也生效</span><br><span class="line">[root@centos7 ~]$ hostname                                                 # 查看下</span><br><span class="line">eiletxie </span><br><span class="line">[root@centos7 ~]$ vim /etc/hosts                                           # 编辑下hosts文件， 给127.0.0.1添加hostname</span><br><span class="line">[root@centos7 ~]$ cat /etc/hosts                                           # 检查</span><br><span class="line">127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4 eiletxie </span><br><span class="line">::1         localhost localhost.localdomain localhost6 localhost6.localdomain6</span><br></pre></td></tr></table></figure>

<hr>
<p>突然想了一下，是不是防火墙的原因。</p>
<p>本地ping的activemq服务器，发现没问题。</p>
<p>在服务器上ping 了一下本地的IP地址</p>
<p>果然我本地的防火墙没关，导致没法访问。</p>
<p>关闭防火墙后再次启动，还是无法打开网页，查看日志</p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/root_error.jpg" class="">



<p><strong>这里已经说</strong> <strong>Host is not allowed to connect to this MySQL server</strong></p>
<p>MySQL不允许root用户远程登录，所以远程登录失败了，解决方法如下：</p>
<ol>
<li><em><strong>在装有MySQL的机器上登录MySQL mysql -u root -p密码</strong></em></li>
<li><em><strong>执行</strong></em><em><strong>use mysql;</strong></em></li>
<li><em><strong>执行</strong></em><em><strong>update user set host &#x3D; ‘%’ where user &#x3D; ‘root’;</strong></em></li>
<li><em><strong>执行</strong></em><em><strong>FLUSH PRIVILEGES; 刷新权限配置</strong></em></li>
</ol>
<p><em><strong>先说明： 这种方式是开放所有IP的root访问权限。如果是正式环境还是建议用 开放指定IP的方式</strong></em></p>
<p><em><strong>这里有两种直接设置的方式</strong></em></p>
<p><em>1. 授权用户root使用密码jb51从任意主机连接到</em><a href="https://www.baidu.com/s?wd=mysql&tn=44039180_cpr&fenlei=mv6quAkxTZn0IZRqIHckPjm4nH00T1Y3PjN9PhRLmH99PymzuycY0ZwV5Hcvrjm3rH6sPfKWUMw85HfYnjn4nH6sgvPsT6KdThsqpZwYTjCEQLGCpyw9Uz4Bmy-bIi4WUvYETgN-TLwGUv3Erjb3PWcknWf"><em>mysql</em></a><em>服务器：</em></p>
<p><em>代码如下:</em></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> <span class="operator">*</span>.<span class="operator">*</span> <span class="keyword">TO</span> <span class="string">&#x27;root&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;jb51&#x27;</span> <span class="keyword">WITH</span> <span class="keyword">GRANT</span> OPTION;</span><br><span class="line"></span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>

<p>2.授权用户root使用密码jb51从指定ip为218.12.50.60的主机连接到mysql服务器：</p>
<p><em>代码如下:</em></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> <span class="operator">*</span>.<span class="operator">*</span> <span class="keyword">TO</span> <span class="string">&#x27;root&#x27;</span><span class="variable">@218</span><span class="number">.12</span><span class="number">.50</span><span class="number">.60</span><span class="string">&#x27;%&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;jb51&#x27;</span> <span class="keyword">WITH</span> <span class="keyword">GRANT</span> OPTION;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>



<p><em>我是本地mysql做测试用的，就直接授予所有IP访问权限了</em></p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/mysql_test.jpg" class="">

<p>测试一下发送信息没问题</p>
<hr>
<p>再查看本地mysql的 activemq 数据库，已经有核心的3张表了</p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/mysql.jpg" class="">

<p>未消费的message存于 activemq_msgs表中</p>
<img src="/2019/08/26/ActiveMQ%E9%85%8D%E7%BD%AEjdbc%E6%8C%81%E4%B9%85%E5%8C%96%E5%87%BA%E7%8E%B0%E7%9A%84%E9%97%AE%E9%A2%98/mysql_msg.jpg" class="">]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>ActiveMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>Ajax，json多数据传输以及解析</title>
    <url>/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h1 id="Ajax，json多数据传输以及解析"><a href="#Ajax，json多数据传输以及解析" class="headerlink" title="Ajax，json多数据传输以及解析"></a>Ajax，json多数据传输以及解析</h1><p>在一次表单传值中，我遇到了一个这样的表单提交情形，这个表单 有一些 字段 ，这些字段 name 对应了 一个 A对象的 属性名称</p>
<p>有两个内联的 table，有一个 不属于 A对象的 reason字段。我该如何传入呢？<br><br/></p>
<h2 id="数据组成结构"><a href="#数据组成结构" class="headerlink" title="数据组成结构"></a>数据组成结构</h2><p><strong>我们 要传输 一个表单对象，两个 table里面的 参数，一个普通字符串</strong> </p>
<p>于是 我就将其用过 JSON.stringify 一次性全部封装</p>
<span id="more"></span>

<p>​          <img src="/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/b1.jpg" class="" title="数据组成"></p>
<p>我从表格中取得了 blameList 和 outBlameList；</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 将责任工序表格数据传入后台</span></span><br><span class="line"><span class="keyword">var</span> blameTrList = $(<span class="string">&quot;#blameProcesses_table tbody&quot;</span>).<span class="title function_">children</span>(<span class="string">&quot;tr&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> blameList = <span class="keyword">new</span> <span class="title class_">Array</span>();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; blameTrList.<span class="property">length</span>; i++) &#123;</span><br><span class="line"><span class="keyword">var</span> blameProcess = <span class="keyword">new</span> <span class="title class_">Object</span>;</span><br><span class="line"><span class="keyword">var</span> tdColumn = blameTrList.<span class="title function_">eq</span>(i).<span class="title function_">children</span>(<span class="string">&quot;td&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> operation_description = tdColumn.<span class="title function_">eq</span>(<span class="number">0</span>).<span class="title function_">text</span>();</span><br><span class="line"><span class="keyword">var</span> standard_operation_code = tdColumn.<span class="title function_">eq</span>(<span class="number">1</span>).<span class="title function_">text</span>();</span><br><span class="line"><span class="keyword">var</span> blame_content = tdColumn.<span class="title function_">eq</span>(<span class="number">2</span>).<span class="title function_">text</span>();</span><br><span class="line"><span class="keyword">var</span> blame_type = tdColumn.<span class="title function_">eq</span>(<span class="number">3</span>).<span class="title function_">text</span>();</span><br><span class="line">blameProcess.<span class="property">blame_type</span> = blame_type;</span><br><span class="line">blameProcess.<span class="property">blame_content</span> = blame_content;</span><br><span class="line">blameProcess.<span class="property">operation_description</span> = operation_description;</span><br><span class="line">blameProcess.<span class="property">standard_operation_code</span> = standard_operation_code;</span><br><span class="line">blameList.<span class="title function_">push</span>(blameProcess);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将所有数据传入</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"> <span class="comment">// 客诉对象数据</span></span><br><span class="line">$.<span class="title function_">ajax</span>(&#123;</span><br><span class="line">    <span class="attr">url</span>: contextPath + <span class="string">&quot;/complaint/editComplaint&quot;</span>,</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&quot;POST&quot;</span>,</span><br><span class="line">    <span class="attr">dataType</span>:<span class="string">&quot;json&quot;</span>,</span><br><span class="line">    <span class="attr">contentType</span>:<span class="string">&quot;application/json&quot;</span>,</span><br><span class="line">    <span class="attr">cache</span>:<span class="literal">false</span>,</span><br><span class="line">    <span class="attr">data</span>:<span class="title class_">JSON</span>.<span class="title function_">stringify</span>( &#123;</span><br><span class="line">        <span class="string">&quot;complaint&quot;</span>: $(<span class="string">&quot;#complaintForm&quot;</span>).<span class="title function_">serializeArray</span>(), <span class="comment">// 最初用的 是 serialize方法</span></span><br><span class="line">        <span class="string">&quot;reason&quot;</span>: $(<span class="string">&quot;#reason_update_input&quot;</span>).<span class="title function_">val</span>(),</span><br><span class="line">        <span class="string">&quot;blameList&quot;</span>: blameList,</span><br><span class="line">        <span class="string">&quot;outBlameList&quot;</span>: outBlameList,</span><br><span class="line">        <span class="string">&quot;base_uid&quot;</span> :  $(<span class="variable language_">this</span>).<span class="title function_">attr</span>(<span class="string">&quot;edit-id&quot;</span>)</span><br><span class="line">    &#125;),</span><br><span class="line">    <span class="attr">success</span>: <span class="keyword">function</span> (<span class="params">result</span>) &#123;</span><br><span class="line">        <span class="title function_">alert</span>(<span class="string">&quot;更新成功！&quot;</span>);</span><br><span class="line">        <span class="variable language_">window</span>.<span class="property">location</span>.<span class="title function_">reload</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<br/>
##  Spring框架取值

<p>我们需要一个 好用的插件 fastjson,它将 json通过数据类型 分为了 String ， JSONObject，JSONArray这三种类型。</p>
<p>也可以使用Java中的jackson，操作基本一致</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;fastjson&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;<span class="number">1.2</span><span class="number">.54</span>&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<p>在后台中 我们用一个 JSONObject来进行 前端参数的接受，注意 要用 @requestBody 进行标注</p>
<p>再取值的过程中 注意 先在前端打印出来 看看数据的情况，</p>
<p>我之前误以为 $(“#complaintForm”).serialize() 在后台拆分时肯定被封装成了一个对象了，所以我试图将其强转过来，</p>
<p>报了  complaint is not cast  不能强转的错误，在打印的过程中我才发现，serialize在 json封装后就直接成了 String类型，无法转变过来，</p>
<p>只能用 $(“#complaintForm”).serializeArray()  让json封装后成一个 JSONArray 这样才能取到字段值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/editComplaint&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Msg <span class="title function_">editComplaint</span><span class="params">(<span class="meta">@RequestBody</span> JSONObject jsonObject, HttpSession session)</span> <span class="keyword">throws</span> Exception&#123;</span><br><span class="line">     <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> (User) session.getAttribute(<span class="string">&quot;user&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">reason</span> <span class="operator">=</span> jsonObject.getString(<span class="string">&quot;reason&quot;</span>);</span><br><span class="line">    <span class="type">JSONArray</span> <span class="variable">complaint</span> <span class="operator">=</span> jsonObject.getJSONArray(<span class="string">&quot;complaint&quot;</span>);</span><br><span class="line"></span><br><span class="line">    System.out.println(complaint);</span><br><span class="line">    List&lt;BlameProcess&gt; blameProcessList = jsonObject.getJSONArray(<span class="string">&quot;blameList&quot;</span>).toJavaList(BlameProcess.class);</span><br><span class="line">    List&lt;BlameProcess&gt; outBlameProcessList = jsonObject.getJSONArray(<span class="string">&quot;outBlameList&quot;</span>).toJavaList(BlameProcess.class);</span><br><span class="line">    <span class="comment">// 新建一个现在的complaint</span></span><br><span class="line">    <span class="type">CustomerComplaint</span> <span class="variable">nowComplaint</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CustomerComplaint</span>();</span><br><span class="line">    nowComplaint.setBase_uid(jsonObject.getString(<span class="string">&quot;base_uid&quot;</span>));</span><br><span class="line">    nowComplaint.setModel(complaint.getJSONObject(<span class="number">0</span>).get(<span class="string">&quot;value&quot;</span>).toString());</span><br><span class="line">    nowComplaint.setCustomcode(complaint.getJSONObject(<span class="number">1</span>).get(<span class="string">&quot;value&quot;</span>).toString());</span><br><span class="line">    <span class="comment">// 这里我们已经将所有的数据取出来了</span></span><br><span class="line">    .........<span class="comment">//省略业务逻辑</span></span><br><span class="line">    <span class="keyword">return</span> Msg.success();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里我们算是会了 复杂的数据如何传值到后台解析的过程 。<br><br/></p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>那么分析一下 为什么 JSONObject 和 JSONArray 能 快速接受数据传入和 数据转换，我也是第一次遇到这种业务情景以及使用 fastjson ，对于它里面的原理有点困惑。</p>
<p>从 两个类的 字段属性 和  继承关系中 ，JSONObject是 用 Map作为底层数据结构，所以在取得对象时我们可以进行 getString(key),get(key) 获取 Object 的字段属性</p>
<img src="/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/b2.jpg" class="" title="b2">

<p>在 JSONObject 除了许多类型校对的 方法 和 原生的 get（key) ,move(key) ，有两个方法需要注意一下</p>
<img src="/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/b3.jpg" class="" title="b3">

<p>特殊一点的， getJSONObject 方法也是 通过 map.get(key) 获取的，只是它会进行 类型判断 instaceOf ,将数据类型强转成 JSONObject</p>
<p>getJSONArray 与  getJSONObject一致，只是 最后强转类型不同罢了。</p>
<p>而JSONArray不一样。JSONArray 以 List为 底层数据结构。</p>
<img src="/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/b4.jpg" class="" title="b4">

<p>通过List<Object>可以看出， list中存放的都当做Object对象，getJSONObject中取值 直接进行强转即可。 </p>
<img src="/2019/08/02/Ajax%EF%BC%8Cjson%E5%A4%9A%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E4%BB%A5%E5%8F%8A%E8%A7%A3%E6%9E%90/b5.jpg" class="" title="b5">

]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>web</tag>
      </tags>
  </entry>
  <entry>
    <title>JAVAIO</title>
    <url>/2019/07/31/JAVAIO/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><h2 id="Java-IO体系"><a href="#Java-IO体系" class="headerlink" title="Java IO体系"></a>Java IO体系</h2><p>IO体系 由<br>字节流操作类 （InputStream 和 OutputStream为字节流基类） 和 字符类操作类 （Reader 和 Writer 为字符流基类） 组成的。</p>
<p>Java的IO包主要关注的是从原始数据源的读取以及输出原始数据到目标媒介。以下是最典型的数据源和目标媒介：</p>
<ul>
<li>文件</li>
<li>管道</li>
<li>网络连接</li>
<li>内存缓存<br>System.in, System.out, System.error(注：Java标准输入、输出、错误输出)<br>下面这张图描绘了一个程序从数据源读取数据，然后将数据输出到其他媒介的原理：</li>
</ul>
<span id="more"></span>
<p>下面这张图描绘了一个程序从数据源读取数据，然后将数据输出到其他媒介的原理：<br><br/></p>
<h2 id="流"><a href="#流" class="headerlink" title="流"></a>流</h2><p>在Java IO中，流是一个核心的概念。流从概念上来说是一个连续的数据流。你既可以从流中读取数据，也可以往流中写数据。流与数据源或者数据流向的媒介相关联。在Java IO中流既可以是字节流(以字节为单位进行读写)，也可以是字符流(以字符为单位进行读写)。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
  </entry>
  <entry>
    <title>Dijkstra算法</title>
    <url>/2020/09/20/Dijkstra%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<blockquote>
<p><strong>Dijkstra算法解决的是带权重的有向图上单源最短路径问题，该算法要求所有边的权重全为非负值</strong></p>
</blockquote>
<hr>
<br/>

<h3 id="Dijkstra算法处理流程"><a href="#Dijkstra算法处理流程" class="headerlink" title="Dijkstra算法处理流程"></a>Dijkstra算法处理流程</h3><ol>
<li>初始化： 先找到从源点V0 到各个终点Vk的直达路径（V0,Vk),即通过一条弧到达的路径</li>
<li>选择： 从这些路径中找出一条长度最短的路径 （V0,u)</li>
<li>更新： 然后对其余各条路径进行适当调整</li>
</ol>
<span id="more"></span> 

<p>​          若在图中存在 弧 （u,Vk), 且 （V0，u) + (u,Vk) &lt; (V0,Vk), 则以 路径 （V0，u) + (u,Vk) 代替 (V0,Vk)</p>
<p>在调整后的各条路径中，再找长度最短的路径，依次类推</p>
<br/>

<h3 id="迪杰斯特拉（Dijkstra-算法：-按照路径长度递增次序产生最短路径"><a href="#迪杰斯特拉（Dijkstra-算法：-按照路径长度递增次序产生最短路径" class="headerlink" title="迪杰斯特拉（Dijkstra)算法： 按照路径长度递增次序产生最短路径"></a>迪杰斯特拉（Dijkstra)算法： 按照路径长度递增次序产生最短路径</h3><p>图中所有的节点组成一个节点数组 V</p>
<p>1、把V分为 两组</p>
<p>  <em>（1） S：已求出最短路径的顶点的集合</em></p>
<p>   <em>（2）T&#x3D; V - S ： 尚未确定最短路径的顶点集合。</em></p>
<p>2、将T 中顶点按照最短路径递增的次序加入到S中</p>
<p>  <strong>保证：</strong> </p>
<p>（1） 从源点V0 到 S中各顶点的最短路径长度都不大于从到T中任何顶点的最短路径长度</p>
<p>（2） <strong>每个顶点对应一个 距离值：</strong></p>
<p>​         <em>S中顶点： 从V0 到此顶点的最短路径长度</em></p>
<p>​         <em>T中顶点： 从V0到此顶点的只包括S中顶点作中间顶点的最短路径长度</em></p>
<img src="/2020/09/20/Dijkstra%E7%AE%97%E6%B3%95/%E5%B8%A6%E6%9D%83%E6%9C%89%E5%90%91%E5%9B%BE.png" class="" title="带权有向图">



<br/>



<h2 id="算法步骤："><a href="#算法步骤：" class="headerlink" title="算法步骤："></a>算法步骤：</h2><p><strong>初始时令 S&#x3D; {V0}，T&#x3D;{其余顶点}</strong></p>
<p>T中顶点对应的距离值用辅助数组D存放</p>
<p>D[i] 初值： 若 &lt;V0,Vi&gt; 存在，则为其权值；否则为 无穷大∞</p>
<p>从T中选取一个其距离值最小的顶点 Vj，加入S</p>
<p>对 T中顶点的距离值进行遍历修改：</p>
<p>若加进 Vj作为中间顶点， 从V0 到 Vi 的距离值比 不加 Vj的路径要短，则修改此距离值。</p>
<p>重复上诉步骤，直到 S &#x3D; V为止。</p>
<br/>

<p>根据上面的带权有向图，我们可以按照该算法步骤做一个统计表格</p>
<img src="/2020/09/20/Dijkstra%E7%AE%97%E6%B3%95/%E9%81%8D%E5%8E%86%E7%BB%9F%E8%AE%A1%E8%A1%A8.png" class="" title="遍历统计表">

<br/>

<p>如果大家还是不太理解上面这个整理的图表，可以看看下面动图中是如何找到最短路径的，就是从起始点开始一次次贪心的寻找到其他节点的最短路径的一个过程。</p>
<img src="/2020/09/20/Dijkstra%E7%AE%97%E6%B3%95/%E9%81%8D%E5%8E%86%E6%B5%81%E7%A8%8B%E5%8A%A8%E5%9B%BE.gif" class="" title="遍历流程动图">





<h3 id="个人理解："><a href="#个人理解：" class="headerlink" title="个人理解："></a>个人理解：</h3><p>首先Dijkstra算法就是一个利用贪心思想的算法，以局部最优扩展到全局最优</p>
<p>这个业务语义版的广度遍历算法的原理在于</p>
<p>假设节点A的邻居节点有4个，分别是 B,C,D</p>
<p>我们把 A去各个邻居节点的路径长度做个比较</p>
<p>如果 刚好是 A -&gt;B,则  A到 B的最短路径就确定了，为什么？ </p>
<p>因为其他的2条邻居节点的长度已经比 A到B的这条要长了，它加上其他的中间节点组成一条路径去B节点是 不可能小于A到B的这个长度3的</p>
<p>因为 节点之间的路径肯定是 大于零的， AB &lt; AD,则 AB &lt; AD + DE + EB肯定是成立的</p>
<img src="/2020/09/20/Dijkstra%E7%AE%97%E6%B3%95/%E5%88%86%E6%9E%90%E5%9B%BE.png" class="" title="分析图">



<br/>

<p>下面有一个代码示例，很直观的体现一次次寻找其他节点最短路径直到遍历完所有节点的一个贪心思想。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Dijkstra</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">MAX</span> <span class="operator">=</span> Integer.MAX_VALUE - <span class="number">10000</span>;</span><br><span class="line">        <span class="comment">// 这里为了方便起见，直接记录了当前index节点的权值</span></span><br><span class="line">        <span class="type">int</span>[][] weight = &#123;</span><br><span class="line">                &#123;<span class="number">0</span>, <span class="number">13</span>, <span class="number">8</span>, MAX, <span class="number">30</span>, MAX, <span class="number">32</span>&#125;,</span><br><span class="line">                &#123;MAX, <span class="number">0</span>, MAX, MAX, MAX, <span class="number">9</span>, <span class="number">7</span>&#125;,</span><br><span class="line">                &#123;MAX, MAX, <span class="number">0</span>, <span class="number">5</span>, MAX, MAX, MAX&#125;,</span><br><span class="line">                &#123;MAX, MAX, MAX, <span class="number">0</span>, <span class="number">6</span>, MAX, MAX&#125;,</span><br><span class="line">                &#123;MAX, MAX, MAX, MAX, <span class="number">0</span>, <span class="number">2</span>, MAX&#125;,</span><br><span class="line">                &#123;MAX, MAX, MAX, MAX, MAX, <span class="number">0</span>, <span class="number">17</span>&#125;,</span><br><span class="line">                &#123;MAX, MAX, MAX, MAX, MAX, MAX, <span class="number">0</span>&#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="type">int</span> <span class="variable">start</span> <span class="operator">=</span> <span class="number">0</span>;  <span class="comment">//选择出发点</span></span><br><span class="line">        <span class="type">int</span>[] sp = getShortPath(weight, start);</span><br><span class="line">        System.out.println(Arrays.toString(sp));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span>[] getShortPath(<span class="type">int</span>[][] weight, <span class="type">int</span> start) &#123;</span><br><span class="line">        <span class="comment">//1、定义一个数组保存各顶点是否被访问过，初始化为0,。因为我们从0号节点出发，设置0号顶点为已访问。</span></span><br><span class="line">        <span class="type">int</span>[] visit = <span class="keyword">new</span> <span class="title class_">int</span>[weight.length]; <span class="comment">//标记某节点是否被访问过</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i : visit)&#123;</span><br><span class="line">            visit[i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        visit[start] = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//2、在循环中逐个访问每个顶点，并不断修正权重。</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> <span class="number">1</span>; k &lt;= weight.length - <span class="number">1</span>; k++) &#123;</span><br><span class="line">            <span class="comment">//循环中每次确定一个新的确定最短路径的节点</span></span><br><span class="line">            <span class="comment">//3、选择一个未标记的且离出发点最近的顶点。</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">dmin</span> <span class="operator">=</span> Integer.MAX_VALUE;</span><br><span class="line">            <span class="type">int</span> <span class="variable">position</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">            <span class="comment">//找出一个未标记的离出发点最近的节点</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; weight.length; i++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (visit[i] == <span class="number">0</span> &amp;&amp; weight[start][i] &lt; dmin &amp;&amp; i != start) &#123;</span><br><span class="line">                    dmin = weight[start][i];</span><br><span class="line">                    position = i;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(<span class="string">&quot;选出一个最短的路径：&quot;</span> + dmin + <span class="string">&quot;;   对应节点是 V&quot;</span> + position);</span><br><span class="line">            <span class="comment">//标记该节点为已经访问过</span></span><br><span class="line">            visit[position] = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">//4、修正从出发顶点经由此顶点，再到其他顶点的最短路径。</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; weight.length; i++) &#123;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (weight[start][position] + weight[position][i] &lt; weight[start][i] &amp;&amp; i != position) &#123;</span><br><span class="line">                    <span class="comment">//为了方便对照图</span></span><br><span class="line">                    System.out.println(<span class="string">&quot;原先的最短路径到 V&quot;</span> + i + <span class="string">&quot; 距离是:&quot;</span> + weight[start][i]);</span><br><span class="line">                    System.out.println(<span class="string">&quot;两者分别是 &quot;</span> + weight[start][position] + <span class="string">&quot;,&quot;</span> + weight[position][i]);</span><br><span class="line">                    weight[start][i] = weight[position][i] + weight[start][position]; <span class="comment">//更新最短路径</span></span><br><span class="line">                    System.out.println(<span class="string">&quot;更新最短路径到 V&quot;</span> + i + <span class="string">&quot; 距离是: &quot;</span> + weight[start][i]);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span>[] shortPath = <span class="keyword">new</span> <span class="title class_">int</span>[weight.length];</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; weight.length; i++) &#123;</span><br><span class="line">            shortPath[i] = weight[<span class="number">0</span>][i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> shortPath;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构与算法</category>
      </categories>
      <tags>
        <tag>Dijkstra</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM的垃圾收集</title>
    <url>/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/</url>
    <content><![CDATA[<h2 id="如何判断一个对象是否可以回收呢？"><a href="#如何判断一个对象是否可以回收呢？" class="headerlink" title="如何判断一个对象是否可以回收呢？"></a>如何判断一个对象是否可以回收呢？</h2><p>​        简单的说就是 怎么确定这个对象是个垃圾。</p>
<p>有两种解决方案：</p>
<h3 id="引用计数算法-reference-counting-："><a href="#引用计数算法-reference-counting-：" class="headerlink" title="引用计数算法(reference counting)："></a>引用计数算法(reference counting)：</h3><p>        判断机制：  给对象中添加一个引用计数器，每当有一个地方引用它时，计数器值就加 1 ，当引用失效时，计数器值就减 1；当任何时候计数器都为 0 的对象就是不可能再被使用的。</p>
<p>​    优点：引用计数算法（Reference Counting）的实现简单，判断效率很高，如微软的COM计数、Python语言以及游戏脚本领域中被应用的Squirrel 等都使用了该算法进行内存管理。</p>
<p>​    缺点：Java语言中没有选用引用计数算法管理内存。最重要的原因就是它无法解决对象之间相互循环引用的问题！！</p>
<br/>

<span id="more"></span>

<h3 id="可达性分析算法"><a href="#可达性分析算法" class="headerlink" title="可达性分析算法"></a>可达性分析算法</h3><p>        通过一系列的GC Roots的对象作为起始点，从这些根节点开始向下搜索，搜索所走过的路径称为引用链（Reference Chain），</p>
<p>当一个对象到GC Roots没有任何引用链相连时，则证明此对象是不可用的。</p>
<p>​        这里会引发出一个问题，对，哪些算是GC Roots对象呢？</p>
<br/>

<h3 id="可作为GC-Roots的对象"><a href="#可作为GC-Roots的对象" class="headerlink" title="可作为GC Roots的对象"></a>可作为GC Roots的对象</h3><ul>
<li><p>虚拟机栈中引用的对象 （栈帧中的本地变量表）</p>
</li>
<li><p>方法区中的常量引用的对象</p>
</li>
<li><p>方法区中的类静态属性引用的对象</p>
</li>
<li><p>本地方法栈中JNI (Native 方法）的引用对象</p>
</li>
</ul>
<p>ps: 如果是对这些对象很模糊，建议看一下相关的 JVM内存管理这块的内容。</p>
<br/>

<h2 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h2><h3 id="标记-清除（Mark-Sweep）算法"><a href="#标记-清除（Mark-Sweep）算法" class="headerlink" title="标记-清除（Mark-Sweep）算法"></a>标记-清除（Mark-Sweep）算法</h3><p>​        最基础的垃圾收集算法是 “标记-清除”（Mark - Sweep） 算法，如它的名字一样，算法分为“标记” 和 “清除” 两个阶段； 首先标记处所有需要回收的对象，在标记完成后统一回收掉所有被标记的对象。</p>
<p>缺点： 由于被回收的对象之间是不连续的，会造成内存碎片，当大对象存入时可能会因为碎片内存太小，无法插入，从而重新进行内存回收。</p>
<img src="/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/mark-sweep.jpg" class="" title="copying">

<br/>

<h3 id="复制算法-Copying"><a href="#复制算法-Copying" class="headerlink" title="复制算法-Copying"></a>复制算法-Copying</h3><p>​        它将可用内存按容量划分为大小相等的两块，每次只使用其中的一块。当其中一块的内存用完了，就将该块还存活着的对象复制到另外一块上面，然后把已使用过的这块内存空间一次清理完，这样每次都只对其中一块进行内存回收，不需要考虑内存碎片等问题，实现简单，运行高效。</p>
<img src="/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/copying.jpg" class="" title="copying">

<br/>

<p><strong>缺点：</strong> 内存使用为原来的一半。</p>
<p><strong>特点：</strong></p>
<ul>
<li>解决碎片化问题</li>
<li>顺序分配内存，比较高效</li>
<li>比较适合 对象存活率低的场景</li>
</ul>
<p>​      现在的商业虚拟机都采用这种收集算法来回收新生代对象。但把内存分为一块较大的Eden空间和两块较小的Survivor空间。每次使用Eden和其中的一块Survivor。当回收时将Eden和Survivor中还存活着的对象一次性地拷贝到另外一块Survior空间上，最后清理掉Eden和刚才使用过的Survivord 的空间。HotSpot 虚拟机中 Eden 和 Survivor 的大小比例是 8：1</p>
<h3 id="标记-整理算法Compacting"><a href="#标记-整理算法Compacting" class="headerlink" title="标记-整理算法Compacting"></a>标记-整理算法Compacting</h3><p>标记： 从根集合进行扫描，对存活对象进行标记</p>
<p>清除： 移动所有存活的对象，且按照内存地址次序依次进行排列，然后将末端内存地址以后的内存全部回收</p>
<p>特点：</p>
<ul>
<li>解决了内存碎片的问题</li>
<li>不用浪费一半的内存进行复制</li>
<li>适用于对象存活率高的场景</li>
</ul>
<img src="/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/compacting.jpg" class="" title="compacting">

<br/>

<h3 id="分代收集算法（Generational-Collector"><a href="#分代收集算法（Generational-Collector" class="headerlink" title="分代收集算法（Generational Collector)"></a>分代收集算法（Generational Collector)</h3><p>​     按照不同区域的对象存活频率来针对的配置 垃圾回收算法，这样效率是最高的</p>
<p>​      现在的商业虚拟机的垃圾收集都采用 “分代收集” （Generational Collection）算法，它是按照对象的存活周期的不同将内存划分为几块。一般将Java堆划分为新生代和老年代，根据各个年代的特点采用最合适的收集算法。 在新生代中，每次垃圾收集中都发现有大批对象死去，只有少量存活，那就选用复制算法，只需要复制少量存活对象，有效降低了复制收集成本。而老年代中因为对象存活率高、没有额外空间对它进行分配担保，就必须使用“标记-清理”或“标记-整理”算法进行回收。</p>
<br/>

<h2 id="垃圾回收操作"><a href="#垃圾回收操作" class="headerlink" title="垃圾回收操作"></a>垃圾回收操作</h2><h3 id="Minor-GC"><a href="#Minor-GC" class="headerlink" title="Minor GC :"></a>Minor GC :</h3><p>​     从年轻代空间（包括 Eden 和 Survivor 区域）回收内存被称为 Minor GC。</p>
<h3 id="Major-GC"><a href="#Major-GC" class="headerlink" title="Major GC:"></a>Major GC:</h3><p>​    针对老年代空间内存回收，其发生之前往往至少会发生一次 Minor GC</p>
 <br/>

<h3 id="FULL-GC"><a href="#FULL-GC" class="headerlink" title="FULL GC:"></a>FULL GC:</h3><p>​       整个堆空间的内存统一回收（包含年轻代和老年代），通常使用 标记-清除 或者 标记 - 整理算法。</p>
<p>触发条件：</p>
<ul>
<li>当老年代空间不足时</li>
<li>JDK7及以下版本永久代空间不足，（永久代,年轻代、老年代这3块区域在堆中，JDK8就没有永久代了，与之替代的是使用本地内存的MetaSpace 元空间）</li>
<li>CMS GC时出现 promotion failed（新建的大对象，年轻代和老年代都放不下时），concurrent mode failure</li>
<li>Minor GC晋升老年代的平均大小 大于 老年代的剩余空间 （ 提前检测，智能啊）</li>
<li>程序中调用 System.gc()</li>
<li>使用RMI来进行RPC或者管理的JDK应用，每小时执行一次 FULL GC</li>
</ul>
 <br/>

<p>Minor GC时， 存活的对象会移动到 From 区域即 Survivor，年龄为 1，将to区域的存活对象移动到 from区域，年龄也 +1，清空To区域</p>
<p>重新交换名称， From 变成了 To, To此时移动了所有对象，区域为空，变成了 From 区域。</p>
<img src="/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/survivor.jpg" class="" title="survivor">

<p>Eden区： 对象被创建时放置的区域</p>
<h3 id="对象如何晋升到老年代"><a href="#对象如何晋升到老年代" class="headerlink" title="对象如何晋升到老年代"></a>对象如何晋升到老年代</h3><ul>
<li>经历一定Minor 次数依然存活的对象</li>
<li>Survivor区中存放不下的对象</li>
<li>新生成的大对象（ -XX: + PretenuerSizeThreshold)</li>
</ul>
 <br/>

<h4 id="常见的调优参数"><a href="#常见的调优参数" class="headerlink" title="常见的调优参数"></a>常见的调优参数</h4><p>- XX:SurvivorRatio： Eden 和 Survivor的比值，默认为 8： 1</p>
<p>- XX:NewRatio: 老年代和年轻代内存大小的比例，默认 2：1</p>
<p>- XX:MaxTenuringThreshold: 对象从年轻代晋升到老年代经过GC次数的最大阈值 ，默认15</p>
<br/>

<h4 id="Survivor区对象晋升位老年代对象的条件"><a href="#Survivor区对象晋升位老年代对象的条件" class="headerlink" title="Survivor区对象晋升位老年代对象的条件"></a>Survivor区对象晋升位老年代对象的条件</h4><p>​         Java虚拟机会记录 Survivor区中的对象一共被来回复制了几次。<strong>如果一个对象被复制的次数为 15 (对应虚拟机参数 -XX:+MaxTenuringThreshold),那么该对象将被晋升为至老年代</strong>，(至于为什么是 15次，原因是 HotSpot会在对象头的中的标记字段里记录年龄，分配到的空间只有4位，所以最多只能记录到15)。</p>
<p>​         另外，<strong>如果单个 Survivor 区已经被占用了 50% (对应虚拟机参数: -XX:TargetSurvivorRatio)，那么较高复制次数的对象也会被晋升至老年代。</strong></p>
<br/>



<h4 id="Stop-the-World"><a href="#Stop-the-World" class="headerlink" title="Stop-the-World"></a>Stop-the-World</h4><ul>
<li>JVM由于要执行GC而停止了应用程序的执行</li>
<li>任何一种GC算法中都会发生</li>
<li>多数GC优化通过减少 Stop-the-world</li>
</ul>
<br/>

<h4 id="Safepoint"><a href="#Safepoint" class="headerlink" title="Safepoint"></a>Safepoint</h4><ul>
<li>分析过程中对象引用关系不会发生变化的点</li>
<li>产生Safepoint的地方： 方法调用，循环跳转，异常跳转等</li>
<li>安全点数量得适中</li>
</ul>
<p>个人理解： 清理某一个区域，如果不能边清理边产生垃圾对象，则需要将这块区域冻结，停止除了垃圾回收线程外的用户线程操作，这块区域就叫做安全点</p>
<br/>



<h2 id="常用的垃圾收集器"><a href="#常用的垃圾收集器" class="headerlink" title="常用的垃圾收集器"></a>常用的垃圾收集器</h2><h3 id="年轻代的垃圾收集器"><a href="#年轻代的垃圾收集器" class="headerlink" title="年轻代的垃圾收集器"></a>年轻代的垃圾收集器</h3><h4 id="Serial-收集器"><a href="#Serial-收集器" class="headerlink" title="Serial 收集器"></a>Serial 收集器</h4><ul>
<li>单线程收集，进行垃圾收集时，必须暂停所有工作线程</li>
<li>简单高效，Client模式下默认的年轻代收集器</li>
<li>-XX: + UseSerialGC ,基于复制算法设计</li>
</ul>
<br/>

<h4 id="ParNew收集器"><a href="#ParNew收集器" class="headerlink" title="ParNew收集器"></a>ParNew收集器</h4><ul>
<li>多线程收集，其余的行为、特点和Serial收集器一样</li>
<li>单核执行效率低于Serial，在多核下执行才有优势</li>
<li>-XX：+ UseParNewGC,基于复制算法设计</li>
</ul>
<br/>

<h4 id="Parallel-Scavenge收集器"><a href="#Parallel-Scavenge收集器" class="headerlink" title="Parallel Scavenge收集器"></a>Parallel Scavenge收集器</h4><ul>
<li>比起前面的收集器关注用户线程停顿时间而已，该收集器更关注系统的吞吐量（ 吞吐量 &#x3D; 用户代码执行时间  &#x2F; (用户代码执行时间 + 垃圾收集时间))</li>
<li>在多核下执行才有优势，Server模式下默认的年轻代收集器</li>
<li>配置： -XX： + UseParallelGC,基于复制算法设计</li>
</ul>
<br/>

<p><strong>这里说明一下：</strong> </p>
<p>停顿时间短意味着与用户交互更频繁，响应速度快，用户体验好。</p>
<p>高吞吐量意味着 CPU的更高效，后台运算性能更佳，适合交互不多的任务。</p>
<p>所以需要根据 系统项目使用的场景来选择相应特点的垃圾收集器。</p>
<br/>



<h3 id="老年代的垃圾收集器"><a href="#老年代的垃圾收集器" class="headerlink" title="老年代的垃圾收集器"></a>老年代的垃圾收集器</h3><h4 id="Serial-Old收集器"><a href="#Serial-Old收集器" class="headerlink" title="Serial Old收集器"></a>Serial Old收集器</h4><ul>
<li>单线程收集，进行垃圾收集时，必须暂停所有工作线程</li>
<li>简单高效，Client模式下默认的老年代收集器</li>
<li>-XX: UserSerialOldGC, 基于标记-整理算法设计</li>
</ul>
<br/>

<h4 id="Parallel-Old收集器"><a href="#Parallel-Old收集器" class="headerlink" title="Parallel Old收集器"></a>Parallel Old收集器</h4><ul>
<li>多线程，吞吐量优先</li>
<li>常与 Parallel Scavenge 这一新生代收集器搭配，适用于 注重吞吐量以及CPU资源敏感的场合</li>
<li>-XX： + UseParallelOldGC,基于标记-整理算法设计</li>
</ul>
<br/>

<h4 id="CMS收集器"><a href="#CMS收集器" class="headerlink" title="CMS收集器"></a>CMS收集器</h4><p>特性：</p>
<ul>
<li>跨时代产品，实现了与用户进程并行的垃圾并发收集</li>
<li>以获取最短回收停顿时间为目标的收集器，</li>
<li>适合 要求系统停顿时间短，服务响应速度快的应用。</li>
<li>-XX: +UseConcMarkSweepGC,基于标记-整理算法设计</li>
</ul>
<p>在周志明出版的深入了解Java虚拟机书中</p>
<p>将CMS收集器的运作过程分为4步</p>
<ul>
<li>初始标记（CMS  initial mark)</li>
<li>并发标记（CMS concurrent mark）</li>
<li>重新标记（CMS remark）</li>
<li>并发清除（CMS concurrent sweep）</li>
</ul>
<br/>

<h4 id="G1收集器"><a href="#G1收集器" class="headerlink" title="G1收集器"></a>G1收集器</h4><p>G1重新定义了堆空间，打破了原有的分代模型，将堆划分为一个个区域。</p>
<ul>
<li><p>并行与并发</p>
</li>
<li><p>可预测的停顿</p>
</li>
<li><p>分代收集</p>
</li>
<li><p>空间整合</p>
</li>
</ul>
<p>配置参数： -XX: +UserG1GC，基于复制+标记-整理算法设计</p>
<p>说明： 由于 CMS与G1垃圾收集器很重要，我将其单独写成一篇博客了来介绍其工作流程和设计思路，这篇我只介绍其特点。</p>
<h3 id="JVM的垃圾收集器之间的组合搭配。"><a href="#JVM的垃圾收集器之间的组合搭配。" class="headerlink" title="JVM的垃圾收集器之间的组合搭配。"></a>JVM的垃圾收集器之间的组合搭配。</h3><p>​       根据垃圾收集器特性，其新生代的垃圾收集器可不能随意地与老年代收集器组合，不同JDK版本诞生的收集器相互之间的兼容性和特性是不同的。</p>
<p>   下图为 垃圾收集器可搭配使用图。</p>
<img src="/2019/10/25/JVM%E7%9A%84%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86/generation.jpg" class="" title="generation">]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM运行时的数据区域和对象访问方式</title>
    <url>/2019/08/01/JVM%E8%BF%90%E8%A1%8C%E6%97%B6%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%E5%92%8C%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>**Java虚拟机：**Java与C++之间进行内存动态分配和垃圾收集技术的虚拟机</p>
<p><strong>JDK（Java Development Kit）：</strong> 由 Java程序设计语言、Java虚拟机、JavaAPI类库这三部分组成，用于支持Java程序开发的最小环境。</p>
<p>**JRE (Java Runtime Environment) ：**由 JavaAPI 类库中的 JavaSE API 子集 和 Java 虚拟机这两部分组成，用于支持Java程序运行的标准环境。</p>
<p>Java虚拟机将其管理的内存划分为若干个不同的数据区域，我们通过对线程、进程经过每个数据区域的作用来了解JVM</p>
<p>运行经过的数据区如图所示：</p>
<img src="/2019/08/01/JVM%E8%BF%90%E8%A1%8C%E6%97%B6%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%E5%92%8C%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/b1.jpg" class="" title="数据区">
<span id="more"></span>

<p><strong>程序计数器（Program Counter Register）:</strong></p>
<p>​       一块较小的内存空间，可以看作当前线程执行的字节码的行号指示器。为了让多线程切换后能恢复到正确的执行位置，每条线程都需要有一个独立的程序计数器，各条线程之间的计数器互不影响。这个计数器的值是选取下一条需要执行的字节码指令，分支、循环、跳转、异常处理、线程恢复等基础功能都需要依赖这个计数器来完成，线程私有。</p>
<p><strong>Java虚拟机栈（Java Virtual Machine Stacks）:</strong></p>
<p>​       描述Java方法执行的内存模型，每一个方法被执行时会同时创建一个栈帧（Stack Frame）用于<strong>存储局部变量表</strong>、操作数栈、动态链接、方法出口等信息。每一个方法被调用直至执行完成的过程，就对应着一个栈帧在虚拟机栈从入栈到出栈的过程。线程私有。</p>
<p><strong>本地方法栈（Native Method Stacks）:</strong></p>
<p>​       本地方法栈与虚拟机栈发挥的作用十分相似，其区别在于虚拟机栈执行Java方法（也就是字节码）服务，而本地方法栈则是为虚拟机使用到的Native方法服务。它们都会抛出 StackOverflowError 和 OutOfMemoryError 异常。</p>
<p><strong>Java堆（Java Heap）:</strong></p>
<p>​      Java虚拟机所管理的内存中最大的一块，被所有线程共享，在虚拟机启动时创建。该内存区域唯一目的就是<strong>存放对象实例</strong>，它是垃圾收集器管理的主要区域，因此也被成为“GC堆”（Garbage Collected Heap）</p>
<p><strong>方法区（Method Area）:</strong></p>
<p>​       线程共享，用于存储已被<strong>虚拟机加载的类信息、常量、静态变量</strong>、即时编译器编译后的代码等数据。和 Java堆不需要连续内存和可以选择固定大小或者可扩展外，还可以选择不实现垃圾收集。当方法区无法满足内存分配需求时将抛出OutOfMemoryError异常。</p>
<h2 id="那么在JAVA语言中，对象访问是如何进行的呢？"><a href="#那么在JAVA语言中，对象访问是如何进行的呢？" class="headerlink" title="那么在JAVA语言中，对象访问是如何进行的呢？"></a><strong>那么在JAVA语言中，对象访问是如何进行的呢？</strong></h2><p>哪怕是最简单的对象访问也会涉及 Java栈、 Java堆、 方法区这三个最重要内存区域之间的关联关系。如下面的这句代码：</p>
<p>​          Object obj &#x3D; new Object();</p>
<p>​        假设这句代码出现在方法体中，那“Object obj” 这部分的语义将会反映到 Java栈的本地变量表中，作为一个reference（引用）类型出现。</p>
<p>​       “new Object()”这部分的语义将会反映到 Java堆中，形成一块存储了Object类型所有实例数据值（Instance Data,对象中各个实例字段的数据）的结构化内存，根据具体类型以及虚拟机实现的对象内存布局的不停，这块内存的长度是不固定的。另外，在Java堆中还必须包含能找到能查找到此对象类型数据（如对象类型、父类、实现的接口、方法等）的地址信息，这些类型数据则存储方法区中。</p>
<p>是不是觉得有点难懂？</p>
<p>​       总结来说， Java栈的本地变量表存放了对象的引用类型，Java堆存放具体实例字段数据和类型数据的地址，方法区存放了该对象类型数据，比如其父类，对象类型，实现了什么接口，具体有什么方法等</p>
<img src="/2019/08/01/JVM%E8%BF%90%E8%A1%8C%E6%97%B6%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%E5%92%8C%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/b2.jpg" class="" title="内存示例">


<p>​         由于reference类型在Java虚拟机规范里面只规定了一个指向对象的引用，并没有定义这个引用应该通过哪种方式去定位，以及访问到Java堆中的对象的具体位置，因此不同虚拟机实现的对象访问方式会有所不同，主流的访问方式有两种：使用句柄和直接指针。</p>
<h2 id="对象访问方式"><a href="#对象访问方式" class="headerlink" title="对象访问方式"></a>对象访问方式</h2><p><strong>使用句柄的对象访问方式：</strong></p>
<p>​         Java堆中将会划分出一块内存来作为句柄池，reference中存储的就是对象的句柄地址，而句柄中包含了对象实例数据和类型数据各自的具体地址信息，如下图所示         </p>
<img src="/2019/08/01/JVM%E8%BF%90%E8%A1%8C%E6%97%B6%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%E5%92%8C%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/b3.jpg" class="" title="句柄">


<p><strong>使用直接指针访问方式：</strong></p>
<p>​       Java堆对象的布局中就必须考虑如何放置访问类型数据的相关信息，reference中直接存储就是对象地址。如下图所示</p>
<img src="/2019/08/01/JVM%E8%BF%90%E8%A1%8C%E6%97%B6%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%E5%92%8C%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/b4.jpg" class="" title="直接指针">



<p>​      这两种对象的访问方式各有优势，使用句柄访问方式的最大好处就是reference中存储的是稳定的句柄地址，在对象被移动（垃圾收集时移动是非常普遍的行为）时只会改变句柄中的实例数据指针，而reference本身不需要被修改。</p>
<p>​     使用直接指针访问方式的最大好处就是速度更快，节省了一次指针定位的时间开销，由于对象的访问在Java中非常频繁，因为这类开销的执行成本也不小。</p>
<h2 id="总结："><a href="#总结：" class="headerlink" title="总结："></a><strong>总结：</strong></h2><p>我们可以把这两种方式看做一级指针访问与二级指针访问的差异比较。句柄访问方式属于二级指针的访问方式，当对象移动十分频繁时，这种访问方式不需要改变Java栈里本地变量表的信息，需要改变的句柄池的指针地址。（改变句柄池的指针地址开销比改变Java栈的变量表信息开销的小）。而直接指针访问方式就是节省访问开销，当系统对象被高频率访问时这种访问方式更好。</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>ConcurrentHashMap源码解析</title>
    <url>/2019/12/27/ConcurrentHashMap%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h3 id="题外话"><a href="#题外话" class="headerlink" title="题外话"></a>题外话</h3><p>说来惭愧，倒腾了几个晚上，也没有完全明白其中的逻辑，只是对其底层组成和核心方法流程有了较深的了解，了解了多线程如何帮助扩容转移节点，这篇博客主讲JDK8的版本，希望对各位理解ConcurrentHashMap有一点帮助。</p>
<br/>
## ConcurrentHashMap结构组成

<p>ConcurrentHashMap 底层结构的实现上与 HashMap一样，也是基于 数组 + 链表 + 红黑树的结果</p>
<span id="more"></span>

  <img src="/2019/12/27/ConcurrentHashMap%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90/tree.jpg" class="" title="结构图">

<br/>

<p>不同的处是赋予了线程操作安全的属性，如何做到线程安全的呢？</p>
<hr>
<h3 id="基础结构"><a href="#基础结构" class="headerlink" title="基础结构"></a>基础结构</h3><p>在介绍之前，必须先介绍其核心结构（我之前打算直接看核心方法的，，后面发现理解基础结构Node的字段对代码的理解和了解并发设计有很大帮助）</p>
<p>所以这里先介绍 节点Node</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;K,V&gt; <span class="keyword">implements</span> <span class="title class_">Map</span>.Entry&lt;K,V&gt; &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="type">int</span> hash;</span><br><span class="line">    <span class="keyword">final</span> K key;</span><br><span class="line">    <span class="keyword">volatile</span> V val;</span><br><span class="line">    <span class="keyword">volatile</span> Node&lt;K,V&gt; next;</span><br><span class="line">    ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<hr>
<h3 id="状态标志"><a href="#状态标志" class="headerlink" title="状态标志"></a>状态标志</h3><p>这里主要关注这个属性字段  hash，如果是数组下标的头节点，那这个节点的hash值就不是我们认识的那个 32的hash了，而是变成了并发中用来表示线程操作的状态标志（你现在不需要理解，暂时记住，待会看put方法你就明白了）。</p>
<p>3种特殊的状态标志 ，当下标的节点（注意：是头节点！！） </p>
<p>Node.hash &#x3D; MOVED即 -1时代表正在扩容或者初始化，为 TREEBIN 即-2代表为 红黑树的根节点  为RESERVED 即-3时 这种源码中解释是用来做 占位符的标志</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">MOVED</span>     <span class="operator">=</span> -<span class="number">1</span>; <span class="comment">// hash 表达转移节点</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TREEBIN</span>   <span class="operator">=</span> -<span class="number">2</span>; <span class="comment">//  hash 表达 树的根节点</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">RESERVED</span>  <span class="operator">=</span> -<span class="number">3</span>; <span class="comment">// hash 表达暂时保留</span></span><br></pre></td></tr></table></figure>

<p>所以只有 头节点的hash &gt;&#x3D; 0才代表该节点为 正常状态的链表节点 ！！</p>
 <br/>

<p>当然红黑树为了匹配多线程时的各种状态，它也和HashMap中的TreeNode的不一样，</p>
<p>是做了封装的TreeBin，它拥有了等待锁和持有读锁或写锁的状态标志。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">TreeBin</span>&lt;K,V&gt; <span class="keyword">extends</span> <span class="title class_">Node</span>&lt;K,V&gt; &#123;</span><br><span class="line">    TreeNode&lt;K,V&gt; root;</span><br><span class="line">    <span class="keyword">volatile</span> TreeNode&lt;K,V&gt; first;</span><br><span class="line">    <span class="keyword">volatile</span> Thread waiter;</span><br><span class="line">    <span class="keyword">volatile</span> <span class="type">int</span> lockState;</span><br><span class="line">    <span class="comment">// values for lockState</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">WRITER</span> <span class="operator">=</span> <span class="number">1</span>; <span class="comment">// 设置当持有写锁的时候</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">WAITER</span> <span class="operator">=</span> <span class="number">2</span>; <span class="comment">// 设置当等待请求写锁的时候</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">READER</span> <span class="operator">=</span> <span class="number">4</span>; <span class="comment">// 读锁时自增值</span></span><br><span class="line">    ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<hr>
<h2 id="核心方法"><a href="#核心方法" class="headerlink" title="核心方法"></a>核心方法</h2><h3 id="put"><a href="#put" class="headerlink" title="put"></a>put</h3><h4 id="方法源码"><a href="#方法源码" class="headerlink" title="方法源码"></a>方法源码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> V <span class="title function_">put</span><span class="params">(K key, V value)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> putVal(key, value, <span class="literal">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> V <span class="title function_">putVal</span><span class="params">(K key, V value, <span class="type">boolean</span> onlyIfAbsent)</span> &#123;</span><br><span class="line">    <span class="comment">// ConcurrentHashMap 中 key value不允许为null</span></span><br><span class="line">    <span class="keyword">if</span> (key == <span class="literal">null</span> || value == <span class="literal">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>();</span><br><span class="line">    <span class="comment">//进行hash重新运算 (h ^ (h &gt;&gt;&gt; 16)) &amp; 0x7fffffff，官方解释 这种做法使得位扩散更合理，碰撞分布更均匀</span></span><br><span class="line">    <span class="comment">// 这个spread(hash)下来，保留了高16位，低16位进行了重新计算，然后又&amp; HASHBIT使得最高位为0，其他位不变</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">hash</span> <span class="operator">=</span> spread(key.hashCode());</span><br><span class="line">    <span class="type">int</span> <span class="variable">binCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (Node&lt;K,V&gt;[] tab = table;;) &#123;</span><br><span class="line">        Node&lt;K,V&gt; f; <span class="type">int</span> n, i, fh;</span><br><span class="line">        <span class="comment">// 当 tab为空,初始化table，该方法是做了线程同步CAS判断的，在下方有对initTable详细的解释</span></span><br><span class="line">        <span class="keyword">if</span> (tab == <span class="literal">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">            tab = initTable();</span><br><span class="line">        <span class="comment">//tabAt方法调用的是Unsafe.getObjectVolatile(tab, ((long)i &lt;&lt; ASHIFT) + ABASE) 可以直接获取指定内存的数据，保证了每次拿到的数组下标数据都是最新的</span></span><br><span class="line">        <span class="comment">// 注意此时已经 f = 数组下标下头节点， i 为 数组下标</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i = (n - <span class="number">1</span>) &amp; hash)) == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 这里cas判断数组下标下 i 的头节点 f是否为空并赋值</span></span><br><span class="line">            <span class="keyword">if</span> (casTabAt(tab, i, <span class="literal">null</span>,</span><br><span class="line">                         <span class="keyword">new</span> <span class="title class_">Node</span>&lt;K,V&gt;(hash, key, value, <span class="literal">null</span>)))</span><br><span class="line">                <span class="keyword">break</span>;                   <span class="comment">// no lock when adding to empty bin</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//此时说明 头节点f不为空 检查 f.hash == (MOVED为 -1) 内部移动，证明在扩容操作,待会看一下扩容时是否将节点hash重新赋值 MOVED标志位</span></span><br><span class="line">        <span class="comment">// 此时帮助还未转移的旧数组节点转移到新数组中</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">            tab = helpTransfer(tab, f);</span><br><span class="line">        <span class="keyword">else</span> &#123; </span><br><span class="line">            <span class="comment">// 当数组不在扩容时， 数组下标下有节点时，synchronize锁住该下标的头结点，f在上面已经赋值 数组对应下标的链表头节点</span></span><br><span class="line">            <span class="type">V</span> <span class="variable">oldVal</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">             <span class="comment">// 给当前数组下标的Node头结点加锁</span></span><br><span class="line">            <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123; <span class="comment">// fh &gt;=0 意味着 不在扩容 MOVED - 1，也不是红黑树 TREEBIN -2</span></span><br><span class="line">                        binCount = <span class="number">1</span>;</span><br><span class="line">                        <span class="comment">// 在遍历链表时 ++bitCount，所以 bitCount代表链表的长度</span></span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K,V&gt; e = f;; ++binCount) &#123;</span><br><span class="line">                            K ek;</span><br><span class="line">                            <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                                ((ek = e.key) == key ||</span><br><span class="line">                                 (ek != <span class="literal">null</span> &amp;&amp; key.equals(ek)))) &#123;</span><br><span class="line">                                oldVal = e.val;</span><br><span class="line">                                <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                    e.val = value;</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                            Node&lt;K,V&gt; pred = e;</span><br><span class="line">                            <span class="keyword">if</span> ((e = e.next) == <span class="literal">null</span>) &#123;</span><br><span class="line">                                pred.next = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;K,V&gt;(hash, key,</span><br><span class="line">                                                          value, <span class="literal">null</span>);</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 判断节点是红黑树时，进行红黑树的插入操作</span></span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123;</span><br><span class="line">                        Node&lt;K,V&gt; p;</span><br><span class="line">                        binCount = <span class="number">2</span>;</span><br><span class="line">                        <span class="keyword">if</span> ((p = ((TreeBin&lt;K,V&gt;)f).putTreeVal(hash, key,</span><br><span class="line">                                                       value)) != <span class="literal">null</span>) &#123;</span><br><span class="line">                            oldVal = p.val;</span><br><span class="line">                            <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                p.val = value;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 这一步做链表长度的判断，当&gt;= 8时转换成红黑树</span></span><br><span class="line">            <span class="keyword">if</span> (binCount != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD)</span><br><span class="line">                    treeifyBin(tab, i);</span><br><span class="line">                <span class="keyword">if</span> (oldVal != <span class="literal">null</span>)</span><br><span class="line">                    <span class="keyword">return</span> oldVal;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 对当前容量大小进行检查，如果超过了阈值threshold（数组大小* 负载因子）就需要扩容</span></span><br><span class="line">    addCount(<span class="number">1L</span>, binCount);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="流程总结"><a href="#流程总结" class="headerlink" title="流程总结"></a>流程总结</h4><p>1、 先判断 传入的 key-value 是否为null，为null抛出异常</p>
<p>2、 spread方法对key重新hash，这样能保证分布到数组中更加的均匀</p>
<p>3 、在循环中进行判断</p>
<p>当tab为空时，进行初始化操作，默认数组大小16</p>
<p>否则 计算hash对应的下标 i，并获取下标对应头节点 f，当头节点为空时CAS直接插入链表节点；</p>
<p>否则判断头节点的 hash</p>
<p>4、当 hash &#x3D; MOVED即-1，代表数组正在扩容或者初始化，此时让该线程帮助转移其他下标节点</p>
<p>5、其hash为其他状态时，用synchronized锁住当前头节点</p>
<p>通过头节点，遍历当前下标，用bitcount记录节点数</p>
<p> <strong>tabAt(tab, i) &#x3D;&#x3D; f</strong> 重新判断当前下标i下的头节点是否还是f，这里还是用到cas </p>
<p>当hash标志位大于0，证明是正常状态下链表节点，直接遍历链表，插入到尾部</p>
<p>否则就是TreeBin 红黑树 ，进行树节点的判断、插入和旋转</p>
<p>6、此时判断bitCount大于等于8时，进行链表转换成红黑树</p>
<p>7、 addCount检查当前容量大小，当超过阈值时触发扩容 transfer</p>
<p>这里看一下上面的CAS判断，全部使用C++编写的 Unsafe类Native方法，确保直接从内存中取值，保证数据的一致性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> &lt;K,V&gt; Node&lt;K,V&gt; <span class="title function_">tabAt</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="type">int</span> i)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (Node&lt;K,V&gt;)U.getObjectVolatile(tab, ((<span class="type">long</span>)i &lt;&lt; ASHIFT) + ABASE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> &lt;K,V&gt; <span class="type">boolean</span> <span class="title function_">casTabAt</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="type">int</span> i,</span></span><br><span class="line"><span class="params">                                    Node&lt;K,V&gt; c, Node&lt;K,V&gt; v)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> U.compareAndSwapObject(tab, ((<span class="type">long</span>)i &lt;&lt; ASHIFT) + ABASE, c, v);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>因为PUT方法中使用到了initTable，这里先看InitTable方法</p>
<h3 id="initTable"><a href="#initTable" class="headerlink" title="initTable"></a>initTable</h3><p>介绍initTable方法前，必须先解释一个重要的参数 SizeCtl</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 表初始化和大小调整控制。 如果为负，则表正在初始化或调整大小：-1用于初始化， else-（1 +活动的调整大小   *线程数）。</span></span><br><span class="line"><span class="comment"> * 除此以外，当table为null时，保留要使用的初始表大小创建，或默认为0。 </span></span><br><span class="line"><span class="comment"> * 初始化后，记录下一个要调整表大小的元素计数值。</span></span><br><span class="line"><span class="comment"> * 注意： 也就是说 大于0时 代表已经初始化了，记录的是下一次的扩容后的阈值</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"> <span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="type">int</span> sizeCtl;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">/ 使用sizeCtl中记录的大小初始化表。</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Node&lt;K,V&gt;[] initTable() &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; <span class="type">int</span> sc;</span><br><span class="line">    <span class="keyword">while</span> ((tab = table) == <span class="literal">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 从上面sizeCtl 解释可知，当 sizeCtl &lt; 0 时，证明其他线程已在初始化数组，所以 thread.yield 让出当前线程执行权，保证只有一个线程在初始化数组</span></span><br><span class="line">        <span class="keyword">if</span> ((sc = sizeCtl) &lt; <span class="number">0</span>)</span><br><span class="line">            Thread.<span class="keyword">yield</span>(); </span><br><span class="line">        <span class="comment">// U是unSafe类,这里再判断一次如果 SIZECTL为0，证明无其他线程初始化tab, 设置 SIZECTL--,禁止其他线程修改tab大小，如果SIZECTL&lt;0,则进行下次循环判断</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="built_in">this</span>, SIZECTL, sc, -<span class="number">1</span>)) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 这里为什么还要判断一次不太明白，毕竟前面CAS已经做了判断，此时应该没有其他线程可以操作，除非是扩容时有其他操作，咋们待会再看</span></span><br><span class="line">                <span class="keyword">if</span> ((tab = table) == <span class="literal">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// 注意此时sc &gt;= 0,只有 sc = 0的时候才等于DEFAULT_CAPACITY 16，实际上当sc &gt; 0时，代表此时数组是已经初始化过的，应该是不会进入initTable该方法了</span></span><br><span class="line">                    <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> (sc &gt; <span class="number">0</span>) ? sc : DEFAULT_CAPACITY;</span><br><span class="line">                    <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">                    Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])<span class="keyword">new</span> <span class="title class_">Node</span>&lt;?,?&gt;[n];</span><br><span class="line">                    table = tab = nt;</span><br><span class="line">                    <span class="comment">// 如果 之前sc =0，则此时 n - 0.25n = 0.75n,也就是阈值 threshold，这种做法就是 取当前数组大小* 0.75</span></span><br><span class="line">                    sc = n - (n &gt;&gt;&gt; <span class="number">2</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="comment">// sizeCtl记录了下一次调整数组大小的阈值</span></span><br><span class="line">                sizeCtl = sc;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>​        我们可以看到， ConcurrentHashMap中的数组初始化方法 initTable使用了 CAS来做了 乐观锁同步判断，判断的标志是 sizeCtl,如果现在还对 sizeCtl 迷糊的同学建议再往上重新看一下 sizeCtl的定义，</p>
<p>sizeCtl : volatile修饰，判断是否有线程在修改数组大小，为正数时，代表下一次的扩容的阈值</p>
<p>还记得put方法中当头节点的hash &#x3D; MOVED即-1时，帮助转移吗？</p>
<p>接下来解析该方法</p>
<h3 id="helpTransfer"><a href="#helpTransfer" class="headerlink" title="helpTransfer"></a>helpTransfer</h3><p>开始之前，这里解释一下 ForwardingNode ，这是一个 静态内部类 static final class ForwardingNode&lt;K,V&gt; extends Node&lt;K,V&gt;</p>
<p>它HASH属性默认是 MOVED移动状态，也就是说这个节点默认是 MOVED移动状态的节点，可以用来做节点的状态比较判断,也可作为一个扩容扫描时下标跳跃的标志。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ForwardingNode</span>&lt;K,V&gt; <span class="keyword">extends</span> <span class="title class_">Node</span>&lt;K,V&gt; &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;K,V&gt;[] nextTable;</span><br><span class="line">    ForwardingNode(Node&lt;K,V&gt;[] tab) &#123;</span><br><span class="line">        <span class="built_in">super</span>(MOVED, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line">        <span class="built_in">this</span>.nextTable = tab;</span><br><span class="line">    &#125; </span><br><span class="line">    ...</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>还有 nextTable,它是下一次扩容时使用数组，扩容时不为null</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;K,V&gt;[] nextTable;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如果f节点在移动状态，则帮助转移</span></span><br><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] helpTransfer(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt; f) &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] nextTab; <span class="type">int</span> sc;</span><br><span class="line">    <span class="keyword">if</span> (tab != <span class="literal">null</span> &amp;&amp; (f <span class="keyword">instanceof</span> ForwardingNode) &amp;&amp;</span><br><span class="line">        (nextTab = ((ForwardingNode&lt;K,V&gt;)f).nextTable) != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// resizeStamp 方法返回用于调整大小为n的表的标记位。向左移动RESIZE_STAMP_SHIFT时必须为负。</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">rs</span> <span class="operator">=</span> resizeStamp(tab.length);</span><br><span class="line">        <span class="keyword">while</span> (nextTab == nextTable &amp;&amp; table == tab &amp;&amp;</span><br><span class="line">               (sc = sizeCtl) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">             <span class="comment">// 如果 sizeCtl 无符号右移  16 不等于 rs （ sc前 16 位如果不等于标识符，则标识符变化了）</span></span><br><span class="line">            <span class="comment">// 或者 sizeCtl == rs + 1  （扩容结束了，不再有线程进行扩容）（默认第一个线程设置 sc ==rs 左移 16 位 + 2，当第一个线程结束扩容了，就会将 sc 减一。这个时候，sc 就等于 rs + 1）</span></span><br><span class="line">            <span class="comment">// 或者 sizeCtl == rs + 65535  （如果达到最大帮助线程的数量，即 65535）</span></span><br><span class="line">            <span class="comment">// 或者转移下标正在调整 （扩容结束）</span></span><br><span class="line">            <span class="comment">// 结束循环，返回 table</span></span><br><span class="line">            <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + <span class="number">1</span> ||</span><br><span class="line">                sc == rs + MAX_RESIZERS || transferIndex &lt;= <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">// 如果以上都不是, 将 sizeCtl + 1, （表示增加了一个线程帮助其扩容）</span></span><br><span class="line">            <span class="keyword">if</span> (U.compareAndSwapInt(<span class="built_in">this</span>, SIZECTL, sc, sc + <span class="number">1</span>)) &#123;</span><br><span class="line">                <span class="comment">//transfer 将每个bin中的节点移动和/或复制到新表中。,此时sc + 1，,SIZECTL就是2的15次方？？？这个数有什么用呢</span></span><br><span class="line">                transfer(tab, nextTab);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> nextTab;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> table;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>好，我们梳理一下helpTransfer的流程</p>
<p>1、判断当数组不为空，头节点处于移动标志时进行操作，不然返回原数组。</p>
<p>2、判断是否并发修改了，判断是否还在扩容。</p>
<p>如果还在扩容，判断标识符是否变化，判断扩容是否结束，判断是否达到最大线程数，判断扩容转移下标是否在调整（扩容结束），如果满足任意条件，结束循环。</p>
<p>如果不满足，并发转移原数组节点。</p>
<p>这个方法中的 sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) !&#x3D; rs || sc &#x3D;&#x3D; rs + 1我都没怎么弄明白意义是什么，网上说是addCount方法中一个判断触发，但我未能理解。</p>
<h3 id="transfer"><a href="#transfer" class="headerlink" title="transfer"></a>transfer</h3><h4 id="源码解析"><a href="#源码解析" class="headerlink" title="源码解析"></a>源码解析</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">transfer</span><span class="params">(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt;[] nextTab)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> tab.length, stride;</span><br><span class="line">    <span class="comment">// 当CPU核数大于1时，stride 等于 桶的数量/8 再除 CPU核数，不然就一个CPU处理所有桶</span></span><br><span class="line">    <span class="comment">//  MIN_TRANSFER_STRIDE 默认16,该值至少应为 DEFAULT_CAPACITY。</span></span><br><span class="line">    <span class="comment">// 这里我们分析一下 当CPU为8核时，stride为0 &lt; MIN_TRANSFER_STRIDE  所以stride = 16</span></span><br><span class="line">    <span class="comment">// 从现有服务器的核数分析 这么基本就是让 stride为16，为什么？  </span></span><br><span class="line">    <span class="comment">// 这里的目的是让每个 CPU 处理的桶一样多，避免出现转移任务不均匀的现象，如果桶较少的话，默认一个 CPU（一个线程）处理 16 个桶</span></span><br><span class="line">    <span class="comment">// 所以我猜测这里是让每个线程处理16个下标对应的桶节点</span></span><br><span class="line">    <span class="keyword">if</span> ((stride = (NCPU &gt; <span class="number">1</span>) ? (n &gt;&gt;&gt; <span class="number">3</span>) / NCPU : n) &lt; MIN_TRANSFER_STRIDE)</span><br><span class="line">        stride = MIN_TRANSFER_STRIDE; <span class="comment">// subdivide range</span></span><br><span class="line">        <span class="comment">// 当扩容数组为空时</span></span><br><span class="line">    <span class="keyword">if</span> (nextTab == <span class="literal">null</span>) &#123;            <span class="comment">// initiating</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span> <span class="comment">// 容量翻倍 n&lt;&lt;1</span></span><br><span class="line">            Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])<span class="keyword">new</span> <span class="title class_">Node</span>&lt;?,?&gt;[n &lt;&lt; <span class="number">1</span>];</span><br><span class="line">            nextTab = nt;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable ex) &#123;      <span class="comment">// try to cope with OOME</span></span><br><span class="line">            sizeCtl = Integer.MAX_VALUE;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// nextTable 是成员变量 、 更新 transferIndex 转移下标，就是原来tab的length</span></span><br><span class="line">        nextTable = nextTab;</span><br><span class="line">        transferIndex = n;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> <span class="variable">nextn</span> <span class="operator">=</span> nextTab.length;</span><br><span class="line">    <span class="comment">//  创建一个 fwd 节点，用于占位。当别的线程发现这个槽位中是 fwd 类型的节点，则跳过这个节点。</span></span><br><span class="line">    ForwardingNode&lt;K,V&gt; fwd = <span class="keyword">new</span> <span class="title class_">ForwardingNode</span>&lt;K,V&gt;(nextTab);</span><br><span class="line">    <span class="comment">//  首次推进为 true，如果等于 true，说明需要再次推进一个下标（i--），反之，如果是 false，那么就不能推进下标，需要将当前的下标处理完毕才能继续推进</span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">advance</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="comment">// 确定数组扩容结束标志</span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">finishing</span> <span class="operator">=</span> <span class="literal">false</span>; <span class="comment">// to ensure sweep before committing nextTab</span></span><br><span class="line">    <span class="comment">//   死循环,i 表示下标，bound 表示当前线程可以处理的当前桶区间最小下标</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>, bound = <span class="number">0</span>;;) &#123;</span><br><span class="line">        Node&lt;K,V&gt; f; <span class="type">int</span> fh;</span><br><span class="line">        <span class="comment">//  如果当前线程可以向后推进；这个循环就是控制 i 递减。同时，每个线程都会进入这里取得自己需要转移的桶的区间</span></span><br><span class="line">        <span class="keyword">while</span> (advance) &#123;</span><br><span class="line">            <span class="type">int</span> nextIndex, nextBound;</span><br><span class="line">              <span class="comment">// 对 i 减一，判断是否大于等于 bound （正常情况下，如果大于 bound 不成立，说明该线程上次领取的任务已经完成了。那么，需要在下面继续领取任务）</span></span><br><span class="line">            <span class="comment">// 如果对 i 减一大于等于 bound（还需要继续做任务），或者完成了，修改推进状态为 false，不能推进了。任务成功后修改推进状态为 true。</span></span><br><span class="line">            <span class="comment">// 通常，第一次进入循环，i-- 这个判断会无法通过，从而走下面的 nextIndex 赋值操作（获取最新的转移下标）。</span></span><br><span class="line">            <span class="comment">//其余情况都是：如果可以推进，将 i 减一，然后修改成不可推进。如果 i 对应的桶处理成功了，改成可以推进。</span></span><br><span class="line">             <span class="keyword">if</span> (--i &gt;= bound || finishing)</span><br><span class="line">                advance = <span class="literal">false</span>;<span class="comment">// 这里设置 false，是为了防止在没有成功处理一个桶的情况下却进行了推进</span></span><br><span class="line">            <span class="comment">// 这里的目的是：1. 当一个线程进入时，会选取最新的转移下标。2. 当一个线程处理完自己的区间时，如果还有剩余区间的没有别的线程处理。再次获取区间。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((nextIndex = transferIndex) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 如果小于等于0，说明没有区间了 ，i 改成 -1，推进状态变成 false，不再推进，表示，扩容结束了，当前线程可以退出了</span></span><br><span class="line">                <span class="comment">// 这个 -1 会在下面的 if 块里判断，从而进入完成状态判断</span></span><br><span class="line">                i = -<span class="number">1</span>;</span><br><span class="line">                advance = <span class="literal">false</span>;<span class="comment">// 这里设置 false，是为了防止在没有成功处理一个桶的情况下却进行了推进</span></span><br><span class="line">            &#125;<span class="comment">// CAS 修改 transferIndex，即 length - 区间值，留下剩余的区间值供后面的线程使用</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt</span><br><span class="line">                     (<span class="built_in">this</span>, TRANSFERINDEX, nextIndex,</span><br><span class="line">                      nextBound = (nextIndex &gt; stride ?</span><br><span class="line">                                   nextIndex - stride : <span class="number">0</span>))) &#123;</span><br><span class="line">                bound = nextBound;<span class="comment">// 这个值就是当前线程可以处理的最小当前区间最小下标</span></span><br><span class="line">                i = nextIndex - <span class="number">1</span>; <span class="comment">// 初次对i 赋值，这个就是当前线程可以处理的当前区间的最大下标</span></span><br><span class="line">                advance = <span class="literal">false</span>; <span class="comment">// 这里设置 false，是为了防止在没有成功处理一个桶的情况下却进行了推进，这样对导致漏掉某个桶。下面的 if (tabAt(tab, i) == f) 判断会出现这样的情况。</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">       <span class="comment">// 注意，这里的n是原数组大小</span></span><br><span class="line">       <span class="comment">// 当i为负数，应该是代表扩容区间扫描完了</span></span><br><span class="line">       <span class="comment">// i &gt;= n 代表到了 新数组的后半段区间</span></span><br><span class="line">       <span class="comment">// 这里 i + n &gt;= nextn,nextn是新数组大小，从而推导出 i此时在后半段区间</span></span><br><span class="line">        <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= n || i + n &gt;= nextn) &#123;</span><br><span class="line">            <span class="type">int</span> sc;</span><br><span class="line">            <span class="comment">// 当转移结束，sizeCtl为 2n * 0.75 即下一次扩容大小，原数组正式替换成新扩容后的数组</span></span><br><span class="line">            <span class="keyword">if</span> (finishing) &#123;</span><br><span class="line">                nextTable = <span class="literal">null</span>;</span><br><span class="line">                table = nextTab;</span><br><span class="line">                sizeCtl = (n &lt;&lt; <span class="number">1</span>) - (n &gt;&gt;&gt; <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;<span class="comment">// 如果没完成扩容</span></span><br><span class="line">          <span class="keyword">if</span> (U.compareAndSwapInt(<span class="built_in">this</span>, SIZECTL, sc = sizeCtl, sc - <span class="number">1</span>)) &#123;</span><br><span class="line">              <span class="comment">// 尝试将 sc -1. 表示这个线程结束帮助扩容了，将 sc 的低 16 位减一。 </span></span><br><span class="line">              <span class="keyword">if</span> ((sc - <span class="number">2</span>) != resizeStamp(n) &lt;&lt; RESIZE_STAMP_SHIFT)</span><br><span class="line">              <span class="comment">// 如果 sc - 2 不等于标识符左移 16 位。如果他们相等了，说明没有线程在帮助他们扩容了。也就是说，扩容结束了。 </span></span><br><span class="line">              <span class="keyword">return</span>;<span class="comment">// 不相等，说明没结束，当前线程结束方法。 </span></span><br><span class="line">              finishing = advance = <span class="literal">true</span>;<span class="comment">// 如果相等，扩容结束了，更新 finising 变量 </span></span><br><span class="line">              i = n; <span class="comment">// 再次循环检查一下整张表 </span></span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">        &#125; <span class="comment">// 当数组对应下标节点为空时，填充为fwd占位标志，代表该节点不需要理会，可直接跳过，注意这里 f被赋值成下标首节点</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i)) == <span class="literal">null</span>)</span><br><span class="line">            advance = casTabAt(tab, i, <span class="literal">null</span>, fwd);</span><br><span class="line">        <span class="comment">// 如果状态为 MOVED，代表有其他线程正在帮助该下标转移数据，推进标志advance，代表可替换到下一个节点</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED) </span><br><span class="line">            advance = <span class="literal">true</span>; <span class="comment">// already processed</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 经过上述判断，确定了当前节点处于正常状态，加同步锁进行数据转移，f在上面判断赋值为当前下标的首节点</span></span><br><span class="line">            <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                 <span class="comment">// 判断 i 下标处的桶节点是否和 f 相同</span></span><br><span class="line">                <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                     Node&lt;K,V&gt; ln, hn;<span class="comment">// low, height 高位桶，低位桶</span></span><br><span class="line">                    <span class="comment">// 如果 f 的 hash 值大于 0,这代表是正常的链表节点 。TreeBin 的 hash 是 -2</span></span><br><span class="line">                    <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">// 对老长度进行与运算（第一个操作数的的第n位于第二个操作数的第n位如果都是1，那么结果的第n为也为1，否则为0）</span></span><br><span class="line">                         <span class="comment">// 注意这里 n是2次幂，所以二进制编码中只有一个1，那么取于 length 只有 2 种结果，一种是 0，一种是1 </span></span><br><span class="line">                        <span class="type">int</span> <span class="variable">runBit</span> <span class="operator">=</span> fh &amp; n;</span><br><span class="line">                        Node&lt;K,V&gt; lastRun = f;</span><br><span class="line">                        <span class="comment">// 遍历链表节点</span></span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K,V&gt; p = f.next; p != <span class="literal">null</span>; p = p.next) &#123;</span><br><span class="line">                            <span class="type">int</span> <span class="variable">b</span> <span class="operator">=</span> p.hash &amp; n;</span><br><span class="line">                            <span class="keyword">if</span> (b != runBit) &#123;</span><br><span class="line">                                runBit = b;</span><br><span class="line">                                lastRun = p;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="comment">// 当runBit为0，放置在低位桶中 ，这里如果看不明白，可以看一下我hashmap对于原桶中链表节点分散成两个链表的处理过程</span></span><br><span class="line">                        <span class="keyword">if</span> (runBit == <span class="number">0</span>) &#123;</span><br><span class="line">                            ln = lastRun;</span><br><span class="line">                            hn = <span class="literal">null</span>;</span><br><span class="line">                        &#125; <span class="comment">// 否则放置在高位桶</span></span><br><span class="line">                        <span class="keyword">else</span> &#123;</span><br><span class="line">                            hn = lastRun;</span><br><span class="line">                            ln = <span class="literal">null</span>;</span><br><span class="line">                        &#125;&#125;<span class="comment">// 再次循环，生成两个链表，lastRun 作为停止条件，这样就是避免无谓的循环（lastRun 后面都是相同的取于结果）</span></span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K,V&gt; p = f; p != lastRun; p = p.next) &#123;</span><br><span class="line">                            <span class="type">int</span> <span class="variable">ph</span> <span class="operator">=</span> p.hash; <span class="type">K</span> <span class="variable">pk</span> <span class="operator">=</span> p.key; <span class="type">V</span> <span class="variable">pv</span> <span class="operator">=</span> p.val;</span><br><span class="line">                            <span class="keyword">if</span> ((ph &amp; n) == <span class="number">0</span>)</span><br><span class="line">                                ln = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;K,V&gt;(ph, pk, pv, ln);</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                hn = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;K,V&gt;(ph, pk, pv, hn);</span><br><span class="line">                        &#125;</span><br><span class="line">                        setTabAt(nextTab, i, ln);</span><br><span class="line">                        setTabAt(nextTab, i + n, hn);</span><br><span class="line">                        setTabAt(tab, i, fwd);</span><br><span class="line">                        advance = <span class="literal">true</span>;</span><br><span class="line">                    &#125; <span class="comment">// 当桶中头节点是红黑树</span></span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123;</span><br><span class="line">                        TreeBin&lt;K,V&gt; t = (TreeBin&lt;K,V&gt;)f;</span><br><span class="line">                        <span class="comment">// 这里的扩容和hashmap的链表resize扩容逻辑几乎一致 分配核心逻辑  hash &amp; n</span></span><br><span class="line">                        TreeNode&lt;K,V&gt; lo = <span class="literal">null</span>, loTail = <span class="literal">null</span>;</span><br><span class="line">                        TreeNode&lt;K,V&gt; hi = <span class="literal">null</span>, hiTail = <span class="literal">null</span>;</span><br><span class="line">                        <span class="type">int</span> <span class="variable">lc</span> <span class="operator">=</span> <span class="number">0</span>, hc = <span class="number">0</span>;</span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K,V&gt; e = t.first; e != <span class="literal">null</span>; e = e.next) &#123;</span><br><span class="line">                            <span class="type">int</span> <span class="variable">h</span> <span class="operator">=</span> e.hash;</span><br><span class="line">                            TreeNode&lt;K,V&gt; p = <span class="keyword">new</span> <span class="title class_">TreeNode</span>&lt;K,V&gt;</span><br><span class="line">                                (h, e.key, e.val, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line">                            <span class="keyword">if</span> ((h &amp; n) == <span class="number">0</span>) &#123;</span><br><span class="line">                                <span class="keyword">if</span> ((p.prev = loTail) == <span class="literal">null</span>)</span><br><span class="line">                                    lo = p;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    loTail.next = p;</span><br><span class="line">                                loTail = p;</span><br><span class="line">                                ++lc;</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="keyword">else</span> &#123;</span><br><span class="line">                                <span class="keyword">if</span> ((p.prev = hiTail) == <span class="literal">null</span>)</span><br><span class="line">                                    hi = p;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    hiTail.next = p;</span><br><span class="line">                                hiTail = p;</span><br><span class="line">                                ++hc;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;]</span><br><span class="line">                        <span class="comment">// 当下标的节点数小于等于 6,由红黑树转换为链表，不然创建一颗新的树</span></span><br><span class="line">                        ln = (lc &lt;= UNTREEIFY_THRESHOLD) ? untreeify(lo) :</span><br><span class="line">                            (hc != <span class="number">0</span>) ? <span class="keyword">new</span> <span class="title class_">TreeBin</span>&lt;K,V&gt;(lo) : t;</span><br><span class="line">                        hn = (hc &lt;= UNTREEIFY_THRESHOLD) ? untreeify(hi) :</span><br><span class="line">                            (lc != <span class="number">0</span>) ? <span class="keyword">new</span> <span class="title class_">TreeBin</span>&lt;K,V&gt;(hi) : t;</span><br><span class="line">                        setTabAt(nextTab, i, ln);</span><br><span class="line">                        setTabAt(nextTab, i + n, hn);</span><br><span class="line">                        setTabAt(tab, i, fwd);</span><br><span class="line">                        advance = <span class="literal">true</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h4 id="流程总结-1"><a href="#流程总结-1" class="headerlink" title="流程总结"></a>流程总结</h4><p>1、判断CPU，这一步让每个CPU处理的数组中的桶数量保持一致，都为16</p>
<p>2、当扩容数组为空时，初始化扩容数组nextTable </p>
<p>3、检测是否已经完成扩容，如果没有，开始循环遍历旧数组</p>
<p>4、当旧数组下标对应节点为空，填入fwd节点，标志可以跳过，如果旧数组下标为MOVED，代表已有线程正常转移该桶的节点，advance为true，进行其他下标判断，当当前下标节点处于正常状态时，开始加同步锁进行数据转移</p>
<p>5、当头节点hash大于0代表为链表节点，遍历所有节点，其中 做了 f.hash &amp; n 判断，该结果只能为0或者1，将这些节点分配成两个链表节点中，一个链表放在新数组的 i坐标下，一个放在新数组的 i + n 坐标下。</p>
<p>6、 当头节点是红黑树时，同样分成两颗树，只是最终做了树的大小判断，当大小小于等于6，将其转换为链表</p>
<p>7、转移完当前下标i中的所有节点时，advance为true，代表可以扫描其他下标了。</p>
<p>8、当所有坐标都转移完成后，finish为true，标志tranfer结束 return</p>
<h3 id="理解-f-hash-n-的含义"><a href="#理解-f-hash-n-的含义" class="headerlink" title="理解  f.hash &amp; n 的含义"></a>理解  f.hash &amp; n 的含义</h3><p>这里相信大家对 hash &amp;  n (n为原数组大小) 为什么可以分成两个节点列表存储，并分配到 原坐标 i 和 i+n中会有疑问。</p>
<p>可以查看我在HashMap解析博客</p>
<p>扩容时涉及链表节点的下标分配的问题，旧数组的下标和新数组的下标有什么关联吗？</p>
<p> 这个需要从下面这个公式讲起。</p>
<p><a href="http://www.eiletxie.cn/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/#%E7%90%86%E8%A7%A3-e-hash-amp-oldCap-0">理解 hash &amp; oldCap &#x3D;&#x3D; 0的含义</a></p>
<p>建议大家去查看一下背后逻辑</p>
<p>对这句话的总结</p>
<p>当 (e.hash &amp; oldCap) &#x3D;&#x3D; 0 时，证明节点Node 在 新数组下标 和旧数组的下标一致，我们用链表A保存</p>
<p>否则 证明 节点Node在 新数组的下标 &#x3D; 旧数组下标 + oldCap一样，用链表B保存。</p>
<h3 id="多线程帮助扩容"><a href="#多线程帮助扩容" class="headerlink" title="多线程帮助扩容"></a>多线程帮助扩容</h3><ol>
<li>ConcurrentHashMap 支持并发扩容，实现方式是，将表拆分，让每个线程处理自己的区间。如下图：</li>
</ol>
 <img src="/2019/12/27/ConcurrentHashMap%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90/transfer.jpg" class="" title="帮助转移">

<p>这里触发的逻辑是 putval时检测当前下标节点的hash是否为 MOVED 即 -1，代表原数组正在扩容，那么当前线程会帮助其他下标节点的遍历转移。</p>
<p>并且多CPU下每个线程遍历处理的下标数量默认是16，如果是单CPU就是数组大小的桶数量</p>
<p>参考： <a href="https://juejin.im/post/5b00160151882565bd2582e0">https://juejin.im/post/5b00160151882565bd2582e0</a></p>
<h3 id="个人建议"><a href="#个人建议" class="headerlink" title="个人建议"></a>个人建议</h3><p>​    ConcurrentHashMap 分析起了真是要我老命了，我参考了几篇博客花了几晚才大致解析完核心方法的。看似 与HashMap实现逻辑差不多，但由于要高性能的并发安全，用了很多变量标志来进行状态判断,这使得有时候看某一段代码一头雾水。</p>
<p>​     这里如果有小伙伴也要分析，注意要理解 关键的状态标志的含义和 上面那就 hash &amp; oldCap &#x3D;&#x3D; 0的含义，理解 线程加锁是加在 线程下标下的头节点，每一次转移是当前下标下的节点转移就好了。</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM内存溢出Demo与JDK版本的关联</title>
    <url>/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/</url>
    <content><![CDATA[<h1 id="JVM内存溢出Demo与JDK版本的关联"><a href="#JVM内存溢出Demo与JDK版本的关联" class="headerlink" title="JVM内存溢出Demo与JDK版本的关联"></a>JVM内存溢出Demo与JDK版本的关联</h1><p>​         最近看经典的JVM书籍，周志明的《深入理解JAVA虚拟机》第三版，看到很多不错JVM内存溢出示例，我这里拿JDK1.6和JDK1.8版本进行了Demo的测试和总结。</p>
<p>通过该文章，您可以了解相关知识</p>
<ul>
<li>不同类型的内存溢出应该是在哪种情形下，哪块区域的问题</li>
<li>相关的JVM参数设置，调优</li>
<li>相同的代码、不同JDK版本造成内存溢出的类型不同</li>
</ul>
<p>先列出内存溢出的几种类型：</p>
<ul>
<li>java.lang.OutOfMemoryError: Java heap space  堆内存溢出</li>
<li>java.lang.OutOfMemoryError: PermGen space 永久代溢出</li>
<li>java.lang.StackOverflowError 栈溢出</li>
<li>java.lang.OutOfMemoryError: Metaspace  元空间溢出</li>
<li>java.lang.OutOfMemoryError  直接内存溢出</li>
</ul>
<span id="more"></span>
<br/>

<h2 id="栈溢出"><a href="#栈溢出" class="headerlink" title="栈溢出"></a>栈溢出</h2><h3 id="java-lang-StackOverflowError"><a href="#java-lang-StackOverflowError" class="headerlink" title="java.lang.StackOverflowError"></a>java.lang.StackOverflowError</h3><p>栈溢出，发生在方法相互调用过多，或者递归无限调用时。</p>
<h4 id="JVM参数"><a href="#JVM参数" class="headerlink" title="JVM参数"></a>JVM参数</h4><p><strong>-Xss   栈内存容量</strong></p>
<h4 id="产生的情形"><a href="#产生的情形" class="headerlink" title="产生的情形"></a>产生的情形</h4><p>class中方法的调用是以栈帧的方式存储在 虚拟机栈中，当栈的深度不足，或者栈帧过大时就会导致虚拟机栈抛出异常。我们这就用递归，当递归无终止条件就会产生 java.lang.StackOverflowError<br><br/></p>
<h4 id="示例代码"><a href="#示例代码" class="headerlink" title="示例代码"></a>示例代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StackDemo3</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="type">int</span> <span class="variable">stackDepth</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span>  <span class="keyword">void</span> <span class="title function_">test1</span><span class="params">()</span>&#123;</span><br><span class="line">		stackDepth++;</span><br><span class="line">		test1();</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">StackDemo3</span> <span class="variable">s</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StackDemo3</span>();</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			s.test1();</span><br><span class="line">		&#125; <span class="keyword">catch</span> (Throwable e) &#123;</span><br><span class="line">			System.out.println(<span class="string">&quot;stack depth: &quot;</span> + s.stackDepth);</span><br><span class="line">			<span class="keyword">throw</span> e;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/stack2.jpg" class="" title="jvm栈参数图"></p>
<h4 id="运行报错"><a href="#运行报错" class="headerlink" title="运行报错"></a>运行报错</h4>  <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/stack.jpg" class="" title="jvm栈异常图">

<br/>

<h3 id="java-lang-OutOfMemoryError"><a href="#java-lang-OutOfMemoryError" class="headerlink" title="java.lang.OutOfMemoryError"></a>java.lang.OutOfMemoryError</h3><p>栈溢出的另外一种异常</p>
<p><strong>这种是发生在虚拟机扩展栈时无法申请到足够的内存空间</strong></p>
<p>例如： 方法中有while(true)不break的情况时</p>
<p>由于JDK1.8 无法限制JAVA线程中方法的内存扩展大小，所以运行到一半我怕我电脑死机停掉了。。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StackDemo4</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">noStop</span><span class="params">()</span>&#123;</span><br><span class="line">		<span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">stackRunNoStop</span><span class="params">()</span>&#123;</span><br><span class="line">		<span class="comment">// 加速扩展栈方法占用的内存</span></span><br><span class="line">		<span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">			<span class="type">Thread</span> <span class="variable">thread</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">				<span class="meta">@Override</span></span><br><span class="line">				<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">					noStop();</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="type">StackDemo4</span> <span class="variable">s</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StackDemo4</span>();</span><br><span class="line">		s.stackRunNoStop();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<br/>
## 堆空间溢出

<h3 id="java-lang-OutOfMemoryError-Java-heap-space"><a href="#java-lang-OutOfMemoryError-Java-heap-space" class="headerlink" title="java.lang.OutOfMemoryError: Java heap space"></a>java.lang.OutOfMemoryError: Java heap space</h3><p>堆空间是存放对象实例的地方 new Object()</p>
<h4 id="JVM参数-1"><a href="#JVM参数-1" class="headerlink" title="JVM参数"></a>JVM参数</h4><ul>
<li><p><strong>-Xmx  最大分配内存，默认为物理内存的 “1 &#x2F; 4”</strong></p>
</li>
<li><p><strong>-Xms  设置初始分配大小，默认为物理内存的 “1 &#x2F; 64”</strong></p>
</li>
<li><p><strong>-XX:+PrintGCDetails     输出详细的GC处理日志</strong></p>
<br/></li>
</ul>
<h4 id="产生的情形-1"><a href="#产生的情形-1" class="headerlink" title="产生的情形"></a>产生的情形</h4><p>​      对象的实例存放在堆空间中，那么我们只需要不停的产生字符串对象即可。</p>
<h4 id="示例代码-1"><a href="#示例代码-1" class="headerlink" title="示例代码"></a>示例代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HeapDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="comment">// 设置JVM参数为几M大小</span></span><br><span class="line">		<span class="type">String</span> <span class="variable">str</span> <span class="operator">=</span> <span class="string">&quot;test HeapOutOfMemory&quot;</span>;</span><br><span class="line">		</span><br><span class="line">		<span class="comment">// 无限生产新的str对象</span></span><br><span class="line">		<span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">			str += str + <span class="keyword">new</span> <span class="title class_">Random</span>().nextInt(<span class="number">66666666</span>) + <span class="keyword">new</span> <span class="title class_">Random</span>().nextInt(<span class="number">99999999</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<br/>
#### 运行报错

  <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/heap.jpg" class="" title="堆空间异常图">

 <br/>

<h2 id="运行时常量池溢出"><a href="#运行时常量池溢出" class="headerlink" title="运行时常量池溢出"></a>运行时常量池溢出</h2><p>这块区域是内存溢出类型最多的，为什么呢？</p>
<ul>
<li>JDK1.6及之前的版本:  常量池在方法区，方法区属于永久代，会报出 OutOfMemoryError: PermGen space</li>
<li>JDK1.7:  常量池是位于堆空间中，它会报 java.lang.OutOfMemoryError</li>
<li>JDK1.8: 方法区和运行时常量池位于元空间中，它报的是元空间溢出</li>
</ul>
<p>由于我电脑上只安装了 JDK1.8和JDK1.6，这里只做这两种版本的示例。<br><br/></p>
<h3 id="java-lang-OutOfMemoryError-Metaspace"><a href="#java-lang-OutOfMemoryError-Metaspace" class="headerlink" title="java.lang.OutOfMemoryError: Metaspace"></a>java.lang.OutOfMemoryError: Metaspace</h3><p><strong>元空间溢出</strong></p>
<p>​         JDK1.8版本提出元空间这个概念，它和新生代、老年代组成JVM内存区域，方法区和运行时常量池在JDK1.8版本中放置在了元空间中，默认元空间使用的是本地内存。</p>
<h4 id="JVM参数-2"><a href="#JVM参数-2" class="headerlink" title="JVM参数"></a>JVM参数</h4><ul>
<li>-XX:MetaspaceSize 初始空间大小，达到该值就会触发垃圾收集进行类型卸载</li>
<li>-XX:MaxMetaspaceSize，最大空间，默认是没有限制的</li>
</ul>
<p>设置元空间大小 -XX:MetaspaceSize&#x3D;5M  -XX:MaxMetaspaceSize&#x3D;10M</p>
<h4 id="产生的情形-2"><a href="#产生的情形-2" class="headerlink" title="产生的情形"></a>产生的情形</h4><p>​         String.intern() 是一个 Native 方法，它的作用是：如果字符串常量池中包含该String对象的常量池中，并且返回该String对象的引用。 </p>
<p>​        我们用一个list作为常量池的引用，防止被GC，然后无限插入新的字符串</p>
<h4 id="示例代码-2"><a href="#示例代码-2" class="headerlink" title="示例代码"></a>示例代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RuntimeConstantPoolOOM</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="comment">// 用List保证常量池引用，避免被FULL GC回收常量池行为</span></span><br><span class="line">		List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">		<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">		    list.add(String.valueOf(i++).intern());</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="运行报错-1"><a href="#运行报错-1" class="headerlink" title="运行报错"></a>运行报错</h4>  <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/pool.jpg" class="" title="常量池异常图">

<h3 id="java-lang-OutOfMemoryError-PermGen-space"><a href="#java-lang-OutOfMemoryError-PermGen-space" class="headerlink" title="java.lang.OutOfMemoryError: PermGen space"></a>java.lang.OutOfMemoryError: PermGen space</h3><h4 id="JVM参数-3"><a href="#JVM参数-3" class="headerlink" title="JVM参数"></a>JVM参数</h4><p>在JDK1.6及之前的版本中，由于常量池分配在永久代内，我们可以通过-XX:PermSize和-XX:MaxPermSize限制方法区大小，从而间接限制其中常量池的容量，你可以给个10M大小</p>
<p>产生情形和示例代码是一致的，区别只是JDK版本不同，常量池所在的内存区域发生变化，从而导致不同内存溢出异常。</p>
<h4 id="运行报错-2"><a href="#运行报错-2" class="headerlink" title="运行报错"></a>运行报错</h4> <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/pool2.jpg" class="" title="常量池异常1.6图">

<h2 id="方法区溢出"><a href="#方法区溢出" class="headerlink" title="方法区溢出"></a>方法区溢出</h2><p>方法区抛出的异常与常量池一致，一个包含关系</p>
<h4 id="产生的情形-3"><a href="#产生的情形-3" class="headerlink" title="产生的情形"></a>产生的情形</h4><p>由于方法区用于存放Class的相关信息：如类名、访问修饰符、常量池</p>
<p>所以测试的话，思路就是 运行程序时产生大量的类填满的类去填满方法区，直到溢出。</p>
<p>这里用CGLib直接操作字节码运行时产生大量的动态类。</p>
<p>刚开始设置的是 方法区的大小，发现没什么效果。</p>
<p>​     <strong>VM Args: -XX:PermSize&#x3D;10M -XX:MaxPermSize&#x3D;10M</strong></p>
<p>​         由于JDK1.8时 已经去除了永久代，方法区在元空间中，而元空间未设置的话使用的是本地内存，所以我测试一直未报溢出。</p>
<p>直到设置的JVM参数  元空间的大小才报元空间溢出的异常</p>
<p>​      -XX:MetaspaceSize&#x3D;5M  -XX:MaxMetaspaceSize&#x3D;5M</p>
<h3 id="java-lang-OutOfMemoryError-Metaspace-1"><a href="#java-lang-OutOfMemoryError-Metaspace-1" class="headerlink" title="java.lang.OutOfMemoryError: Metaspace"></a>java.lang.OutOfMemoryError: Metaspace</h3><h4 id="示例代码-3"><a href="#示例代码-3" class="headerlink" title="示例代码"></a>示例代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 借助CGLib方法区出现内存溢出异常 </span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> XC-Eilet</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *         CGLIB是一个强大、高性能的字节码生成库，它用于在运行时扩展Java类和实现接口；本质上它是通过动态的生成一个子类去覆盖所要代理的类（</span></span><br><span class="line"><span class="comment"> *         非final修饰的类和方法）。Enhancer是一个非常重要的类，它允许为非接口类型创建一个JAVA代理，</span></span><br><span class="line"><span class="comment"> *         Enhancer动态的创建给定类的子类并且拦截代理类的所有的方法，和JDK动态代理不一样的是不管是接口还是类它都能正常工作。</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> *         net.sf.cglib.proxy.Callback接口：在cglib包中是一个很关键的接口，所有被net.sf.cglib.proxy</span></span><br><span class="line"><span class="comment"> *         .Enhancer类调用的回调（callback）接口都要继承这个接口。</span></span><br><span class="line"><span class="comment"> *         net.sf.cglib.proxy.MethodInterceptor接口:是通用的回调（callback）类型，</span></span><br><span class="line"><span class="comment"> *         他经常被AOP用来实现拦截（intercept）方法的调用；</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JavaMethodAreaOOM</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">			<span class="comment">/*</span></span><br><span class="line"><span class="comment">			 * Enhancer可能是CGLIB中最常用的一个类，和Java1.3动态代理中引入的Proxy类差不多(如果对Proxy不懂，可以参考这里)。</span></span><br><span class="line"><span class="comment">			 * 和Proxy不同的是，Enhancer既能够代理普通的class，也能够代理接口。</span></span><br><span class="line"><span class="comment">			 */</span></span><br><span class="line">			<span class="type">Enhancer</span> <span class="variable">enhancer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Enhancer</span>();</span><br><span class="line">			enhancer.setSuperclass(OOMObject.class);</span><br><span class="line">			enhancer.setUseCache(<span class="literal">false</span>);</span><br><span class="line">			enhancer.setCallback(<span class="keyword">new</span> <span class="title class_">MethodInterceptor</span>() &#123;</span><br><span class="line">				</span><br><span class="line">				<span class="keyword">public</span> Object <span class="title function_">intercept</span><span class="params">(Object obj, Method method, Object[] args, MethodProxy proxy)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">					<span class="keyword">return</span> proxy.invokeSuper(obj, args);</span><br><span class="line">				&#125;</span><br><span class="line">				</span><br><span class="line">			&#125;);</span><br><span class="line">			enhancer.create();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">OOMObject</span> &#123;</span><br><span class="line">		</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="运行报错-3"><a href="#运行报错-3" class="headerlink" title="运行报错"></a>运行报错</h4> <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/method.jpg" class="" title="方法区异常图">

<h3 id="java-lang-OutOfMemoryError-PermGen-space-1"><a href="#java-lang-OutOfMemoryError-PermGen-space-1" class="headerlink" title="java.lang.OutOfMemoryError: PermGen space"></a>java.lang.OutOfMemoryError: PermGen space</h3><br/>

<p>JDK1.6版本</p>
<p>用JDK1.6 版本测试时，由于限制了方法区大小，就开始报永久代错误了。</p>
<h4 id="JVM参数-4"><a href="#JVM参数-4" class="headerlink" title="JVM参数"></a>JVM参数</h4><p>设置方法区大小</p>
<p>-XX:PermSize&#x3D;10M -XX:MaxPermSize&#x3D;10M</p>
<p>代码与上面示例代码保持一致</p>
<h4 id="运行报错-4"><a href="#运行报错-4" class="headerlink" title="运行报错"></a>运行报错</h4> <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/method2.jpg" class="" title="方法区异常图">

<br/>

<h2 id="本机直接内存溢出"><a href="#本机直接内存溢出" class="headerlink" title="本机直接内存溢出"></a>本机直接内存溢出</h2><h3 id="java-lang-OutOfMemoryError-1"><a href="#java-lang-OutOfMemoryError-1" class="headerlink" title="java.lang.OutOfMemoryError"></a>java.lang.OutOfMemoryError</h3><h4 id="JVM参数-5"><a href="#JVM参数-5" class="headerlink" title="JVM参数"></a>JVM参数</h4><p>DirectMemory 容量通过-XX:MaxDirectMemorySize 指定，如果不指定的话，默认等于Java堆的最大值（-Xmx）一样。</p>
<p>这里用 反射获取Unsafe实例进行直接内存分配。</p>
<h4 id="示例代码-4"><a href="#示例代码-4" class="headerlink" title="示例代码"></a>示例代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * VM Args: -Xmx20M -XX:MaxDirectMemorySize=10M</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> XC-Eilet</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DirectMemoryOOM</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">_1MB</span> <span class="operator">=</span> <span class="number">1024</span>*<span class="number">1024</span>;</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">      <span class="type">Field</span> <span class="variable">unsafeField</span> <span class="operator">=</span> Unsafe.class.getDeclaredFields()[<span class="number">0</span>];</span><br><span class="line">      unsafeField.setAccessible(<span class="literal">true</span>);</span><br><span class="line">      <span class="type">Unsafe</span> <span class="variable">unsafe</span> <span class="operator">=</span> (Unsafe) unsafeField.get(<span class="literal">null</span>);</span><br><span class="line">      <span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">    	  unsafe.allocateMemory(_1MB);</span><br><span class="line">      &#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="运行报错-5"><a href="#运行报错-5" class="headerlink" title="运行报错"></a>运行报错</h4> <img src="/2019/09/16/JVM%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BADemo%E4%B8%8EJDK%E7%89%88%E6%9C%AC%E7%9A%84%E5%85%B3%E8%81%94/direct.jpg" class="" title="直接内存异常图">]]></content>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>Java内存模型-JMM</title>
    <url>/2019/11/12/Java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B-JMM/</url>
    <content><![CDATA[<p>Java 多线程与并发离不开 Java内存模型,但网上的博文我有点不能理解，这次看完 Java多并发实战和相关视频，做一份简单的总结。</p>
<p>内容大概分为下面几块。</p>
<ul>
<li>JMM是什么,起到了什么作用？</li>
<li>JMM的基本原理？</li>
<li>Happens-before是什么，它又起到什么作用？</li>
<li>指令重排的优缺点。</li>
</ul>
<br/>

<h3 id="JMM是什么？"><a href="#JMM是什么？" class="headerlink" title="JMM是什么？"></a>JMM是什么？</h3><p>​        它与JVM的内存模型不一样，JVM的内存模型是物理的内存区域划分。而 Java内存模型（即Java Memory Model，简称 JMM)是一种对虚拟机的一组最小保证，这组保证规定了对变量的写入操作在何时将对其他线程可见。</p>
<span id="more"></span>
<br/>

<h2 id="JMM下的数据交换流程"><a href="#JMM下的数据交换流程" class="headerlink" title="JMM下的数据交换流程"></a>JMM下的数据交换流程</h2><p>​         由于JVM运行程序的实体是线程，而每个线程创建时JVM都会为其创建一个工作内存（有些地方称为栈空间），工作内存是每个线程的私有数据区域，而Java内存模型中规定所有变量都存储在主内存，主内存是共享内存区域，所有线程都可以访问，但线程对变量的操作（读取赋值等）必须在工作内存中进行，首先要将变量从主内存拷贝的自己的工作内存空间，然后对变量进行操作，操作完成后再将变量写会主内存，不能直接操作主内存中的变量，各个线程中的工作内存中存储着主内存中的变量副本拷贝，因此不同的线程间无法访问对方的工作内存，线程间的通信（传值）必须通过主内存来完成，其简要访问过程如下图：</p>
  <img src="/2019/11/12/Java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B-JMM/mainMemory.jpg" class="" title="JMM图">

<br/>

<h3 id="JMM-中的主内存"><a href="#JMM-中的主内存" class="headerlink" title="JMM 中的主内存"></a>JMM 中的主内存</h3><ul>
<li>存储Java实例对象</li>
<li>包括 成员变量、类信息、常量、静态变量等</li>
<li>属于数据共享的区域，多线程并发操作时会引发线程安全问题</li>
</ul>
<h3 id="JMM中的工作内存"><a href="#JMM中的工作内存" class="headerlink" title="JMM中的工作内存"></a>JMM中的工作内存</h3><ul>
<li>存储当前方法的所有本地变量信息，本地变量对于其他线程不可见</li>
<li>字节码行号指示器，Native方法信息</li>
<li>属于线程私有数据区域，不存在线程安全问题</li>
</ul>
<h3 id="强调"><a href="#强调" class="headerlink" title="强调"></a>强调</h3><p>JMM的主内存 存放的是线程公有的共享数据</p>
<p>JMM的工作内存 是线程私有的数据副本</p>
<br/>

<h3 id="个人对数据交换流程理解补充"><a href="#个人对数据交换流程理解补充" class="headerlink" title="个人对数据交换流程理解补充"></a>个人对数据交换流程理解补充</h3><p>​        线程中的变量如果是被Volatile修饰，或者是类似static修饰的共享变量，当数据被线程的工作内存中修改后，这个时候写回主内存，并且强制其他线程来重新读取该修改的变量。</p>
<p>​        可能有人会说，如果线程很多的话，那这种数据回写后强制通知的机制岂不是很费时吗？当时我也是这么认为的，直到我了解到 实际上计算机跑的线程数并不多，往往一个 6核的CPU并发处理 6个线程，通常分配的每个核1到3个线程，当然这不是绝对的，要看应用程序使用什么调度策略。但可以看出实际线程数并不多，通知开销没有想象的这么大。</p>
<p>        题外话，如果对线程池有一定了解，那么会知道，我们是不用系统提供的3种策略的线程池的，而是定制 线程池，设置最大的线程数为你CPU的逻辑核数 + 1，同时并发的线程数在Java生成中也是不多的。</p>
<br/>

<h2 id="什么是Happens-Before"><a href="#什么是Happens-Before" class="headerlink" title="什么是Happens-Before"></a>什么是Happens-Before</h2><p>​        Java 内存模型是通过各种操作来定义的，包括对变量的读&#x2F;写操作，监视器的加锁和释放操作，以及线程的启动和合并操作。</p>
<p>​       JMM 为程序中的所有操作定义了一个偏序关系，称之为 Happens-Before</p>
<br/>

<h3 id="Happens-Before的作用是什么？"><a href="#Happens-Before的作用是什么？" class="headerlink" title="Happens-Before的作用是什么？"></a>Happens-Before的作用是什么？</h3><p>​     不管是多线程，还是单线程，如果要保证 <strong>执行操作B的线程看到操作A的结果，那么 A和B之间必须满足 Happens-Before关系</strong>。如果两个操作之间缺乏Happen-Before关系，那么JVM可以对它们任意地重排序。</p>
<br/>

<h3 id="Happens-Before的规则如下："><a href="#Happens-Before的规则如下：" class="headerlink" title="Happens-Before的规则如下："></a>Happens-Before的规则如下：</h3><ol>
<li>程序顺序规则： 如果程序中操作A在操作B之前，那么在线程中A操作将在B操作之前执行。</li>
<li>监视器锁规则： 在监视器锁上的解锁操作必须在同一个监视器锁上的加锁操作之前执行。</li>
<li>volatile变量规则： 对volatile 变量的写入操作必须在该变量的读操作之前执行。</li>
<li>线程启动规则： 在线程上 对 Thread.start 的调用必须在该线程中执行任何操作之前执行。</li>
<li>线程结束规则： 线程中的任何操作都必须在其他线程检测到该线程已经结束之前执行，或者从 Thread.join 中 成功返回，或者在调用 Thread.isAlive 时返回 false.</li>
<li>中断规则： 当一个线程在另一个线程上调用 interrupt 时，必须在被 中断线程检测到 interrupt 调用 之前执行（通过 抛出InterruptedException,或者调用 isInterrupted 和 interrupted)。</li>
<li>终结器规则： 对象的构造函数必须在启动该对象的终结器之前执行完成。</li>
<li>传递性： 如果操作A在操作B之前执行，操作B在操作C之前执行，那么操作A在操作C之前执行。</li>
</ol>
<br/>

<p>​        这些规则不推荐大家强记，个人认为学习相关的线程知识点，这些规则好像就是理所应当的，不然无法满足 我们开发中遇见的一些 串行一致性的现象，就是按照多线程的规则书写，就表现的像单线程一样的正确运行。</p>
<p>​        例如我们对一个资源类的方法加synchronized 重量锁，那么线程A拿到锁在方法中的操作再解锁的数据变化是对下一个拿到这把锁的线程是可见的，这就是监视器规则，要始终理解到 只有满足 Happens-before 的规则才能满足执行操作B的线程看到操作A的结果，这就是 Happens-before的强大之处。</p>
<br/>

<h2 id="关于指令重排的理解"><a href="#关于指令重排的理解" class="headerlink" title="关于指令重排的理解"></a>关于指令重排的理解</h2><p>​         首先需要对指令重排做一段阐述。</p>
<p>​       对于程序执行的操作，在JVM编译的过程中，为了优化指令，重新排列的指令的执行顺序，指令重排序包括编译器重排序和运行时重排序。JVM规范规定，指令重排序可以在不影响单线程程序执行结果前提下进行。</p>
<p>​        当然重排序在单线程中是不能影响数据的依赖性和最终结果的，怎么理解？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">i = <span class="number">1</span>; <span class="comment">// 语句1</span></span><br><span class="line"></span><br><span class="line">x = i; <span class="comment">// 语句2</span></span><br><span class="line"></span><br><span class="line">y = <span class="number">2</span>; <span class="comment">//语句3</span></span><br></pre></td></tr></table></figure>

<p>这里的话，语句2 就不能出现在 语句1前面，会影响数据依赖性，而 语句3可以出现在语句1前，也可以出现在 语句2前。</p>
<p>​        但是在多线程中就有 包含共享变量操作的语句重排序，由于内存可见性，就有可能会造成数据不一致的问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PossibleReordering</span> &#123;</span><br><span class="line"><span class="keyword">static</span> <span class="type">int</span> <span class="variable">x</span> <span class="operator">=</span> <span class="number">0</span>, y = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">0</span>, b = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">one</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">            a = <span class="number">1</span>;</span><br><span class="line">            x = b;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="type">Thread</span> <span class="variable">other</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">            b = <span class="number">1</span>;</span><br><span class="line">            y = a;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    one.start();other.start();</span><br><span class="line">    one.join();other.join();</span><br><span class="line">    System.out.println(“(” + x + “,” + y + “)”);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<br/>
​        这段代码的运行结果可能为(1,0)、(0,1)或(1,1)，因为线程one可以在线程two开始之前就执行完了，也有可能反之，甚至有可能二者的指令是同时或交替执行的。

<p>​       然而，这段代码的执行结果也可能是(0,0). 因为，在实际运行时，代码指令可能并不是严格按照代码语句顺序执行的。得到(0,0)结果的语句执行过程，如下图所示。值得注意的是，a&#x3D;1和x&#x3D;b这两个语句的赋值操作的顺序被颠倒了，或者说，发生了指令“重排序”(reordering)。</p>
<p>​        而Volatile作为轻量级的同步机制，可以禁止指令重排序，所以Java源码中JUC包下有很多变量都使用了 Volatile来修饰。</p>
<p>​       个人在理解JMM相关点，往往不能一下子理解明白，是通过学习JUC的其他知识点再进行联想的才慢慢算是了解一点这个机制，不能保证写的没有问题，有误的话，还请大家指正。</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>JUC</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL中事务与隔离级别</title>
    <url>/2019/10/07/MySQL%E4%B8%AD%E4%BA%8B%E5%8A%A1%E4%B8%8E%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB/</url>
    <content><![CDATA[<h2 id="事务以及隔离性级别"><a href="#事务以及隔离性级别" class="headerlink" title="事务以及隔离性级别"></a>事务以及隔离性级别</h2><h3 id="事务特性-ACID"><a href="#事务特性-ACID" class="headerlink" title="事务特性 ACID"></a>事务特性 ACID</h3><p><strong>1. 原子性（Atomicity）</strong></p>
<p>事务被视为不可分割的最小单元，事务的所有操作要么全部提交成功，要么全部失败回滚。</p>
<p>回滚可以用回滚日志来实现，回滚日志记录着事务所执行的修改操作，在回滚时反向执行这些修改操作即可。</p>
<p><strong>2. 一致性（Consistency）</strong></p>
<p>数据库在事务执行前后都保持一致性状态。在一致性状态下，所有事务对一个数据的读取结果都是相同的。</p>
<p><strong>3. 隔离性（Isolation）</strong></p>
<p>一个事务所做的修改在最终提交以前，对其它事务是不可见的。</p>
<p><strong>4. 持久性（Durability）</strong></p>
<p>一旦事务提交，则其所做的修改将会永远保存到数据库中。即使系统发生崩溃，事务执行的结果也不能丢失。</p>
<span id="more"></span><br/>

<h3 id="并发一致性产生的问题"><a href="#并发一致性产生的问题" class="headerlink" title="并发一致性产生的问题"></a>并发一致性产生的问题</h3><p>​	修改无效： A 和 B 两个事务都对一个数据进行修改，A 先修改，B 随后修改，B 的修改覆盖了 A 的修改</p>
<p>​	脏读：  A事务修改了值，B事务查询到该值，当A事务回滚修改时，B读的就是脏数据</p>
<p>​	不可重复读 :  A事务读取了数据，如果B事务修改该数据，则A再次读取时数值不一致</p>
<p>​	幻读 ： A事务查询某范围的数量，B事务插入一条数据，范围数量发生了改变（即使事务隔离性，操作不可见，但物理数据行号会改变，操作检测的是物理行号）</p>
<br/>

<h3 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h3><p>READ UNCOMMITED(未提交读): 事务未提交，但其他事务可以查看到该事务的修改，该级别及以上可避免修改无效</p>
<p>READ COMMITTED(提交读）： 事务未提交前对其他事务是不可见的。该级别及以上可避免脏读（ORACLE默认级别）</p>
<p>REPEATABLE READ(可重复读）: 事务与事务之间的操作相互隔离，互不影响，（MYSQL默认级别）</p>
<p>SERIALIZE(可串行化）： 避免了幻读，执行顺序化，无并发可言</p>
<br/>

<img src="/2019/10/07/MySQL%E4%B8%AD%E4%BA%8B%E5%8A%A1%E4%B8%8E%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB/isolation.jpg" class="" title="isolation">



<p><strong>mysql设置事务隔离级别(设置未提交读）</strong></p>
<p><em>set session transaction isolation level read uncommitted;</em> </p>
<br/>

<h3 id="AUTOCOMMIT"><a href="#AUTOCOMMIT" class="headerlink" title="AUTOCOMMIT"></a>AUTOCOMMIT</h3><p>MySQL 默认采用自动提交模式。也就是说，如果不显式使用START TRANSACTION语句来开始一个事务，那么每个查询都会被当做一个事务自动提交</p>
]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL中GAP Lock</title>
    <url>/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/</url>
    <content><![CDATA[<blockquote>
<p>​     前言：之前Gap Lock写的很烂，我是在本地做一些事务操作的时候发现有间隙锁这个东西，然后做了一些测试，马虎的写了一篇总结，后面对间隙锁针对性的做了一定了解，现在重写Gap Lock的这篇博客，对自己有一个交代。</p>
</blockquote>
<span id="more"></span>
<br/>

<p>我会从以下几个方面介绍Gap Lock</p>
<ul>
<li>Gap Lock是什么？</li>
<li>Gap Lock注意点</li>
<li>Gap Lock的加锁规则</li>
<li>对Gap Lock的测试</li>
</ul>
<br/>

<h2 id="Gap-Lock是什么？"><a href="#Gap-Lock是什么？" class="headerlink" title="Gap Lock是什么？"></a>Gap Lock是什么？</h2><p>​         在可重复读级别下，为了解决幻读的问题,MySQL推出了间隙锁，它会对索引上两个值之间的空隙加锁。当用户对间隙锁的区间进行插入操作时会被阻止等待，直到创建间隙锁的事务结束才可以继续执行。</p>
<br/>

<h2 id="Gap-Lock注意点"><a href="#Gap-Lock注意点" class="headerlink" title="Gap Lock注意点"></a>Gap Lock注意点</h2><ul>
<li>在添加间隙锁时，往往会和行锁一起组成一个前开后闭的区间，简称为 Next-Key Lock</li>
<li>在并发中，多个事务可能创建了多个间隙锁，间隙锁与间隙锁之间不存在冲突关系，因为它们本质是保护对应的区间不能进行插入，避免幻读问题。</li>
<li>间隙锁对应加锁区间下只和其他事务想对该区间进行插入的操作存在冲突。</li>
<li>间隙锁是对索引的间隙进行加锁</li>
</ul>
<br/>

<p>从上面的注意点可以看出，间隙锁Gap Lock对并发事务是有一定影响的，它可能会造成死锁的问题。</p>
<h3 id="为什么可能会造成死锁呢？"><a href="#为什么可能会造成死锁呢？" class="headerlink" title="为什么可能会造成死锁呢？"></a>为什么可能会造成死锁呢？</h3><p>看我举个栗子</p>
<p>假设表T有 id, a, b 三个字段,id是主键,并插入了 t(5,5,5)和t(10,10,10)这两条记录0</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/deadlock.png" class="" title="死锁图">

<p>思考明白，为什么会死锁了吗？ </p>
<p>这是因为事务A,B在select语句的时候，</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> t <span class="keyword">where</span> id <span class="operator">=</span> <span class="number">9</span> <span class="keyword">for</span> <span class="keyword">update</span></span><br></pre></td></tr></table></figure>

<p>对区间(5,10)都加上了间隙锁,它是对主键索引的间隙加锁，间隙锁之间是不存在冲突的。</p>
<p><em>这里你们可能会疑问为什么是区间(5,10)，是因为通过主键索引的扫描，从id&#x3D;5开始，到id&#x3D;10不符合条件结束</em></p>
<p>此时事务A插入ID&#x3D;9的记录会被事务B的间隙锁阻塞进入等待</p>
<p>然后事务B插入ID&#x3D;9的记录会被事务A的间隙锁阻塞进入等待</p>
<p>死锁产生。</p>
<br/>

<h2 id="Gap-Lock的加锁规则"><a href="#Gap-Lock的加锁规则" class="headerlink" title="Gap Lock的加锁规则"></a>Gap Lock的加锁规则</h2><ul>
<li><strong>加锁的基本单位是 next-key lock,next-key lock是前开后闭的区间</strong></li>
<li><strong>查找过程中查询到的对象才会加锁</strong></li>
</ul>
<blockquote>
<p>如果访问的是普通索引，而且通过覆盖索引并不需要回表查主键索引，那么主键索引上是没有加任何锁的，因为并没有访问主键索引树上的“对象”。</p>
</blockquote>
<p><strong>优化规则</strong></p>
<ol>
<li>索引上的等值查询，<strong>如果是对唯一索引的等值查询</strong>(注意：是要where条件全部命中的情况下，也就是能找到对应记录的时候)，next-key lock会优化成行锁</li>
<li>索引上的等值查询，如果是<strong>对普通索引的等值查询</strong>，向右遍历时且最后一个值不满足等值条件的时候，next-key lock 退化为间隙锁。</li>
</ol>
<p><strong>一个 bug：</strong></p>
<ul>
<li><strong>唯一索引上的范围查询</strong>会访问到不满足条件的第一个值为止。</li>
</ul>
<br/>

<p>这些规则可能很难理解，下面做几个示例来加深一下</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> `t` (</span><br><span class="line">  `id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">  `c` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  `d` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`id`),</span><br><span class="line">  KEY `c` (`c`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB;</span><br><span class="line"></span><br><span class="line"><span class="keyword">insert into</span> t <span class="keyword">values</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>),(<span class="number">5</span>,<span class="number">5</span>,<span class="number">5</span>),</span><br><span class="line">(<span class="number">10</span>,<span class="number">10</span>,<span class="number">10</span>),(<span class="number">15</span>,<span class="number">15</span>,<span class="number">15</span>),(<span class="number">20</span>,<span class="number">20</span>,<span class="number">20</span>),(<span class="number">25</span>,<span class="number">25</span>,<span class="number">25</span>);</span><br></pre></td></tr></table></figure>

<p>接下来的例子基本都是配合着图片说明的，所以我建议你可以对照着文稿看，有些例子可能会“毁三观”，也建议你读完文章后亲手实践一下。</p>
<p>说明:下面示例来自丁奇的《MySQL45讲》</p>
<h4 id="案例一：等值查询间隙锁"><a href="#案例一：等值查询间隙锁" class="headerlink" title="案例一：等值查询间隙锁"></a>案例一：等值查询间隙锁</h4><p>等值查询间隙锁第一个例子是关于等值条件操作间隙：</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/example1.png" class="" title="示例1图">



<p>由于表 t 中没有 id&#x3D;7 的记录，所以用我们上面提到的加锁规则判断一下的话：</p>
<p>根据原则 1，加锁单位是 next-key lock，session A 加锁范围就是 (5,10]；</p>
<p>同时根据优化 2，这是一个等值查询 (id&#x3D;7)，而 id&#x3D;10 不满足查询条件，next-key lock 退化成间隙锁，因此最终加锁的范围是 (5,10)。</p>
<p>所以，session B 要往这个间隙里面插入 id&#x3D;8 的记录会被锁住，但是 session C 修改 id&#x3D;10 这行是可以的。</p>
<br/>

<h4 id="案例二：非唯一索引等值锁"><a href="#案例二：非唯一索引等值锁" class="headerlink" title="案例二：非唯一索引等值锁"></a>案例二：非唯一索引等值锁</h4><p>第二个例子是关于覆盖索引上的锁</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/example2.png" class="" title="示例图">

<p>看到这个例子，你是不是有一种“该锁的不锁，不该锁的乱锁”的感觉？我们来分析一下吧。</p>
<p>这里 session A 要给索引 c 上 c&#x3D;5 的这一行加上读锁。</p>
<ol>
<li><strong>根据原则 1，加锁单位是 next-key lock，因此会给 (0,5]加上 next-key lock。</strong></li>
<li><strong>要注意 c 是普通索引，因此仅访问 c&#x3D;5 这一条记录是不能马上停下来的，需要向右遍历，查到 c&#x3D;10 才放弃。根据原则 2，访问到的都要加锁，因此要给 (5,10]加 next-key lock。</strong></li>
<li><strong>但是同时这个符合优化 2：等值判断，向右遍历，最后一个值不满足 c&#x3D;5 这个等值条件，因此退化成间隙锁 (5,10)。</strong></li>
<li><strong>根据原则 2 ，只有访问到的对象才会加锁，这个查询使用覆盖索引，并不需要访问主键索引，所以主键索引上没有加任何锁，这就是为什么 session B 的 update 语句可以执行完成。</strong></li>
</ol>
<p>但 session C 要插入一个 (7,7,7) 的记录，就会被 session A 的间隙锁 (5,10) 锁住。</p>
<p>需要注意，在这个例子中，lock in share mode 只锁覆盖索引，但是如果是 for update 就不一样了。 执行 for update 时，系统会认为你接下来要更新数据，因此会顺便给主键索引上满足条件的行加上行锁。</p>
<p>这个例子说明，锁是加在索引上的；同时，它给我们的指导是，如果你要用 lock in share mode 来给行加读锁避免数据被更新的话，就必须得绕过覆盖索引的优化，在查询字段中加入索引中不存在的字段。比如，将 session A 的查询语句改成 select d from t where c&#x3D;5 lock in share mode。你可以自己验证一下效果。</p>
<br/>

<h4 id="案例三：主键索引范围锁"><a href="#案例三：主键索引范围锁" class="headerlink" title="案例三：主键索引范围锁"></a>案例三：主键索引范围锁</h4><p>第三个例子是关于范围查询的。举例之前，你可以先思考一下这个问题：对于我们这个表 t，下面这两条查询语句，加锁范围相同吗？</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql<span class="operator">&gt;</span> <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> t <span class="keyword">where</span> id<span class="operator">=</span><span class="number">10</span> <span class="keyword">for</span> <span class="keyword">update</span>;</span><br><span class="line">mysql<span class="operator">&gt;</span> <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> t <span class="keyword">where</span> id<span class="operator">&gt;=</span><span class="number">10</span> <span class="keyword">and</span> id<span class="operator">&lt;</span><span class="number">11</span> <span class="keyword">for</span> <span class="keyword">update</span>;</span><br></pre></td></tr></table></figure>

<p>你可能会想，id 定义为 int 类型，这两个语句就是等价的吧？其实，它们并不完全等价。</p>
<p>在逻辑上，这两条查语句肯定是等价的，但是它们的加锁规则不太一样。现在，我们就让 session A 执行第二个查询语句，来看看加锁效果。</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/example3.png" class="" title="示例图">

<p>现在我们就用前面提到的加锁规则，来分析一下 session A 会加什么锁呢？</p>
<ol>
<li><strong>开始执行的时候，要找到第一个 id&#x3D;10 的行，因此本该是 next-key lock(5,10]。 根据优化 1， 主键 id 上的等值条件，退化成行锁，只加了 id&#x3D;10 这一行的行锁。</strong></li>
<li><strong>范围查找就往后继续找，找到 id&#x3D;15 这一行停下来，因此需要加 next-key lock(10,15]。</strong></li>
</ol>
<p>所以，session A 这时候锁的范围就是主键索引上，<strong>行锁 id&#x3D;10 和 next-key lock(10,15]</strong>。这样，session B 和 session C 的结果你就能理解了。</p>
<p>这里你需要注意一点，<strong>首次 session A 定位查找 id&#x3D;10 的行的时候，是当做等值查询来判断的，而向右扫描到 id&#x3D;15 的时候，用的是范围查询判断。</strong></p>
<br/>

<h4 id="案例四：非唯一索引范围锁"><a href="#案例四：非唯一索引范围锁" class="headerlink" title="案例四：非唯一索引范围锁"></a>案例四：非唯一索引范围锁</h4><p>接下来，我们再看两个范围查询加锁的例子，你可以对照着案例三来看。</p>
<p>需要注意的是，与案例三不同的是，案例四中查询语句的 where 部分用的是字段 c。</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/example4.png" class="" title="示例图">

<p>这次 session A 用字段 c 来判断，加锁规则跟案例三唯一的不同是：在第一次用 c&#x3D;10 定位记录的时候，索引 c 上加了 (5,10]这个 next-key lock 后，由于索引 c 是非唯一索引，没有优化规则，也就是说不会蜕变为行锁，因此最终 sesion A 加的锁是，索引 c 上的 (5,10] 和 (10,15] 这两个 next-key lock。</p>
<p>所以从结果上来看，sesson B 要插入（8,8,8) 的这个 insert 语句时就被堵住了。这里需要扫描到 c&#x3D;15 才停止扫描，是合理的，因为 InnoDB 要扫到 c&#x3D;15，才知道不需要继续往后找了。</p>
<br/>

<h4 id="案例五：唯一索引范围锁-bug"><a href="#案例五：唯一索引范围锁-bug" class="headerlink" title="案例五：唯一索引范围锁 bug"></a>案例五：唯一索引范围锁 bug</h4><p>前面的四个案例，我们已经用到了加锁规则中的两个原则和两个优化，接下来再看一个关于加锁规则中 bug 的案例。</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/example5.png" class="" title="示例图">

<p>session A 是一个范围查询，按照原则 1 的话，应该是索引 id 上只加 (10,15]这个 next-key lock，并且因为 id 是唯一键，所以循环判断到 id&#x3D;15 这一行就应该停止了。</p>
<p>但是实现上，InnoDB 会往前扫描到第一个不满足条件的行为止，也就是 id&#x3D;20。而且由于这是个范围扫描，因此索引 id 上的 (15,20]这个 next-key lock 也会被锁上。</p>
<p>所以你看到了，session B 要更新 id&#x3D;20 这一行，是会被锁住的。同样地，session C 要插入 id&#x3D;16 的一行，也会被锁住。照理说，这里锁住 id&#x3D;20 这一行的行为，其实是没有必要的。因为扫描到 id&#x3D;15，就可以确定不用往后再找了。但实现上还是这么做了，因此我认为这是个 bug。</p>
<br/>

<h2 id="GAP锁模拟测试"><a href="#GAP锁模拟测试" class="headerlink" title="GAP锁模拟测试"></a><strong>GAP锁模拟测试</strong></h2><p>新建一个表 test（id int primary key,num int)</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/table.jpg" class="" title="table">

<blockquote>
<p>select @@tx_isolation 查看当前session的事务级别 是默认的级别REPEATABLE READ</p>
</blockquote>
<p>做实验时，可以使用下面语句查看事务是否开启或者关闭</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    trx_id <span class="keyword">AS</span>  `事务ID`,</span><br><span class="line">    trx_state <span class="keyword">AS</span> `事务状态`,</span><br><span class="line">    trx_requested_lock_id  <span class="keyword">AS</span>  `事务需要等待的资源`,</span><br><span class="line">    trx_wait_started    <span class="keyword">AS</span>  `事务开始等待时间`,</span><br><span class="line">    trx_tables_in_use <span class="keyword">AS</span> `事务使用表`,</span><br><span class="line">    trx_tables_locked <span class="keyword">AS</span> `事务拥有锁`,</span><br><span class="line">    trx_rows_locked  <span class="keyword">AS</span> `事务锁定行`,</span><br><span class="line">    trx_rows_modified  <span class="keyword">AS</span> `事务更改行`</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">    information_schema.innodb_trx ;</span><br></pre></td></tr></table></figure>

<h3 id="非索引列的命中"><a href="#非索引列的命中" class="headerlink" title="非索引列的命中"></a><strong>非索引列的命中</strong></h3><p><strong>事务A开启事务</strong></p>
<p><em>DELETE FROM TEST WHERE NUM &#x3D; 50;</em></p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/a1.jpg" class="" title="a1">

<br/>

<p><strong>事务B开启事务</strong></p>
<ul>
<li><p>插入ID &#x3D; 2,NUM &#x3D;20 堵塞</p>
</li>
<li><p>插入ID&#x3D;6，NUM &#x3D; 60 堵塞</p>
</li>
<li><p>插入ID&#x3D;12，NUM &#x3D; 120 堵塞</p>
</li>
</ul>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/a2.jpg" class="" title="a2">

<p>当where中以非索引列作为更新，发现整个表均被锁住，<strong>这种完全不走索引的锁方式是表锁</strong></p>
<br/>

<h3 id="唯一索引的全部命中"><a href="#唯一索引的全部命中" class="headerlink" title="唯一索引的全部命中"></a><strong>唯一索引的全部命中</strong></h3><p>我将num字段作为唯一索引</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/unique_key.jpg" class="" title="unique_key">

<p>EXPLAIN 查看一下发现查询走的是唯一索引了</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/explain.jpg" class="" title="explain">

<br/>

<p>然后我们再试一下</p>
<p><strong>事务A开启事务</strong></p>
<p><em>SELECT * FROM TEST WHERE NUM &#x3D; 50 FOR UPDATE;</em></p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/b1.jpg" class="" title="b1">

<p><strong>事务B开启事务</strong></p>
<ul>
<li><p>ID &#x3D; 4,NUM &#x3D; 40 成功</p>
</li>
<li><p>ID &#x3D; 6,NUM &#x3D; 60 成功</p>
</li>
<li><p>ID &#x3D; 8,NUM &#x3D; 70 成功</p>
</li>
<li><p>插入其他范围记录没问题</p>
</li>
</ul>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/b2.jpg" class="" title="b2">

<br/>

<p>只有当NUM &#x3D; 50的时候是不允许的，那是因为NUM为唯一索引，值不能一致的限制</p>




<h3 id="唯一索引的部分命中"><a href="#唯一索引的部分命中" class="headerlink" title="唯一索引的部分命中"></a><strong>唯一索引的部分命中</strong></h3><p><strong>事务A开启事务</strong></p>
<p><em>SELECT * FROM TEST WHERE NUM IN (50,45) FOR UPDATE;</em></p>
<p>这里的45是不存在的，所以只命中了50</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/part_b1.jpg" class="" title="part_b1">

<br/>

<p><strong>事务B开启事务</strong></p>
<p>INSERT INTO TEST(ID,NUM) VALUES (6,60) </p>
<ul>
<li><p>ID &#x3D; 2,NUM &#x3D; 20  成功</p>
</li>
<li><p>ID &#x3D; 2,NUM &#x3D; 29 成功</p>
</li>
<li><p>ID &#x3D; 2,NUM &#x3D; 30 成功 会提示唯一索引冲突 Duplicate entry ‘30’ for key ‘index_num</p>
</li>
<li><p>ID&#x3D; 4,NUM &#x3D; 40, 堵塞</p>
</li>
<li><p>ID &#x3D; 6,NUM &#x3D; 60 成功</p>
</li>
<li><p>ID &#x3D; 8,NUM &#x3D; 71 成功</p>
</li>
</ul>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/part_b2.jpg" class="" title="part_b2">

<p>从而可以确定唯一索引的部分命中情况下 GAP锁的上锁范围是 左间隙（30，50) 也就是左间隙</p>
<br/>

<h3 id="普通索引的全部命中"><a href="#普通索引的全部命中" class="headerlink" title="普通索引的全部命中"></a><strong>普通索引的全部命中</strong></h3><p>当我改为非唯一索引时</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/c1.jpg" class="" title="c1">

<p><strong>事务A开启事务</strong></p>
<p>在事务A中删除NUM&#x3D;50,这下就是全部命中</p>
<p><em>DELETE FROM TEST WHERE NUM &#x3D; 50;</em></p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/c2.jpg" class="" title="c2">

<br/>

<p><strong>事务B开启事务</strong></p>
<ul>
<li><p>插入 ID &#x3D; 2,NUM &#x3D; 29 成功 ROLLBACK,开启事务</p>
</li>
<li><p>插入 ID &#x3D; 2,NUM &#x3D; 30 成功 </p>
</li>
<li><p>插入 ID&#x3D;55,NUM&#x3D;30 堵塞</p>
</li>
<li><p>插入 ID&#x3D;55,NUM&#x3D;70 成功</p>
</li>
<li><p>插入 ID &#x3D; 4,NUM &#x3D; 50 堵塞 ROLLBACK,开启事务</p>
</li>
<li><p>插入 ID &#x3D; 4,NUM &#x3D; 40 堵塞</p>
</li>
<li><p>插入 ID &#x3D; 6,NUM &#x3D; 60 堵塞</p>
</li>
<li><p>插入 ID&#x3D;8,NUM&#x3D;70 成功 ROLLBACK,开启事务</p>
</li>
<li><p>插入 ID&#x3D;8,NUM&#x3D;80 成功</p>
</li>
</ul>
<p>因此我们可以得出结论，在非唯一索引中的全部命中情况下，间隙（GAP)锁会在 索引左右间隙</p>
<p>这里是 （30，50],[50，70) 区间上锁。边界值 30，70 是否堵塞与主键值相关联</p>
<h3 id="普通索引的部分命中"><a href="#普通索引的部分命中" class="headerlink" title="普通索引的部分命中"></a><strong>普通索引的部分命中</strong></h3><p><strong>事务A开启事务</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> TEST <span class="keyword">WHERE</span> NUM <span class="operator">&gt;=</span> <span class="number">30</span> <span class="keyword">AND</span> NUM <span class="operator">&lt;=</span> <span class="number">45</span></span><br></pre></td></tr></table></figure>

<p>NUM &#x3D; 45是不存在的，所以为部分命中</p>
<img src="/2019/10/07/MySQL%E4%B8%ADGAP%EF%BC%88%E9%97%B4%E9%9A%99%EF%BC%89%E9%94%81%E7%9A%84%E6%8E%A2%E7%B4%A2/part_c1.jpg" class="" title="part_c1">

<br/>

<p><strong>事务B开启事务</strong></p>
<ul>
<li>INSERT INTO TEST VALUES(2,25); 阻塞</li>
<li>select num from test where num &#x3D; 10 for update; 执行成功</li>
<li>select num from test where num &#x3D; 50 for update; 阻塞</li>
<li>但是<br>INSERT INTO TEST VALUES(2,50); 执行成功了！！</li>
</ul>
<p><strong>个人认为是因为锁定的是覆盖索引 num的记录，而insert语句，是没法通过覆盖索引 index_num</strong><br><strong>来锁定的。</strong></p>
<p><strong>证明间隙锁区间为 （10,50]</strong></p>
<br/>

<h2 id="间隙锁总结："><a href="#间隙锁总结：" class="headerlink" title="间隙锁总结："></a><strong>间隙锁总结：</strong></h2><p><strong>对于当前读：</strong></p>
<h3 id="（1）对于主键索引和唯一索引的当前读"><a href="#（1）对于主键索引和唯一索引的当前读" class="headerlink" title="（1）对于主键索引和唯一索引的当前读"></a>（1）对于主键索引和唯一索引的当前读</h3><p>如果范围条件都命中，不会使用Gap锁，只会使用行锁。</p>
<p>如果范围条件部分命中或者都不命中，则使用Gap锁，锁住索引左间隙</p>
<h3 id="（2）对于普通索引的当前读"><a href="#（2）对于普通索引的当前读" class="headerlink" title="（2）对于普通索引的当前读"></a>（2）对于普通索引的当前读</h3><p>如果范围条件都命中，使用Gap锁，锁住索引左右间隙</p>
<p>如果范围条件部分命中，使用Gap锁，锁住索引左右间隙</p>
<h3 id="（3）对于不走索引的当前读"><a href="#（3）对于不走索引的当前读" class="headerlink" title="（3）对于不走索引的当前读"></a>（3）对于不走索引的当前读</h3><p>  锁住整个表，上的是表锁</p>
]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL中索引模块</title>
    <url>/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/</url>
    <content><![CDATA[<h2 id="索引模块"><a href="#索引模块" class="headerlink" title="索引模块"></a>索引模块</h2><p>我们通过问题来了解MySQL中索引模块相关的知识</p>
<h3 id="为什么要使用索引？"><a href="#为什么要使用索引？" class="headerlink" title="为什么要使用索引？"></a>为什么要使用索引？</h3><p>因为索引能够避免全表扫描，提升查询的效率和速度</p>
<h3 id="什么样的信息能够成为索引？"><a href="#什么样的信息能够成为索引？" class="headerlink" title="什么样的信息能够成为索引？"></a>什么样的信息能够成为索引？</h3><p>主键，唯一键等能让数据具备一定区分的字段</p>
<h3 id="索引的数据结构"><a href="#索引的数据结构" class="headerlink" title="索引的数据结构"></a>索引的数据结构</h3><p>B-Tree 、 B+-Tree、 BitMap、 Hash</p>
<h3 id="索引数据结构的推理"><a href="#索引数据结构的推理" class="headerlink" title="索引数据结构的推理"></a>索引数据结构的推理</h3><h4 id="二叉树可以满足吗？"><a href="#二叉树可以满足吗？" class="headerlink" title="二叉树可以满足吗？"></a>二叉树可以满足吗？</h4><p>一种极端的情况是 根节点为最大值或者最小值时，那么二叉树就退变成了一个链表</p>
<p>所以 PASS掉 </p>
<p><br/><span id="more"></span></p>
<h4 id="那平衡二叉树可以满足吗？"><a href="#那平衡二叉树可以满足吗？" class="headerlink" title="那平衡二叉树可以满足吗？"></a>那平衡二叉树可以满足吗？</h4><p>用平衡二叉树，当左右子树高度相差大于1时，进行左旋右旋自平衡操作</p>
<p>这种的查找时间复杂度 O(log2(n))</p>
<p>1、如果数据量为一百万时，树的高度为20，10亿数据量，数的高度为30</p>
<p>2、查询效率不稳定，如果查询的数据落在根节点，只需要一次IO，如果是叶子节点或者是支节点，会需要多次IO才可以。</p>
<p>3、节点存储的数据内容太少。没有很好利用操作系统和磁盘数据交换特性，也没有利用好磁盘IO的预读能力，这一点在下面谈谈自己理解。</p>
<br/>

<h4 id="那有没有一种结构能够解决二叉树的这种问题呢？"><a href="#那有没有一种结构能够解决二叉树的这种问题呢？" class="headerlink" title="那有没有一种结构能够解决二叉树的这种问题呢？"></a>那有没有一种结构能够解决二叉树的这种问题呢？</h4><p>有，多路平衡查找树：（Balance Tree）：</p>
<p>B Tree 是一个绝对平衡树，所有的叶子节点在同一高度，如下图所示：</p>
<p>详细可以查看这篇博客： <a href="https://my.oschina.net/u/3370829/blog/1301732">https://my.oschina.net/u/3370829/blog/1301732</a></p>
<h3 id="谈谈自己对于-树与磁盘IO次数的关系的理解"><a href="#谈谈自己对于-树与磁盘IO次数的关系的理解" class="headerlink" title="谈谈自己对于 树与磁盘IO次数的关系的理解"></a>谈谈自己对于 树与磁盘IO次数的关系的理解</h3><p>树的节点是存在 磁盘的页中的，</p>
<p>之所以树高度越高，磁盘IO次数越多，是因为一颗多阶树的每个节点是存放指针指向 页面的，一个页面中存放的就是就是一个节点指向的指针和节点的大小。</p>
<p>当阶数为2时，一个磁盘页面存放的就是左节点指针和右节点指针和当前节点值 ， 二叉平衡树的高度为3时，如果目标节点为叶子节点，那么它就得经过3次比较，读取3个磁盘页面，做3次IO操作。</p>
<p>那如果阶数是1001呢？ 那一个磁盘页面就可以存放的是 1000个值和1001一个指针。那假设它的目标节点也是 叶子节点并且高度为3，那这个总数据量是多少？  1000的3次方，10个亿的数据量。</p>
<p>这就是多阶平衡查找树的厉害之处，基本你的目标数据在树的高度为2-3，也就是你的磁盘IO次数只要2，3次。</p>
<p>那如果10亿数据量放在二叉平衡树中，树的高度是多少呢？</p>
<p>30次 ，如果是二叉平衡树，你最多需要寻找 30次。</p>
<p>这就是为什么要选择 多阶平衡查找树的原因。</p>
<p>系统从磁盘读取数据到内存时是以磁盘块（block）为基本单位的，位于同一个磁盘块中的数据会被一次性读取出来，而不是需要什么取什么。</p>
<p>InnoDB存储引擎中有页（Page）的概念，页是其磁盘管理的最小单位。<strong>InnoDB存储引擎中默认每个页的大小为16KB</strong>，可通过参数innodb_page_size将页的大小设置为4K、8K、16K，在<a href="http://lib.csdn.net/base/mysql">MySQL</a>中可通过如下命令查看页的大小：</p>
<p><em>show variables like ‘innodb_page_size’;</em></p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/page_size.jpg" class="" title="page_size">

<br/>

<h3 id="B-Tree规则："><a href="#B-Tree规则：" class="headerlink" title="B-Tree规则："></a>B-Tree规则：</h3><p>（1）树种的每个节点最多拥有m个子节点且m&gt;&#x3D;2,空树除外（注：m阶代表一个树节点最多有多少个查找路径，m阶&#x3D;m路,当m&#x3D;2则是2叉树,m&#x3D;3则是3叉）；</p>
<p>（2）除根节点外每个节点的关键字数量大于等于ceil(m&#x2F;2)-1个小于等于m-1个;（注：ceil()是个朝正无穷方向取整的函数 如ceil(1.1)结果为2)</p>
<p>（3）所有叶子节点均在同一层、叶子节点除了包含了关键字和关键字记录的指针外也有指向其子节点的指针只不过其指针地址都为null对应下图最后一层节点的空格子</p>
<p>（4）如果一个非叶节点有N个子节点，则该节点的关键字数等于N-1;</p>
<p>（5）所有节点关键字是按递增次序排列，并遵循左小右大原则；</p>
<p><strong>B TREE和B+TREE区别是什么？</strong></p>
<p>1、B+TREE 关键字的搜索采用的是左闭合区间，之所以采用左闭合区间是因为他要最好的去支持自增id，这也是mysql的设计初衷。即，如果id &#x3D; 1命中，会继续往下查找，直到找到叶子节点中的1。</p>
<p>2、B+TREE 根节点和支节点没有数据区，关键字对应的数据只保存在叶子节点中。即只有叶子节点中的关键字数据区才会保存真正的数据内容或者是内容的地址。而在B树种，如果根节点命中，则会直接返回数据。并且在B+TREE中，叶子节点不会去保存子节点的引用。</p>
<p>3、B+TREE叶子节点是顺序排列的，并且相邻的节点具有顺序引用的关系，如上图中叶子节点之间有指针相连接。</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/baddtree.jpg" class="" title="baddtree">

<br/>

<p><strong>MYSQL为什么最终要去选择B+TREE？</strong></p>
<p>注意这里可以不是3阶，多阶平衡树，可以设计多少阶就是多少阶</p>
<p>1、B+TREE是B TREE的变种，B TREE能解决的问题，B+TREE也能够解决（降低树的高度，增大节点存储数据量）</p>
<p>2、 B+TREE扫库和扫表能力更强，如果我们要根据索引去进行数据表的扫描，对B TREE进行扫描，需要把整棵树遍历一遍，而B+TREE只需要遍历他的所有叶子节点即可（叶子节点之间有引用）。</p>
<p>3、B+TREE磁盘读写能力更强，他的根节点和支节点不保存数据区，所有根节点和支节点同样大小的情况下，保存的关键字要比B TREE要多。而叶子节点不保存子节点引用。所以，B+TREE读写一次磁盘加载的关键字比B TREE更多。</p>
<p>4、B+TREE排序能力更强，如上面的图中可以看出，B+TREE天然具有排序功能。</p>
<p>5、B+TREE查询效率更加稳定，每次查询数据，高度波动不大。</p>
<h3 id="哈希索引"><a href="#哈希索引" class="headerlink" title="哈希索引"></a>哈希索引</h3><p>基于哈希表实现，对于每一行数据，存储引擎都会对所有的索引列计算一个哈希码（利用索引列对应的键值来计算）</p>
<p>哈希索引将所有的哈希码存储在索引中，同时在哈希表保存指向每个数据行的指针。</p>
<br/>

<p><strong>优点：</strong> </p>
<p>索引只需存储对应的哈希值，所以结构紧凑，查找速度非常快</p>
<p><strong>缺点：</strong></p>
<p>哈希索引数据并不是按照索引值顺序存储，所以无法用于排序或任何的范围查询</p>
<p>当出现哈希冲突时，存储引擎必须遍历链表中的所有对应行的引用，逐行比较，直到找到符合全部条件的行。所以当哈希冲突多的时候，索引的维护操作就很高，表中没删除一行，存储引擎需要遍历对应哈希值的链表中每一行，找到并删除，冲突越多，代价越大。</p>
<h3 id="密集索引和稀疏索引的区别"><a href="#密集索引和稀疏索引的区别" class="headerlink" title="密集索引和稀疏索引的区别"></a>密集索引和稀疏索引的区别</h3><p>密集索引：</p>
<p>叶子节点保存的是整行数据，由于密集索引决定了表的物理排列顺序，一个表只有一个物理排列顺序，所以一个表只能创建一个密集索引</p>
<p>稀疏索引：</p>
<p>叶子节点仅保存了键位信息以及该行数据的地址，有的稀疏索引只保存了键位信息机器主键</p>
<p>聚簇索引：</p>
<p>表数据按顺序存储，即索引顺序和表记录物理存储顺序一致。</p>
<p>聚簇索引 叶子节点存储数据行和B-Tree索引。</p>
<p>在一个表中只能有一个聚簇索引，因为真实物理存储顺序只能有一种</p>
<h4 id="InnoDB"><a href="#InnoDB" class="headerlink" title="InnoDB:"></a>InnoDB:</h4><p><strong>表</strong> 根据主键顺序以<strong>B+ 树索引的形式存放</strong>的,</p>
<p>这种存储方式的表称为<strong>索引</strong>组织表</p>
<ul>
<li><p>主键索引(<strong>聚簇索引</strong>): 叶子节点存的是整行数据 是 密集索引</p>
</li>
<li><p>非主键索引(<strong>二级索引</strong>): 叶子节点内容是主键的值, 是稀疏索引</p>
<p>有且只有一个密集索引。密集索引的选取规则如下：</p>
</li>
<li><p>若一个主键被定义，该主键则作为密集索引</p>
</li>
<li><p>若没有主键被定义，该表的第一个唯一非空索引则作为密集索引</p>
</li>
<li><p>若不满足以上条件，innoDB内部会生成一个隐藏主键（密集索引，默认6字节，随着表增大而增大）</p>
</li>
<li><p>非主键索引存储相关键位和其对应的主键值，包含两次查找</p>
</li>
</ul>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/innodb.jpg" class="" title="innodb">

<br/>



<h4 id="MyISAM"><a href="#MyISAM" class="headerlink" title="MyISAM"></a>MyISAM</h4><p>对MyISAM 来说, 主键索引和其他索引没有任何区别, 都是稀疏索引 , </p>
<p>表数据存储在独立的地方, 表数据和索引的分开的, 索引用地址指向表数据</p>
<p>表person_info_large是InnoDB表</p>
<p>表shop_info_small是MyISAM表（该模式下 索引和数据是分开存放的，看后缀MYI是文件是存放索引的，MYD是存放数据的，这里特意没有插入数据）</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/myisam.jpg" class="" title="myisam">

<br/>



<h2 id="索引衍生的面试题："><a href="#索引衍生的面试题：" class="headerlink" title="索引衍生的面试题："></a>索引衍生的面试题：</h2><h3 id="如何定位并优化慢查询Sql"><a href="#如何定位并优化慢查询Sql" class="headerlink" title="如何定位并优化慢查询Sql"></a>如何定位并优化慢查询Sql</h3><p>大致思路</p>
<ol>
<li>根据慢日志定位慢查询Sql</li>
<li>使用explain等工具分析sql</li>
<li>修改sql或尽量让sql走索引</li>
</ol>
<br/>

<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/log_setting.jpg" class="" title="log_setting">

<p>slow_query_log 慢日志是否开启，默认OFF</p>
<p>slow_query_log_file: 慢日志地址</p>
<p>long_query_time： 默认10s,当sql语句执行超过默认值就记录到慢日志中</p>
<p>SHOW STATUS LIKE ‘%slow_queries%’  查询慢查询的数量</p>
<p>首先：打开慢日志记录,设置默认为慢查询的执行时间为 1S</p>
<p><em>SET GLOBAL SLOW_QUERY_LOG &#x3D; ON;</em></p>
<p><em>SET GLOBAL LONG_QUERY_TIME &#x3D; 1;</em></p>
<p>注意，这里要关闭连接再重连数据库才能生效</p>
<br/>

<p>当我们有一条查询语句执行超过1S，就会记录下来</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/slow_log.jpg" class="" title="slow_log">

<p>实际花费了 query_time： 6.33 S</p>
<br/>

<p>可以在查询语句前+ EXPLAIN 进行分析</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/explain.jpg" class="" title="explain">



<h3 id="EXPLAIN重要列字段含义"><a href="#EXPLAIN重要列字段含义" class="headerlink" title="EXPLAIN重要列字段含义"></a>EXPLAIN重要列字段含义</h3><h4 id="type"><a href="#type" class="headerlink" title="type"></a>type</h4><p><em><strong>system &gt; const &gt; eq_ref &gt; ref &gt; fulltext &gt; ref_or_null &gt; index_merge &gt;</strong></em></p>
<p><em><strong>unqiue_subquery &gt; index_subquery &gt; range &gt; index &gt; all</strong></em></p>
<p>当 type &#x3D; all，说明是全表扫描</p>
<br/>

<h4 id="extra"><a href="#extra" class="headerlink" title="extra"></a>extra</h4><img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/extra.jpg" class="" title="extra">



<h3 id="联合索引的最左匹配原则成因"><a href="#联合索引的最左匹配原则成因" class="headerlink" title="联合索引的最左匹配原则成因"></a>联合索引的最左匹配原则成因</h3><p>最左匹配原则： 联合索引只匹配 左侧索引或者全部索引,当从左至右遇到范围查询 （&gt;,&lt;,between,like) 就停止匹配，只使用之前匹配的作为索引。</p>
<p>如果是 &#x3D; 或者 in 可以乱序，mysql可以识别</p>
<h4 id="示例1："><a href="#示例1：" class="headerlink" title="示例1："></a>示例1：</h4><p>User表有一个联合索引 Index_find(name,country) ，</p>
<p>则当 查询语句 where name &#x3D; ‘’ and country &#x3D; ‘’ 走的是 index_find索引 </p>
<p>当 where name &#x3D; ‘ ‘  走的也是联合索引</p>
<p>当 where country &#x3D; ‘ ‘  ； 时如果没有country列的单独索引就会走全表扫描 用 explain查看 type &#x3D; all</p>
<h4 id="示例2："><a href="#示例2：" class="headerlink" title="示例2："></a>示例2：</h4><p>  demo表中 有 一个联合索引 index_find( A,B,C,D)</p>
<p>那么当 where A&#x3D; 1 AND B &#x3D; 1 AND C &gt; 1 AND D &#x3D; 1 时，匹配到 C &gt; 1会终止匹配，注意：会使用索引，索引为 （A,B,C)</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/s1.jpg" class="" title="s1">

<p>当我让 C为范围查询时， 联合索引到 C终止，注意是执行了联合索引了的， 只是执行的是 （A,B,C)这个联合索引，这个表中的一个key的长度为5</p>
<p>此时你估计会有疑问，不是只有一个联合索引（A,B,C,D)吗？ ，其实这个联合索引是包含从左侧开始的索引的 如： （A),(A,B),(A,B,C)</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/s2.jpg" class="" title="s2">

<p>当我们不执行C的范围查询时，这个时候用的是 （A,B,C,D) 这个 20长度的联合索引</p>


<br/>

<p><strong>注意这个where 检查顺序是按照联合索引从左至右的检测的</strong></p>
<p>当我们把索引改为 (A,B,D,C)时</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/index.jpg" class="" title="index">

<p>发现C执行范围查询并不影响D的并入，<strong>因为 D在 C的前面</strong></p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/left.jpg" class="" title="left">



<h3 id="索引越多越好吗？"><a href="#索引越多越好吗？" class="headerlink" title="索引越多越好吗？"></a>索引越多越好吗？</h3><p>一个索引的建立意味着要用额外的空间记录 索引列对应表数据的地址</p>
<img src="/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/index_show.jpg" class="" title="index_show">

<p>数据变更的用时要维护索性，更多的索引意味着更多的维护成本</p>
<p>所以当数据量较小的时候，是没有必要维护索引的。</p>
]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL打怪之路</title>
    <url>/2020/11/28/MySQL%E6%89%93%E6%80%AA%E4%B9%8B%E8%B7%AF/</url>
    <content><![CDATA[<p>​      </p>
<p>​    最近花时间看了一部分的《高性能MySQL》 红宝书，以及 丁奇大佬的 《MySQL实战45讲》，对 MySQL的理解又加深了不少，要保持日常MySQL的高性能，避免犯一些低级错误，需要我们对 MySQL的体系结构、逻辑设计、每个模块都有一定的理解，这里便萌生了写几篇总结的博客的想法。</p>
<p>​        计划是针对各个重要的模块分别做 知识点总结以及高性能需要注意的点的阐述。当前的这篇就做一个全篇的概述吧，针对一个开发日常面对MySQL如何保证整体的高性能，需要注意哪些坑。</p>
<span id="more"></span> 

<br/>

<p>接下来我最主要概述以下几点</p>
<ul>
<li>MySQL的逻辑架构</li>
<li>什么才是日常开销最大的地方？</li>
<li>MySQL因此做了哪些措施？</li>
<li>索引必须了解的知识</li>
<li>安全性是如何保证的？</li>
</ul>
<p>​        MVCC事务多版本控制、索引、日志系统的设计都是很大一块的内容，也是核心，后续我会一一补充上来，这里暂时不做详细说明。</p>
<br/>



<h2 id="MySQL的逻辑架构"><a href="#MySQL的逻辑架构" class="headerlink" title="MySQL的逻辑架构"></a>MySQL的逻辑架构</h2><img src="/2020/11/28/MySQL%E6%89%93%E6%80%AA%E4%B9%8B%E8%B7%AF/b1.jpg" class="" title="架构图">

<p>上图是对 一个查询语句在MySQL中需要经历哪些模块</p>
<p>在MySQL中主要分为两大模块 <strong>Server层和引擎层</strong></p>
<h4 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a><strong>客户端</strong></h4><p><strong>Server层（在内存中操作）</strong></p>
<ul>
<li>连接器： 用户权限校验</li>
<li>缓存：对操作语句涉及加载的数据存储在内存中</li>
<li>分析器： 对sql语句的词法、语法校验</li>
<li>优化器：sql优化，提供自己认为最佳的执行方式</li>
<li>执行器：操作执行引擎并返回结果</li>
</ul>
<p><strong>引擎层（在磁盘中操作）</strong></p>
<p>大家知道sql语句会涉及哪些模块，这些模块有哪些用即可，不需要做过深的研究。</p>
<br/>

<h2 id="什么才是日常开销最大的地方？"><a href="#什么才是日常开销最大的地方？" class="headerlink" title="什么才是日常开销最大的地方？"></a>什么才是日常开销最大的地方？</h2><p>​        在MySQL中单论各模块的执行，什么是开销最大的呢？<strong>是对磁盘的读写，也就是IO开销最大</strong>，首先我们要知道数据是存储在磁盘，而磁盘的数据分布在各个数据页中，页（Page）是 Innodb 存储引擎用于管理数据的最小磁盘单位，一个数据页为 16 K。假设一行数据记录为16字节，那么一个数据页最多可以存放1024条记录（不考虑数据空洞的情况）。</p>
<p>​        我们必须要明白一点，那就是不管你是用索引(主键索引、普通索引、覆盖索引）还是全表扫描，只要你所需的数据没有在缓存中命中，你就必须去磁盘中加载数据页，把一页数据页里的数据记录加载到内存中进行遍历来找到对应的记录，MySQL的索引通过B+树的结构保证数据能在3次左右的磁盘IO读取便能定位到一条数据所在的磁盘数据页位置。</p>
<p>​         大家对索引的B+树结构和磁盘数据页不太理解的话，可以看往期的文章 <a href="http://www.eiletxie.cn/2019/10/07/MySQL%E4%B8%AD%E7%B4%A2%E5%BC%95%E6%A8%A1%E5%9D%97/">MySQL的索引模块</a>，这里不做过多说明。</p>
<p>​         因此我们日常要注意的和MySQL所要做的都是尽可能的减少 磁盘的读写，在内存中进行操作或者业务的设计来尽量避免大量IO读写数据。</p>
<br/>

<h3 id="那么MySQL是怎么设计来减少磁盘读写的呢？"><a href="#那么MySQL是怎么设计来减少磁盘读写的呢？" class="headerlink" title="那么MySQL是怎么设计来减少磁盘读写的呢？"></a><strong>那么MySQL是怎么设计来减少磁盘读写的呢？</strong></h3><h5 id="1、通过缓存命中来减少磁盘读"><a href="#1、通过缓存命中来减少磁盘读" class="headerlink" title="1、通过缓存命中来减少磁盘读"></a><strong>1、通过缓存命中来减少磁盘读</strong></h5><p>​       假设表A是在系统的很多业务模块都会被用到，即表A经常要被查询，比如系统配置、用户信息等，难道每次都去数据库查吗？肯定是加载到缓存中，如果缓存中有直接返回结果来减少IO开销的。</p>
<br/>

<h5 id="2、通过change-buffer来减少磁盘写"><a href="#2、通过change-buffer来减少磁盘写" class="headerlink" title="2、通过change buffer来减少磁盘写"></a><strong>2、通过change buffer来减少磁盘写</strong></h5><p>​      既然可以通过缓存记录数据，减少去磁盘读取，那能不能有一种方式在缓存中记录客户端的更新语句，等到合适的时候再写入到磁盘中，假如这个时候要查询数据，先加载磁盘中的数据到缓存，再将更新语句逻辑merge到对应的数据记录中，然后返回结果。没错，MySQL就有这种东西，叫做change buffer。</p>
<p>​      这里需要说明change buffer是什么？ （mysql5.5之前只支持记录插入语句，又称为insert buffer,mysql 5.5以后支持更新和删除语句的记录）。</p>
<p>​      我们看一下MySQL官网5.7版本对于 change buffer的说明</p>
<img src="/2020/11/28/MySQL%E6%89%93%E6%80%AA%E4%B9%8B%E8%B7%AF/b2.jpg" class="" title="说明">

<br/>

<p>​       change buffer是一种特殊的数据结构，用于在二级索引页不在缓冲池中时将更改缓存到二级索引页（非聚簇索引即非主键索引，这里你直接理解成普通索引和数据页就行，因为我们要更新的就是这些数据）。缓冲的更改（可能由INSERT，UPDATE或DELETE操作（DML）引起）将在以后通过其他读取操作将页面加载到缓冲池中时合并。</p>
<br/>

<img src="/2020/11/28/MySQL%E6%89%93%E6%80%AA%E4%B9%8B%E8%B7%AF/b3.jpg" class="" title="change buffer">

<p>​      可能你还不能理解，我这里举一个例子，假设 一个用户行为记录分析表，它的数据是经常需要更新的，当用户做了几百次操作，如果没有change buffer，你需要读取一次索引对应磁盘数据页到内存中，更新了再写入到磁盘中，重复上百次。这里你需要明白一个机制（<strong>如果表更改了，那么使用这个表的所有缓存查询将不再有效，查询缓存中值相关条目会被清空，这是为了保证数据的一致性</strong>）</p>
<p>​       这个时候你就明白change buffer的作用了，它不仅减少了随机的磁盘写，转变成了顺序磁盘写，某种意义上如果是重复数据的更新，也减少了磁盘读。</p>
 <br/>

<p>细心的读者会发现，光有change buffer是不行的，因为一旦MySQL异常重启，岂不是缓存中的数据会全部丢失?  这就得用到 redo log,如果发生异常的情况，会根据redo log做数据的更新，<strong>那如果 是刚写入change buffer，还没有写入到 redo log的时候挂掉了咋办？</strong></p>
<p>​     DML语句是会开启事务的，只有redo log成功写入提交后，当前事务才会commit的，如果redo log没写就失败是会直接回滚事务的，又或者服务端挂掉则缓存清空，redo log没有记录到对应更新而已，这种情况需要重头再来，不会导致数据不一致的。</p>
<br/>

<h2 id="索引小知识"><a href="#索引小知识" class="headerlink" title="索引小知识"></a>索引小知识</h2><p>​      大家都了解，索引就像是书的目录，是为了快速查找数据。它是由B+树的结构，正常的业务在亿级别以下的表，能够做到3次查询即可找到对应的数据页。</p>
<p>​      但是索引也可能因为你的查询方式，或者一些限制因素从而失效，或者Mysql选错了索引等。</p>
<p>​      关于查询方式不对导致未使用到的索引的情况有很多，范围查询，类型转换（尤其是很少注意的隐式转换），使用内置函数，复合索引查询条件顺序不对等等。这是一块很大的内容，这篇文章只讲总览，就不针对性的说，大家这方面不太了解的，我推荐可以学习B站上 尚硅谷的Mysql高级篇，那个视频对这块有比较详细的讲述。</p>
<p>​       我这块说和大家说的是一些平时很少会注意的点。</p>
<br/>

<h3 id="普通索引和唯一索引的区别"><a href="#普通索引和唯一索引的区别" class="headerlink" title="普通索引和唯一索引的区别"></a><strong>普通索引和唯一索引的区别</strong></h3><p>​        唯一索引要求 数据列组合的值是唯一的，普通索引没有这个要求，这个大家都知道。还有呢？大家有没有想过，这两种索引对DML语句有什么影响吗？</p>
<p><strong>对SELECT语句来说：</strong></p>
<p>如果是等值查询，在索引遍历时，找到符合要求的记录后，唯一索引是不需要往下遍历，因为只有一条主键ID，而普通索引可能存在多条，需要继续遍历，直到找到的数据记录不符合要求为止，不过这个对性能几乎没有影响。</p>
<br/>

<p><strong>对UPDATE、INSERT语句来说：</strong></p>
<p>​        这就是考察大家的地方，我们前面说过 change buffer。再回顾一下它的作用，把用户的非SELECT的DML语句记录到缓冲池，当读取对应表记录到缓存时，将顺序的把这些DML语句执行更新该记录，然后再写回数据页，大大减少了磁盘读和磁盘写。</p>
<p>​        如果是唯一索引，能不能将INSERT语句缓存起来，作为change buffer呢？明显不能，为什么？</p>
<p>​       因为表里面可能已经存在对应的数据，你必须去查表判断唯一性，而普通索引则没这个必要，这就是我们需要注意的区别。</p>
<p>​        那你此时还有疑问，那为什么UPDATE语句也不能作为change buffer呢？</p>
<p>​       一样的数据列唯一性问题，唯一索引又不一定非得是主键索引，假设我设置唯一索引列是name,update user set name &#x3D; ‘李四’ where name &#x3D; ‘张三’ ，你必须在更新前去数据库查询索引判断name&#x3D;’李四’存不存在。</p>
 <br/>

<h3 id="Change-Buffer的适用场景"><a href="#Change-Buffer的适用场景" class="headerlink" title="Change Buffer的适用场景"></a><strong>Change Buffer的适用场景</strong></h3><p>​      change buffer是将更新语句存储在缓冲区中，当执行读取语句时再将对应的更新语句顺序更新，这个操作叫merge，然后刷回磁盘。这种是特别适合写多读少的场景的。</p>
<p>​       但是如果是读多写少的场景或者是写完马上就要读的场景，这种情况其实是不适合的，将更新先记录在 change buffer，但之后由于马上要访问这个数据页，会立即触发 merge 过程。这样随机访问 IO 的次数不会减少，反而增加了 change buffer 的维护代价。所以，对于这种业务模式来说，change buffer 反而起到了副作用。</p>
<br/>

<h3 id="字符串加索引注意点"><a href="#字符串加索引注意点" class="headerlink" title="字符串加索引注意点"></a><strong>字符串加索引注意点</strong></h3><p>​       如果你需要给一个邮箱或者是身份证号加索引，你怎么加好呢？索引是有开销的，如果你就按照整个身份证号来存，开销较大，在一些业务场景下，有很多东西是没必要存的。</p>
<p>​      假设你只需要记录某个县的居民身份证号，那前面的6位是地址码，就不用记录。又例如记录某个公司的用户邮箱 后缀.com是不是可以去除，其实在某些情况下我们如何取字符串作为索引，一个是通过先去除无用的字符，二是通常查询来判断</p>
<p>​     使用前缀索引，定义好长度，就可以做到既节省空间，又不用额外增加太多的查询成本。</p>
<p>我们可以截取对应长度的字段来判断区分度</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> </span><br><span class="line">  <span class="built_in">count</span>(<span class="keyword">distinct</span> <span class="keyword">left</span>(email,<span class="number">4</span>)）<span class="keyword">as</span> L4,</span><br><span class="line">  <span class="built_in">count</span>(<span class="keyword">distinct</span> <span class="keyword">left</span>(email,<span class="number">5</span>)）<span class="keyword">as</span> L5,</span><br><span class="line">  <span class="built_in">count</span>(<span class="keyword">distinct</span> <span class="keyword">left</span>(email,<span class="number">6</span>)）<span class="keyword">as</span> L6,</span><br><span class="line">  <span class="built_in">count</span>(<span class="keyword">distinct</span> <span class="keyword">left</span>(email,<span class="number">7</span>)）<span class="keyword">as</span> L7,</span><br><span class="line"><span class="keyword">from</span> <span class="keyword">User</span>;</span><br></pre></td></tr></table></figure>



<p>当然，使用前缀索引很可能会损失区分度，所以你需要预先设定一个可以接受的损失比例，比如 5%。然后，在返回的 L4~L7 中，找出不小于 L * 95% 的值，假设这里 L6、L7 都满足，你就可以选择前缀长度为 6。</p>
<p>​      当然你也可以考虑使用其他的方法来保证区分度。</p>
<br/>

<p><strong>hash对应字段的值作为索引</strong></p>
<p>​      这种在很多业务上都经常见到，我举一个我公司的例子，假设我要确定相关的记录，没法通过主键id去找，我在业务流的上面中只能获取十几个字段的值，怎么快速找到下面业务流中符合要求的数据呢？ 我们在业务的设计上，在插入记录时新增一个 hash字段，将这十几个字段的值组合成一个字符串然后hash出一个值存储。这样我们在查找时在应用层直接算出hash值就能够根据hash值建的索引进行快速查找，避免了全表扫描。</p>
<br/>

<p><strong>倒序存储</strong></p>
<p>​       这种业务上遇到的不多，例如身份证号前面的区分度很低，我们可以倒序的然后查询区分度，选择保留的长度，裁剪字符串，然后再存起来。</p>
<br/>

<h3 id="安全性问题"><a href="#安全性问题" class="headerlink" title="安全性问题"></a><strong>安全性问题</strong></h3><p>​     安全性问题涉及 主备同步、crash-safe，其实需要了解的还是日志的设计，redo log（重做日志），binlog(归档日志）</p>
<p>​      redo log在许多应用场景中一般只存储DML语句执行后的结果值，binlog是详细记录了sql语句，通过这两黄金搭档来保证crash-safe和主备同步。后续会单独写一篇博客，一是涉及内容过多，二是我现在也不能说对其完全了解，尤其是其结构设计这块。</p>
]]></content>
      <categories>
        <category>数据库优化</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis-漏斗限流</title>
    <url>/2021/04/18/Redis-%E6%BC%8F%E6%96%97%E9%99%90%E6%B5%81/</url>
    <content><![CDATA[<p>​      Redis可以实现限流，这个有所耳闻，只是我没想到可以支持一种高级特性-漏斗限流，话不多说，开始今天的限流总结吧。</p>
<p>申明：本人主要语言是Java，但是去年末学的Python不用已经忘的差不多了，所以博客中的代码尽量会用Python来举例</p>
<span id="more"></span> 

<h3 id="限流需求"><a href="#限流需求" class="headerlink" title="限流需求"></a><strong>限流需求</strong></h3><p>所有的限流需求基本都是 限定的时间内只能允许进行有限次数的操作</p>
<p>所以我们先假设一个限流需求： 某个账户信息API只允许单个用户每分钟申请访问5次，否则拒绝访问。</p>
<br/>

<h3 id="解决方案："><a href="#解决方案：" class="headerlink" title="解决方案："></a><strong>解决方案：</strong></h3><p>​      对数据结构算法比较了解的同学，我说一个场景你就能猜到用什么来处理这种需求了，写一个方法判断一个数组中固定区域内的数值和是否大于某个值（如果会频繁调用你怎么优化？）。</p>
<p>你可能会想的是直接遍历该区域判断，但是为了性能考虑就会用到滑动窗口。</p>
<p>在这个限流需求中，我们可以把时间当做坐标轴，固定区域为过去的一分钟内，统计固定区域内的请求成功次数</p>
<p>下面的代码删除一分钟以前的数据是为了性能考虑，避免时间一长，单个key过大。</p>
<img src="/2021/04/18/Redis-%E6%BC%8F%E6%96%97%E9%99%90%E6%B5%81/slipwindow.png" class="" title="滑动窗口图">

<br/>

<br/>

<h3 id="简易限流算法"><a href="#简易限流算法" class="headerlink" title="简易限流算法"></a>简易限流算法</h3><p>假如用Redis来实现咋玩？</p>
<p>首先我们需要有一种结构来存储请求API的信息，这些信息包括用户ID、请求时间、API标识，就用zset,同时有一个可以判断是否可以请求的API</p>
<br/>

<h4 id="SimpleRateLimiter-py"><a href="#SimpleRateLimiter-py" class="headerlink" title="SimpleRateLimiter.py"></a>SimpleRateLimiter.py</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">client = redis.StrictRedis(host=<span class="string">&#x27;xxxx&#x27;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, password=<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">is_action_allowed</span>(<span class="params">user_id, action_way, period, max_count</span>):</span><br><span class="line">    key = <span class="string">&#x27;accountInfo:%s:%s&#x27;</span> % (user_id, action_way)</span><br><span class="line">    now_ts = <span class="built_in">int</span>(time.time() * <span class="number">1000</span>)  <span class="comment"># 毫秒时间戳</span></span><br><span class="line">    <span class="keyword">with</span> client.pipeline() <span class="keyword">as</span> pipe:</span><br><span class="line">        <span class="comment"># 记录行为</span></span><br><span class="line">        <span class="comment"># value和score都使用毫秒时间戳</span></span><br><span class="line">        <span class="built_in">dict</span> = &#123;&#125;</span><br><span class="line">        <span class="built_in">dict</span>[now_ts] = now_ts</span><br><span class="line">        pipe.zadd(key, <span class="built_in">dict</span>)</span><br><span class="line">        <span class="comment"># 移除时间窗口之前的行为记录，剩下的都是时间窗口之内的</span></span><br><span class="line">        pipe.zremrangebyscore(key, <span class="number">0</span>, now_ts - period * <span class="number">1000</span>)</span><br><span class="line">        <span class="comment"># 获取窗口内的行为数量</span></span><br><span class="line">        pipe.zcard(key)</span><br><span class="line">        <span class="comment"># 设置zset过期时间，避免冷用户持续占用内存</span></span><br><span class="line">        <span class="comment"># 过期时间应该等于时间窗口的长度，再多宽限一秒</span></span><br><span class="line">        pipe.expire(key, period + <span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 批量执行</span></span><br><span class="line">        _, _, current_count, _ = pipe.execute()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 比较数量是否超标</span></span><br><span class="line">    <span class="keyword">return</span> current_count &lt;= max_count</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">20</span>):</span><br><span class="line">    <span class="built_in">print</span>(is_action_allowed(<span class="string">&#x27;eilet&#x27;</span>, <span class="string">&#x27;apply&#x27;</span>, <span class="number">60</span>, <span class="number">5</span>))</span><br></pre></td></tr></table></figure>

<br/>

<h4 id="其他说明："><a href="#其他说明：" class="headerlink" title="其他说明："></a>其他说明：</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>、redis pipeline有什么用？  </span><br><span class="line">管道技术，指的是客户端允许将多个请求依次发给服务器，过程中而不需要等待请求的回复，在最后再一并读取结果即可。 批处理指令，减少IO和主机内核态与用户态的切换，提供性能，需要客户端和服务端都支持 pipeline机制可以优化吞吐量，但无法提供原子性/事务保障，而这个可以通过Redis-Multi等命令实现 </span><br><span class="line"></span><br><span class="line"><span class="number">2</span>、zremrangebyscore api 介绍 </span><br><span class="line">Zremrangebyscore 命令用于移除有序集中，指定分数（score）区间内的所有成员。</span><br></pre></td></tr></table></figure>

<br/>

<h4 id="算法缺点："><a href="#算法缺点：" class="headerlink" title="算法缺点："></a>算法缺点：</h4><p>1、无法保证原子性，即事务特点</p>
<p>2、如果是1分钟允许几十万次这种，也是不太合适的，数据量过大了，会消耗大量的存储空间。</p>
<br/>

<h3 id="漏斗限流"><a href="#漏斗限流" class="headerlink" title="漏斗限流"></a>漏斗限流</h3><p>那如果是大容量的限流，可以考虑这种高级限流算法-漏斗限流</p>
<p>​     把数据容器看做成一个漏斗，漏嘴流出数据，漏顶流入数据，流出数据的速率是固定的，就是 限流需求中 5个 &#x2F; 60s，流入通常是1，当然你也可以批量申请，只要漏斗中的剩余容量还足够。</p>
<img src="/2021/04/18/Redis-%E6%BC%8F%E6%96%97%E9%99%90%E6%B5%81/funnel.png" class="" title="漏斗图">

<br/>

<br/>

<h4 id="FunnelRateLimiter-py"><a href="#FunnelRateLimiter-py" class="headerlink" title="FunnelRateLimiter.py"></a>FunnelRateLimiter.py</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Funnel</span>(<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self,capacity,leaking_rate</span>):</span><br><span class="line">        <span class="variable language_">self</span>.capacity = capacity           <span class="comment">#漏斗容量</span></span><br><span class="line">        <span class="variable language_">self</span>.leaking_rate = leaking_rate   <span class="comment">#漏嘴流水速率</span></span><br><span class="line">        <span class="variable language_">self</span>.left_quota = capacity         <span class="comment">#漏斗剩余空间</span></span><br><span class="line">        <span class="variable language_">self</span>.leaking_ts = time.time()      <span class="comment">#上一次漏水时间</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">make_space</span>(<span class="params">self</span>):</span><br><span class="line">        now_ts = time.time()</span><br><span class="line">        <span class="comment"># 距离上一次漏水过去了多久</span></span><br><span class="line">        interval_ts = now_ts - <span class="variable language_">self</span>.leaking_ts</span><br><span class="line">        <span class="comment"># 腾出来的空间</span></span><br><span class="line">        interval_quota = interval_ts * <span class="variable language_">self</span>.leaking_rate</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;interval_quota = %s , left_quota = %s &quot;</span> % (interval_quota,<span class="variable language_">self</span>.left_quota))</span><br><span class="line">        <span class="comment"># 腾出来的空间太少，则return</span></span><br><span class="line">        <span class="keyword">if</span> interval_quota &lt; <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        <span class="variable language_">self</span>.left_quota += interval_quota</span><br><span class="line">        <span class="variable language_">self</span>.leaking_ts = now_ts</span><br><span class="line">        <span class="comment"># 剩余空间不得大于容量</span></span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.left_quota &gt; <span class="variable language_">self</span>.capacity:</span><br><span class="line">            <span class="variable language_">self</span>.left_quota = <span class="variable language_">self</span>.capacity</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">watering</span>(<span class="params">self,quota</span>):</span><br><span class="line">        <span class="variable language_">self</span>.make_space()</span><br><span class="line">        <span class="comment"># 判断剩余空间是否足够</span></span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.left_quota &gt;= quota:</span><br><span class="line">            <span class="variable language_">self</span>.left_quota -= quota</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">funnels = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># capacity 漏斗容量</span></span><br><span class="line"><span class="comment"># leaking_rate 漏嘴流水速率 quota/s0</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">is_action_allowed</span>(<span class="params">user_id,action_key,capacity,leaking_rate</span>):</span><br><span class="line">    key = <span class="string">&#x27;%s:%s&#x27;</span> % (user_id,action_key)</span><br><span class="line">    funnel = funnels.get(key)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> funnel:</span><br><span class="line">        funnel = Funnel(capacity,leaking_rate)</span><br><span class="line">        funnels[key] = funnel</span><br><span class="line">    <span class="keyword">return</span> funnel.watering(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">15</span>):</span><br><span class="line">    <span class="built_in">print</span>(is_action_allowed(<span class="string">&#x27;eilet&#x27;</span>,<span class="string">&#x27;reply&#x27;</span>,<span class="number">5</span>,<span class="number">5</span> / <span class="number">60</span>))</span><br></pre></td></tr></table></figure>

<p>   Funnel对象的make_space方法是漏斗算法的核心，其次是每次灌水前都会被调用以触发漏水，给漏斗腾出空间来。能腾出多少空间取决于过去了多久以及流水的速率。</p>
  <br/>



<p>从上面的代码我们可以看到 储存的funnel数组是放在python对应的机器中，这就意味是实际是单机算法，当然你觉得放在redis中不就好了吗？</p>
<p>​     那就错了，你忽略了 漏斗限流算法是超大数据量下的高频调用，高频网络API调用，涉及内核态到用户态的互相切换，funnel对象的序列化和反序列化，啧啧啧，这性能绝对不会比上面的简易限流算法好多少，甚至会在这种场景下直接崩了。</p>
<p>​     第二个问题是 make_space中对数据的判断，流水，灌水这些组合操作不具备原子性，如果加锁就要考虑加锁失败的可能，以及重试或者放弃，啧啧啧，有点麻烦了。</p>
<p>那我们想一想有没有什么办法可以解决这些麻烦的问题？</p>
<p>   如果我来做的话，我会将Redis中封装一个API，在Redis中内部将key对应的漏斗做限流，这样可以保证原子性也可以统一重试逻辑，用户只要调用我这个API，传入参数即可得知是否被限流，同时避免了单机的尴尬或分布式下funnel数组存放在redis反序列和序列化的过程。</p>
<p>  万幸的事，Redis4.0 提供了一个限流Redis模块，它叫Redis-Cell。该模块也使用了漏斗算法，并提供了原子的限流指令。</p>
<br/>

<h3 id="Redis-Cell"><a href="#Redis-Cell" class="headerlink" title="Redis-Cell"></a>Redis-Cell</h3><p>​    该模块只有一条指令 cl.throttle</p>
<p> cl.throttle  key  容量-1   允许的令牌数   时间范围   申请的令牌数</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">127.0.0.1:6379&gt; cl.throttle eilet:reply 4 5 60 1</span><br><span class="line">1)(<span class="built_in">integer</span>) 0 <span class="comment"># 0表示允许，1表示拒绝</span></span><br><span class="line">2)(<span class="built_in">integer</span>) 5 <span class="comment">#漏斗容量 capacity，它是你输入值 + 1，我这里输入的是4</span></span><br><span class="line">3)(<span class="built_in">integer</span>) 4 <span class="comment">#漏斗剩余空间 left_quota</span></span><br><span class="line">4)(<span class="built_in">integer</span>) -1 <span class="comment"># 如果被拒绝了，需要多长时间后再试（单位秒）</span></span><br><span class="line">5)(<span class="built_in">integer</span>) 12 <span class="comment"># 表示多长时间后漏斗完全存满</span></span><br></pre></td></tr></table></figure>

<p>上面的这个指令代表从一个初始容量为 5的令牌桶，60s内允许获取5个令牌，后面的那个1是可选参数，代表本次申请的令牌数，默认是1</p>
<br/>



<p><strong>题外：</strong></p>
<p>为了尝试redis-cell,我将redis3卸载了，安装了最新的redis6.2.1版本，然后下载 redis-cell,修改redis.conf，重启时没反应，设置日志输出后，日志报这个错误</p>
<p><strong>version &#96;GLIBC_2.18’ not found</strong></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">9608:C 18 Apr 2021 15:05:58.618 <span class="comment"># oO0OoO0OoO0Oo Redis is starting oO0OoO0OoO0Oo</span></span><br><span class="line">9608:C 18 Apr 2021 15:05:58.618 <span class="comment"># Redis version=6.2.1, bits=64, commit=00000000, modified=0, pid=9608, just started</span></span><br><span class="line">9608:C 18 Apr 2021 15:05:58.618 <span class="comment"># Configuration loaded</span></span><br><span class="line">9608:M 18 Apr 2021 15:05:58.619 * monotonic clock: POSIX clock_gettime</span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 * Running mode=standalone, port=6379.</span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 <span class="comment"># WARNING: The TCP backlog setting of 511 cannot be enforced because /proc/sys/net/core/somaxconn is set to the lower value of 128.</span></span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 <span class="comment"># Server initialized</span></span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 <span class="comment"># WARNING overcommit_memory is set to 0! Background save may fail under low memory condition. To fix this issue add &#x27;vm.overcommit_memory = 1&#x27; to /etc/sysctl.conf and then reboot or run the command &#x27;sysctl vm.overcommit_memory=1&#x27; for this to take effect.</span></span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 <span class="comment"># Module /usr/local/redis-cell/libredis_cell.so failed to load: /lib64/libc.so.6: version `GLIBC_2.18&#x27; not found (required by /usr/local/redis-cell/libredis_cell.so)</span></span><br><span class="line">9608:M 18 Apr 2021 15:05:58.620 <span class="comment"># Can&#x27;t load module from /usr/local/redis-cell/libredis_cell.so: server aborting</span></span><br></pre></td></tr></table></figure>

<p>需要安装对应的<strong>GLIBC_2.18</strong>包后启动正常，并且日志中会打印已加载对应module，具体怎么安装请自行百度</p>
<p>测试可以拿我上面这条指令，看能否打印出上述的数据。</p>
<br/>

<h3 id="参考："><a href="#参考：" class="headerlink" title="参考："></a>参考：</h3><blockquote>
<p>《Redis深度历险》- 老钱</p>
<p>  Redis中文社区： <a href="http://www.redis.cn/documentation.html">http://www.redis.cn/documentation.html</a></p>
</blockquote>
]]></content>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis设计-字符串、列表、字典</title>
    <url>/2021/09/05/Redis%E8%AE%BE%E8%AE%A1-%E5%AD%97%E7%AC%A6%E4%B8%B2%E3%80%81%E5%88%97%E8%A1%A8%E3%80%81%E5%AD%97%E5%85%B8/</url>
    <content><![CDATA[<p>源码版本：redis-6.2.3</p>
<p>源码地址：<a href="https://github.com/redis/redis">https://github.com/redis/redis</a></p>
<p>首先说明：Redis主要是用C语言开发的</p>
<br/>

<span id="more"></span> 

<h2 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h2><p> Redis中的字符串叫做：SDS,Simple Dynamic String。它的结构是一个带长度信息的字节数组。</p>
<p>源码中sds.h文件下sdshdr结构表示一个SDS值</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sdshdr</span>&#123;</span></span><br><span class="line">     <span class="type">int</span> len; <span class="comment">// 记录buf中已经使用的数组空间长度</span></span><br><span class="line">     <span class="type">int</span> <span class="built_in">free</span>; <span class="comment">// 记录buf中未使用的数组空间长度</span></span><br><span class="line">     <span class="type">char</span> buf[]; <span class="comment">// 字节数组，存放字符串</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实际源码的sdshdr根据需求泛型成多个结构，但是核心设计这3个属性就可以解释清楚。感兴趣的可以看看<a href="https://github.com/redis/redis/blob/unstable/src/sds.h">对应源码网址</a></p>
<br/>

<h4 id="为什么在其他的SDS结构用泛型-T-来表示数组容量和长度，而不是用int？"><a href="#为什么在其他的SDS结构用泛型-T-来表示数组容量和长度，而不是用int？" class="headerlink" title="为什么在其他的SDS结构用泛型 T 来表示数组容量和长度，而不是用int？"></a>为什么在其他的SDS结构用泛型 T 来表示数组容量和长度，而不是用int？</h4><p>​    为了给内存节约空间，当字符串长度较小时，可以用 byte （1个字节）和 short（2个字节）来表示，而int占用4个字节。（这里的字节大小是在C语言中）</p>
<br/>

<h4 id="为什么不直接使用C语言中的字符串呢？"><a href="#为什么不直接使用C语言中的字符串呢？" class="headerlink" title="为什么不直接使用C语言中的字符串呢？"></a>为什么不直接使用C语言中的字符串呢？</h4><p>​        C语言字符串是由字符数组组成，每次获取字符串长度需要从头遍历直到遇到 \0 结尾符，其时间复杂度为O(n)级别，相比 SDS 这种O(1)级别来说在获取字符串长度这个操作差了一个数量级。</p>
<p>​        或许会奇怪为什么获取字符串长度这个操作这么重要，主要是使用C语言字符串无法解决缓存区溢出的问题，因为C语言的特性，字符串字节数组大小是已占用的字符长度+1，额外的一位是用来保存空字符，此时要strcat 拼接一个 “dynamic”字符串，它拼接的时候会发现原有数组大小不够用，从而造成缓冲区溢出，必须要进行一次内存重分配。而SDS不会，它可以通过空闲空间free大小从而决定是否扩容，并且由于有空闲空间的存在，面对Redis这种频繁更改数据的特性，能有效避免内存重分配。</p>
<p>​      SDS中的buf数组是字节数组，对于数据都是以二进制的形式进行处理，这使得SDS可以存储任何格式的数据，包括音频、视频，而C语言的字符串只能保存文本。值得一提的是SDS的结尾还是用C语言习惯的空字符，这样可以使用部分&lt;string.h&gt;中的函数。</p>
   <br/>    

<h4 id="减少内存重分配的意义："><a href="#减少内存重分配的意义：" class="headerlink" title="减少内存重分配的意义："></a><strong>减少内存重分配的意义：</strong></h4><p>​    	内存重分配在操作系统中其实是一种比较复杂耗时的操作，它涉及到对应的内存分配算法和对应的系统调用。</p>
<pre><code>	一般来说，如果字符串不会频繁变更，那每次更改都涉及一次内存重分配其实也是可以接受的。但是Redis不同，redis是高性能的缓存记录中间件，字符串变更非常频繁。如果每次变更都涉及内存重分配，第一是占用了很大一部分的字符串变更时间，第二是对性能是有一定的影响的。
</code></pre>
<br/>

<h4 id="空间的预分配和惰性空间释放："><a href="#空间的预分配和惰性空间释放：" class="headerlink" title="空间的预分配和惰性空间释放："></a><strong>空间的预分配和惰性空间释放：</strong></h4><p>​        所以Redis使用SDS结构来存储字符串，SDS实现了空间的预分配和惰性空间释放两种优化策略。</p>
<p>  空间预分配： 当要进行空间扩展时，如果目前SDS小于1M，那么分配free空闲空间会等于 扩展后数组占用实际长度空间，假设目前有实际占用了5个字节，拼接了一个7个字节的字符串，那么占用空间为 12个字节，则新分配的free空闲空间为 12字节，总buf数组大小为 12 + 12 + 1(额外一节用来保存空字符） 共25个字节。</p>
<p>​       如果目前SDS大于1M，那么每次新分配的空闲空间会固定为1M，不管你当前的SDS是1M还是100M，每次就留1M的空闲空间。其实也能够理解，因为redis中的字符串大小往往不大，并且Redis规定字符串长度不能超过512M。</p>
<p>​      通过预留空闲空间，Redis能有效的减少内存重分配次数。</p>
<p>​      惰性空间释放：当字符串通过sdscat进行缩容时，也不需要马上进行内存重分配释放空闲空间，只需要修改len属性的长度即可，这样当进行扩容的时候就方便了不少，当然SDS面对内存空间不足时有对应的回收机制，这个不需要担心，当内存充足时，惰性空间释放也能有效的减少扩容次数。</p>
<br/>

<h2 id="链表"><a href="#链表" class="headerlink" title="链表"></a><strong>链表</strong></h2><p>Redis列表的实现方式之一、慢查询、发布与订阅等也都是由链表实现的，让我们看看Redis源码中adlist.h文件下listNode结构</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> &#123;</span></span><br><span class="line">    <span class="comment">// 上个节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> *<span class="title">prev</span>;</span></span><br><span class="line">    <span class="comment">// 下个节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> *<span class="title">next</span>;</span></span><br><span class="line">    <span class="comment">//当前节点值</span></span><br><span class="line">    <span class="type">void</span> *value;</span><br><span class="line">&#125; listNode; </span><br></pre></td></tr></table></figure>

<br/>

<img src="/2021/09/05/Redis%E8%AE%BE%E8%AE%A1-%E5%AD%97%E7%AC%A6%E4%B8%B2%E3%80%81%E5%88%97%E8%A1%A8%E3%80%81%E5%AD%97%E5%85%B8/ListNode.png" class="" title="链表图">

<p>虽然说我们可以直接用ListNode组成一个链表，但是为了操作的方便，Redis在adlist.h文件下封装了一个list结构</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">list</span> &#123;</span></span><br><span class="line">    <span class="comment">//表头节点</span></span><br><span class="line">    listNode *head;</span><br><span class="line">    <span class="comment">// 表尾节点</span></span><br><span class="line">    listNode *tail;</span><br><span class="line">    <span class="comment">// 节点值复制函数</span></span><br><span class="line">    <span class="type">void</span> *(*dup)(<span class="type">void</span> *ptr);</span><br><span class="line">    <span class="comment">// 节点值释放函数</span></span><br><span class="line">    <span class="type">void</span> (*<span class="built_in">free</span>)(<span class="type">void</span> *ptr);</span><br><span class="line">    <span class="comment">// 节点值对比函数</span></span><br><span class="line">    <span class="type">int</span> (*match)(<span class="type">void</span> *ptr, <span class="type">void</span> *key);</span><br><span class="line">    <span class="comment">// 链表长度</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> len;</span><br><span class="line">&#125; <span class="built_in">list</span></span><br></pre></td></tr></table></figure>

<p>关于Redis链表的特性，其实大家可以直接从list结构中看出来，主要将head、tail节点以及链表长度的获取时间复杂度为O(1)级别，以及规范了链表API的使用。</p>
<h2 id="字典"><a href="#字典" class="headerlink" title="字典"></a><strong>字典</strong></h2><p>Redis中字典又称为符号表或、映射map等，是一种用来保存键值对(key-value pair)的抽象数据结构。由于Redis使用的C语言没有内置这种数据结构，于是Redis构建了自己的字典实现。</p>
<p><strong>Redis中字典的用途：</strong></p>
<p>​        除了数据库中用于基本的键值存储字典外，字典也是哈希表的实现方式之一，当哈希表中存储的键值对较多或者键值对中有较大的字符串时便会使用到字典作为底层实现。</p>
<p> Redis中字典的哈希表由 <a href="https://github.com/redis/redis/blob/unstable/src/dict.h">dict.h</a>文件中的dictht结构定义</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">dictht</span> &#123;</span></span><br><span class="line">    <span class="comment">// 哈希表数组</span></span><br><span class="line">    dictEntry **table;</span><br><span class="line">    <span class="comment">// 哈希表大小</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> size;</span><br><span class="line">    <span class="comment">// 哈希表大小掩码，用于计算索引，总是等于 size - 1</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> sizemask;</span><br><span class="line">    <span class="comment">// 哈希表已有节点数量</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> used;</span><br><span class="line">&#125; dicth  </span><br></pre></td></tr></table></figure>

<p>table属性是一个数组，数组中每一个元素是指向dict.h文件中的dictEntry结构的指针，每一个dictEntry结构中保存了一个键值对。</p>
<p>我们来看一下  <a href="https://github.com/redis/redis/blob/unstable/src/dict.h">dict.h</a>文件中的dictEntry结构定义</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">dictEntry</span> &#123;</span></span><br><span class="line">    <span class="comment">// 键key</span></span><br><span class="line">    <span class="type">void</span> *key;</span><br><span class="line">    <span class="comment">// 值 val</span></span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        <span class="type">void</span> *val;</span><br><span class="line">        <span class="type">uint64_t</span> u64;</span><br><span class="line">        <span class="type">int64_t</span> s64;</span><br><span class="line">        <span class="type">double</span> d;</span><br><span class="line">    &#125; v;</span><br><span class="line">    <span class="comment">// 指向下一个节点，形成一个链表</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dictEntry</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125; dictEntry;</span><br></pre></td></tr></table></figure>



<p>key属性保存着键值对中的键，而v属性则保存着键值对中的值，其中键值对的值可以是一个指针，或者是一个uint64_t整数，又或者是一个int64_t整数。</p>
<p>next属性是指向另一个哈希表节点的指针，这个指针可以将多个哈希值相同的键值对连接在一次，以此来解决键冲突（collision）的问题。</p>
<h4 id="结构示例图"><a href="#结构示例图" class="headerlink" title="结构示例图"></a>结构示例图</h4><br/>

<img src="/2021/09/05/Redis%E8%AE%BE%E8%AE%A1-%E5%AD%97%E7%AC%A6%E4%B8%B2%E3%80%81%E5%88%97%E8%A1%A8%E3%80%81%E5%AD%97%E5%85%B8/dictEntry.png" class="" title="字典图">

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">dict</span> &#123;</span></span><br><span class="line">    <span class="comment">// 类型特定数据</span></span><br><span class="line">    dictType *type;</span><br><span class="line">    <span class="comment">// 私有数据</span></span><br><span class="line">    <span class="type">void</span> *privdata;</span><br><span class="line">    <span class="comment">// 哈希表</span></span><br><span class="line">    dictht ht[<span class="number">2</span>];</span><br><span class="line">    <span class="comment">// 索引计数器 用来做rehash</span></span><br><span class="line">    <span class="type">long</span> rehashidx; <span class="comment">/* rehashing not in progress if rehashidx == -1 */</span></span><br><span class="line">    <span class="type">int16_t</span> pauserehash; <span class="comment">/* If &gt;0 rehashing is paused (&lt;0 indicates coding error) */</span></span><br><span class="line">&#125; dict;</span><br></pre></td></tr></table></figure>



<ul>
<li>type属性和privdata属性是针对不同类型的键值对，为创建多态字典而设置的：</li>
<li>type属性是一个指向dictType结构的指针，每个dictType结构保存了一簇用于操作特定类型键值对的函数，Redis会为用途不同的字典设置不同的类型特定函数。</li>
<li>而privdata属性则保存了需要传给那些类型特定函数的可选参数。</li>
<li>ht属性是一个包含两个项的数组，数组中的每个项都是一个dictht哈希表，一般情况下，字典只使用ht[0]哈希表，ht[1]哈希表只会在对ht[0]哈希表进行rehash时使用</li>
</ul>
<p>当要将一个新的键值对添加到字典里面时，程序需要先根据键值对的键计算出哈希值和索引值，然后再根据索引值，将包含新键值对的哈希表节点放到哈希表数组的指定索引上面。</p>
<p>字典的哈希键使用MurmurHash2算法来计算键的哈希值</p>
<p>当发生Hash冲突时，由于这种数组+链表的结构，并且dictEntry只有next指针，所以新增的键是放置链表表头，避免遍历链表。</p>
<h4 id="扩容和缩容场景："><a href="#扩容和缩容场景：" class="headerlink" title="扩容和缩容场景："></a><strong>扩容和缩容场景：</strong></h4><p>​      扩容和缩容的场景是导致为什么dict结构的ht数组为什么是2的原因，当需要扩容时，为ht[1]分配内存空间，创建一个新的哈希表，然后将ht[0]中哈希表元素进行rehash重新分配到ht[1]中，当ht[0]包含的所有键值对都迁移到了ht[1]之后（ht[0]变为空表），释放ht[0]，将ht[1]设置为ht[0]，并在ht[1]新创建一个空白哈希表，为下一次rehash做准备，缩容也是同理。</p>
<h4 id="字典详细数据结构图"><a href="#字典详细数据结构图" class="headerlink" title="字典详细数据结构图"></a>字典详细数据结构图</h4><img src="/2021/09/05/Redis%E8%AE%BE%E8%AE%A1-%E5%AD%97%E7%AC%A6%E4%B8%B2%E3%80%81%E5%88%97%E8%A1%A8%E3%80%81%E5%AD%97%E5%85%B8/dictStructure.png" class="" title="字典数据结构图">

<br/>

<h4 id="渐进式rehash"><a href="#渐进式rehash" class="headerlink" title="渐进式rehash"></a><strong>渐进式rehash</strong></h4><p>​       假如哈希表中的键值对数量不多，那直接进行rehash是没问题的，但如果哈希表的键值对达到百万级时，大量的rehash对整个系统的性能有一定的影响，同时也阻塞了其他操作，当然你可以另起一个线程去执行也可以，但没这个必要，因为rehash并不是紧急的需求。</p>
<p>​      这里Redis采用的是分而治之的策略。当需要进行rehash时，会进行如下步骤：</p>
<p>1）为ht[1]分配空间，让字典同时持有ht[0]和ht[1]两个哈希表。</p>
<p>2）在字典中维持一个索引计数器变量rehashidx，并将它的值设置为0，表示rehash工作正式开始。</p>
<p>3）在rehash进行期间，每次对字典执行添加、删除、查找或者更新操作时，程序除了执行指定的操作以外，还会顺带将ht[0]哈希表在rehashidx索引上的所有键值对rehash到ht[1]，当rehash工作完成之后，程序将rehashidx属性的值增一。</p>
<p>4）随着字典操作的不断执行，最终在某个时间点上，ht[0]的所有键值对都会被rehash至ht[1]，这时程序将rehashidx属性的值设为-1，表示rehash操作已完成。</p>
<p>—引用自《Redis设计与实现》</p>
<p>​        渐进式rehash的好处在于它采取分而治之的方式，将rehash键值对所需的计算工作均摊到对字典的每个添加、删除、查找和更新操作上，从而避免了集中式rehash而带来的庞大计算量。</p>
<br/>

<p>​      <strong>值得注意的是</strong>：在rehash期间，字典的操作在ht[0]和ht[1]中进行的，例如查询一个key，如果ht[0]没有就会去ht[1]中查找，并且新增key只会保存在ht[1]中，这样做是为了避免ht[0]到ht[1]会rehash这个key，也保证ht[0]的键值对只减不增。</p>
]]></content>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis设计-跳跃表、整数集合、压缩列表</title>
    <url>/2021/06/06/Redis%E8%AE%BE%E8%AE%A1-%E8%B7%B3%E8%B7%83%E8%A1%A8%E3%80%81%E6%95%B4%E6%95%B0%E9%9B%86%E5%90%88%E3%80%81%E5%8E%8B%E7%BC%A9%E5%88%97%E8%A1%A8/</url>
    <content><![CDATA[<h2 id="跳跃表"><a href="#跳跃表" class="headerlink" title="跳跃表"></a><strong>跳跃表</strong></h2><p>​        跳跃表（skiplist）是一种有序数据结构，它通过在每个节点中维持多个指向其他节点的指针，从而达到快速访问节点的目的。</p>
<p>​        跳跃表支持平均O（logN）、最坏O（N）复杂度的节点查找，还可以通过顺序性操作来批量处理节点。在大部分情况下，跳跃表的效率可以和平衡树相媲美，并且因为跳跃表的实现比平衡树要来得更为简单，所以有不少程序都使用跳跃表来代替平衡树。</p>
<span id="more"></span> 

<p>​        Redis使用跳跃表作为有序集合键的底层实现之一，如果一个有序集合包含的元素数量比较多，又或者有序集合中元素的成员（member）是比较长的字符串时，Redis就会使用跳跃表来作为有序集合键的底层实现</p>
<br/>

<h3 id="跳跃表示例"><a href="#跳跃表示例" class="headerlink" title="跳跃表示例"></a><strong>跳跃表示例</strong></h3><img src="/2021/06/06/Redis%E8%AE%BE%E8%AE%A1-%E8%B7%B3%E8%B7%83%E8%A1%A8%E3%80%81%E6%95%B4%E6%95%B0%E9%9B%86%E5%90%88%E3%80%81%E5%8E%8B%E7%BC%A9%E5%88%97%E8%A1%A8/skiplist.png" class="" title="跳跃表示例图">     



<p>位于上图最左边的是zskiplist结构，该结构包含以下属性：</p>
<ul>
<li>header：指向跳跃表的表头节点。</li>
<li>tail：指向跳跃表的表尾节点。</li>
<li>level：记录目前跳跃表内，层数最大的那个节点的层数（表头节点的层数不计算在内）。</li>
<li>length：记录跳跃表的长度，也即是，跳跃表目前包含节点的数量（表头节点不计算在内）。</li>
</ul>
<p>位于zskiplist结构右方的是四个zskiplistNode结构，该结构包含以下属性：</p>
<ul>
<li>level： 层级，节点中用L1表示1层，L2表示二层，以此类推，每一层有两个属性： 前进指针和跨度。前进指针用来访问后面的节点，而跨度则记录了当前节点与前进指针指向的节点之间的距离。从上图可以看出，箭头代表着前进指针，箭头中的数字表示跨度。</li>
<li>backward: 后退指针，图中的BW就代表着后退指针，它指向位于当前节点的前一个节点。后退指针在程序从表尾向表头遍历时使用。</li>
<li>分值（score）：各个节点中的1.0、2.0和3.0是节点所保存的分值。在跳跃表中，节点按各自所保存的分值从小到大排列。</li>
<li>成员对象（obj）：各个节点中的o1、o2和o3是节点所保存的成员对象。</li>
</ul>
<p>注意表头节点和其他节点的构造是一样的：表头节点也有后退指针、分值和成员对象，不过表头节点的这些属性都不会被用到，所以图中省略了这些部分，只显示了表头节点的各个层。</p>
<h3 id="跳跃表节点"><a href="#跳跃表节点" class="headerlink" title="跳跃表节点"></a><strong>跳跃表节点</strong></h3><p>Redis的跳跃表由redis.h文件(在最新的redis-6.2.3版本中发是在<a href="https://github.com/redis/redis/blob/unstable/src/server.h">server.h</a>)中的zskiplistNode结构和zskiplist两个结构定义</p>
<p>其中zskiplistNode结构用于表示跳跃表节点，而zskiplist结构则用于保存跳跃表节点的信息，如表头和表位指针，节点数量等。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> &#123;</span></span><br><span class="line">    <span class="comment">// 成员对象</span></span><br><span class="line">    sds ele;</span><br><span class="line">    <span class="comment">// 分值</span></span><br><span class="line">    <span class="type">double</span> score;</span><br><span class="line">    <span class="comment">// 后台指针</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">backward</span>;</span></span><br><span class="line">    <span class="comment">// 层</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistLevel</span> &#123;</span></span><br><span class="line">        <span class="comment">// 前进指针</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">forward</span>;</span></span><br><span class="line">        <span class="comment">// 跨度</span></span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> span;</span><br><span class="line">    &#125; level[];</span><br><span class="line">&#125; zskiplistNode;</span><br></pre></td></tr></table></figure>



<p><strong>属性解析：</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>zskiplistLevel</td>
<td>level数组可以包含多个元素，每个元素都包含一个指向其他节点的指针，一般来说，层的数量越多，访问其他节点的速度越快。                                                                                  每次创建一个新跳跃表节点，程序都根据28原则，随机生成一个1到32之间的值作为level数组的大小，这个大小就是层的高度。                                                                                          前进指针 forward,用于从表头到表尾，每一个level都是一次跳跃，level越高跳的越远。      跨度span 用来记录两个节点之间的距离，跨度是用来计算排位rank的，查找某个节点沿途路径的rank累计起来。指向null的所有前进指针的跨度为0，因为他们没有连向任何节点。</td>
</tr>
<tr>
<td>backward</td>
<td>用于表尾向表头方向访问节点，每次只能回退至前一个节点</td>
</tr>
<tr>
<td>score</td>
<td>节点的分值（score属性）是一个double类型的浮点数，跳跃表中所有节点按照分值从小到大来排序。分值相同的节点将按照成员对象ele的字典序大小来排序，也是从小到大排</td>
</tr>
<tr>
<td>ele</td>
<td>成员对象，是一个指针，指向一个字符串sds对象，同一个跳跃表各节点的成员对象必须唯一，set集合嘛</td>
</tr>
</tbody></table>
<br/>

<p>虽然说通过zskiplistNode就可以组成一个跳跃表，不过有 zskiplist来持有这些节点，程序可以更加方便的操作整个跳跃表</p>
<p>zskiplist结构的定义如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplist</span> &#123;</span></span><br><span class="line">    <span class="comment">// 表头节点、表尾节点</span></span><br><span class="line">     structz zskiplistNode *header,*tail;</span><br><span class="line">     <span class="comment">// 表中的节点数量</span></span><br><span class="line">     <span class="type">unsigned</span> <span class="type">long</span> length;</span><br><span class="line">     <span class="comment">// 表中层次最大的节点的层数</span></span><br><span class="line">     <span class="type">int</span> level;</span><br><span class="line">&#125; zskiplist; </span><br></pre></td></tr></table></figure>

<p>大家可以直接看源码就得知其大致意思，这里省略每个属性的解释了。</p>
<br/>

<h2 id="整数集合"><a href="#整数集合" class="headerlink" title="整数集合"></a><strong>整数集合</strong></h2><p>​        整数集合（intset）是集合键的底层实现之一，当一个集合只包含整数值元素，并且这个集合的元素数量不多时，Redis就会使用整数集合作为集合键的底层实现。</p>
<p>​       为了节约内存，intset的数组格式是根据存储的数据来决定的，如果可以int16_t来存，就会一直用它存，当添加超过int16_t的数据时数组会升级到可以存这个数的数据类型。但是intset不支持降级，并且intset是有序集合。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">intset</span> &#123;</span></span><br><span class="line">    <span class="comment">// 编码方式</span></span><br><span class="line">    <span class="type">uint32_t</span> encoding;</span><br><span class="line">    <span class="comment">// 集合包含的元素长度</span></span><br><span class="line">    <span class="type">uint32_t</span> length;</span><br><span class="line">    <span class="comment">// 存储的元素的数组，别看这里的类型是int8_t，它会根据存储的元素类型做相应的升级</span></span><br><span class="line">    <span class="type">int8_t</span> contents[];</span><br><span class="line">&#125; intset;</span><br></pre></td></tr></table></figure>



<h4 id="为什么整数集合不支持降级呢？"><a href="#为什么整数集合不支持降级呢？" class="headerlink" title="为什么整数集合不支持降级呢？"></a><strong>为什么整数集合不支持降级呢？</strong></h4><p>​       因为整体性价比很低，如果要考虑降级的话，它和升级不是一个复杂度，你需要考虑两个问题才行。</p>
<p>1、什么情况下触发降级？</p>
<p>2、具体降级到什么数据类型？</p>
<p>​     你会发现这两个问题都需要通过遍历数组来确定，当你删除一个较大的元素，你要遍历确定是否没有这个级别的数据类型值，并且确定可以降到哪个级别，这是个O(N)的操作，然后如果再加一个先前这么大的数据时，你又要再次升级，这样频繁的升级降级，非常不合理。</p>
<p>​       那为什么还要弄一个升级呢？因为在大多数业务场景中你存储的数据基本具有一样的特征，也就是它们的差值基本不会太大，这就意味这升级其实触发的不多（最多也就四次从int8_t到 int64_t)，并且升级是不要遍历数组的，当添加的元素超出当前数据的数据类型范围限制就可以升级，升级就是代价极小的情况下要考虑节约空间。</p>
<h4 id="为什么C语言将-int分为-int8-t，int16-t-int32-t-int64-t-不同的类型？"><a href="#为什么C语言将-int分为-int8-t，int16-t-int32-t-int64-t-不同的类型？" class="headerlink" title="为什么C语言将 int分为 int8_t，int16_t ,int32_t ,int64_t 不同的类型？"></a><strong>为什么C语言将 int分为 int8_t，int16_t ,int32_t ,int64_t 不同的类型？</strong></h4><p>​     int_t 为一个结构的标注，可以理解为type&#x2F;typedef的缩写，表示它是通过typedef定义的，即对C语言的数据类型起一个别名，而不是一种新的数据类型。因为跨平台，不同的平台会有不同的字长，所以利用预编译和typedef可以最有效的维护代码。</p>
<p>​      unsigned修饰代表存储的最小值为0，不能为负数 </p>
<p>​     Int8，Int16，Int32，int64，后面的数字就代表这个数据类型占据的空间，即位数，Int32代表这个类型存储32位，即4个字节的整数，而C语言中的long，int类型是在win32,win64不同的平台存储的字节也不同。这样开发可以根据实际需要灵活的选择要存储的数据</p>
<h2 id="压缩列表"><a href="#压缩列表" class="headerlink" title="压缩列表"></a><strong>压缩列表</strong></h2><p>压缩列表（ziplist）是列表键和哈希键的底层实现之一。</p>
<p>当一个列表键只包含少量列表项，并且每个列表项要么就是小整数值，要么就是长度比较短的字符串，那么Redis就会使用压缩列表来做列表键的底层实现。</p>
<h3 id="压缩列表的组成"><a href="#压缩列表的组成" class="headerlink" title="压缩列表的组成"></a><strong>压缩列表的组成</strong></h3><img src="/2021/06/06/Redis%E8%AE%BE%E8%AE%A1-%E8%B7%B3%E8%B7%83%E8%A1%A8%E3%80%81%E6%95%B4%E6%95%B0%E9%9B%86%E5%90%88%E3%80%81%E5%8E%8B%E7%BC%A9%E5%88%97%E8%A1%A8/ziplist.png" class="" title="压缩列表结构图">

<br/>

<h3 id="压缩列表节点的组成"><a href="#压缩列表节点的组成" class="headerlink" title="压缩列表节点的组成"></a><strong>压缩列表节点的组成</strong></h3><p>每个压缩列表节点可以保存一个字节数组或者一个整数值。</p>
<p>其中，字节数组可以是以下三种长度的其中一种：</p>
<ul>
<li>长度小于等于63（2的6次方–1）字节的字节数组；</li>
<li>长度小于等于16383（2的14次方–1）字节的字节数组；</li>
<li>长度小于等于4294967295（2的32次方–1）字节的字节数组；</li>
</ul>
<p>而整数值则可以是以下六种长度的其中一种：</p>
<ul>
<li>4位长，介于0至12之间的无符号整数；</li>
<li>1字节长的有符号整数；</li>
<li>3字节长的有符号整数；</li>
<li>int16_t类型整数；</li>
<li>int32_t类型整数；</li>
<li>int64_t类型整数。</li>
</ul>
<p>每个压缩列表节点都由previous_entry_length、encoding、content三个部分组成，如下图所示</p>
<img src="/2021/06/06/Redis%E8%AE%BE%E8%AE%A1-%E8%B7%B3%E8%B7%83%E8%A1%A8%E3%80%81%E6%95%B4%E6%95%B0%E9%9B%86%E5%90%88%E3%80%81%E5%8E%8B%E7%BC%A9%E5%88%97%E8%A1%A8/ziplistNode.png" class="" title="压缩列表节点图">

<br/>

<h4 id="previous-entry-length"><a href="#previous-entry-length" class="headerlink" title="previous_entry_length"></a><strong>previous_entry_length</strong></h4><p>节点的previous_entry_length属性以字节为单位，记录了压缩列表中前一个节点的长度。previous_entry_length属性的长度可以是1字节或者5字节：</p>
<ul>
<li>如果前一节点的长度小于254字节，那么previous_entry_length属性的长度为1字节：前一节点的长度就保存在这一个字节里面。</li>
<li>如果前一节点的长度大于等于254字节，那么previous_entry_length属性的长度为5字节：其中属性的第一字节会被设置为0xFE（十进制值254），而之后的四个字节则用于保存前一节点的长度。</li>
</ul>
<p>压缩列表的从表尾向表头遍历操作就是使用previous_entry_length实现的，只要我们拥有了一个指向某个节点起始地址的指针，那么通过这个指针以及这个节点的previous_entry_length属性，程序就可以一直向前一个节点回溯，最终到达压缩列表的表头节点。</p>
<h4 id="encoding"><a href="#encoding" class="headerlink" title="encoding"></a><strong>encoding</strong></h4><p>节点的encoding属性记录了节点的content属性所保存数据的类型以及长度</p>
<h4 id="content"><a href="#content" class="headerlink" title="content"></a><strong>content</strong></h4><p>节点的content属性负责保存节点的值，节点值可以是一个字节数组或者整数，值的类型和长度由节点的encoding属性决定。</p>
<h3 id="连锁更新问题"><a href="#连锁更新问题" class="headerlink" title="连锁更新问题"></a><strong>连锁更新问题</strong></h3><p>前面说过，每个节点的previous_entry_length属性都记录了前一个节点的长度：</p>
<p>现在，考虑这样一种情况：在一个压缩列表中，有多个连续的、长度介于250字节到253字节之间的节点e1至eN。</p>
<p>那这种情况下，当e1的字节长度等于254字节时，e2的previous_entry_length长度就会从1个字节转换成5字节，从而导致e2节点长度超过254,，</p>
<p>这样的话，e3节点也会因为e2的变长做改变，同理会这样推到下去直到eN。</p>
<p>要注意的是，尽管连锁更新的复杂度较高，但它真正造成性能问题的几率是很低的：</p>
<ul>
<li>首先，压缩列表里要恰好有多个连续的、长度介于250字节至253字节之间的节点，连锁更新才有可能被引发，在实际中，这种情况并不多见；</li>
<li>其次，即使出现连锁更新，但只要被更新的节点数量不多，就不会对性能造成任何影响：比如说，对三五个节点进行连锁更新是绝对不会影响性能的；</li>
</ul>
<p>大家只需了解一下即可。</p>
]]></content>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Ribbon在SpringCloud是怎么使用的？</title>
    <url>/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/</url>
    <content><![CDATA[<h1 id="Ribbon"><a href="#Ribbon" class="headerlink" title="Ribbon"></a>Ribbon</h1><p>Ribbon是一款SpringCloud处理客户端http与tcp访问的负载均衡策略的工具</p>
<h2 id="Ribbon如何与springcloud组件结合？"><a href="#Ribbon如何与springcloud组件结合？" class="headerlink" title="Ribbon如何与springcloud组件结合？"></a>Ribbon如何与springcloud组件结合？</h2><p>在Spring中使用 RestTemplate 获取 http请求时，为了简约</p>
<p>我们常会 @LoadBalanced 注解 标记 RestTemplate</p>
<p>让SpringCloud为 RestTemplate 设置负载均衡策略。</p>
<p>那么它的如何设置的呢？</p>
<span id="more"></span>

<p>该注解是这样解释的</p>
<p><em>Annotation to mark a RestTemplate bean to be configured to use a LoadBalancerClient</em></p>
<p>标记一个RestTemplate的bean为被 LoadBalancerClient所配置</p>
<p>我们查看这个 LoadBalancerClient 类</p>
<p>简单的截个图</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b1.jpg" class="" title="loadBalance">

<p>这里的接口解释为： 表示客户端负载均衡器，其中它继承了 ServiceInstanceChooser接口</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b2.jpg" class="" title="ServiceInstanceChooser">

<p>从 ServiceInstanceChooser的这个接口名称和类解释上我们不难看出，这个接口的设计就是为了从负载均衡器中选择一个 具体的服务实例</p>
<h3 id="接口结构图："><a href="#接口结构图：" class="headerlink" title="接口结构图："></a>接口结构图：</h3><img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b3.jpg" class="" title="loadBalance-interface">


<h3 id="3个核心方法："><a href="#3个核心方法：" class="headerlink" title="3个核心方法："></a>3个核心方法：</h3><ol>
<li>父类 ServiceInstanceChooser的 choose方法：选择一个具体的服务实例</li>
<li>LoadBalancerClient 中 execute，这里传参不同有两个方法，但实际上功能是一致的，都是使用LoadBalancer中的ServiceInstance执行指定的请求。</li>
<li>URI reconstructURI(ServiceInstance instance, URI original) ：该方法创建一个真实的主机地址和端口，因为我们访问时给定的地址是用服务名访问的 <a href="http://myservice/path/to/service">http://myservice/path/to/service</a></li>
</ol>
<p>​      它会将我们给定的服务名进行转成成一个可用的http请求url</p>
<p>这些都是接口方法，那么具体</p>
<h2 id="它是怎么实现的，是如何做到自动装配负载均衡的呢？"><a href="#它是怎么实现的，是如何做到自动装配负载均衡的呢？" class="headerlink" title="它是怎么实现的，是如何做到自动装配负载均衡的呢？"></a>它是怎么实现的，是如何做到自动装配负载均衡的呢？</h2><p>我们查看 负载均衡包 package org.springframework.cloud.client.loadbalancer 下的相关类</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b4.jpg" class="" title="loadBalance-package">

<p>查看一下它的配置类 LoadBalancerAutoConfiguration.class</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b5.jpg" class="" title="loadBalance-configure">

<p>看到没有，配置类上的3个注解，它将 RestTemplate,和 LoadBalancerClient以及 负载均衡的相关配置信息注入进来处理。</p>
<p>参数中 先注入了所有RestTemplate，然后在下面的初始化中 采用单例模式在遍历的过程中返回具体的实例，这里可以看出默认是循环遍历，也就是轮播的机制。</p>
<h2 id="那当我们在application，或者其他地方自定义负载均衡策略时，它又是怎么处理的呢？"><a href="#那当我们在application，或者其他地方自定义负载均衡策略时，它又是怎么处理的呢？" class="headerlink" title="那当我们在application，或者其他地方自定义负载均衡策略时，它又是怎么处理的呢？"></a>那当我们在application，或者其他地方自定义负载均衡策略时，它又是怎么处理的呢？</h2><p>在LoadBalancerAutoConfiguration类中有一段这样的代码</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b6.jpg" class="" title="loadBalance-factory">

<p>它初始获取了Spring容器中的负载均衡请求转换列表，有一个类 LoadBalancerRequestTransformer ，这个类是用来干嘛的呢</p>
<h3 id="LoadBalancerRequestTransformer"><a href="#LoadBalancerRequestTransformer" class="headerlink" title="LoadBalancerRequestTransformer"></a>LoadBalancerRequestTransformer</h3><img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b7.jpg" class="" title="loadBalance-transform">

<p>类解释为：允许应用程序转换给定的负载均衡{@link HttpRequest} 所选{@link ServiceInstance}</p>
<p>这下懂了把，它会获取所有配置中用户给定的负载均衡策略，再根据 给定的策略定制 负载均衡请求工厂。</p>
<p>但是其实我还是很迷糊的，我需要知道</p>
<h2 id="Ribbon对于RestTemplate的请求的拦截处理是咋样的？"><a href="#Ribbon对于RestTemplate的请求的拦截处理是咋样的？" class="headerlink" title="Ribbon对于RestTemplate的请求的拦截处理是咋样的？"></a>Ribbon对于RestTemplate的请求的拦截处理是咋样的？</h2><p>发现 LoadBalancerAutoConfiguration类中 包含了一个 静态的内部类 </p>
<p>它是一个负载均衡的拦截器配置类LoadBalancerInterceptorConfig</p>
<h3 id="LoadBalancerInterceptorConfig"><a href="#LoadBalancerInterceptorConfig" class="headerlink" title="LoadBalancerInterceptorConfig"></a>LoadBalancerInterceptorConfig</h3><img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b8.jpg" class="" title="interceptConfig">

<p>包含了两个方法，这两个方法解决了我的疑问。</p>
<p>第一个方法，返回了一个负载均衡的拦截器，我们发现它传入了客户端和 LoadBalancerRequestFactory</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LoadBalancerInterceptor <span class="title function_">ribbonInterceptor</span><span class="params">(</span></span><br><span class="line"><span class="params">LoadBalancerClient loadBalancerClient,</span></span><br><span class="line"><span class="params">LoadBalancerRequestFactory requestFactory)</span></span><br></pre></td></tr></table></figure>

<p>在前面 LoadBalancerRequestFactory 是在装配的时候就已经填入了 用户自定义的负载均衡策略了的！</p>
<p>return new LoadBalancerRequestFactory(loadBalancerClient, transformers)；</p>
<p>同时设计者将这些参数组合成了一个 LoadBalancerInterceptor类。</p>
<p>看一下这个负载均衡拦截类的设计，</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b9.jpg" class="" title="loadBalance-intercept">

<p>看到有两个构造器了吗？</p>
<p>第二个构造器应该是当检测到用户如果没有指定负载均衡策略的时候跑的。</p>
<p>第一个构造器带 factory的是用户定制负载均衡策略跑的。</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/funny.gif" class="" title="funny">

<p>It’s funny！！</p>
<p>第二个方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> RestTemplateCustomizer <span class="title function_">restTemplateCustomizer</span><span class="params">(</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">​      <span class="keyword">final</span> RetryLoadBalancerInterceptor loadBalancerInterceptor)</span>；</span><br></pre></td></tr></table></figure>

<p>这一步是将上一步的拦截了http请求塞入了定制的负载均衡策略的拦截器作为规则重新修改</p>
<p>RestTemplate，将其返回。</p>
<p>其实还有一个RetryInterceptorAutoConfiguration类，其做法负载均衡拦截器类的基本一致，并且未注入类资源使用。我不能查看RetryTemplate源码就google了一下。</p>
<h3 id="RetryTemplate-class"><a href="#RetryTemplate-class" class="headerlink" title="RetryTemplate.class:"></a>RetryTemplate.class:</h3><p>可重试操作封装在RetryCallback接口的实现中，并使用提供的execute方法之一执行。</p>
<p>默认情况下，如果抛出异常的任何异常或子类，则重试操作。可以使用setRetryPolicy（RetryPolicy）方法更改此行为。</p>
<p>此类是线程安全的，适用于执行操作和执行配置更改时的并发访问。因此，可以动态更改重试次数，以及使用的BackOffPolicy，并且不会影响正在进行的可重试操作。</p>
<p>这是一个用于并发访问时做可重试操作的东东，应该是拦截http请求时报错后使用这个东西，这里就不深入研究了。</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b10.jpg" class="" title="loadBalance-retry">

<p>这个时候梳理一下Ribbon在Springcloud中的大体工作流程。</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b11.jpg" class="" title="work-img">



<h2 id="Ribbon整个流程处理的类图中类之间的关联情况是啥样的？"><a href="#Ribbon整个流程处理的类图中类之间的关联情况是啥样的？" class="headerlink" title="Ribbon整个流程处理的类图中类之间的关联情况是啥样的？"></a>Ribbon整个流程处理的类图中类之间的关联情况是啥样的？</h2><p>我将比较核心的类的关联关系理了一下。</p>
<img src="/2019/08/20/Ribbon%E5%9C%A8SpringCloud%E6%98%AF%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%E7%9A%84%EF%BC%9F/b12.jpg" class="" title="class-link">

<p>这里梳理了一下工作流和类的关系，个人暂时就探索到这里了，项目中遇到什么对Ribbon的疑问再到这篇文章下面补充。</p>
<blockquote>
<p>注：由于这里是自己暂时摸索了一下源码，有错误的地方麻烦各位指正一下。后续会看书进行再次疏通</p>
</blockquote>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>分布式</tag>
        <tag>Ribbon</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring-默认标签解析</title>
    <url>/2022/07/17/Spring-%E9%BB%98%E8%AE%A4%E6%A0%87%E7%AD%BE%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<p>Spring的标签包括默认标签和自定义标签两种，本文章主要来分析默认标签的解析过程。</p>
<p>默认标签的解析在 parseDefaultElement函数中进行，主要分为四类标签，import、alias、bean、beans，每种对应不同的处理方式。</p>
<span id="more"></span> 

<br/>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">parseDefaultElement</span><span class="params">(Element ele, BeanDefinitionParserDelegate delegate)</span> &#123;</span><br><span class="line">   <span class="keyword">if</span> (delegate.nodeNameEquals(ele, IMPORT_ELEMENT)) &#123;</span><br><span class="line">      importBeanDefinitionResource(ele);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (delegate.nodeNameEquals(ele, ALIAS_ELEMENT)) &#123;</span><br><span class="line">      processAliasRegistration(ele);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (delegate.nodeNameEquals(ele, BEAN_ELEMENT)) &#123;</span><br><span class="line">      processBeanDefinition(ele, delegate);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (delegate.nodeNameEquals(ele, NESTED_BEANS_ELEMENT)) &#123;</span><br><span class="line">      <span class="comment">// recurse</span></span><br><span class="line">      doRegisterBeanDefinitions(ele);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="1、Bean标签的解析及注册"><a href="#1、Bean标签的解析及注册" class="headerlink" title="1、Bean标签的解析及注册"></a><strong>1、Bean标签的解析及注册</strong></h2><p>在4种标签的解析中，对 bean标签的解析最为复杂也最为重要。首先来看看 processBeanDefinition函数</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">processBeanDefinition</span><span class="params">(Element ele, BeanDefinitionParserDelegate delegate)</span> &#123;</span><br><span class="line">   <span class="type">BeanDefinitionHolder</span> <span class="variable">bdHolder</span> <span class="operator">=</span> delegate.parseBeanDefinitionElement(ele);</span><br><span class="line">   <span class="keyword">if</span> (bdHolder != <span class="literal">null</span>) &#123;</span><br><span class="line">      bdHolder = delegate.decorateBeanDefinitionIfRequired(ele, bdHolder);</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Register the final decorated instance.</span></span><br><span class="line">         BeanDefinitionReaderUtils.registerBeanDefinition(bdHolder, getReaderContext().getRegistry());</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">catch</span> (BeanDefinitionStoreException ex) &#123;</span><br><span class="line">         getReaderContext().error(<span class="string">&quot;Failed to register bean definition with name &#x27;&quot;</span> +</span><br><span class="line">               bdHolder.getBeanName() + <span class="string">&quot;&#x27;&quot;</span>, ele, ex);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// Send registration event.</span></span><br><span class="line">      getReaderContext().fireComponentRegistered(<span class="keyword">new</span> <span class="title class_">BeanComponentDefinition</span>(bdHolder));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>大致的逻辑总结如下：</p>
<p>1、首先委托BeanDefinationDelegate类的parseBeanDefinationElement方法进行元素解析，返回BeanDefinitionHolder 类型的实例 beHolder,经过这个方法后，beHolder实例已经包含我们配置文件中配置的各种属性了，例如 class、name、id、alias之类的属性。</p>
<p>2、当返回的beHolder不为空的情况下，若存在默认标签的子节点下再有自定义属性，还需要再次对自定义标签进行解析。</p>
<p>3、解析完成后，需要对解析后的beHolder进行注册，同样，注册操作委托给了 BeanDefinationReadUtils 的 registerBeanDefination 方法</p>
<p>4、最后发出响应事件，通知相关的监听器，这个bean已经加载完成了。</p>
<h3 id="1-1-解析BeanDefination"><a href="#1-1-解析BeanDefination" class="headerlink" title="1.1 解析BeanDefination"></a><strong>1.1 解析BeanDefination</strong></h3><p>首先我们从元素解析及信息提取开始，也就是 BeanDefinitionParserDelegate 类的parseBeanDefinitionElement方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> BeanDefinitionHolder <span class="title function_">parseBeanDefinitionElement</span><span class="params">(Element ele, <span class="meta">@Nullable</span> BeanDefinition containingBean)</span> &#123;</span><br><span class="line">   <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> ele.getAttribute(ID_ATTRIBUTE);</span><br><span class="line">   <span class="type">String</span> <span class="variable">nameAttr</span> <span class="operator">=</span> ele.getAttribute(NAME_ATTRIBUTE);</span><br><span class="line"></span><br><span class="line">   List&lt;String&gt; aliases = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">   <span class="keyword">if</span> (StringUtils.hasLength(nameAttr)) &#123;</span><br><span class="line">      String[] nameArr = StringUtils.tokenizeToStringArray(nameAttr, MULTI_VALUE_ATTRIBUTE_DELIMITERS);</span><br><span class="line">      aliases.addAll(Arrays.asList(nameArr));</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="type">String</span> <span class="variable">beanName</span> <span class="operator">=</span> id;</span><br><span class="line">   <span class="keyword">if</span> (!StringUtils.hasText(beanName) &amp;&amp; !aliases.isEmpty()) &#123;</span><br><span class="line">      beanName = aliases.remove(<span class="number">0</span>);</span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">         logger.debug(<span class="string">&quot;No XML &#x27;id&#x27; specified - using &#x27;&quot;</span> + beanName +</span><br><span class="line">               <span class="string">&quot;&#x27; as bean name and &quot;</span> + aliases + <span class="string">&quot; as aliases&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (containingBean == <span class="literal">null</span>) &#123;</span><br><span class="line">      checkNameUniqueness(beanName, aliases, ele);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="type">AbstractBeanDefinition</span> <span class="variable">beanDefinition</span> <span class="operator">=</span> parseBeanDefinitionElement(ele, beanName, containingBean);</span><br><span class="line">   <span class="keyword">if</span> (beanDefinition != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!StringUtils.hasText(beanName)) &#123;</span><br><span class="line">         <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (containingBean != <span class="literal">null</span>) &#123;</span><br><span class="line">               beanName = BeanDefinitionReaderUtils.generateBeanName(</span><br><span class="line">                     beanDefinition, <span class="built_in">this</span>.readerContext.getRegistry(), <span class="literal">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">               beanName = <span class="built_in">this</span>.readerContext.generateBeanName(beanDefinition);</span><br><span class="line">               <span class="comment">// Register an alias for the plain bean class name, if still possible,</span></span><br><span class="line">               <span class="comment">// if the generator returned the class name plus a suffix.</span></span><br><span class="line">               <span class="comment">// This is expected for Spring 1.2/2.0 backwards compatibility.</span></span><br><span class="line">               <span class="type">String</span> <span class="variable">beanClassName</span> <span class="operator">=</span> beanDefinition.getBeanClassName();</span><br><span class="line">               <span class="keyword">if</span> (beanClassName != <span class="literal">null</span> &amp;&amp;</span><br><span class="line">                     beanName.startsWith(beanClassName) &amp;&amp; beanName.length() &gt; beanClassName.length() &amp;&amp;</span><br><span class="line">                     !<span class="built_in">this</span>.readerContext.getRegistry().isBeanNameInUse(beanClassName)) &#123;</span><br><span class="line">                  aliases.add(beanClassName);</span><br><span class="line">               &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">               logger.debug(<span class="string">&quot;Neither XML &#x27;id&#x27; nor &#x27;name&#x27; specified - &quot;</span> +</span><br><span class="line">                     <span class="string">&quot;using generated bean name [&quot;</span> + beanName + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            error(ex.getMessage(), ele);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      String[] aliasesArray = StringUtils.toStringArray(aliases);</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BeanDefinitionHolder</span>(beanDefinition, beanName, aliasesArray);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>在开始对属性展开全面解析钱，Spring在外层又做了一个当前层的功能架构，在当前层完成的主要工作包括如下内容。</p>
<ol>
<li>提取元素中的id以及name属性。</li>
<li>进一步解析其他所有属性并统一封装至GenericBeanDefination类型的实例中。</li>
<li>如果检测到bean没有beanName,那么使用默认规则为此Bean生成beanName。</li>
<li>将获取到的信息封装到BeanDefinationHolder的实例中。</li>
</ol>
<p>进一步查看步骤2中堆标签其他属性的解析过程</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> AbstractBeanDefinition <span class="title function_">parseBeanDefinitionElement</span><span class="params">(</span></span><br><span class="line"><span class="params">      Element ele, String beanName, <span class="meta">@Nullable</span> BeanDefinition containingBean)</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="built_in">this</span>.parseState.push(<span class="keyword">new</span> <span class="title class_">BeanEntry</span>(beanName));</span><br><span class="line"></span><br><span class="line">   <span class="type">String</span> <span class="variable">className</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (ele.hasAttribute(CLASS_ATTRIBUTE)) &#123;</span><br><span class="line">      className = ele.getAttribute(CLASS_ATTRIBUTE).trim();</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="type">String</span> <span class="variable">parent</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">   <span class="keyword">if</span> (ele.hasAttribute(PARENT_ATTRIBUTE)) &#123;</span><br><span class="line">      parent = ele.getAttribute(PARENT_ATTRIBUTE);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="comment">// 创建用于承载属性的AbstractBeanDefinition 类型的GenericBeanDefination</span></span><br><span class="line">      <span class="type">AbstractBeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> createBeanDefinition(className, parent);</span><br><span class="line">      <span class="comment">// 硬编码解析默认bean的各种属性（奇怪的是scope为单例会记录错误，这是为啥？）</span></span><br><span class="line">      parseBeanDefinitionAttributes(ele, beanName, containingBean, bd);</span><br><span class="line">      bd.setDescription(DomUtils.getChildElementValueByTagName(ele, DESCRIPTION_ELEMENT));</span><br><span class="line">      <span class="comment">// 解析元数据</span></span><br><span class="line">      parseMetaElements(ele, bd);</span><br><span class="line">      parseLookupOverrideSubElements(ele, bd.getMethodOverrides());</span><br><span class="line">      parseReplacedMethodSubElements(ele, bd.getMethodOverrides());</span><br><span class="line">      <span class="comment">// 解析构造函数参数、property、qualifier子元素</span></span><br><span class="line">      parseConstructorArgElements(ele, bd);</span><br><span class="line">      parsePropertyElements(ele, bd);</span><br><span class="line">      parseQualifierElements(ele, bd);</span><br><span class="line"></span><br><span class="line">      bd.setResource(<span class="built_in">this</span>.readerContext.getResource());</span><br><span class="line">      bd.setSource(extractSource(ele));</span><br><span class="line"></span><br><span class="line">      <span class="keyword">return</span> bd;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (ClassNotFoundException ex) &#123;</span><br><span class="line">      error(<span class="string">&quot;Bean class [&quot;</span> + className + <span class="string">&quot;] not found&quot;</span>, ele, ex);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (NoClassDefFoundError err) &#123;</span><br><span class="line">      error(<span class="string">&quot;Class that bean class [&quot;</span> + className + <span class="string">&quot;] depends on not found&quot;</span>, ele, err);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      error(<span class="string">&quot;Unexpected failure during bean definition parsing&quot;</span>, ele, ex);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">finally</span> &#123;</span><br><span class="line">      <span class="built_in">this</span>.parseState.pop();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从上面的方法可以看出，bean标签的所有属性都被我们看到了，只有一些复杂属性需要进一步解析。</p>
<h4 id="1、创建用于属性承载的BeanDefination"><a href="#1、创建用于属性承载的BeanDefination" class="headerlink" title="1、创建用于属性承载的BeanDefination"></a><strong>1、创建用于属性承载的BeanDefination</strong></h4><p>BeanDefination是一个接口，在Spring中存在三种实现： RootBeanDefination、ChildBeanDefination、以及GenericBeanDefination.三种实现均继承了 AbstractBeanDefination，其中 BeanDefination 是配置文件 <bean>元素标签在容器中的内部表示形式。 <bean>元素标签拥有 class、scope、lazy-init等配置属性。BeanDefination则提供了相应的beanClass、scope、lazyInit属性。</p>
<p>​    在配置文件中可以定义父<bean>和子<bean>,父<bean>用RootBeanDefination表示，而子<bean>用ChildBeanDefination表示。</p>
<p>​    Spring通过BeanDefination将配置文件中的<bean>配置信息转换为容器的内部表示，并将这些BeanDefinition注册到BeanDefinitionRegistry中。Spring容器的BeanDefinitionRegistry就像是Spring配置信息的内存数据库，主要是以map的形式保存，后续操作直接从BeanDefinitionRegistry中读取配置信息。</p>
<img src="/2022/07/17/Spring-%E9%BB%98%E8%AE%A4%E6%A0%87%E7%AD%BE%E8%A7%A3%E6%9E%90/BeanDefinition.png" class="" title="BeanDefinition">



<p>由此可知，要解析属性首先要创建用于承载属性的实例，也就是创建 GenericBeanDefination类型的实例，而代码 createBeanDefinition(className, parent)的作用就是实现此功能。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> AbstractBeanDefinition <span class="title function_">createBeanDefinition</span><span class="params">(<span class="meta">@Nullable</span> String className, <span class="meta">@Nullable</span> String parentName)</span></span><br><span class="line">      <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> BeanDefinitionReaderUtils.createBeanDefinition(</span><br><span class="line">         parentName, className, <span class="built_in">this</span>.readerContext.getBeanClassLoader());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>BeanDefinitionReaderUtils.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> AbstractBeanDefinition <span class="title function_">createBeanDefinition</span><span class="params">(</span></span><br><span class="line"><span class="params">      <span class="meta">@Nullable</span> String parentName, <span class="meta">@Nullable</span> String className, <span class="meta">@Nullable</span> ClassLoader classLoader)</span> <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line"></span><br><span class="line">   <span class="type">GenericBeanDefinition</span> <span class="variable">bd</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GenericBeanDefinition</span>();</span><br><span class="line">   bd.setParentName(parentName);</span><br><span class="line">   <span class="keyword">if</span> (className != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (classLoader != <span class="literal">null</span>) &#123;</span><br><span class="line">         bd.setBeanClass(ClassUtils.forName(className, classLoader));</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">         bd.setBeanClassName(className);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> bd;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="2、解析各种属性"><a href="#2、解析各种属性" class="headerlink" title="2、解析各种属性"></a><strong>2、解析各种属性</strong></h4><p> 待补充</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ系列-架构初探</title>
    <url>/2021/11/19/RocketMQ%E7%B3%BB%E5%88%97-%E6%9E%B6%E6%9E%84%E5%88%9D%E6%8E%A2/</url>
    <content><![CDATA[<p><strong>前言：</strong></p>
<p>​    RocketMQ由于是阿里出品，已经有相当不错的官方文档了。这里本人为啥还想写一个技术系列呢？</p>
<p>1、主要是发现自己对RocketMQ的理解还有不少问题，并且我猜想大家也会跟我一样对某些概念出现了理解偏差，这里想强调说明一下。</p>
<p>2、官网对于某些模块描述的有点简单了，如果你不细心的去查看有可能就会错过一些设计理念或者出现误解。</p>
<p>3、提出一些个人见解同时提升学习效果，便于以后查看复习。</p>
<span id="more"></span> 

<br/>

<h2 id="RocketMQ技术架构图："><a href="#RocketMQ技术架构图：" class="headerlink" title="RocketMQ技术架构图："></a><strong>RocketMQ技术架构图：</strong></h2><img src="/2021/11/19/RocketMQ%E7%B3%BB%E5%88%97-%E6%9E%B6%E6%9E%84%E5%88%9D%E6%8E%A2/structure.png" class="" title="技术架构图">

<br/>

<h4 id="特别说明："><a href="#特别说明：" class="headerlink" title="特别说明："></a><strong>特别说明：</strong></h4><p>​    **Broker：**不要单纯认为Broker是一个基础组件概念，它实际上是一台代理服务器，里面存储Topic、队列，持久化消息，同时有对应的API接口与生产者和消费者做对接，一个Broker就是一台服务器。</p>
<p>​    <strong>Topic:</strong>    Topic虽然存储在Broker服务器中，但是它不是Broker的附属模块，它是独立的一个设计概念，一个Topic可以存储在多个Broker中，同时一个Broker也可以存储多个Topic，它们之间是一个关联关系，而不是一个从属关系。</p>
<p>   <strong>NameServer:</strong> 与分布式架构的注册中心类似，主要是用来维护 Topic和Broker的对应关系，使得生产者和消费者可以从NameServer中获取映射关系来找到对应的Topic所在的Broker IP进行对接。</p>
<p>   <strong>集群：</strong> 基本不会有单台服务器搭建MQ的情况，虽然你确实可以将Broker和NameServer都部署到一台服务器上，但是为了保证MQ的高可用，即避免单点故障、网络问题等情况，同时也避免单台服务器无法满足高峰流量出现消息投递和消费过快的限流问题。无论是NameServer还是 Broker Server基本都会以集群方式来搭建。</p>
<p>   **长连接：**NameServer与 生产者、消费者、Broker在通信时都是保持长连接的形式，第一个是避免重复的建立连接，减少开销。第二个是为了方便实时监听Broker的运行情况，更新Topic和Broker之间映射关系，并及时通知对应的关联方。</p>
<br/>

<h4 id="模块说明："><a href="#模块说明：" class="headerlink" title="模块说明："></a><strong>模块说明：</strong></h4><ul>
<li><p><strong>Producer：</strong> 消息发布的角色。应用在接入MQ对应Jar包后通过NameServer获取了Broker列表后，生产者内部会用MQ的负载均衡模块来进行消息的投递，投递的过程是异步的，支持快速失败。</p>
</li>
<li><p><strong>Consumer：</strong> 消息消费的角色。支持以Broker主动推送给Consumer，消费者主动拉取这两种模式对消息进行消费。同时也支持集群方式和广播方式的消费，它提供实时消息订阅机制，可以满足大多数用户的需求。</p>
</li>
<li><ul>
<li>集群方式： 一个消息会根据负载均衡只分发给对应消费组中的其中一个消费者，平均分推的形式。</li>
<li>广播方式：一个消息会分发给对应消费组中的所有消费者，全量消费的形式。</li>
</ul>
</li>
<li><p>**NameServer：**NameServer是一个非常简单的Topic路由注册中心，其角色类似Dubbo中的zookeeper，支持Broker的动态注册与发现。主要包括两个功能：Broker管理，NameServer接受Broker集群的注册信息并且保存下来作为路由信息的基本数据。然后提供心跳检测机制，检查Broker是否还存活；路由信息管理，每个NameServer将保存关于Broker集群的整个路由信息和用于客户端查询的队列信息。然后Producer和Conumser通过NameServer就可以知道整个Broker集群的路由信息，从而进行消息的投递和消费。NameServer通常也是集群的方式部署，各实例间相互不进行信息通讯。Broker是向每一台NameServer注册自己的路由信息，所以每一个NameServer实例上面都保存一份完整的路由信息。当某个NameServer因某种原因下线了，Broker仍然可以向其它NameServer同步其路由信息，Producer,Consumer仍然可以动态感知Broker的路由的信息。</p>
</li>
<li><p>**BrokerServer：**Broker主要负责消息的存储、投递和查询以及服务高可用保证，为了实现这些功能，Broker包含了以下几个重要子模块。</p>
</li>
</ul>
<br/>

<img src="/2021/11/19/RocketMQ%E7%B3%BB%E5%88%97-%E6%9E%B6%E6%9E%84%E5%88%9D%E6%8E%A2/brokerServer.png" class="" title="brokerServer">





<h4 id="RocketMQ-网络部署特点"><a href="#RocketMQ-网络部署特点" class="headerlink" title="RocketMQ 网络部署特点"></a><strong>RocketMQ 网络部署特点</strong></h4><ul>
<li>NameServer是一个几乎无状态节点，可集群部署，节点之间无任何信息同步。</li>
<li>Broker部署相对复杂，Broker分为Master与Slave，一个Master可以对应多个Slave，但是一个Slave只能对应一个Master，Master与Slave 的对应关系通过指定相同的BrokerName，不同的BrokerId 来定义，BrokerId为0表示Master，非0表示Slave。Master也可以部署多个。每个Broker与NameServer集群中的所有节点建立长连接，定时注册Topic信息到所有NameServer。 注意：当前RocketMQ版本在部署架构上支持一Master多Slave，但只有BrokerId&#x3D;1的从服务器才会参与消息的读负载。</li>
<li>Producer与NameServer集群中的其中一个节点（随机选择）建立长连接，定期从NameServer获取Topic路由信息，并向提供Topic 服务的Master建立长连接，且定时向Master发送心跳。Producer完全无状态，可集群部署。</li>
<li>Consumer与NameServer集群中的其中一个节点（随机选择）建立长连接，定期从NameServer获取Topic路由信息，并向提供Topic服务的Master、Slave建立长连接，且定时向Master、Slave发送心跳。Consumer既可以从Master订阅消息，也可以从Slave订阅消息，消费者在向Master拉取消息时，Master服务器会根据拉取偏移量与最大偏移量的距离（判断是否读老消息，产生读I&#x2F;O），以及从服务器是否可读等因素建议下一次是从Master还是Slave拉取。</li>
</ul>
<h4 id="RocketMQ网络部署流程："><a href="#RocketMQ网络部署流程：" class="headerlink" title="RocketMQ网络部署流程："></a><strong>RocketMQ网络部署流程：</strong></h4><ul>
<li>启动NameServer，NameServer起来后监听端口，等待Broker、Producer、Consumer连上来，相当于一个路由控制中心。</li>
<li>Broker启动，跟所有的NameServer保持长连接，定时发送心跳包。心跳包中包含当前Broker信息(IP+端口等)以及存储所有Topic信息。注册成功后，NameServer集群中就有Topic跟Broker的映射关系。</li>
<li>收发消息前，先创建Topic，创建Topic时需要指定该Topic要存储在哪些Broker上，也可以在发送消息时自动创建Topic。</li>
<li>Producer发送消息，启动时先跟NameServer集群中的其中一台建立长连接，并从NameServer中获取当前发送的Topic存在哪些Broker上，轮询从队列列表中选择一个队列，然后与队列所在的Broker建立长连接从而向Broker发消息。</li>
<li>Consumer跟Producer类似，跟其中一台NameServer建立长连接，获取当前订阅Topic存在哪些Broker上，然后直接跟Broker建立连接通道，开始消费消息。</li>
</ul>
<h3 id="关键特性："><a href="#关键特性：" class="headerlink" title="关键特性："></a><strong>关键特性：</strong></h3><h4 id="1、消息重投："><a href="#1、消息重投：" class="headerlink" title="1、消息重投："></a><strong>1、消息重投：</strong></h4><p>​    当生产者投递消息给Broker在指定时间内未收到ack确认时，或者直接投递失败，便会触发消息重投。消息重投保证消息尽可能发送成功、不丢失，但可能会造成消息重复，消息重复在RocketMQ中是无法避免的问题。消息重复在一般情况下不会发生，当出现消息量大、网络抖动，消息重复就会是大概率事件。另外，生产者主动重发、consumer负载变化也会导致重复消息。如下方法可以设置消息重试策略：</p>
<ul>
<li>retryTimesWhenSendFailed:同步发送失败重投次数，默认为2，因此生产者会最多尝试发送retryTimesWhenSendFailed + 1次。不会选择上次失败的broker，尝试向其他broker发送，最大程度保证消息不丢。超过重投次数，抛出异常，由客户端保证消息不丢。当出现RemotingException、MQClientException和部分MQBrokerException时会重投。</li>
<li>retryTimesWhenSendAsyncFailed:异步发送失败重试次数，异步重试不会选择其他broker，仅在同一个broker上做重试，不保证消息不丢。</li>
<li>retryAnotherBrokerWhenNotStoreOK:消息刷盘（主或备）超时或slave不可用（返回状态非SEND_OK），是否尝试发送到其他broker，默认false。十分重要消息可以开启。</li>
</ul>
<h4 id="2、消息可靠性"><a href="#2、消息可靠性" class="headerlink" title="2、消息可靠性"></a><strong>2、消息可靠性</strong></h4><p>​    RocketMQ支持消息的高可靠，影响消息可靠性的几种情况：</p>
<ol>
<li>Broker非正常关闭</li>
<li>Broker异常Crash</li>
<li>OS Crash</li>
<li>机器掉电，但是能立即恢复供电情况</li>
<li>机器无法开机（可能是cpu、主板、内存等关键设备损坏）</li>
<li>磁盘设备损坏</li>
</ol>
<p>1)、2)、3)、4) 四种情况都属于硬件资源可立即恢复情况，RocketMQ在这四种情况下能保证消息不丢，或者丢失少量数据（依赖刷盘方式是同步还是异步）。</p>
<p>5)、6)属于单点故障，且无法恢复，一旦发生，在此单点上的消息全部丢失。RocketMQ在这两种情况下，通过异步复制，可保证99%的消息不丢，但是仍然会有极少量的消息可能丢失。通过同步双写技术可以完全避免单点，同步双写势必会影响性能，适合对消息可靠性要求极高的场合，例如与Money相关的应用。注：RocketMQ从3.0版本开始支持同步双写。</p>
<h4 id="3、事务消息"><a href="#3、事务消息" class="headerlink" title="3、事务消息"></a><strong>3、事务消息</strong></h4><p>​    RocketMQ事务消息（Transactional Message）是指应用本地事务和发送消息操作可以被定义到全局事务中，要么同时成功，要么同时失败。RocketMQ的事务消息提供类似 X&#x2F;Open XA 的分布事务功能，通过事务消息能达到分布式事务的最终一致。</p>
<p>   XA是DTP的一部分接口规范，聊到分布式事务就要说一下DTP(Distributed Transaction Processing)分布式事务处理模型了。</p>
<p>DTP有三种角色组成：</p>
<p>  <strong>应用程序（Application Program，AP）</strong>：这个角色要做两件事情，一方面是定义构成整个事务所需要的所有操作，另一方面是亲自访问资源节点来执行操作。</p>
<p> <strong>资源管理器（Resource Managers，RM）：</strong> 这个角色是管理着某些共享资源的自治域，比如说一个MySQL数据库实例。在DTP里面，还有两个要求，一是RM自身必须是支持事务的，二是RM能够根据映射关系将全局（分布式）事务标识定位到自己内部的对应事务。</p>
<p>**事务管理器（Transaction Manager，TM）：**这个角色能与AP和RM直接通信，协调AP和RM来实现分布式事务的完整性。主要的工作是提供AP注册全局事务的接口，颁发全局事务标识（GTID之类 的），存储&#x2F;管理全局事务的内容和决策并指挥RM做commit&#x2F;rollback。</p>
<p>XA在哪里？<strong>XA是RM和TM的交互规范和接口定义</strong></p>
<p><strong>extended Architecture（XA）</strong></p>
<p>TM和RM们之间使用的是上文提到的《ISO&#x2F;TEC DIS 10026-1 (1991) (model)》所定义的二阶段提交。在XA规范的描述中，两阶段提交TM协调RM们完成已定义的全局事务的方法，<em>AP找TM申请&#x2F;注册全局事务</em>的动作并不是二阶段提交的保障内容。</p>
<p><strong>二阶段提交（two-phase commit）</strong></p>
<p>对于单个全局（分布式）事务，在DTP环境中，二阶段提交流程大致如下：</p>
<ol>
<li><em>第一阶段（Phase 1）</em></li>
</ol>
<p>TM请求<em>所有RM</em>进行<em>准备</em>（prepare commit, or prepare），并告知它们各自需要做的局部事务（transaction branche）。RM收到请求后，如果判断可以完成自己的局部事务，那就持久化局部事务的工作内容，再给TM肯定答复；要是发生了其他情况，那给TM的都是否定答复。在发送了否定答复并回滚了局部事务之后，RM才能丢弃持久化了的局部事务信息。</p>
<ol>
<li><em>第二阶段（Phase 2）</em></li>
</ol>
<p>TM根据情况（比如说所有RM都prepare成功，或者，AP通知它要rollback等），先持久化它对这个全局事务的处理决定和所涉及的RM清单，然后通知所有涉及的RM去提交（commit）或者回滚（rollback）它们的局部事务。RM们处理完自己的局部事务后，将返回值告诉TM之后，TM才可以清除掉包括刚才持久化的处理决定和RM清单在内的这个全局事务的信息。</p>
<h4 id="4、定时消息"><a href="#4、定时消息" class="headerlink" title="4、定时消息"></a>4、定时消息</h4><p>定时消息（延迟队列）是指消息发送到broker后，不会立即被消费，等待特定时间投递给真正的topic。 broker有配置项messageDelayLevel，默认值为“1s 5s 10s 30s 1m 2m 3m 4m 5m 6m 7m 8m 9m 10m 20m 30m 1h 2h”，18个level。可以配置自定义messageDelayLevel。注意，messageDelayLevel是broker的属性，不属于某个topic。发消息时，设置delayLevel等级即可：msg.setDelayLevel(level)。level有以下三种情况：</p>
<ul>
<li>level &#x3D;&#x3D; 0，消息为非延迟消息</li>
<li>1&lt;&#x3D;level&lt;&#x3D;maxLevel，消息延迟特定时间，例如level&#x3D;&#x3D;1，延迟1s</li>
<li>level &gt; maxLevel，则level&#x3D;&#x3D; maxLevel，例如level&#x3D;&#x3D;20，延迟2h</li>
</ul>
<p>定时消息会暂存在名为SCHEDULE_TOPIC_XXXX的topic中，并根据delayTimeLevel存入特定的queue，queueId &#x3D; delayTimeLevel – 1，即一个queue只存相同延迟的消息，保证具有相同发送延迟的消息能够顺序消费。broker会调度地消费SCHEDULE_TOPIC_XXXX，将消息写入真实的topic。</p>
<p>需要注意的是，定时消息会在第一次写入和调度写入真实topic时都会计数，因此发送数量、tps都会变高。</p>
]]></content>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ系列-事务消息与消息存储</title>
    <url>/2021/12/06/RocketMQ%E7%B3%BB%E5%88%97-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E4%B8%8E%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8/</url>
    <content><![CDATA[<p>​          RocketMQ在4.3.0版本已支持分布式事务消息，使用的是2PC(二阶段提交)思想来提交事务消息，同时增加一个补偿逻辑来处理二阶段超时或者失败的消息。</p>
<span id="more"></span> 

<br/>

<img src="/2021/12/06/RocketMQ%E7%B3%BB%E5%88%97-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E4%B8%8E%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8/transactionMsg.png" class="" title="transactionMsg">

<h2 id="RocketMQ事务消息流程概要"><a href="#RocketMQ事务消息流程概要" class="headerlink" title="RocketMQ事务消息流程概要"></a><strong>RocketMQ事务消息流程概要</strong></h2><p>上图说明了事务消息的大致方案，其中分为两个流程：正常事务消息的发送及提交、事务消息的补偿流程。</p>
<p><strong>1.事务消息发送及提交：</strong></p>
<p>(1) 发送消息（half消息）。</p>
<p>(2) 服务端响应消息写入结果。</p>
<p>(3) 根据发送结果执行本地事务（如果写入失败，此时half消息对业务不可见，本地逻辑不执行）。</p>
<p>(4) 根据本地事务状态执行Commit或者Rollback（Commit操作生成消息索引，消息对消费者可见）</p>
<p>补充： </p>
<p>1、上图中第4步结束，如果MQ Server正常收到了来自MQ Producer客户端本地事务的提交情况，则会根据提交结果来决定是否commit，让消息可见，还是rollback直接删除一阶段提交的消息，只有一直未收到消息，后台定时线程才会判定事务超时，进行图中的第五步检查生产组中的local transaction status</p>
<p>2、half消息就是事务提交一阶段发送的消息，至于为什么叫half倒是挺奇怪的，我估计当初设计者认为half代表还未完成的意思，是想表示为发送流程完成了一半的消息吧。</p>
<p><strong>2.补偿流程：</strong></p>
<p>(1) 对没有Commit&#x2F;Rollback的事务消息（pending状态的消息），从服务端发起一次“回查”</p>
<p>(2) Producer收到回查消息，检查回查消息对应的本地事务的状态</p>
<p>(3) 根据本地事务状态，重新Commit或者Rollback</p>
<p>其中，补偿阶段用于解决消息Commit或者Rollback发生超时或者失败的情况。</p>
<h2 id="消息存储"><a href="#消息存储" class="headerlink" title="消息存储"></a><strong>消息存储</strong></h2><img src="/2021/12/06/RocketMQ%E7%B3%BB%E5%88%97-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E4%B8%8E%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8/msgSave.png" class="" title="msgSave">

<p>上图来自官网，个人认为画的很清晰了便没有再重新构图。</p>
<h3 id="消息存储整体架构"><a href="#消息存储整体架构" class="headerlink" title="消息存储整体架构"></a><strong>消息存储整体架构</strong></h3><p>消息存储架构图中主要有下面三个跟消息存储相关的文件构成。</p>
<p>(1) CommitLog：消息主体以及元数据的存储主体，存储Producer端写入的消息主体内容,消息内容不是定长的。单个文件大小默认1G, 文件名长度为20位，左边补零，剩余为起始偏移量，比如00000000000000000000代表了第一个文件，起始偏移量为0，文件大小为1G&#x3D;1073741824；当第一个文件写满了，第二个文件为00000000001073741824，起始偏移量为1073741824，以此类推。消息主要是顺序写入日志文件，当文件满了，写入下一个文件；</p>
<p>(2) ConsumerQueue：消息消费队列，引入的目的主要是提高消息消费的性能，由于RocketMQ是基于主题topic的订阅模式，消息消费是针对主题进行的，如果要遍历commitlog文件中根据topic检索消息是非常低效的。Consumer即可根据ConsumerQueue来查找待消费的消息。其中，ConsumerQueue（逻辑消费队列）作为消费消息的索引，保存了指定Topic下的队列消息在CommitLog中的起始物理偏移量offset，消息大小size和消息Tag的HashCode值。consumerqueue文件可以看成是基于topic的commitlog索引文件，故consumerqueue文件夹的组织方式如下：topic&#x2F;queue&#x2F;file三层组织结构，具体存储路径为：$HOME&#x2F;store&#x2F;consumerqueue&#x2F;{topic}&#x2F;{queueId}&#x2F;{fileName}。同样consumerqueue文件采取定长设计，每一个条目共20个字节，分别为8字节的commitlog物理偏移量、4字节的消息长度、8字节tag hashcode，<strong>单个文件由30W个条目组成，可以像数组一样随机访问每一个条目</strong>，每个ConsumerQueue文件大小约5.72M；</p>
<p>(3) IndexFile：IndexFile（索引文件）提供了一种可以通过key或时间区间来查询消息的方法。Index文件的存储位置是：$HOME \store\index${fileName}，文件名fileName是以创建时的时间戳命名的，固定的单个IndexFile文件大小约为400M，一个IndexFile可以保存 2000W个索引，IndexFile的底层存储设计为在文件系统中实现HashMap结构，故rocketmq的索引文件其底层实现为hash索引。</p>
<p><strong>个人理解：</strong></p>
<p>1、 ConsumerQueue设置成类似数组的结构，这样方便Consumer顺序获取索引，只是最终读commitLog时是随机读盘，至于为什么不设计成hash索引或者B+树这种结果，是因为ConsumerQueue是用来消费者顺序消费队列消息的，并不是用来做单个key的查询（这种已经有indexFile索引)。</p>
<p>2、 IndexFile的主要作用是为了出现生产问题时，通过key来查询消息的相关信息方便定位是MQ的问题还是客户端的问题，例如消息内容、提交时间、消费时间等。</p>
<h3 id="RocketMQ事务消息设计"><a href="#RocketMQ事务消息设计" class="headerlink" title="RocketMQ事务消息设计"></a><strong>RocketMQ事务消息设计</strong></h3><p><strong>1.事务消息在一阶段对用户不可见</strong></p>
<p>在RocketMQ事务消息的主要流程中，一阶段的消息如何对用户不可见。其中，事务消息相对普通消息最大的特点就是一阶段发送的消息对用户是不可见的。那么，如何做到写入消息但是对用户不可见呢？</p>
<p>​          RocketMQ事务消息的做法是：如果消息是half消息，将备份原消息的主题与消息消费队列，然后改变主题为RMQ_SYS_TRANS_HALF_TOPIC。由于消费组未订阅该主题，故消费端无法消费half类型的消息，然后RocketMQ会开启一个定时任务，从Topic为RMQ_SYS_TRANS_HALF_TOPIC中拉取消息进行消费，根据生产者组获取一个服务提供者发送回查事务状态请求，根据事务状态来决定是提交或回滚消息。</p>
<p>在RocketMQ中，消息在服务端的存储结构如下，每条消息都会有对应的索引信息，Consumer通过ConsumeQueue这个二级索引来读取消息实体内容，其流程如下：</p>
<br/>

<p><strong>补充：</strong> </p>
<p>我对官方文档中ConsumeQueue是二级索引这句话是有争议的，因为我认为ConsumerQueue中存储的offset已经可以找到commitlog中消息所在的具体磁盘位置了，所以是一个一级索引，起初我以为是官网写错了，当我在github上提出相应的issue后，RocketMQ的Contribute回复了我，他的理解中只有通过 topic、queueID,offset才能完全标识一个message。而 ConsumerQueue中缺少Topic和QueueID，所以算一个二级索引。。。问题是Consumer在消费consumerQueue的时候肯定是能够知道这个文件的路径的，而这个路径中包含了topic和queueID，他认为这一步算一级就有点让人费解。</p>
<p>并且commitlog是完全依照offset来设计的文件名称，可以通过offset直接获取到具体的message磁盘位置，并且只是返回的消息中需要指明topic和queueID而已</p>
<p>感兴趣的同学可以看看我提的这个issues</p>
<p><a href="https://github.com/apache/rocketmq/issues/3584">https://github.com/apache/rocketmq/issues/3584</a></p>
<img src="/2021/12/06/RocketMQ%E7%B3%BB%E5%88%97-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E4%B8%8E%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8/consumerMsg.png" class="" title="consumerMsg">

<p>RocketMQ的具体实现策略是：写入的如果事务消息，对消息的Topic和Queue等属性进行替换，同时将原来的Topic和Queue信息存储到消息的属性中，正因为消息主题被替换，故消息并不会转发到该原主题的消息消费队列，消费者无法感知消息的存在，不会消费。其实改变消息主题是RocketMQ的常用“套路”，回想一下延时消息的实现机制。</p>
<br/>

<p><strong>2.Commit和Rollback操作以及Op消息的引入</strong></p>
<p>在完成一阶段写入一条对用户不可见的消息后，二阶段如果是Commit操作，则需要让消息对用户可见；如果是Rollback则需要撤销一阶段的消息。先说Rollback的情况。对于Rollback，本身一阶段的消息对用户是不可见的，其实不需要真正撤销消息（实际上RocketMQ也无法去真正的删除一条消息，因为是顺序写文件的）。但是区别于这条消息没有确定状态（Pending状态，事务悬而未决），需要一个操作来标识这条消息的最终状态。RocketMQ事务消息方案中引入了Op消息的概念，用Op消息标识事务消息已经确定的状态（Commit或者Rollback）。如果一条事务消息没有对应的Op消息，说明这个事务的状态还无法确定（可能是二阶段失败了）。引入Op消息后，事务消息无论是Commit或者Rollback都会记录一个Op操作。Commit相对于Rollback只是在写入Op消息前创建Half消息的索引(这里说的索引是指事务topic对应的consumerQueue类似数组一样的记录了offset）。</p>
<br/>

<p><strong>3.Op消息的存储和对应关系</strong></p>
<p>RocketMQ将Op消息写入到全局一个特定的Topic中通过源码中的方法—TransactionalMessageUtil.buildOpTopic()；这个Topic是一个内部的Topic（像Half消息的Topic一样），不会被用户消费。Op消息的内容为对应的Half消息的存储的Offset，这样通过Op消息能索引到Half消息进行后续的回查操作。</p>
<img src="/2021/12/06/RocketMQ%E7%B3%BB%E5%88%97-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E4%B8%8E%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8/opMsg.png" class="" title="opMsg">

<p><strong>补充：</strong></p>
<p>这里的Half Topic指的是RMQ_SYS_TRANS_HALF_TOPIC，不要被上图误解，Op Topic存的不是Half Topic的指针，而是commitLog对应的Offset，吐槽一下，官方给的这个图Op Topic指向Half Topic就很诡异，应该只是表示一个关联映射的关系，这里的User Topic是为了二阶段提交commit成功后，把Op Topic中的msg转换到其实际Topic对应的Queue中</p>
<br/>

<p><strong>4.Half消息的索引构建</strong></p>
<p>在执行二阶段Commit操作时，需要构建出Half消息的索引。一阶段的Half消息由于是写到一个特殊的Topic，所以二阶段构建索引时需要读取出Half消息，并将Topic和Queue替换成真正的目标的Topic和Queue，之后通过一次普通消息的写入操作来生成一条对用户可见的消息。所以RocketMQ事务消息二阶段其实是利用了一阶段存储的消息的内容，在二阶段时恢复出一条完整的普通消息，然后走一遍消息写入流程。</p>
<br/>

<p><strong>5.如何处理二阶段失败的消息？</strong></p>
<p>如果在RocketMQ事务消息的二阶段过程中失败了，例如在做Commit操作时，出现网络问题导致Commit失败，那么需要通过一定的策略使这条消息最终被Commit。RocketMQ采用了一种补偿机制，称为“回查”。Broker端对未确定状态的消息发起回查，将消息发送到对应的Producer端（同一个Group的Producer），由Producer根据消息来检查本地事务的状态，进而执行Commit或者Rollback。Broker端通过对比Half消息和Op消息进行事务消息的回查并且推进CheckPoint（记录那些事务消息的状态是确定的）。</p>
<p>值得注意的是，rocketmq并不会无休止的的信息事务状态回查，默认回查15次，如果15次回查还是无法得知事务状态，rocketmq默认回滚该消息。</p>
]]></content>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>Tomcat的日志切分与定时删除</title>
    <url>/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/</url>
    <content><![CDATA[<p>在我负责的一个小系统中，Linux环境下，由于默认日志都是写入在 cattalina.out中，</p>
<p>我查看日志catalina.out 竟然已经到了 40G了，我想做一下 文件内容检索来追踪问题都无法进行。</p>
<p>于是我决定删除以前的无用日志，以每日作为单位将其进行切分，并写一个定时脚本，定时删除1个月以前的日志数据。</p>
<span id="more"></span>

<p>Google了一下，大部分都是使用 Cronolog 工具进行切分日志，这里我也是第一次接触，记录一下操作过程方便日后再次使用。</p>
<p>话不多说，先看 cronolog 的官网介绍和下载</p>
<p>这里我试着用 yum install cronolog 发现不行，阿里云镜像找不到，就下载包的方式进行安装了。</p>
<p>github上有其项目，网址 <a href="https://github.com/fordmason/cronolog">https://github.com/fordmason/cronolog</a></p>
<br/>

<h3 id="cronolog简易介绍："><a href="#cronolog简易介绍：" class="headerlink" title="cronolog简易介绍："></a>cronolog简易介绍：</h3><p><em>CRONOLOG version 1.6.1  “cronolog” is a simple program that reads log messages from its input and writes them to a set of output files, the names of which are constructed using a template and the current date and time.  The template uses the same format specifiers as the Unix date command (which are the same as the standard C strftime library function).  “cronolog” is intended to be used in conjunction with a Web server, such as Apache, to split the access log into daily or monthly logs.</em> </p>
<br/>

<p><strong>大体意思：</strong></p>
<p>​      “ cronolog”是一个简单程序，可从其输入中读取日志消息并将它们写入一组输出文件，其名称为使用模板名和当前日期和时间进行构造。 </p>
<p>模板使用与Unix date命令相同的格式说明符（与标准C strftime库函数相同）。</p>
<p>​      “ cronolog”旨在与Web服务器结合使用，例如 作为Apache，将访问日志分为每日或每月日志。</p>
<p>文件下载 github上的是 zip包，适合windows的部署，</p>
<p>至于 cronolog的详细使用方法和参数介绍，可以 github上的 readme.txt，介绍的还行，我这里就大致了解一下，后续有需要再回来增加用法。</p>
<p>Linux的tar.gz 包下载地址： <a href="https://fossies.org/linux/www/old/cronolog-1.6.2.tar.gz/cloc.html">https://fossies.org/linux/www/old/cronolog-1.6.2.tar.gz/cloc.html</a></p>
<img src="/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/cronolog.jpg" class="" title="cronolog">

<br/>

<h3 id="cronolog的安装"><a href="#cronolog的安装" class="headerlink" title="cronolog的安装"></a>cronolog的安装</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">2、解压缩</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">tar -zxvf cronolog-1.6.2.tar.gz</span></span><br><span class="line"></span><br><span class="line">3、进入cronolog安装文件所在目录</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash"><span class="built_in">cd</span> cronolog-1.6.2</span></span><br><span class="line"></span><br><span class="line">4、运行安装 [进入文件夹，使用./configure命令进行编译，可以加--prefix指定安装目录]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">./configure</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">make</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">make install</span></span><br><span class="line"></span><br><span class="line">5、查看cronolog安装后所在目录（验证安装是否成功）</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"> &gt; </span><span class="language-bash"><span class="built_in">which</span> cronolog</span></span><br><span class="line"></span><br><span class="line">一般情况下显示为：/usr/local/sbin/cronolog</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<br/>

<h3 id="catalina-sh的修改"><a href="#catalina-sh的修改" class="headerlink" title="catalina.sh的修改"></a>catalina.sh的修改</h3><p>进行 TOMCAT目录下的 bin文件夹下</p>
<p>修改 catalina.sh 文件</p>
<p>注意： 这里假设你是TOMCAT7及版本以上，不过因为我有一个TOMCAT6的项目要维护，下面给出了TOMCAT6切分日志的步骤</p>
<p>需要修改3处地方：</p>
<p>1、 将</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">if [ -z &quot;$CATALINA_OUT&quot; ] ; then CATALINA_OUT=&quot;$CATALINA_BASE&quot;/logs/catalina.out fi </span><br></pre></td></tr></table></figure>

<p>改为</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">if [ -z &quot;$CATALINA_OUT&quot; ] ; then   CATALINA_OUT=&quot;$CATALINA_BASE&quot;/logs/catalina.%Y-%m-%d.out   fi </span><br></pre></td></tr></table></figure>



<p>2、将</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">touch &quot;$CATALINA_OUT&quot; </span><br></pre></td></tr></table></figure>

<p>注释掉</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="built_in">touch</span> <span class="string">&quot;<span class="variable">$CATALINA_OUT</span>&quot;</span></span> </span><br></pre></td></tr></table></figure>



<p>3、 将</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">org.apache.catalina.startup.Bootstrap &quot;$@&quot; start \ &gt;&gt; &quot;$CATALINA_OUT&quot; 2&gt;&amp;1 &quot;&amp;&quot; </span><br></pre></td></tr></table></figure>

<p>改为</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">org.apache.catalina.startup.Bootstrap &quot;$@&quot; start 2&gt;&amp;1 \ | /usr/local/sbin/cronolog &quot;$CATALINA_OUT&quot; &gt;&gt; /dev/null &amp; </span><br></pre></td></tr></table></figure>

<br/>

<h4 id="注意："><a href="#注意：" class="headerlink" title="注意："></a>注意：</h4><p>1、有两处都改了，以前的内容不要注释，要直接删除了，不然会引发log报错</p>
<p>2、以上的“&#x2F;usr&#x2F;local&#x2F;sbin&#x2F;cronolog”配置的是cronolog的安装目录，这里要根据你的cronolog安装目录进行配置。</p>
<p>配置完成之后，重启tomcat就可以了。重启访问应用之后就会发现，Catalina.out不会再输出日志，日志会输入到一个catalina.日期.out的文件中。</p>
<img src="/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/catalina.jpg" class="" title="catalina">

<br/>

<p><strong>修改完该3处内容后重启tomcat</strong></p>
<p><strong>如果是TOMCAT6,没有 CATALINA_OUT变量名的话，第一步跳过，第二步要，第3步改成这个，其实就是因为  CATALINA_OUT&#x3D;”$CATALINA_BASE”&#x2F;logs&#x2F;catalina.%Y-%m-%d.out 进行替换即可。</strong></p>
<br/>

<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">org.apache.catalina.startup.Bootstrap &quot;$@&quot; start 2&gt;&amp;1 \  | /usr/local/sbin/cronolog &quot;$CATALINA_BASE&quot;/logs/catalina.%Y-%m-%d.out &gt;&gt; /dev/null &amp; </span><br></pre></td></tr></table></figure>

<p>亲测这种写法在tomcat6可行!</p>
<img src="/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/show1.jpg" class="" title="show1">

<br/>

<h3 id="效果示意："><a href="#效果示意：" class="headerlink" title="效果示意："></a>效果示意：</h3><p>未加切分日志时的catalina文件</p>
<img src="/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/show2.jpg" class="" title="show2">



<p>加了 Cornolog 工具进行切分后的log文件夹显示</p>
<img src="/2019/10/15/Tomcat%E7%9A%84%E6%97%A5%E5%BF%97%E5%88%87%E5%88%86%E4%B8%8E%E5%AE%9A%E6%97%B6%E5%88%A0%E9%99%A4/show3.jpg" class="" title="show3">



<p>新打印的日志已按照每日来划分</p>
<p>因为这句话，如果数据量较小，你也可以按照每月为单位进行切分  catalina.%Y-%m.out</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">CATALINA_OUT=&quot;$CATALINA_BASE&quot;/logs/catalina.%Y-%m-%d.out</span><br></pre></td></tr></table></figure>

<br/>

<h3 id="定时脚本删除"><a href="#定时脚本删除" class="headerlink" title="定时脚本删除"></a>定时脚本删除</h3><p>新建脚本 vim tomcat_log_auto_del.sh </p>
<p>注意该脚本的位置是假设你放在tomcat的bin中</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd `dirname $0` #返回这个脚本文件放置的目录</span><br><span class="line"></span><br><span class="line">cd ../logs  #进入你tomcat的logs下,当然你也可以直接用绝对路径</span><br><span class="line"></span><br><span class="line">d=`date -d &#x27;30 day ago&#x27; +%Y-%m-%d` </span><br><span class="line"></span><br><span class="line">rm -rf catalina.$&#123;d&#125;.out</span><br></pre></td></tr></table></figure>



<p>crontab -e &#x2F;&#x2F;编辑当前用户的crontab文件，可指定具体的用户</p>
<p>加入我们的定时任务(表示每天3点跑一次脚本)：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0 3 * * * /usr/local/tomcat_log_auto_del.sh &gt;/dev/null 2&gt;&amp;1 </span><br></pre></td></tr></table></figure>

<br/>



<h3 id="另外一种解决思路："><a href="#另外一种解决思路：" class="headerlink" title="另外一种解决思路："></a>另外一种解决思路：</h3><p>用shell定时脚本来复制， cp 当天的日志 指定日期命名 来复制当天的日志</p>
<p>清空 catalina.out 的内容，echo “” &gt;  </p>
<p>随便可以删除1个月以前的日志</p>
<p>注意该脚本的位置是假设你放在tomcat的bin中</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash </span></span><br><span class="line"><span class="built_in">cd</span> `<span class="built_in">dirname</span> <span class="variable">$0</span>`  <span class="comment">#返回这个脚本文件放置的目录 </span></span><br><span class="line">d=`<span class="built_in">date</span> +%Y-%m-%d` </span><br><span class="line">d30=`<span class="built_in">date</span> -d <span class="string">&#x27;30 day ago&#x27;</span> +%Y-%m-%d`  <span class="comment">#输出30天前的日期 </span></span><br><span class="line"><span class="built_in">cd</span> ../logs/ <span class="comment">#从bin -&gt; logs </span></span><br><span class="line"><span class="built_in">cp</span> catalina.out catalina.<span class="variable">$&#123;d&#125;</span>.out</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;&quot;</span> &gt; catalina.out <span class="comment"># 清空日志 </span></span><br><span class="line"><span class="built_in">rm</span> -rf catalina.<span class="variable">$&#123;d30&#125;</span>.out <span class="comment">#删除30天前的日期对应的日志 </span></span><br></pre></td></tr></table></figure>

<br/>

<h3 id="参考博文："><a href="#参考博文：" class="headerlink" title="参考博文："></a>参考博文：</h3><p><a href="http://www.mrliangqi.com/909.html">http://www.mrliangqi.com/909.html</a></p>
<p><a href="https://blog.csdn.net/fly910905/article/details/78528652">https://blog.csdn.net/fly910905/article/details/78528652</a></p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>tomcat</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring源码系列-Spring容器的基本实现</title>
    <url>/2022/06/26/Spring%E6%BA%90%E7%A0%81%E7%B3%BB%E5%88%97-Spring%E5%AE%B9%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p><strong>前言：</strong> </p>
<p>Spring的容器是Spring的核心之一，也是IOC和DP思想的基础实现，但是对于容器是如何实现，如何容器中的Bean是如何加载，这些我都一知半解，乘着自己工作3年多后对学什么比较迷茫，新技术的学习在工作中没有应用场景很快就会被遗忘，那就沉下心来看看优秀框架的源码，夯实基础，慢慢前行吧。</p>
<span id="more"></span> 

<br/>

<h2 id="1-Spring核心类的介绍"><a href="#1-Spring核心类的介绍" class="headerlink" title="1.Spring核心类的介绍"></a><strong>1.Spring核心类的介绍</strong></h2><h3 id="1-1、DefaultListableBeanFactory"><a href="#1-1、DefaultListableBeanFactory" class="headerlink" title="1.1、DefaultListableBeanFactory"></a><strong>1.1、DefaultListableBeanFactory</strong></h3><p>DefaultListableBeanFactory是整个bean加载的核心部分，是Spring注册及加载bean的默认实现。</p>
<img src="/2022/06/26/Spring%E6%BA%90%E7%A0%81%E7%B3%BB%E5%88%97-Spring%E5%AE%B9%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%AE%9E%E7%8E%B0/DefaultListableBeanFactory.png" class="" title="DefaultListableBeanFactory_UML">

<p>AliasRegistry: 定义对alias的简单增删改等操作。</p>
<p>SimpleAliasRegistry: 主要使用map作为alias的缓存，并对接口 AliasRegistry进行实现。</p>
<h3 id="1-2、XmlBeanDefinitionReader"><a href="#1-2、XmlBeanDefinitionReader" class="headerlink" title="1.2、XmlBeanDefinitionReader"></a><strong>1.2、XmlBeanDefinitionReader</strong></h3><p>  XML配置文件的读取是Spring中重要的功能，因为Spring的大部分功能都是以配置作为切入点的，那么我们可以从XmlBeanDefinitionReader中梳理一下资源文件读取，解析及注册的大致脉络。</p>
<p>首先我们看看各个类的功能。</p>
<ul>
<li>ResourceLoader：定义资源加载器，主要应用于根据给定的资源文件地址返回对应的Resource。</li>
<li>BeanDefinitionReader： 主要定义资源文件读取并转换为BeanDefinition的各个功能。</li>
<li>EnvironmentCapable: 定义获取 Environment方法。</li>
<li>DocumentLoader:定义从资源文件加载到转换为 Document的功能。</li>
<li>AbstractBeanDefinitionReader： 对EnvironmentCapable、BeanDefinitionReader类定义的功能进行实现。</li>
<li>BeanDefinitionDocumentReader: 定义读取Document并注册BeanDefination功能。</li>
<li>BeanDefinitionParserDelegate：定义解析Element的各种方法。</li>
</ul>
<img src="/2022/06/26/Spring%E6%BA%90%E7%A0%81%E7%B3%BB%E5%88%97-Spring%E5%AE%B9%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%AE%9E%E7%8E%B0/XmlBeanDefinitionReader.png" class="" title="XmlBeanDefinitionReader_UML">

<p><strong>XmlBeanDefinitionReader主要包含以下几步的处理</strong>：</p>
<p>1、通过继承 AbstractBeanDefinitionReader 中的方法，来使用 ResourceLoader将资源文件路径转换为对应的 Resource 文件。</p>
<p>2、通过DocumentLoader 对 Resource文件进行转换，将 Resource 文件 转换为Document文件。</p>
<p>3、通过实现接口 BeanDefinitionDocumentReader类对 Document进行解析，并使用 BeanDefinitionParserDelegate对 Element进行解析。</p>
<p>可以看这个方法： org.springframework.beans.factory.xml.XmlBeanDefinitionReader#loadBeanDefinitions(org.springframework.core.io.Resource)</p>
<p>疑问点： ThreadLocal&lt;Set<EncodedResource>&gt; resourcesCurrentlyBeingLoaded 为什么容器加载的资源是单线程隔离的？它的读取使用场景是怎样的，设计的好处？</p>
<h2 id="2-容器的基础-XmlBeanFactory"><a href="#2-容器的基础-XmlBeanFactory" class="headerlink" title="2.容器的基础 XmlBeanFactory"></a><strong>2.容器的基础 XmlBeanFactory</strong></h2><p>org.springframework.beans.factory.xml.XmlBeanFactory#XmlBeanFactory(org.springframework.core.io.Resource)</p>
<p>查看XmlBeanFactory的构造器可以发现XmlBeanFactory类也是以xml文件被加载,通过XmlBeanDefinitionReader.loadBeanDefinitions，从 文件 -&gt; Resource -&gt; Document -&gt; Bean</p>
<h3 id="2-1、配置文件封装"><a href="#2-1、配置文件封装" class="headerlink" title="2.1、配置文件封装"></a><strong>2.1、配置文件封装</strong></h3><p> Spring的配置文件读取是通过 ClassPathResource 进行封装的，如 new ClassPathResource(“beanFactoryTest.xml”),那么ClassPathResouce完成了什么功能呢？</p>
<p>  在Java中，将不同来源的资源抽象成URL,通过注册不同的handler（URLStreamHandler）来处理不同来源的资源的读取逻辑，但是URL没有默认定义相对Classpath或ServletContext等资源的hanlder。并且URL也没有提供基础的方法，例如检查当前资源是否存在，检查当前资源是否可读等。所以Spring对内部是使用的资源实现了自己的抽象结果： Resource接口封装底层资源。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">XmlBeanFactory</span><span class="params">(Resource resource, BeanFactory parentBeanFactory)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">   <span class="built_in">super</span>(parentBeanFactory);</span><br><span class="line">   <span class="built_in">this</span>.reader.loadBeanDefinitions(resource);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">AbstractAutowireCapableBeanFactory</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="built_in">super</span>();</span><br><span class="line">   ignoreDependencyInterface(BeanNameAware.class);</span><br><span class="line">   ignoreDependencyInterface(BeanFactoryAware.class);</span><br><span class="line">   ignoreDependencyInterface(BeanClassLoaderAware.class);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>new XmlBeanFactory方法会调用父类构造器，其中ignoreDependencyInterface方法会忽略几个我们眼熟的接口。</p>
<p>这个方法是用来忽略给定接口的自动装配功能。这样做的目的是什么？</p>
<p>举个栗子： 例如 类A有属性B，那么初始化A时，会初始化B，但是有时候我们不想通过new的形式来初始化，而是通过上下文Aplication来自动装配注入，这样的好处是节省资源开销，尤其是在Spring中单例思想盛行的情况下。当然在B中配置一个 BeanNameAware接口，使得初始化B的时候通过 BeanFactoryAware或者Aplication来进行注入，这种也是可行的办法。</p>
<h3 id="2-2、加载Bean"><a href="#2-2、加载Bean" class="headerlink" title="2.2、加载Bean"></a><strong>2.2、加载Bean</strong></h3><p>插曲： 一种好的代码写法 在org.springframework.beans.factory.xml.XmlBeanDefinitionReader#loadBeanDefinitions(org.xml.sax.InputSource, java.lang.String)</p>
<p>loadBeanDefinitions方法中，加载bean之前需要做很多处理，例如资源校验，资源存储校验和存储集合初始化等操作，这些操作与该方法的核心语义关系不是很大，属于准备阶段的东西。</p>
<p>为了防止方法臃肿，那么可以新增doLoadBeanDefinitions来实际做方法语义操作，外面loadBeanDefinitions方法只做准备工作和try catch finally等无关又必须的操作。</p>
<h2 id="3、获取XML的验证模式"><a href="#3、获取XML的验证模式" class="headerlink" title="3、获取XML的验证模式"></a><strong>3、获取XML的验证模式</strong></h2><h3 id="3-1-DTD与XSD区别"><a href="#3-1-DTD与XSD区别" class="headerlink" title="3.1 DTD与XSD区别"></a><strong>3.1 DTD与XSD区别</strong></h3><p>DTD与XSD是两种常用的验证模式。</p>
<p><strong>DTD(Document Type Defination)</strong>: 文档类型定义，是一种XML约束模式语言。它用来保证XML文档格式是否正确，校验元素和标签是否使用规范。（DTD包含了 元素、元素间的定义规则，元素可以使用的属性等）</p>
<p><strong>XSD(XML Schemas Defination)</strong>: XML Schema描述了XML文档的结构，校验XML文档命令空间是否正确，语法机构是否符合要求。</p>
<h4 id="所以这两种验证模式的核心区别是啥？"><a href="#所以这两种验证模式的核心区别是啥？" class="headerlink" title="所以这两种验证模式的核心区别是啥？"></a>所以这两种验证模式的核心区别是啥？</h4><p>我理解DTD是一种通用的XML的元素语法校验，它是以具体XML中 &lt;!DOCTYPE 开头标注申明，Spring也是通过这个来判断是不是DTD的验证模式。而XSD是一种针对性的元素结构之间的关系校验。</p>
<h3 id="3-2、验证模式的获取"><a href="#3-2、验证模式的获取" class="headerlink" title="3.2、验证模式的获取"></a><strong>3.2、验证模式的获取</strong></h3><p>如果手动指定了，就用指定的，不然通过自动检测</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">getValidationModeForResource</span><span class="params">(Resource resource)</span> &#123;</span><br><span class="line">   <span class="type">int</span> <span class="variable">validationModeToUse</span> <span class="operator">=</span> getValidationMode();</span><br><span class="line">   <span class="comment">// 判断是否有手动指定</span></span><br><span class="line">   <span class="keyword">if</span> (validationModeToUse != VALIDATION_AUTO) &#123;</span><br><span class="line">      <span class="keyword">return</span> validationModeToUse;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="comment">// 如果未指定就使用自动检测</span></span><br><span class="line">   <span class="type">int</span> <span class="variable">detectedMode</span> <span class="operator">=</span> detectValidationMode(resource);</span><br><span class="line">   <span class="keyword">if</span> (detectedMode != VALIDATION_AUTO) &#123;</span><br><span class="line">      <span class="keyword">return</span> detectedMode;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> VALIDATION_XSD;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有一个中间方法没啥用我就直接省略了，核心就一行</p>
<p>return this.validationModeDetector.detectValidationMode(inputStream);</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">detectValidationMode</span><span class="params">(InputStream inputStream)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">   <span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(inputStream));</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="type">boolean</span> <span class="variable">isDtdValidated</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">      String content;</span><br><span class="line">      <span class="keyword">while</span> ((content = reader.readLine()) != <span class="literal">null</span>) &#123;</span><br><span class="line">         content = consumeCommentTokens(content);</span><br><span class="line">         <span class="keyword">if</span> (<span class="built_in">this</span>.inComment || !StringUtils.hasText(content)) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// 判断是不是DTD验证模式，&lt;!DOCTYPE 开头,不包含这个就是 XSD验证模式</span></span><br><span class="line">         <span class="keyword">if</span> (hasDoctype(content)) &#123;</span><br><span class="line">            isDtdValidated = <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">if</span> (hasOpeningTag(content)) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> (isDtdValidated ? VALIDATION_DTD : VALIDATION_XSD);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (CharConversionException ex) &#123;</span><br><span class="line">      <span class="comment">// Choked on some character encoding...</span></span><br><span class="line">      <span class="comment">// Leave the decision up to the caller.</span></span><br><span class="line">      <span class="keyword">return</span> VALIDATION_AUTO;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">finally</span> &#123;</span><br><span class="line">      reader.close();</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="4、获取Document"><a href="#4、获取Document" class="headerlink" title="4、获取Document"></a><strong>4、获取Document</strong></h2><p>简单来说就是通过SAX解析XML文档，只是有一个专门的DocumentBuilderFactory 来处理</p>
<p><strong>DefaultDocumentLoader.java</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Document <span class="title function_">loadDocument</span><span class="params">(InputSource inputSource, EntityResolver entityResolver,</span></span><br><span class="line"><span class="params">      ErrorHandler errorHandler, <span class="type">int</span> validationMode, <span class="type">boolean</span> namespaceAware)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">   <span class="type">DocumentBuilderFactory</span> <span class="variable">factory</span> <span class="operator">=</span> createDocumentBuilderFactory(validationMode, namespaceAware);</span><br><span class="line">   <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">      logger.debug(<span class="string">&quot;Using JAXP provider [&quot;</span> + factory.getClass().getName() + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="type">DocumentBuilder</span> <span class="variable">builder</span> <span class="operator">=</span> createDocumentBuilder(factory, entityResolver, errorHandler);</span><br><span class="line">   <span class="keyword">return</span> builder.parse(inputSource);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-1-EntityResolver-用法"><a href="#4-1-EntityResolver-用法" class="headerlink" title="4.1 EntityResolver 用法"></a><strong>4.1 EntityResolver 用法</strong></h3><p>官网这样解释：</p>
<p> 如果SAX应用程序需要实现自定义处理外部实体，则必须实现loadDocument接口并使用setEntityResolver 方法向SAX驱动器注册一个实例。</p>
<p>EntityResolver 的作用是项目本身就可以提供一个如何寻找DTD声明的方法，即由程序来实现寻找DTD声明的过程。（就是我们把DTD文件放到项目的某个地方，然后实现时直接把这个文件读取返回给SAX，这样就避免了通过网络来寻找相应的声明。）</p>
<p>简单点说就是支持本地配置DTD的映射规则，并支持读取解析的这么个功能，从而优化了用户体验，快速加载。</p>
<p>不同的验证模式，Spring使用不同的解析器解析</p>
<p><strong>DelegatingEntityResolver.Java</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> InputSource <span class="title function_">resolveEntity</span><span class="params">(<span class="meta">@Nullable</span> String publicId, <span class="meta">@Nullable</span> String systemId)</span></span><br><span class="line">      <span class="keyword">throws</span> SAXException, IOException &#123;</span><br><span class="line">   <span class="keyword">if</span> (systemId != <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (systemId.endsWith(DTD_SUFFIX)) &#123;</span><br><span class="line">         <span class="keyword">return</span> <span class="built_in">this</span>.dtdResolver.resolveEntity(publicId, systemId);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (systemId.endsWith(XSD_SUFFIX)) &#123;</span><br><span class="line">         <span class="keyword">return</span> <span class="built_in">this</span>.schemaResolver.resolveEntity(publicId, systemId);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>个人心得： 在调用第三方接口的时候，通过WSDL等XML文件来定义接口规则并调用的时候，有时候会报 Marshaller解析错误，这种大概率就是因为没有通过SAX的验证，即你配的元素不对，不符合规范，需要仔细检查一下新增的内容是否符合接口的元素定义规范。</p>
<h2 id="5、解析及注册-BeanDefinitions"><a href="#5、解析及注册-BeanDefinitions" class="headerlink" title="5、解析及注册 BeanDefinitions"></a><strong>5、解析及注册 BeanDefinitions</strong></h2><p>当把文件转换为Document后，接下来的提取及注册bean就是我们的重头戏。</p>
<p><strong>XmlBeanDefinitionReader.java</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">registerBeanDefinitions</span><span class="params">(Document doc, Resource resource)</span> <span class="keyword">throws</span> BeanDefinitionStoreException &#123;</span><br><span class="line">   <span class="type">BeanDefinitionDocumentReader</span> <span class="variable">documentReader</span> <span class="operator">=</span> createBeanDefinitionDocumentReader();</span><br><span class="line">   <span class="type">int</span> <span class="variable">countBefore</span> <span class="operator">=</span> getRegistry().getBeanDefinitionCount();</span><br><span class="line">   documentReader.registerBeanDefinitions(doc, createReaderContext(resource));</span><br><span class="line">   <span class="comment">// 记录本次加载的BeanDefination个数</span></span><br><span class="line">   <span class="keyword">return</span> getRegistry().getBeanDefinitionCount() - countBefore;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>真正解析bean是通过DefaultBeanDefinitionDocumentReader来处理的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerBeanDefinitions</span><span class="params">(Document doc, XmlReaderContext readerContext)</span> &#123;</span><br><span class="line">   <span class="built_in">this</span>.readerContext = readerContext;</span><br><span class="line">   logger.debug(<span class="string">&quot;Loading bean definitions&quot;</span>);</span><br><span class="line">   <span class="type">Element</span> <span class="variable">root</span> <span class="operator">=</span> doc.getDocumentElement();</span><br><span class="line">   doRegisterBeanDefinitions(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">doRegisterBeanDefinitions</span><span class="params">(Element root)</span> &#123;</span><br><span class="line">   <span class="type">BeanDefinitionParserDelegate</span> <span class="variable">parent</span> <span class="operator">=</span> <span class="built_in">this</span>.delegate;</span><br><span class="line">   <span class="built_in">this</span>.delegate = createDelegate(getReaderContext(), root, parent);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (<span class="built_in">this</span>.delegate.isDefaultNamespace(root)) &#123;</span><br><span class="line">       <span class="comment">// 处理profile属性，看是加载哪个环境的，test、dev、production等</span></span><br><span class="line">      <span class="type">String</span> <span class="variable">profileSpec</span> <span class="operator">=</span> root.getAttribute(PROFILE_ATTRIBUTE);</span><br><span class="line">      <span class="keyword">if</span> (StringUtils.hasText(profileSpec)) &#123;</span><br><span class="line">         String[] specifiedProfiles = StringUtils.tokenizeToStringArray(</span><br><span class="line">               profileSpec, BeanDefinitionParserDelegate.MULTI_VALUE_ATTRIBUTE_DELIMITERS);</span><br><span class="line">         <span class="keyword">if</span> (!getReaderContext().getEnvironment().acceptsProfiles(specifiedProfiles)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (logger.isInfoEnabled()) &#123;</span><br><span class="line">               logger.info(<span class="string">&quot;Skipped XML bean definition file due to specified profiles [&quot;</span> + profileSpec +</span><br><span class="line">                     <span class="string">&quot;] not matching: &quot;</span> + getReaderContext().getResource());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   preProcessXml(root);</span><br><span class="line">   parseBeanDefinitions(root, <span class="built_in">this</span>.delegate);</span><br><span class="line">   postProcessXml(root);</span><br><span class="line"></span><br><span class="line">   <span class="built_in">this</span>.delegate = parent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-1-解析并注册-BeanDefination"><a href="#5-1-解析并注册-BeanDefination" class="headerlink" title="5.1 解析并注册 BeanDefination"></a>5.1 解析并注册 BeanDefination</h3><p>DefaultBeanDefinitionDocumentReader.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">parseBeanDefinitions</span><span class="params">(Element root, BeanDefinitionParserDelegate delegate)</span> &#123;</span><br><span class="line">   <span class="keyword">if</span> (delegate.isDefaultNamespace(root)) &#123;</span><br><span class="line">      <span class="type">NodeList</span> <span class="variable">nl</span> <span class="operator">=</span> root.getChildNodes();</span><br><span class="line">      <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; nl.getLength(); i++) &#123;</span><br><span class="line">         <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> nl.item(i);</span><br><span class="line">         <span class="keyword">if</span> (node <span class="keyword">instanceof</span> Element) &#123;</span><br><span class="line">            <span class="type">Element</span> <span class="variable">ele</span> <span class="operator">=</span> (Element) node;</span><br><span class="line">            <span class="keyword">if</span> (delegate.isDefaultNamespace(ele)) &#123;</span><br><span class="line">               parseDefaultElement(ele, delegate);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">               delegate.parseCustomElement(ele);</span><br><span class="line">            &#125;</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      delegate.parseCustomElement(root);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Spring的XML配置里面有两大类Bean声明，一个是默认的，如：</p>
<bean id="test" class="test.TestBean" /> 

<p>另一个类就是自定义的，如：</p>
<p>&lt;tx:annotation&#x3D;driven &#x2F;&gt; </p>
<p>两种方式的读取及解析差别是非常大的，自定义的方式需要用户实现一些接口及配置。</p>
<p>具体还得看下一章节的标签解析。</p>
<p>容器基本实现章节到此结束。</p>
]]></content>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>代理模式</title>
    <url>/2020/10/12/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="代理模式-Proxy"><a href="#代理模式-Proxy" class="headerlink" title="代理模式(Proxy)"></a>代理模式(Proxy)</h2><h4 id="代理模式的基本介绍"><a href="#代理模式的基本介绍" class="headerlink" title="代理模式的基本介绍"></a>代理模式的基本介绍</h4><p>1、代理模式： 为一个对象提供一个替身，以控制对这个对象的访问。即通过代理对象访问目标对象，这样做的好处是： 可以在目标对象实现的基础上，增强额外的功能操作，即扩展目标对象的功能。</p>
<p>2、被代理的对象可以是远程对象、创建开销大的对象或需要安全控制的对象</p>
<p>3、代理模式有不同的形式，主要分三种</p>
<ul>
<li>静态代理</li>
<li>动态代理(JDK代理）</li>
<li>Cglib代理(在内存中动态创建对象，不需要接口，其实也可以看做是动态代理的一种)</li>
</ul>
<span id="more"></span> 

<br/>

<h2 id="静态代理"><a href="#静态代理" class="headerlink" title="静态代理"></a>静态代理</h2><h4 id="基本介绍"><a href="#基本介绍" class="headerlink" title="基本介绍"></a>基本介绍</h4><p>静态代理在使用时，需要定义接口或者父类，被代理对象(即目标对象）与代理对象一起实现相同的接口或者是继承相同的父类</p>
<h4 id="应用实例"><a href="#应用实例" class="headerlink" title="应用实例"></a>应用实例</h4><p>具体要求</p>
<p>1、定义一个接口 ITeacherDao,有一个teach方法</p>
<p>2、目标对象 TeacherDao 实现接口 ITeacherDAO</p>
<p>3、使用静态代理模式，就需要在代理对象 TeacherDaoProxy 中 也实现 ITeacherDao</p>
<p>4、调用的时候通过调用代理对象的方法来调用目标对象</p>
<p>5、特别提醒： 代理对象与目标对象要实现相同的接口，然后通过调用相同的方法来调用目标对象的方法。</p>
<img src="/2020/10/12/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/staticUml.png" class="" title="静态代理类图">

<br/>

<p>代理的核心思想是 为了添加原有对象之外的其他功能，例如日志记录，时间记录、方法过滤，权限控制等</p>
<h4 id="代理类"><a href="#代理类" class="headerlink" title="代理类"></a>代理类</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TeacherDaoProxy</span> <span class="keyword">implements</span> <span class="title class_">ITeacherDao</span>&#123;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">private</span> ITeacherDao target; <span class="comment">// 目标对象，通过接口来聚合</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">TeacherDaoProxy</span><span class="params">(ITeacherDao target)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.target = target;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">teach</span><span class="params">()</span> &#123;</span><br><span class="line">		System.out.println(<span class="string">&quot;开始代理  完成校验、过滤、记录等操作----&gt; &quot;</span>);<span class="comment">//方法</span></span><br><span class="line">		target.teach();</span><br><span class="line">		System.out.println(<span class="string">&quot;结束代理------&gt;&quot;</span>);<span class="comment">//方法</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="静态代理优缺点"><a href="#静态代理优缺点" class="headerlink" title="静态代理优缺点"></a>静态代理优缺点</h4><p>优点： 在不修改目标对象的功能前提下，能通过代理对象对目标功能扩展</p>
<p>缺点：因为代理对象需要与目标对象实现一样的接口，所以会有很多代理类</p>
<p>一旦接口增加方法，目标对象与代理对象都要维护</p>
<br/>

<h2 id="动态代理模式"><a href="#动态代理模式" class="headerlink" title="动态代理模式"></a>动态代理模式</h2><h4 id="基本介绍-1"><a href="#基本介绍-1" class="headerlink" title="基本介绍"></a>基本介绍</h4><p>1、代理对象，不需要实现接口，但目标对象要实现接口，否则不能用动态代理</p>
<p>2、代理对象的生成，是利用JDK的API，动态的在内存中构建代理对象</p>
<p>3、动态代理也叫做： JDK代理、接口代理</p>
<p>JDK中生成代理对象的API</p>
<p>1、代理类所在包： java.lang.reflect.Proxy</p>
<p>2、JDK实现代理只需要使用 newProxyInstance方法，但该方法需要接受三个参数，完整的写法是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> Object <span class="title function_">newProxyInstance</span><span class="params">(ClassLoader loader,Class&lt;?&gt;[] interfaces,InvocationHandler h)</span></span><br></pre></td></tr></table></figure>



<h4 id="动态代理类图"><a href="#动态代理类图" class="headerlink" title="动态代理类图"></a>动态代理类图</h4><img src="/2020/10/12/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/dynamicUml.png" class="" title="动态代理类图">

<br/>

<h4 id="代理类-1"><a href="#代理类-1" class="headerlink" title="代理类"></a>代理类</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ProxyFactory</span> &#123;</span><br><span class="line"> </span><br><span class="line">	<span class="keyword">private</span> Object target;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ProxyFactory</span><span class="params">(Object target)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.target = target;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * public static Object newProxyInstance(ClassLoader loader,</span></span><br><span class="line"><span class="comment">                                          Class&lt;?&gt;[] interfaces,</span></span><br><span class="line"><span class="comment">                                          InvocationHandler h)</span></span><br><span class="line"><span class="comment">        ClassLoader loader: 指定当前目标对象的类加载器</span></span><br><span class="line"><span class="comment">        Class&lt;?&gt;[] interfaces: 目标对象实现的接口类型</span></span><br><span class="line"><span class="comment">        InvocationHandler h: 事件处理，执行目标对象的方法时，会触发事件处理器方法，会把当前目标对象的方法当作参数传入</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="comment">// 给目标对象一个代理对象</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">getProxyInstance</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> Proxy.newProxyInstance(target.getClass().getClassLoader(), target.getClass().getInterfaces(), <span class="keyword">new</span> <span class="title class_">InvocationHandler</span>() &#123;</span><br><span class="line">			</span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="keyword">public</span> Object <span class="title function_">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">				System.out.println(<span class="string">&quot;动态代理 对目标对象做拦截&quot;</span>);</span><br><span class="line">				<span class="type">Object</span> <span class="variable">returnval</span> <span class="operator">=</span> method.invoke(target, args);</span><br><span class="line">				<span class="keyword">return</span> returnval;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<br/>

<h2 id="Cglib代理模式"><a href="#Cglib代理模式" class="headerlink" title="Cglib代理模式"></a>Cglib代理模式</h2><h4 id="基本介绍-2"><a href="#基本介绍-2" class="headerlink" title="基本介绍"></a>基本介绍</h4><p>1、静态代理和JDK代理模式都要求目标对象是实现一个接口，但是有时候目标对象只是一个单独的对象，并</p>
<p>没有实现任何的接口，这个时候可使用目标对象子类来实现代理，这就是Cglib代理</p>
<p>2、Cglib代理也叫做 子类代理，它是在内存中构建一个子类对象，从而实现对目标对象（不能是final类）功能扩展，有些书也将Cglib代理归属于动态代理</p>
<p>3、Cglib是一个强大的高性能的代码生成包，它可以在运行期扩展java类与实现java接口，它广泛的被许多AOP的框架使用，</p>
<p>例如 Spring AOP ，实现方法拦截</p>
<p>4、在AOP编程中如何选择代理模式：</p>
<p>   目标对象需要实现接口，用JDK代理</p>
<p>   目标对象不需要实现接口，用Cglib代理</p>
<p>5、Cglib包的底层是通过使用字节码处理框架ASM来转换字节码并生成新的类</p>
<h4 id="注意点："><a href="#注意点：" class="headerlink" title="注意点："></a><strong>注意点：</strong></h4><ul>
<li>目标对象的方法如果是 final或static，就不会被拦截，即不会执行目标对象额外的业务方法。因为子类无法对这些方法进行重写</li>
<li>在内存中动态构建子类，需要注意代理类不能是final修饰的，会导致报错 java.lang.IllegalArgumentException</li>
</ul>
<h4 id="Cglib代理类图"><a href="#Cglib代理类图" class="headerlink" title="Cglib代理类图"></a>Cglib代理类图</h4><img src="/2020/10/12/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/cglibUml.png" class="" title="Cglib代理类图">

<br/>

<h4 id="代理类-2"><a href="#代理类-2" class="headerlink" title="代理类"></a>代理类</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.Enhancer;</span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.MethodInterceptor;</span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.MethodProxy;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ProxyFactory</span> <span class="keyword">implements</span> <span class="title class_">MethodInterceptor</span>&#123;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">private</span> Object target;</span><br><span class="line">	</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ProxyFactory</span><span class="params">(Object target)</span> &#123;</span><br><span class="line">		<span class="built_in">this</span>.target = target;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 返回 target的代理对象</span></span><br><span class="line"><span class="comment">	 * 1、创建一个工具类</span></span><br><span class="line"><span class="comment">	 * 2、设置父类</span></span><br><span class="line"><span class="comment">	 * 3、设置回调函数</span></span><br><span class="line"><span class="comment">	 * 4、创建子类对象即代理对象</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">getProxyInstance</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="type">Enhancer</span> <span class="variable">enhancer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Enhancer</span>();</span><br><span class="line">		enhancer.setSuperclass(target.getClass());</span><br><span class="line">		enhancer.setCallback(<span class="built_in">this</span>);</span><br><span class="line">		<span class="keyword">return</span> enhancer.create();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> Object <span class="title function_">intercept</span><span class="params">(Object arg0, Method method, Object[] args, MethodProxy arg3)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">		System.out.println(<span class="string">&quot;Cglib 代理开始 ------&gt;&quot;</span>);</span><br><span class="line">		<span class="type">Object</span> <span class="variable">returnVal</span> <span class="operator">=</span> method.invoke(target, args);</span><br><span class="line">		System.out.println(<span class="string">&quot;Cglib 代理提交 ------&gt;&quot;</span>);</span><br><span class="line">		<span class="keyword">return</span> returnVal;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>



<h3 id="个人思考："><a href="#个人思考：" class="headerlink" title="个人思考："></a>个人思考：</h3><h4 id="为什么动态代理一定要接口，不可以直接读取该类对应的方法吗？"><a href="#为什么动态代理一定要接口，不可以直接读取该类对应的方法吗？" class="headerlink" title="为什么动态代理一定要接口，不可以直接读取该类对应的方法吗？"></a>为什么动态代理一定要接口，不可以直接读取该类对应的方法吗？</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title function_">newProxyInstance</span><span class="params">(ClassLoader loader,</span></span><br><span class="line"><span class="params">                                         Class&lt;?&gt;[] interfaces,</span></span><br><span class="line"><span class="params">                                         InvocationHandler h)</span></span><br></pre></td></tr></table></figure>

<p>​    即便是 Cglib代理的方式，也是直接在子类调用父类的方法，method.invoke 时做一个嵌套调用而已。</p>
<p>这是因为我们在使用 Proxy.newProxyInstance方法时，返回的对象 其对应的父类是 class java.lang.reflect.Proxy 类型</p>
<p>这个不用为什么吧，要被代理对象的类型并不只有一种，不应该使用代理对象的class作为父类类型</p>
<p><strong>由于Java的特性，一个类只能有一个父类，所以要保证方法的正确调用，就必须通过接口来统一。</strong> </p>
<img src="/2020/10/12/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/proxy.png" class="" title="proxy父类图">

<br/>

<p>这里我思考过，可不可以在 newProxyInstance方法的第二个参数传入目标类的申明方法 target.getClass().getDeclaredMethods()</p>
<p>但是我们看一下 getDeclaredMethods的JDK注解</p>
<blockquote>
<p>Returns an array containing {@code Method} objects reflecting all the declared methods of the class or interface represented by this {@code Class} object, including public, protected, default (package) access, and private methods, but excluding inherited methods.</p>
</blockquote>
<p>相比于接口而言，它多了 protected,default,private这些方法，这些从系统设计的角度来看这些方法是不应该被代理对象来调用的，所以我个人认为使用接口才是最好的选择。</p>
<br/>

<h4 id="动态代理和静态代理的区别在哪里？"><a href="#动态代理和静态代理的区别在哪里？" class="headerlink" title="动态代理和静态代理的区别在哪里？"></a>动态代理和静态代理的区别在哪里？</h4><p>虽然都需要实现接口，但是静态代理的原理是 创建指定类的代理类对象，每一个类都需要创建代理类，代理类也需要实现对应的接口</p>
<p>而动态代理不同，动态代理是利用 Proxy.newProxyInstance方法，本质是通过传入的 对象反射调用方法，并在反射调用的前后做自己的一些业务而已，它不需要创建多个代理类，只需要创建一个 ProxyFactory,让调用者传入需要代理的对象即可</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>代理模式</tag>
      </tags>
  </entry>
  <entry>
    <title>前端表单传值的感悟</title>
    <url>/2019/08/02/%E5%89%8D%E7%AB%AF%E8%A1%A8%E5%8D%95%E4%BC%A0%E5%80%BC%E7%9A%84%E6%84%9F%E6%82%9F/</url>
    <content><![CDATA[<h1 id="前端表单传值的感悟"><a href="#前端表单传值的感悟" class="headerlink" title="前端表单传值的感悟"></a>前端表单传值的感悟</h1><p>Ajax传值到后台时，后台的数据接收框架，可能是springmvc,或者是springBoot，也有可能是servlet的接受 request.getParameter（attr）的形式</p>
<p>这里后台使用的是 SpringBoot </p>
<span id="more"></span>

<h2 id="单个数据的传输"><a href="#单个数据的传输" class="headerlink" title="单个数据的传输"></a>单个数据的传输</h2><p>对于单个的数据参数，我们是直接传，后台申明 RequestParam进行接受即可</p>
<h3 id="前端传值"><a href="#前端传值" class="headerlink" title="前端传值"></a>前端传值</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$.<span class="title function_">ajax</span>(&#123;</span><br><span class="line">    <span class="attr">url</span>:<span class="string">&quot;/complaint/delComplaint&quot;</span>,</span><br><span class="line">    <span class="attr">type</span>:<span class="string">&quot;DELETE&quot;</span>,</span><br><span class="line">    <span class="attr">data</span>:<span class="string">&quot;base_uid=&quot;</span> + base_uid,</span><br><span class="line">    <span class="attr">dataType</span>:<span class="string">&quot;json&quot;</span>,</span><br><span class="line">    <span class="attr">success</span>:<span class="keyword">function</span>(<span class="params">result</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(result.<span class="property">code</span> == <span class="number">100</span>) &#123;</span><br><span class="line">            <span class="title function_">alert</span>(<span class="string">&quot;删除成功&quot;</span>);</span><br><span class="line">            <span class="variable language_">window</span>.<span class="property">location</span>.<span class="title function_">reload</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="title function_">alert</span>(result.<span class="property">extend</span>.<span class="property">message</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="后台接收"><a href="#后台接收" class="headerlink" title="后台接收"></a>后台接收</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/editById&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Msg <span class="title function_">editComplaintById</span><span class="params">(<span class="meta">@RequestParam(&quot;base_uid&quot;)</span> String base_uid)</span> &#123;</span><br><span class="line">    <span class="type">CustomerComplaint</span> <span class="variable">c</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CustomerComplaint</span>();</span><br><span class="line">    System.out.println(<span class="string">&quot;base_uid : &quot;</span> + base_uid);</span><br><span class="line">    <span class="keyword">if</span> (base_uid != <span class="literal">null</span>) &#123;</span><br><span class="line">        c = customerComplaintService.getComplaintByBaseUid(base_uid);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> Msg.success().add(<span class="string">&quot;complaint&quot;</span>, c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="表单数据的传输"><a href="#表单数据的传输" class="headerlink" title="表单数据的传输"></a>表单数据的传输</h2><p>其中 为了 返回消息的规范性，最好是自己设计一个 HaseMap的 Msg <strong>返回消息类</strong></p>
<p>而对于 整个表单对象的传入，一定要记住 不要设置 dataType 参数了，不然设置为 “json“时，后台是接受不到的</p>
<h3 id="前端传值-1"><a href="#前端传值-1" class="headerlink" title="前端传值"></a>前端传值</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$.<span class="title function_">ajax</span>(&#123;</span><br><span class="line"><span class="attr">url</span>:<span class="string">&quot;/complaint/editComplaint/&quot;</span>+$(<span class="variable language_">this</span>).<span class="title function_">attr</span>(<span class="string">&quot;edit-id&quot;</span>),</span><br><span class="line"><span class="attr">type</span>:<span class="string">&quot;POST&quot;</span>,</span><br><span class="line"><span class="attr">data</span>:$(<span class="string">&quot;#complaintForm&quot;</span>).<span class="title function_">serialize</span>(),</span><br><span class="line"><span class="attr">success</span>:<span class="keyword">function</span>(<span class="params">result</span>)&#123;</span><br><span class="line"><span class="title function_">alert</span>(<span class="string">&quot;更新成功！&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="后台接受"><a href="#后台接受" class="headerlink" title="后台接受"></a>后台接受</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/editComplaint/&#123;base_uid&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Msg <span class="title function_">editComplaint</span><span class="params">(CustomerComplaint complaint)</span> &#123;</span><br><span class="line">    System.out.println(complaint);</span><br><span class="line">     customerComplaintService.updateComplaint(complaint);</span><br><span class="line">    <span class="keyword">return</span> Msg.success();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="表单传输原理"><a href="#表单传输原理" class="headerlink" title="表单传输原理"></a>表单传输原理</h3><p>这是为什么呢？</p>
<p>我们先看一下 serialize方法的定义</p>
<p><strong>serialize() 方法通过序列化表单值，创建 URL 编码文本字符串。</strong></p>
<p><strong>您可以选择一个或多个表单元素（比如 input 及&#x2F;或 文本框），或者 form 元素本身。</strong></p>
<p><strong>序列化的值可在生成 AJAX 请求时用于 URL 查询字符串中。</strong></p>
<p>也就是说 其实是将表单的多个参数封装加载在 url的字符串中</p>
<p>我们打印看看</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">name</span>=<span class="string">&quot;a&quot;</span> <span class="attr">value</span>=<span class="string">&quot;1&quot;</span> <span class="attr">id</span>=<span class="string">&quot;a&quot;</span> /&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">name</span>=<span class="string">&quot;b&quot;</span> <span class="attr">value</span>=<span class="string">&quot;2&quot;</span> <span class="attr">id</span>=<span class="string">&quot;b&quot;</span> /&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;c&quot;</span> <span class="attr">value</span>=<span class="string">&quot;3&quot;</span> <span class="attr">id</span>=<span class="string">&quot;c&quot;</span> /&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">textarea</span> <span class="attr">name</span>=<span class="string">&quot;d&quot;</span> <span class="attr">rows</span>=<span class="string">&quot;8&quot;</span> <span class="attr">cols</span>=<span class="string">&quot;40&quot;</span>&gt;</span>4<span class="tag">&lt;/<span class="name">textarea</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">select</span> <span class="attr">name</span>=<span class="string">&quot;e&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">&quot;5&quot;</span> <span class="attr">selected</span>=<span class="string">&quot;selected&quot;</span>&gt;</span>5<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">&quot;6&quot;</span>&gt;</span>6<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">&quot;7&quot;</span>&gt;</span>7<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">select</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">name</span>=<span class="string">&quot;f&quot;</span> <span class="attr">value</span>=<span class="string">&quot;8&quot;</span> <span class="attr">id</span>=<span class="string">&quot;f&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span> <span class="attr">name</span>=<span class="string">&quot;g&quot;</span> <span class="attr">value</span>=<span class="string">&quot;Submit&quot;</span> <span class="attr">id</span>=<span class="string">&quot;g&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="string">&#x27;form&#x27;</span>).<span class="title function_">submit</span>(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">alert</span>($(<span class="variable language_">this</span>).<span class="title function_">serialize</span>());</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;);</span><br><span class="line">输出标准的查询字符串：</span><br><span class="line">a=<span class="number">1</span>&amp;b=<span class="number">2</span>&amp;c=<span class="number">3</span>&amp;d=<span class="number">4</span>&amp;e=<span class="number">5</span></span><br></pre></td></tr></table></figure>

<p>**注释：**只会将”成功的控件“序列化为字符串。如果不使用按钮来提交表单，则不对提交按钮的值序列化。如果要表单元素的值包含到序列字符串中，元素必须使用 name 属性。</p>
<p>serializeArray() 方法序列化表单元素（类似 <a href="http://www.w3school.com.cn/jquery/ajax_serialize.asp">.serialize() 方法</a>），返回 JSON 数据结构数据。</p>
<p>**注意：**此方法返回的是 JSON 对象而非 JSON 字符串。需要使用插件或者第三方库进行字符串化操作。</p>
<p>而 $.ajax方法中的datatype参数为json ，指的是json字符串对象，非字符串。</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>web</tag>
      </tags>
  </entry>
  <entry>
    <title>企业慢SQL优化总结</title>
    <url>/2021/01/17/%E4%BC%81%E4%B8%9A%E6%85%A2SQL%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<blockquote>
<p>前言： 最近由于组内大佬出差，我临时顶替来处理我们项目组修复慢SQL缺陷的任务，抱着头大和觉得能学到东西快乐的心情弄了2周，有一些总结和心得在此记录一下。</p>
</blockquote>
<span id="more"></span>
<br/>

<p>处理的流程简要如下：</p>
<p>1、公司检测超过指定时间的慢SQL，生成缺陷BUG发送给对应项目组</p>
<p>2、开发拿到性能缺陷Bug，通过关键词查询对应环境的Monitor日志信息定位具体业务场景</p>
<p>3、查看explain执行计划和 SQL语句的使用场景，进行分析和修复</p>
<br/>

<h3 id="慢SQL类型"><a href="#慢SQL类型" class="headerlink" title="慢SQL类型"></a>慢SQL类型</h3><p>我在处理的过程中发现企业中的慢SQL基本分为3种类型</p>
<p>1、一些核心数据的统计业务</p>
<p>例如：各应用首页的数据统计展示，用户数据及核心表数据的分析</p>
<p>特点：查询条件比较模糊，查询数据量较大</p>
<p>解决方式： 这种是比较头疼的，目前我和同事讨论的解决方式有几种</p>
<ul>
<li><ul>
<li>如果查询的数据是用户特有的，数据变化不大，实时性不强的查询业务产生。那么使用缓存机制，每隔1小时查询一次放入Redis中，设置过期时间。</li>
<li>如果查询的数据是实时性很强的，这就是头疼的地方，我们的核心业务在目前的情况数据量已经达到数百万级，这种暂时是考虑业务上优化，精确查询的范围，并将核心的数据表进行水平分表。</li>
<li>加入慢SQL黑名单，暂时忽略它</li>
</ul>
</li>
</ul>
<p>2、报表查询</p>
<p>报表是企业中使用很多的地方，是用来统计不同业务的用户数据。例如：当前组织的入库、出库的物料、金额汇总，物料的账龄等等</p>
<p>特点： 联查的数据表很多，尤其是考虑不同国家的多语言的情况，联查的数据表可能翻倍。Join表可能会出现type&#x3D;ALL的全表扫描情况，或者是使用 BNL(Block Nest-Loop Join)算法和NLJ(Index Nest-Loop Join)算法</p>
<br/>

<p>遇到的问题：</p>
<p>Join联查的表明明有索引，但是走的是全表扫描</p>
<p>这种可能有多种情况</p>
<p>1、驱动表A与被驱动表B的联查字段的类型或者双方的字符集不一样导致的</p>
<p>这种情况下数据库会隐式转换类型来匹配，从而索引失效了，通常开发是不会犯字段类型不一样的这种比较低级错误的，大多数情况下是因为历史升级的原因导致了两个表相关字段的字符集不一致。</p>
<p>2、联查的数据表其要查询的数据列很少，并且数据记录很多，查询到对应的主键索引ID再查询所需记录的性能不如直接全表扫描</p>
<p>​      例如 Select A.*,B.age from A join B on A.name &#x3D; B.name and B.local &#x3D; ‘ZH_cn’ 这种，如果走B.name索引，那么需要通过B.name的索引找到记录的主键索引ID，通过该ID找到主键索引对应的记录所在的物理页，取出该记录下的age字段。</p>
<p>​       这样会导致一个问题，就是B.name索引顺序扫描读取的主键ID集合不是有序的，会造成随机读盘，这样效率不高，如果B表中有相关一部分比例是匹配的，那么优化器会认为全表扫描的顺序读盘效率更高，从而使用全表扫描的策略。</p>
<p>解决办法：</p>
<ul>
<li><ul>
<li>在确定大部分是这种场景下，可以扩展原来的 B.name索引为联合索引 IDX_B(name,local)、</li>
<li>开启MRR，这种权限不是普通开发能接触到的，暂时忽略，关于MRR，下面我会说明。</li>
</ul>
</li>
</ul>
<br/>

<p>3、普通业务SQL</p>
<p>这种导致性能慢的原因有很多，常见的有 未使用索引、复合索引未遵循最左匹配，二开业务的字段与联查表字段类型不一等</p>
<p> 1、未使用到索引</p>
<p>​      这种情况比较好解决，可以考虑使用的频率，以及联查字段的区分度考虑是否需要添加索引，或者 如果该联查多字段顺序类型的场景比较频繁，可以考虑扩展索引成复合索引。</p>
<p>​      需要注意的地方是，小表的全表扫描其实影响不大。一个只有几百的某些全局配置表，logN与N几乎处于同一个数量级，要思考其他导致慢SQL的原因。尽量小表驱动大表，虽然这个通常在业务上很难做到。</p>
<p>2、复合索引未使用最左匹配导致索引失效的</p>
<p>​     这种情况如果你公司使用的是普通的查询SQL，不是ORM其实都很好解决，调整SQL中匹配的字段顺序即可，但是在ORM中其实是不太好解决的。</p>
<p>3、二开业务慢SQL</p>
<p>​     在确定不是自身业务导致慢SQL的情况下，转给二开部门来处理。</p>
<br/>

<h3 id="一个有趣的问题"><a href="#一个有趣的问题" class="headerlink" title="一个有趣的问题"></a>一个有趣的问题</h3><p>where中 in 一个大的list集合，与将 list存储在一个临时表来 join联查匹配，这两种方式，哪个更快？</p>
<p>​        我在一些慢SQL中发现公司平台有将 where in list 转换为 join 临时表 on 来进行处理，这引起了我的兴趣。这两种方式，哪种更好呢？我觉得需要分情况讨论，在stackoverflow中也有对这个问题的讨论，感兴趣的同学也可以搜搜。</p>
<p>1、如果 where in list 中，list集合会经常改变，并且该慢SQL执行频率高</p>
<p>​      这种情况，我认为是使用 where in list 整体对系统影响较小，因为频繁的创建和销毁临时表对整个系统的性能是有一定影响的。并且在实现上，完全可以将list的数据在业务代码中进行判断，通过内存来过滤，效果更佳。</p>
<p>2、如果 list集合很大，并且对应慢SQL执行频率很低。</p>
<p>​     这种情况，应该是创建临时表的性能更佳，假设list有10万数据，通过临时表 + 创建临时表索引联查的方式，与 where in list 相比，是 logN VS N的时间复杂度，但注意，要list集合很大才有性价比。</p>
<h3 id="Join相关知识"><a href="#Join相关知识" class="headerlink" title="Join相关知识"></a>Join相关知识</h3><p>在慢SQL中大部分情况都是有联查表处理的，掌握Join相关的知识对整体慢SQL的解析有一定的帮助，这边来说说我最近学习总结到的Join知识。</p>
<p>在下面的SQL语句中，创建X，Y表，Y表和X表结构一样，X表插入一千条数据，Y表插入一百条</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> `X` (</span><br><span class="line">  `id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">  `a` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  `b` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`id`),</span><br><span class="line">  KEY `a` (`a`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB;</span><br></pre></td></tr></table></figure>

<p>由于在日常业务中，绝大数情况都是使用left join，并且是大表联查小表,这里就以 left join举例， 在下面语句中，x为驱动表，Y为被驱动表</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> X <span class="keyword">left</span> <span class="keyword">join</span> Y <span class="keyword">on</span> X.a <span class="operator">=</span> Y.a;</span><br></pre></td></tr></table></figure>

<img src="/2021/01/17/%E4%BC%81%E4%B8%9A%E6%85%A2SQL%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/NLJ.png" class="" title="NLJ图">

<p>当驱动表联查被驱动表，并且使用到了被驱动表的索引，这种场景被称为 NLJ</p>
<h4 id="NLJ-Index-Nest-Loop-Join"><a href="#NLJ-Index-Nest-Loop-Join" class="headerlink" title="NLJ(Index Nest-Loop Join)"></a>NLJ(Index Nest-Loop Join)</h4><p>执行过程如下</p>
<p>1、从表X中读取一行记录R</p>
<p>2、从记录R中取得字段a到Y表中匹配</p>
<p>3、取得Y表中匹配的行与记录R组成一行，作为结果集数据的一行</p>
<p>4、重复执行步骤1到3，直到表X全部记录扫描完毕</p>
<p>在整个执行过程中，驱动表X是全表扫描，为N，N为1000行，而对于被驱动表 Y而言，由于使用到了索引 a,时间为 2 * log2M(这里由于a是普通索引，要进行回表操作，所以是 2 * log2M),M为100行</p>
<p>所以整体执行时间为： N + N * 2 *log2M</p>
<p>从这里我们可以看出，N的影响因素更大，即驱动表更影响执行时间</p>
<p><strong>结论</strong>：在 NLJ中 尽量使用 小表作为驱动表，效率更高。</p>
<br/>

<p>假设被驱动表Y未使用到索引，这种情况如何？</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> X <span class="keyword">left</span> <span class="keyword">join</span> Y <span class="keyword">on</span> X.a <span class="operator">=</span> Y.b;</span><br></pre></td></tr></table></figure>

<img src="/2021/01/17/%E4%BC%81%E4%B8%9A%E6%85%A2SQL%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/BNL.png" class="" title="BNL图">

<p>当被驱动表Y未使用索引时，MySQL使用了一种 BNL算法</p>
<h4 id="BNL-Block-Nested-Loop"><a href="#BNL-Block-Nested-Loop" class="headerlink" title="BNL(Block Nested Loop)"></a>BNL(Block Nested Loop)</h4><p>执行过程如下</p>
<p>1、将X表的记录扫描放入Join Buffer中，这里用的是select * 放的就是整条记录</p>
<p>2、扫描被驱动表Y表，将Y表的所有记录取出来与 Join Buffer中的记录进行比对，满足Join条件的，将其加入结果集</p>
<p>从explain中我们可以看到，驱动表X和被驱动表Y都进行了全表扫描，这个执行时间为 M + N,即1100</p>
<p>其中Join Buffer的联查匹配执行时间为 M * N，即 1000 * 100 &#x3D; 10万行</p>
<p>值得注意的是 Join Buffer的比较是在内存中，所以性能没有我们想的那么差。</p>
<p>整体执行时间为： M + N + M*N</p>
<p>当从公式来看，无论是大表还是小表作为驱动表，执行时间都一样。</p>
<p>但是 Join Buffer的size是有限的，通常不能将驱动表一次性的放入，需要拆成多个size的块，这也是为啥叫做 Block Nested Loop的由来</p>
<p>这个时候，假设驱动表N需要拆分成 T次才能放入，由于Join Buffer的size是固定的，所以 N越大，T越大，即  T &#x3D; λ*N, λ 为(0,1)</p>
<p>所以，在这个算法的执行过程中：</p>
<p>扫描行数是 N+λ<em>N</em>M；</p>
<p>内存判断 N*M 次。</p>
<p>注意，当驱动表被分成T段时，被驱动表需要被读取T次，这个与MySQL的内存设计有关，这里不做深究。</p>
<p>**结论：**整体来看，当Join Buffer的大小不足以装下整个驱动表时，N影响更大，即使用小表作为驱动表更好。</p>
<p>但是令人头疼的是，在实际的业务中，往往需要 大表联查小表，因为大表意味着是核心数据表，是需要经常被查询的，并且通常使用的是 Left Join而不是 Join，如果使用的是NLJ那还好，有时候使用的就是 BNL，你还不能创建索引进行优化，因为 Left Join 联查的小表字段往往区分度达不到要求，那有什么其他的办法呢？</p>
<p>这里简要介绍一下两种优化思想</p>
<br/>

<h3 id="Join优化"><a href="#Join优化" class="headerlink" title="Join优化"></a>Join优化</h3><h4 id="MRR-Multi-Range-Read-优化"><a href="#MRR-Multi-Range-Read-优化" class="headerlink" title="MRR(Multi-Range Read)优化"></a>MRR(Multi-Range Read)优化</h4><p>Multi-Range Read 优化 (MRR)。这个优化的主要目的是尽量使用顺序读盘。</p>
<p>先说一下回表的概念</p>
<p>回表：InnoDB 在普通索引 a 上查到主键 id 的值后，再根据一个个主键 id 的值到主键索引上去查整行数据的过程。</p>
<p>​       如果随着 a 的值递增顺序查询的话，id 的值就变成随机的，那么就会出现随机访问，性能相对较差。虽然“按行查”这个机制不能改，但是调整查询的顺序，还是能够加速的。</p>
<p>​        因为大多数的数据都是按照主键递增顺序插入得到的，所以我们可以认为，如果按照主键的递增顺序查询的话，对磁盘的读比较接近顺序读，能够提升读性能。</p>
<p>这，就是 MRR 优化的设计思路。此时，语句的执行流程变成了这样：</p>
<ol>
<li>根据索引 a，定位到满足条件的记录，将 id 值放入 read_rnd_buffer 中 ;</li>
<li>将 read_rnd_buffer 中的 id 进行递增排序；</li>
<li>排序后的 id 数组，依次到主键 id 索引中查记录，并作为结果返回。</li>
</ol>
<p>开启MRR优化：set optimizer_switch&#x3D;”mrr_cost_based&#x3D;off”。</p>
<p>（官方文档的说法，是现在的优化器策略，判断消耗的时候，会更倾向于不使用 MRR，把 mrr_cost_based 设置为 off，就是固定使用 MRR 了。）</p>
<p>下面两幅图就是使用了 MRR 优化后的执行流程</p>
<img src="/2021/01/17/%E4%BC%81%E4%B8%9A%E6%85%A2SQL%E4%BC%98%E5%8C%96%E6%80%BB%E7%BB%93/MRR.png" class="" title="MRR图">

<br/>

<h4 id="BKA-Batched-Key-Access-优化"><a href="#BKA-Batched-Key-Access-优化" class="headerlink" title="BKA(Batched Key Access)优化"></a><strong>BKA(Batched Key Access)优化</strong></h4><p>BNL转BKA算法</p>
<p>其实BKA算法就是在 驱动表 join 被驱动表时，给被驱动表上添加索引。</p>
<p>需要注意的是 BKA算法是依赖于MRR优化的，你必须先开启MRR，因为<strong>BKA算法本质是 MRR + NLJ的合并处理</strong>。即先将 驱动表在Join Buffer的数据拿出来，根据主键索引的ID顺序排序，减少随机IO，再与被驱动表的索引进行匹配，最大化的减少随机读盘。</p>
<p>BKA虽好，但有一些情况是不太适合直接在 被驱动表上加索引的，假如下面这条SQL语句的执行频率很低，这就有点浪费资源了。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> X <span class="keyword">left</span> <span class="keyword">join</span> Y <span class="keyword">on</span> X.b <span class="operator">=</span> Y.b <span class="keyword">where</span> Y.b <span class="operator">&gt;</span><span class="number">1</span> <span class="keyword">and</span> Y.b <span class="operator">&lt;</span> <span class="number">1000</span>; </span><br></pre></td></tr></table></figure>

<p>可以考虑创建临时表，插入where过滤后的数据，并创建索引 b即可。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> temporary <span class="keyword">table</span> temp_y(id <span class="type">int</span> <span class="keyword">primary key</span>, a <span class="type">int</span>, b <span class="type">int</span>, index(b)) engine<span class="operator">=</span>innodb; </span><br><span class="line"><span class="keyword">insert into</span> temp_t <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> Y <span class="keyword">where</span> b<span class="operator">&gt;</span><span class="number">1</span> <span class="keyword">and</span> b<span class="operator">&lt;</span> <span class="number">1000</span>; <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> X <span class="keyword">join</span> temp_y <span class="keyword">on</span> (X.b<span class="operator">=</span>temp_t.b); </span><br></pre></td></tr></table></figure>

<p>这样的速度也会非常快</p>
<p>但是在企业那种ORM封装后是不可能使用这种语句的，建议在业务层 查询 where Y.b &gt;1 and Y.b &lt; 1000的数据ID存储到 hashset中，使用hash来比对，这样的效果也不会太差的</p>
<p>总体来看，不论是在原表上加索引，还是用有索引的临时表，我们的思路都是让 join 语句能够用上被驱动表上的索引，来触发 BKA 算法，提升查询性能。</p>
<hr>
<blockquote>
<p>Join参考：丁奇的MySQL45讲</p>
</blockquote>
]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>命令模式</title>
    <url>/2019/12/25/%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p>接触命令模式是了解SpringCloud中的Hystrix组件时遇到的，觉得挺有意思的便单独拿出来写成一篇博客来加深自己的理解。</p>
<br/>

<h3 id="简要定义："><a href="#简要定义：" class="headerlink" title="简要定义："></a>简要定义：</h3><p>​        命令模式（Command) : 将一个请求封装为一个对象，从而使你可用不同的请求对客户进行参数化；对请求排队或记录请求日志，以及支持可撤销的操作。</p>
<br/>
<span id="more"></span>

<p>我们通过该模式的类图来分析调用关系</p>
 <img src="/2019/12/25/%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/UML.jpg" class="" title="类图">

<br/>

<p>分析这5个角色的作用</p>
<ul>
<li>Client-客户端： 客户端调用相关代码，创建具体命令对象ConcreateCommand并设定其接受者Receiver</li>
<li>Invoker-调用类： 传入命令实现类，要求相关命令执行该请求</li>
<li>Receiver-接受类：它是实际的执行者，知道如何实施与执行一个请求相关的操作，任何类都有可能成为一个接受者</li>
<li>Command-命令接口： 用来声明执行操作的接口</li>
<li>ConcreateCommand-命令实现类： 将一个接受者对象绑定于一个动作，调用接受者相应的操作已实现Execute</li>
</ul>
<br/>

<p>可能看这些定义，你们对调用者、接受者、命令比较模糊，我先举两个简单的例子</p>
<h4 id="从微服务调用来看"><a href="#从微服务调用来看" class="headerlink" title="从微服务调用来看"></a>从微服务调用来看</h4><p>调用者(A服务) -&gt; 下达命令 (即请求消费其他服务功能) -&gt; 接受者（其他服务B,C） 接受命令执行具体逻辑</p>
<br/>

<h4 id="从生活场景来看"><a href="#从生活场景来看" class="headerlink" title="从生活场景来看"></a>从生活场景来看</h4><p>调用者(用户) -&gt; 下达命令（命令A：买苹果，命令B：买香蕉）-&gt; 接受者(水果店老板) 接受命令切水果算总价</p>
<br/>



<h4 id="来理一下类图中的调用："><a href="#来理一下类图中的调用：" class="headerlink" title="来理一下类图中的调用："></a>来理一下类图中的调用：</h4><p>Command接口来对应不同的命令实现类，每个命令实现类中有一个Receiver属性，命令执行的方法实际具体执行对象是Receiver对应的相关的action方法。</p>
<p>Invoker调用者中包含 Command属性，进行具体命令的调用。</p>
<br/>



<p><strong>这种比直接的 invoker调用Receiver的好处有哪些呢？</strong></p>
<p>首先 invoker不直接调用 Receiver，而是通过相关命令对象来执行action，实现了松耦合</p>
<p>另外我们可以在invoker中根据业务情景加入日志记录，删除命令等功能</p>
<p>缺点： 如果命令很多就得创建多个具体的命令实现类了</p>
<br/>

<p>这里可能无法理解，我通过代码和一个业务场景让大家来理解。</p>
<p>来看简要的代码</p>
<p>Receiver-接受类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Receiver</span> &#123;</span><br><span class="line">     <span class="comment">// 真正执行命令相应的操作</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">action</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;执行操作&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>Command接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Command</span> &#123;</span><br><span class="line">    <span class="comment">// 执行方法</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>ConcreteCommand-命令具体实现类(买苹果、香蕉)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConcreteCommand</span> <span class="keyword">implements</span> <span class="title class_">Command</span> &#123;</span><br><span class="line">    <span class="comment">//持有相应的接收者对象</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">Receiver</span> <span class="variable">receiver</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="comment">// 构造方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ConcreteCommand</span><span class="params">(Receiver receiver)</span>&#123;</span><br><span class="line">        <span class="built_in">this</span>.receiver = receiver;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">//通常会转调接收者对象的相应方法，让接收者来真正执行功能</span></span><br><span class="line">        receiver.action();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>Invoker-调用者(发命令的人)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Invoker</span> &#123;</span><br><span class="line">     <span class="comment">// 持有命令对象</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">Command</span> <span class="variable">command</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">     <span class="comment">//构造方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Invoker</span><span class="params">(Command command)</span>&#123;</span><br><span class="line">        <span class="built_in">this</span>.command = command;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//行动方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">action</span><span class="params">()</span>&#123;</span><br><span class="line">        command.execute();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>客户端（组装整套流程）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Client</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">//创建接收者</span></span><br><span class="line">        <span class="type">Receiver</span> <span class="variable">receiver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Receiver</span>();</span><br><span class="line">        <span class="comment">//创建命令对象，设定它的接收者</span></span><br><span class="line">        <span class="type">Command</span> <span class="variable">command</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ConcreteCommand</span>(receiver);</span><br><span class="line">        <span class="comment">//创建请求者，把命令对象设置进去</span></span><br><span class="line">        <span class="type">Invoker</span> <span class="variable">invoker</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Invoker</span>(command);</span><br><span class="line">        <span class="comment">//执行方法</span></span><br><span class="line">        invoker.action();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>现在我们看这个代码，感觉直接调用效果差不多，那么我们换一种业务场景</p>
<p>用户下单，要买10种水果，每种水果不同的价钱，不同的切法，中途不要两种水果了，如果你是店家你该如何接受记录这些命令来计算价钱和日志记录呢？</p>
<p>来看看大概的业务代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 水果店老板 接受者</span></span><br><span class="line"><span class="type">Receiver</span> <span class="variable">receiver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Receiver</span>()；</span><br><span class="line"></span><br><span class="line"><span class="type">Command</span> <span class="variable">apple</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AppleCommand</span>(receiver);</span><br><span class="line"><span class="type">Command</span> <span class="variable">banana</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BananaCommand</span>(receiver);  <span class="comment">// 每个水果命令中实际包含价格计算，日志记录</span></span><br><span class="line">.... <span class="number">10</span>种水果</span><br><span class="line"></span><br><span class="line"><span class="comment">//这里新建一个 List&lt;Command&gt;</span></span><br><span class="line">List&lt;Command&gt; lists = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">list.add(banana);</span><br><span class="line">list.add(apple); <span class="comment">//.... 添加10种</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 买家 请求者</span></span><br><span class="line">  <span class="type">Invoker</span> <span class="variable">invoker</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Invoker</span>(lists);</span><br><span class="line">  invoker.removeCommand(apple); <span class="comment">//..删除水果命令</span></span><br><span class="line">  invoker.action(); <span class="comment">// 结账</span></span><br></pre></td></tr></table></figure>



<p>从这个示例我们可以看出什么？</p>
<p>接受者 Receiver 和 调用者 Invoker 之间几乎没有耦合，双方都不需要理会对方是如何操作的，只需要关注命令即可，这就是命令模式最大的好处。</p>
<br/>

<p>好我们重新再来理解一下命令模式的优缺点。</p>
<h3 id="命令模式的优点"><a href="#命令模式的优点" class="headerlink" title="命令模式的优点"></a>命令模式的优点</h3><p>​     <strong>封装性</strong>：每个命令都被封装起来，对于客户端来说，需要什么功能就去调用相应的命令，而无需知道命令具体是怎么执行的。比如有一组文件操作的命令：新建文件、复制文件、删除文件。如果把这三个操作都封装成一个命令类，客户端只需要知道有这三个命令类即可，至于命令类中封装好的逻辑，客户端则无需知道。</p>
<p>​     <strong>可拓展性</strong>：在命令模式中，在接收者类中一般会对操作进行最基本的封装，命令类则通过对这些基本的操作进行二次封装，当增加新命令的时候，对命令类的编写一般不是从零开始的，有大量的接收者类可供调用，也有大量的命令类可供调用，代码的复用性很好。比如，文件的操作中，我们需要增加一个剪切文件的命令，则只需要把复制文件和删除文件这两个命令组合一下就行了，非常方便。</p>
<h3 id="命令模式的缺点"><a href="#命令模式的缺点" class="headerlink" title="命令模式的缺点"></a>命令模式的缺点</h3><p>那就是命令如果很多，开发起来就要头疼了。特别是很多简单的命令，实现起来就几行代码的事，而使用命令模式的话，不用管命令多简单，都需要写一个命令类来封装。</p>
<br/>

 



<h3 id="命令模式的适用场景"><a href="#命令模式的适用场景" class="headerlink" title="命令模式的适用场景"></a>命令模式的适用场景</h3><p>​       对于大多数请求-响应模式的功能，比较适合使用命令模式，正如命令模式定义说的那样，命令模式对实现记录日志、撤销操作等功能比较方便。</p>
<p>Command模式通常可应用到很多场景中，我这里列举常见的四种： </p>
<p> 1、Multi-level undo（多级undo操作） </p>
<p>​    如果系统需要实现多级回退操作，这时如果所有用户的操作都以command对象的形式实现，系统可以简单地用stack来保存最近执行的命令，如果用户需要执      行undo操作，系统只需简单地popup一个最近的command对象然后执行它的undo()方法既可。 </p>
<br/>

<p>2、Progress bars（状态条） </p>
<p>​    假如系统需要按顺序执行一系列的命令操作，如果每个command对象都提供一个 </p>
<p>​    getEstimatedDuration()方法，那么系统可以简单地评估执行状态并显示出合适的状态条。</p>
<br/>

<p>3、Thread pools（线程池） </p>
<p>​    通常一个典型的线程池实现类可能有一个名为addTask()的public方法，用来添加一项工作任务到任务 </p>
<p>​    队列中。该任务队列中的所有任务可以用command对象来封装，通常这些command对象会实现一个通用的 </p>
<p>​    接口比如java.lang.Runnable。 </p>
<br/>

<p>4、Networking(网络服务调用)</p>
<p>​     通过网络发送command命令到其他机器上运行。 </p>
<br/>

<p>5、Wizards（导航） </p>
<p>​     注意：这里的导航指的是网站导航</p>
<p>​    通常一个使用多个wizard页面来共同完成一个简单动作。一个自然的方法是使用一个command对象来封 </p>
<p>​    装wizard过程，该command对象在第一个wizard页面显示时被创建，每个wizard页面接收用户输入并设 </p>
<p>​    置到该command对象中，当最后一个wizard页面用户按下“Finish”按钮时，可以简单地触发一个事件 </p>
<p>​    调用execute()方法执行整个动作。通过这种方法，command类不包含任何跟用户界面有关的代码，可以 </p>
<p>​    分离用户界面与具体的处理逻辑。</p>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><p>博文：<a href="https://blog.csdn.net/zhengzhb/article/details/7550895">https://blog.csdn.net/zhengzhb/article/details/7550895</a></p>
<p>书籍： 大话设计模式</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>命令模式</tag>
      </tags>
  </entry>
  <entry>
    <title>小白学Netty之基础篇</title>
    <url>/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>​              最近几个月除了新公司的业务熟悉，在业余时间看了相关的netty、flink的视频和书籍，但是Netty这个东西背后涉及 网络、并发、以及操作系统原理等，我即使花了时间看视频，看Netty实战，总结成思维导图依旧没有那种对一门技术的掌握感，为此我决定将其总结成一系列的技术博客来加深自己对Netty的理解，就叫 《小白学Netty》吧。至于Flink，目前我平常开发的内容并未涉及Flink，我这边暂时不深入学习，后续有机会再开展。</p>
<br/>

<p>这第一篇文章我从Netty的起源、Netty的作用，以及Netty的大体设计来做一个概述。</p>
<span id="more"></span> 

<h3 id="1、-什么是Netty"><a href="#1、-什么是Netty" class="headerlink" title="1、 什么是Netty"></a>1、 什么是<strong>Netty</strong></h3><p>​     Netty是一款异步的事件驱动的网络应用程序框架，支持快速地开发可维护的高性能的面向协议的服务器和客户端</p>
<br/>

<h3 id="2、Netty的起因"><a href="#2、Netty的起因" class="headerlink" title="2、Netty的起因"></a>2、Netty的起因</h3><p>​    早期的网络编程是 阻塞IO的交互，在早期的Java API（Java.net)只支持由本地系统套接字库提供的所谓的阻塞函数。</p>
<p><strong>举个阻塞IO的栗子：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ServerSocket</span> <span class="variable">serverSocket</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServerSocket</span>(portNumber);</span><br><span class="line"><span class="type">Socket</span> <span class="variable">clientSocket</span> <span class="operator">=</span> serverSocket.accept();            </span><br><span class="line"><span class="type">BufferedReader</span> <span class="variable">in</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(                    </span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(clientSocket.getInputStream()));</span><br><span class="line"><span class="type">PrintWriter</span> <span class="variable">out</span> <span class="operator">=</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">PrintWriter</span>(clientSocket.getOutputStream(), <span class="literal">true</span>);</span><br><span class="line">String request, response;</span><br><span class="line"><span class="keyword">while</span> ((request = in.readLine()) != <span class="literal">null</span>) &#123;                 </span><br><span class="line">    <span class="keyword">if</span> (<span class="string">&quot;Done&quot;</span>.equals(request)) &#123;                         </span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">response = processRequest(request);                        </span><br><span class="line">out.println(response);  </span><br></pre></td></tr></table></figure>

<p>这里需要说明 serverSocket的 accept()方法会一直阻塞到一个连接生成，随后会返回一个新的Socket用于客户端和服务器之间的通信。</p>
<img src="/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/%E5%9B%BE1-1.png" class="" title="图1-1">



<p>那么看代码，我们可以知道，这种阻塞IO的写法一次只能处理一个连接，如果要同时管理过个并发客户端，需要为每个新的客户端对应的Socket连接创建一个新的Thread来处理。</p>
<br/>

<h4 id="这种做法的缺点是什么？"><a href="#这种做法的缺点是什么？" class="headerlink" title="这种做法的缺点是什么？"></a>这种做法的缺点是什么？</h4><p>1、任何时刻都会可能存在大量线程处于闲置状态，不停的在等待输入或输出数据准备就绪，这是一种资源的浪费。</p>
<p>2、需要为每个线程的调用栈都分配内存，其默认值大小区间为 64KB 到 1MB，取决于操作系统</p>
<p>3、第三点涉及操作系统的原理，CPU执行大量的线程时，上下文切换开销比较大，任务执行速度远慢于 CPU核数 * 2左右的线程数（这个感兴趣的小伙伴可以深入去了解为什么会这样？）</p>
<p>那会有小伙伴说了2002年起Java不是支持 NIO 非阻塞IO吗？</p>
<p>对的，在NIO中 java.nio.channels.Selector 的非阻塞IO实现的关键。</p>
<p>它是使用事件通知API来确定在一组 <strong>非阻塞套接字</strong> 中有哪些已经就绪能够进入IO相关的操作，因为任何时间检查任意的读写操作的完成状态，所以一个<strong>单一的线程便可以处理多个并发的连接</strong></p>
<img src="/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/%E5%9B%BE1-2.png" class="" title="图1-2">

<br/>

<h4 id="NIO与-阻塞IO-比较"><a href="#NIO与-阻塞IO-比较" class="headerlink" title="NIO与 阻塞IO 比较"></a>NIO与 阻塞IO 比较</h4><ul>
<li>使用较少的线程处理许多的连接，从而减少了内存管理和上下文切换所带来的开销</li>
<li>当没有IO操作需要处理的时候，线程也可以被用于其他任务</li>
</ul>
<p>这里可能大家对第二点不够理解，我这里再做说明</p>
<blockquote>
<p>NIO 是一种<strong>同步非阻塞</strong>的 IO 模型。</p>
<p>同步是指线程不断轮询 IO 事件是否就绪，非阻塞是指线程在等待 IO 的时候，可以同时做其他任务。</p>
<p>同步的核心就是 Selector，Selector 代替了线程本身轮询 IO 事件，避免了阻塞，同时减少了不必要的线程消耗；</p>
<p>非阻塞的核心就是通道Channel 和缓冲区 Buffer，当 IO 事件就绪时，可以通过写到缓冲区，保证 IO 的成功，而无需线程阻塞式地等待。</p>
</blockquote>
<br/>

<p>那么Netty比 原生的NIO又强大在于哪里呢？</p>
<p>Java是一门面向对象的语言，通过抽象来隐藏其背后NIO的复杂性，基于操作系统和网络协议等多方面因素考虑，将NIO进行了封装，提供更加简单的API,更强大的性能，以及更强的安全性。</p>
<img src="/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/%E5%9B%BE1-3.png" class="" title="图1-3">

<br/>

<h3 id="Netty的核心组件"><a href="#Netty的核心组件" class="headerlink" title="Netty的核心组件"></a>Netty的核心组件</h3><p>1、Channel</p>
<p>2、回调</p>
<p>3、Future</p>
<p>4、事件 和 ChannelHandler</p>
<br/>

<h4 id="Channel"><a href="#Channel" class="headerlink" title="Channel"></a><strong>Channel</strong></h4><p>   Channel，你可以把它看做是 传入（入站） 或者 传出（出站）数据的载体，类似 我们常见的水在水管中流动，数据在Channel中传输。</p>
<br/>

<h4 id="回调"><a href="#回调" class="headerlink" title="回调"></a><strong>回调</strong></h4><p>   一个回调其实就是一个方法，一个指向已经被提供给另外一个方法的方法的引用，这使得接受回调的方法 可以在适当的时候 调用前者。其实就是用来操作完成后通知相关方，Netty在内部使用回调来处理事件，当一个回调被触发，相关的事件会被 一个 ChannelHandler 对应的实现来处理。</p>
<br/>

<h4 id="Future"><a href="#Future" class="headerlink" title="Future"></a><strong>Future</strong></h4><p>  Future提供了另外一种在操作完成时通知应用程序的方式。 这个对象可以看作是一个异步操作的结果占位符。</p>
<p>  它将在未来的某个时刻完成，并提供对其结果的访问。</p>
<p>  JDK提供的 java.util.concurrent.Future是只能通过 手动检查其是否完成 或者 一直阻塞直到操作完成，这是比较笨的。</p>
<p>  Netty提供了 ChannelFuture 可以执行异步操作，监听器对应的回调方法 operationComplete() 会在对应操作完成时被调用</p>
<p>  Netty是完全是异步和事件驱动的</p>
<p><strong>事件和ChannelHandler</strong></p>
<p>​     Netty通过使用不同的事件来通知我们状态的改变或者操作的状态。</p>
<br/>

<h3 id="NIO-Reactor模型"><a href="#NIO-Reactor模型" class="headerlink" title="NIO Reactor模型"></a>NIO Reactor模型</h3><img src="/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/%E5%9B%BE1-4.png" class="" title="图1-4">

<br/>

<h4 id="Netty-Reactor模型架构干了什么事情？"><a href="#Netty-Reactor模型架构干了什么事情？" class="headerlink" title="Netty Reactor模型架构干了什么事情？"></a>Netty Reactor模型架构干了什么事情？</h4><p>1、 Netty将一个网络通信的处理对应的多个步骤 accept、read、decode、process、encode、write、send，转换成了多个Task，每个Task对应一个事件，这样的好处在于 细粒度，每个事件都可以异步处理。</p>
<p>2、每当一个Task完成，即一个事件完成后会通知该 Reactor架构，Reactor调用对应网络事件的ChannelHandler来处理</p>
<br/>

<h3 id="工作原理解析："><a href="#工作原理解析：" class="headerlink" title="工作原理解析："></a>工作原理解析：</h3><ol>
<li><p>Netty抽象出两组线程池 BossGroup 专门负责接收客户端的连接, WorkerGroup 专门负责网络的读写</p>
</li>
<li><p>BossGroup 和 WorkerGroup 类型都是 NioEventLoopGroup</p>
</li>
<li><p>NioEventLoopGroup 相当于一个事件循环组, 这个组中含有多个事件循环 ，每一个事件循环是 NioEventLoop</p>
</li>
<li><p>NioEventLoop 表示一个不断循环的执行处理任务的线程， 每个NioEventLoop 都有一个selector , 用于监听绑定在其上的socket的网络通讯</p>
</li>
<li><p>NioEventLoopGroup 可以有多个线程, 即可以含有多个NioEventLoop</p>
</li>
<li><p>每个Boss NioEventLoop 循环执行的步骤有3步</p>
</li>
<li><ol>
<li>轮询accept 事件</li>
<li>处理accept 事件 , 与client建立连接 , 生成NioSocketChannel , 并将其注册到某个worker NIOEventLoop 上的 selector</li>
<li>处理任务队列的任务 ， 即 runAllTasks</li>
</ol>
</li>
<li><p>每个 Worker NIOEventLoop 循环执行的步骤</p>
</li>
<li><ol>
<li>轮询read, write 事件</li>
<li>处理i&#x2F;o事件， 即read , write 事件，在对应NioSocketChannel 处理</li>
<li>处理任务队列的任务 ， 即 runAllTasks</li>
</ol>
</li>
<li><p>每个Worker NIOEventLoop  处理业务时，会使用pipeline(管道), pipeline 中包含了 channel , 即通过pipeline 可以获取到对应通道, 管道中维护了很多的处理器</p>
</li>
</ol>
<br/>



<h3 id="知识点思维导图"><a href="#知识点思维导图" class="headerlink" title="知识点思维导图"></a><strong>知识点思维导图</strong></h3><p>初略了总结了一下Netty的常用API，后续针对核心的API做源码讲解和流程阐述。</p>
<img src="/2020/08/17/%E5%B0%8F%E7%99%BD%E5%AD%A6Netty%E4%B9%8B%E5%9F%BA%E7%A1%80%E7%AF%87/%E5%9B%BE1-5.png" class="" title="图1-5">]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>Netty</tag>
      </tags>
  </entry>
  <entry>
    <title>并查集</title>
    <url>/2019/08/05/%E5%B9%B6%E6%9F%A5%E9%9B%86/</url>
    <content><![CDATA[<h1 id="并查集"><a href="#并查集" class="headerlink" title="并查集"></a>并查集</h1><h2 id="定义："><a href="#定义：" class="headerlink" title="定义："></a>定义：</h2><p>​         并查集，在一些有N个元素的集合应用问题中，我们通常是在<strong>开始时让每个元素构成一个单元素的集合，然后按一定顺序将属于同一组的元素所在的集合合并</strong>，其间要反复查找一个元素在哪个集合中。</p>
<p>​         这一类问题近几年来反复出现在信息学的国际国内赛题中，其特点是看似并不复杂，但数据量极大，若用正常的数据结构来描述的话，往往在空间上过大，计算机无法承受；即使在空间上勉强通过，运行的<a href="https://baike.baidu.com/item/%E6%97%B6%E9%97%B4%E5%A4%8D%E6%9D%82%E5%BA%A6/1894057">时间复杂度</a>也极高，根本就不可能在比赛规定的运行时间（1～3秒）内计算出试题需要的结果，只能用并查集来描述。</p>
<p>​           并查集是一种树型的数据结构，<strong>用于处理一些不相交集合（Disjoint Sets）的合并及查询问题</strong>。</p>
<h2 id="个人感悟："><a href="#个人感悟：" class="headerlink" title="个人感悟："></a>个人感悟：</h2><p>通俗的比喻就是 给你一群人，每个人你都给他一个编号排好队，会太极的把他编号改成武当，会棍法的把他们的编号改为少林，女生分到峨眉派，将所有人都分门分派，<strong>只要把所有人分一次，你就知道这群人里有多少个门派，这两个人之间是不是一个门派的。</strong></p>
<p>在算法题和开发中常用来计算 区域连接取数量和迷宫中两点之间是否有一条路径这类的问题</p>
<span id="more"></span>

<h2 id="主要方法："><a href="#主要方法：" class="headerlink" title="主要方法："></a>主要方法：</h2><h3 id="初始化-init"><a href="#初始化-init" class="headerlink" title="初始化 init"></a>初始化 init</h3><p>将集合中的每个元素作为初始元素集合，只需要执行一次。</p>
<p>Java是在构造器中将其初始化</p>
<h3 id="查找-find"><a href="#查找-find" class="headerlink" title="查找 find"></a>查找 find</h3><p>查找当前的节点对应的根节点</p>
<h3 id="合并-union"><a href="#合并-union" class="headerlink" title="合并 union"></a>合并 union</h3><p>将两个元素的值统一，合并成一个值。</p>
<p>这一步用来判断两两元素之间是否为一个集合</p>
<h2 id="路径压缩："><a href="#路径压缩：" class="headerlink" title="路径压缩："></a>路径压缩：</h2><p>路径压缩是让并查集查找速度变快的一个很重要的点。</p>
<p>有3个人，1号是2号的大哥，2号是3号的大哥</p>
<p>未压缩之前：</p>
<p>A[1] &#x3D; 1, A[2] &#x3D; 1, A[3] &#x3D; 2</p>
<p>路径压缩：</p>
<p>3号的大哥直接是2号的大哥,他不要上报给2号就可以找到最大的头</p>
<p>A[1] &#x3D; 1, A[2] &#x3D; 1,A[3] &#x3D; 1;</p>
<h2 id="具体代码："><a href="#具体代码：" class="headerlink" title="具体代码："></a>具体代码：</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UnionFind</span> &#123;</span><br><span class="line">		<span class="keyword">private</span> <span class="type">int</span>[] parent;</span><br><span class="line">		<span class="keyword">private</span> <span class="type">int</span> <span class="variable">count</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="title function_">UnionFind</span><span class="params">(<span class="type">int</span> size)</span> &#123;</span><br><span class="line">			parent = <span class="keyword">new</span> <span class="title class_">int</span>[size];</span><br><span class="line">			count = size;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; size; i++) &#123;</span><br><span class="line">				parent[i] = i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">find</span><span class="params">(<span class="type">int</span> p)</span> &#123;</span><br><span class="line">			<span class="comment">// 递归的时候进行路径压缩</span></span><br><span class="line">			<span class="keyword">if</span> (p != parent[p])</span><br><span class="line">				p = find(parent[p]);</span><br><span class="line">			<span class="keyword">return</span> p;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这里q为父节点</span></span><br><span class="line">		<span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">union</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> q)</span> &#123;</span><br><span class="line">			p = find(p);</span><br><span class="line">			q = find(q);</span><br><span class="line">			<span class="keyword">if</span> (p == q)</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			parent[p] = q;</span><br><span class="line">			<span class="comment">// 当两个父节点不一致时数量-1</span></span><br><span class="line">			count--;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isConnnected</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> q)</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> find(p) == find(q);</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="comment">// 获取当前有多少颗树，也就是多少个门派</span></span><br><span class="line">		<span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getCount</span><span class="params">()</span>&#123;</span><br><span class="line">			<span class="keyword">return</span> count;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>并查集</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之内存管理</title>
    <url>/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<blockquote>
<p>前言：对于操作系统，主要是了解的作用，本文大部分知识来自《现代操作系统》，个人只是对内存管理章节做总结并加入一点自己的看法，便于后续查阅。</p>
</blockquote>
<p><strong>地址空间</strong>： 是一个进程可用于寻址内存的一套地址集合。</p>
<p>每个进程都有一个自己的地址空间，并且这个地址空间独立于其他进程的地址空间。</p>
<span id="more"></span> 

<p>处理内存超载的两种通用方法</p>
<h4 id="交换（Swapping）技术："><a href="#交换（Swapping）技术：" class="headerlink" title="交换（Swapping）技术："></a><strong>交换（Swapping）技术：</strong></h4><p> 将一个进程完整调入内存，使该进程运行一段时间，然后把它存回磁盘。空闲进程主要存储在磁盘上，所以当他们不运行时就不会占用内存。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/swapping.png" class="" title="交换技术图">





<h4 id="虚拟内存（Virtual-memeory"><a href="#虚拟内存（Virtual-memeory" class="headerlink" title="虚拟内存（Virtual memeory):"></a><strong>虚拟内存（Virtual memeory):</strong></h4><p>该策略甚至能使程序只有一部分被调入内存的情况下运行。</p>
<p><strong>内存紧缩（memory compaction)</strong></p>
<p>交换技术在内存中产生多个空闲区（hole，可以认为是空洞），通过将所有进程向下移动，将这些空闲区合成一大块。</p>
<p>缺点： 耗时严重，通常不做该操作。</p>
<br/>

<h3 id="空闲内存管理"><a href="#空闲内存管理" class="headerlink" title="空闲内存管理"></a>空闲内存管理</h3><p>在动态分配内存时，操作系统必须对其管理。</p>
<p><strong>1、使用位图的存储管理</strong></p>
<p>在位图方法中，内存可能被划分为 几个字节到 几千个字节不等的分配单元。每个分配单元对应于位图中一位，0 表示空闲，1 表示占用。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/bitsave.png" class="" title="位图存储图">

<p>所以分配单元的大小是一个重要的因素，分配单元越小，位图越大。</p>
<p><strong>位图的缺点：</strong> 当要把一个占k个分配单元的进程调入内存时，存储管理器必须扫描位图，找到有k个连续0的串，这个操作是耗时的</p>
<p><strong>2、使用链表的存储管理</strong></p>
<p>维护一个记录已分配内存段和空闲内存段的链表。</p>
<p>链表中的每一个结点都包含以下部分： </p>
<p><strong>空闲区(H) 或 进程(P) 的指示标志、起始地址、长度和指向下一个结点的指针</strong></p>
<p>像上图中的 3-6C部分一样，一个链表节点 P-0-5-结点指针，P为进程标志，0为起始地址，长度为5</p>
<p>当然，段链表使用双向链表更易于找到上一个结点，并当一个结点为进程P被释放时，方便检查是否可以合并。</p>
<p>我们需要考虑一个问题，怎么给创建的进程或从磁盘换入的已存在的进程分配内存呢？</p>
<p>常用的三种算法：</p>
<p><strong>首次适配算法：</strong></p>
<p>存储管理器沿着段链表进行搜索，直到找到一个足够大的空闲区，并且将空闲区分为两部分，一部分供进程使用，另一部分形成新的空闲区。</p>
<p><strong>最佳适配算法：</strong></p>
<p>最佳适配算法搜索整个链表（从开始到结束），找到能够容纳进程的最小的空闲区。</p>
<p><strong>最差适配算法：</strong></p>
<p>总是分配最大的可用空闲区，使新的空闲区比较大从而可以继续使用。</p>
<br/>

<h3 id="虚拟内存的基本思想："><a href="#虚拟内存的基本思想：" class="headerlink" title="虚拟内存的基本思想："></a>虚拟内存的基本思想：</h3><p>​         每个程序拥有自己的地址空间，这些空间被分割多个块，每个块称作一页或页面（page）。每一页有连续的地址范围。</p>
<p>​        这些页被映射到物理内存，但并不是所有的页都必须在内存中才能运行程序。</p>
<p>​       当程序引用到一部分在物理内存中的地址空间时，由硬件立刻执行必要的映射加载进行内存。</p>
<p>​        当程序引用到一部分不在物理内存中的地址空间时，由操作系统负责将缺失的部分装入物理内存中并重新执行失败的指令。</p>
<p>个人理解：物理内存就是内存，磁盘是硬盘，不是物理内存</p>
<p>.</p>
<p>由程序产生的这些地址称为虚拟地址，它们构成了一个虚拟地址空间。</p>
<p>在使用虚拟内存的时候，虚拟内存不会直接送到内存总线上，而是被送到<strong>内存管理单元</strong>（Memory Management Unit,MMU),MMU会把虚拟地址映射为物理内存地址</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/MMU.png" class="" title="MMU">

<br/>

<h4 id="分页技术"><a href="#分页技术" class="headerlink" title="分页技术"></a>分页技术</h4><p>虚拟地址空间按照固定大小划分成被称为页面(page)的若干单元。在物理内存中对应的单元称为<strong>页框(page frame)</strong> （<strong>也叫页帧</strong>），页面和页框的大小通常是一样的，在本示例下为4KB,但实际系统的页面大小从512字节到1GB.</p>
<p>对于示例中的64KB的虚拟地址空间和32KB的物理内存，可得到16个虚拟页面和8个页帧。RAM和磁盘之间的交换总是以整个页面为单元进行的。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/virtualAddress.png" class="" title="虚拟地址交换">

<p>例如 <strong>MOV REG，8192</strong> ,意味着将虚拟地址 8192 被映射到 物理地址 24576（对应的页框即页帧是6）</p>
<p>即被MMU转换成 <strong>MOV REG，24576</strong></p>
<br/>

<h4 id="虚拟地址转换成物理内存的地址的过程是咋样的？"><a href="#虚拟地址转换成物理内存的地址的过程是咋样的？" class="headerlink" title="虚拟地址转换成物理内存的地址的过程是咋样的？"></a>虚拟地址转换成物理内存的地址的过程是咋样的？</h4><p>我程序运行的虚拟地址有部分要加载进物理内存了，这个能理解，因为物理内存无法装下整个程序，但是怎么理解，要替换的物理内存页帧，这部分的内容是暂时不需要的了呢？</p>
<p>首先会发生缺页中断，然后基于页面置换算法进行页面的置换，具体置换页面的选择要看算法的特性，下面会介绍对应的页面置换算法。</p>
<p>在上图3-9中只有8个物理页框，于是只有8个虚拟页面被映射到了物理内存中，图中使用 叉号表示没有映射。</p>
<p>当程序访问一个未映射的页面，例如 执行指令 MOV REG，32780</p>
<p>​        MMU会注意到该地址对应的虚拟页面8还未映射，于是CPU会交给操作系统处理，这个叫做<strong>缺页中断或缺页错误</strong>。操作系统会找到一个最少使用的页框且把页框中的内容写入磁盘（如果它不再磁盘上并是对应的页面置换算法），随后把要访问的页面读到刚才回收的页框中，修改映射关系，然后重新启动刚才引起陷阱的指令。</p>
<br/>

<h4 id="页表"><a href="#页表" class="headerlink" title="页表"></a>页表</h4><p>从数学的角度上看，页表本质是一个映射函数，将虚拟页号与物理页框号的关系关联起来。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/pageStructure.png" class="" title="页表项结构">



<h5 id="页表项结构"><a href="#页表项结构" class="headerlink" title="页表项结构"></a><strong>页表项结构</strong></h5><p> 不同计算机的页表项大小可能不一样，但32位是一个常用的大小。最重要的域是页框号。毕竟页映射的目的就是找到页框号，其次是 “在&#x2F;不在”位。这个位是1时表示该表项是有效的，可以使用；如果是0，则表示该表项对应的虚拟页面现在不在内存中，访问该页面会引起一个缺页中断。（再次重申：物理页框号对应的是物理内存的地址，虚拟页号对应的是磁盘的地址）</p>
<p><strong>保护位</strong>指出一个页允许什么类型的访问，简单的形式是只有一位，0表示读写，1表示只读。较先进的方法是使用三位，分别对应表示是否启用读、写、执行该页面。</p>
<p> <strong>修改位（modified bit）</strong>，这个在操作系统重新分配页框时有用，如果一个页面已经修改过(即“脏”页时),必须把它写回磁盘。如果一个页面没有被修改过，则只简单把丢弃就行，此时磁盘上的副本依旧是最新的。这一位有时也被称为脏位(dirty bit),因为它反映了该页面的状态。</p>
<p> 不论是读还是写，系统都会在该页面被访问时设置访问位。它的值被用来帮助操作系统在发生缺页中断时选择要被淘汰的页面。不再使用的页面要比正在使用的页面值得淘汰。</p>
<p> 高速缓存禁止位是用于禁止该页面被高速缓存。这种对一些设备寄存器的内存页面很有用，因为假如操作系统正在等待IO设备对发出的命令做出响应时，需要保证硬件是从IO设备中读取数据而不是读取高速缓存的副本这种多此一举的行为。</p>
<br/>

<h3 id="加速分页过程"><a href="#加速分页过程" class="headerlink" title="加速分页过程"></a><strong>加速分页过程</strong></h3><p>分页系统需要考虑的两个问题</p>
<p>1、虚拟地址到物理地址的映射必须非常快</p>
<p>2、如果虚拟地址空间很大，页表也会很大</p>
<p>第一个问题是 因为每次访问内存都需要进行虚拟地址到物理地址的映射。</p>
<p>第二个问题是现在计算机使用要么是32位的虚拟地址，要么是64位的。假设页面大小为4KB，32位的地址空间将有100万页。而64位就更多了。而页表会记录虚拟空间的所有页（每个进程都需要自己的页表，因为执行的程序和存储的虚拟地址不一样）</p>
<h4 id="1、转换检测缓冲区"><a href="#1、转换检测缓冲区" class="headerlink" title="1、转换检测缓冲区"></a><strong>1、转换检测缓冲区</strong></h4><p>   如果将页表放在内存中，那么要访问到内存中的地址需要访问两次内存，为什么呢？第一次拿着虚拟地址去内存中找到页表上对应的物理地址返回，通过该物理内存地址第二次访问内存，返回数据。这种效率不高，所以提出了一种解决方案：</p>
<p>   由于少量的页面会进行大量的访问，我们只需要保证少量的页面能够快速被访问到即可，于是为计算机设置一个小型的硬件设备，将虚拟直接映射到物理地址，不需要访问页表，这种设备叫做<strong>转换检测缓冲区（Translation Lookaside Buffer,TLB)</strong>,又称为快表，通常在MMU中，在实际中很少会超过256个。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/TLB.png" class="" title="TLB">



<p>第二个问题，如何处理巨大的虚拟地址空间</p>
<p>针对大内存的页表的解决方案</p>
<p>1、多级页表</p>
<p>32位的虚拟地址被切分10位PT1域、10位的PT2域和12位的Offset(偏移量）域。因为偏移量是12位，所以页面大小为4KB，共用2的20次方个页面</p>
<br/>

<h3 id="页面置换算法"><a href="#页面置换算法" class="headerlink" title="页面置换算法"></a>页面置换算法</h3><p>​        当发生缺页中断时，操作系统必须在内存中选择一个页面将其换出内存中，以便为即将调入的页面腾出空间。</p>
<p>​       如果要换出的页面在内存驻留期间已经被修改过，就必须把它写回磁盘以更新该页面在磁盘上的副本。如果没有修改过，则磁盘中的副本已经是最新的，不需要回写。</p>
<h4 id="最优页面置换算法"><a href="#最优页面置换算法" class="headerlink" title="最优页面置换算法"></a>最优页面置换算法</h4><p>​     假设计算机知道页面在后续的指令中还隔多少道才会被使用，那么就能够将性能最大化，例如 A页面在500万指令不会被使用，B页面在300万指令内不会被使用，那么可以优先替换A页面。但可惜计算机是无法知道各个页面下一次将在什么时候被访问，所以只能作为一个参考的最优算法，方便其他算法与其进行比较。</p>
<h4 id="最近未使用页面置换算法"><a href="#最近未使用页面置换算法" class="headerlink" title="最近未使用页面置换算法"></a>最近未使用页面置换算法</h4><p>​      在大多数具有虚拟内存的计算机中，系统为每一页面设置了两个状态位。当页面被访问（读或写）时设置R位；当页面被写入(即修改)时设置M位。这些位包含在每个页表项中</p>
<p>​     当启动一个进程时，它的所有页面的两个位都会被操作系统设置为0，R位被定期地(比如在每次时钟中断时）清零，以区别最近没有被访问的页面和被访问的页面。</p>
<p>​      当发生缺页中断时，操作系统检查所有的页面并根据它们当前的R位和M位的值，把它们分为4类：</p>
<p>第0类：没有被访问，没有被修改</p>
<p>第1类：没有被访问，已被修改</p>
<p>第2类：已被访问，没有被修改</p>
<p>第3类：已被访问，已被修改</p>
<p>虽然第1类看起来似乎不可能，但是当一个第3类的页面在它的R位被时钟中断清零后就成了第1类。</p>
<p><strong>NRU(Not Recently Used,最近未使用）算法随机地从类编号最小的非空类中挑选一个页面淘汰</strong>。这个算法隐含的意思是，在最近一个时钟滴答中(典型的时间是大约20ms)淘汰一个没有被访问的已修改页面要比淘汰一个被频繁使用的”干净”页面好。</p>
<p>NRU的主要优点是易于理解和能够有效地被实现。</p>
<h4 id="先进先出页面置换算法"><a href="#先进先出页面置换算法" class="headerlink" title="先进先出页面置换算法"></a>先进先出页面置换算法</h4><p>​      由操作系统维护一个所有当前在内存中的页面的链表，最新进入的页面放在表尾，最早进入的页面放在表头，当发生缺页中断时，淘汰表头的页面并把新调入的页面加到表尾，这个FIFO算法是很常用的，但在计算机中存在一个问题，就是可能一些常用的页面在不断的置换过程后被淘汰，基于这个原因，很少使用纯粹的FIFO算法。</p>
<h4 id="第二次机会页面置换算法"><a href="#第二次机会页面置换算法" class="headerlink" title="第二次机会页面置换算法"></a>第二次机会页面置换算法</h4><p>​      FIFO算法可能会把经常使用的页面置换出去，为了避免这一问题，对该算法做一个简单的修改；检查最老的页面的R位，如果R位是0，那么这个页面又老又没有被使用，可以立刻被置换掉；如果是1，就将R位清0，并将该页面放到链表的尾端，修改它的装入时间使得它就像刚装入的一样，然后继续搜索。</p>
<p>​      这个算法称为第二次机会(second chance)算法。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/secondChance.png" class="" title="secondChance">

<p>​     第二次机会算法就是寻找一个在最近的时钟间隔内没有被访问过的页面。如果所有的页面都被访问过了，该算法就简化为纯粹的FIFO算法，因为将一个时钟间隔内的所有页面移动一次后，他们的R位已经清为0了，这就意味着循环一次后第一个页面A会被淘汰。</p>
<h4 id="时钟页面置换算法"><a href="#时钟页面置换算法" class="headerlink" title="时钟页面置换算法"></a>时钟页面置换算法</h4><p>​       虽然第二次机会算法是一个比较合理的算法，但它经常要在链表中移动页面，既降低了效率又不是很必要，有一个更好的办法是把所有的页面都保存在一个类似钟面的环形链表中，一个表针指向最老的页面。</p>
<p>​       当发生缺页指端时，算法会先检查表针指向的页面，如果它的R位是0就淘汰该页面，并把新的页面插入这个位置，然后把表针前移一个位置；如果R位是1就清除R位并把表针前移一个位置。重复这个过程直到找到了一个R位为0的页面为止。这也是为什么成为时钟(clock)算法。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/clock.png" class="" title="clock">



<h4 id="最近最少使用页面置换算法"><a href="#最近最少使用页面置换算法" class="headerlink" title="最近最少使用页面置换算法"></a>最近最少使用页面置换算法</h4><p>​        **LRU(Least Recently Used,最近最少使用)**页面置换算法是大家非常熟悉的算法，广泛的被应用到各个领域，它非常近似于最优算法，理论上可以实现，但代价很高。</p>
<p>​        为了实现LRU，需要在内存中维护一个所有页面的链表，最近最多的页面在表头，最近最少使用的页面在表尾。困难的是每次访问内存时都必须要更新整个链表、在链表中找一个页面删除它，然后把它移动到表头是一个非常耗时的操作。</p>
<p>​         然而，还是有一些特殊硬件实现LRU的方法。先考虑一个最简单方法，就是要求硬件有一个64位计数器C，它在每条指令执行完后自动加1，每个页表项必须有一个足够容纳这个计数器值的域。每次访问内存后，将当前的C值保存到被访问页面的页表项中，一旦发生缺页中断，操作系统就会检查所有页表项中计数器的值，找到值最小的一个页面，这个页面就是最近最少使用的页面。</p>
<h4 id="工作集页面置换算法"><a href="#工作集页面置换算法" class="headerlink" title="工作集页面置换算法"></a>工作集页面置换算法</h4><p>​       刚启动进程时，在内存中并没有页面，在CPU试图请求第一条指令时就会产生一次缺页中断，使得操作系统装入含有第一条指令的页面。随后不断重复这个过程，直到所需的大部分页面都在内存中，进程开始在较少的缺页中断情况下运行，这个策略称为请求调页(demand paging).因为页面是在需要时调入，而不是预先装入。</p>
<p>​       <strong>一个进程当前正在使用的页面的集合称为它的工作集</strong>。如果整个工作集都被装入到了内存中，那么进程在运行到下一运行阶段之前，不会产生很多的缺页中断。如果内存在下无法装下整个工作集，那么在进程运行的过程中会产生大量的缺页中断，导致运行速度也会变得很缓慢。</p>
<p>​        在多道程序设计系统中，进程会把进程转移到磁盘上(即从内存中移走所有的页面），这样可以让其他进程有机会占有CPU。有一个问题是，当该进程再次调回来以后应该怎么办？从技术的角度上将，并不需要做什么，一直发送缺页中断直到工作集全部被装入内存。然而，这样不停的产生缺页中断，速度太慢了，有相当多的CPU时间被浪费。</p>
<p>​      所以不少分页系统都会设法跟踪进程的工作集，以确保在让进程运行以前，它的工作集就已在内存中。该方法称为<strong>工作集模型</strong>，其目的在于大大减少缺页中断率。在进程运行前预先装入其工作集页面也称为<strong>预先调页</strong>(prepaging).</p>
<p>​        在任一时刻t,都存在一个集合，它包含所有最近k次内存访问所访问过的页面。这个集合w(k,t)就是工作集，w(k,t)是k的单调非递减函数，因为 k&gt;1次访问过的页面一定不会小于k&#x3D;1次访问的页面，同时w(k,t)不会无限增大，因为程序不可能访问比它的地址空间所能容纳的页面数量上限还多的页面。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/workset.png" class="" title="工作集示例">

<p>​      在该置换算法中，每一个页表项至少包含两条信息：<strong>该页面上次使用时间和R位</strong>。置换过程与第二次机会算法类似，在每一个时钟周期，当发生缺页中断时，扫描页表项。</p>
<p>​        如果R位是1，则将当前实际时间写入到页表项的”上次使用时间”，也表示发生缺页中断时这个页面正在被使用。</p>
<p>​        如果R位是0，表示当前时钟周期下，这个页面还没被访问过，它就是可以作为候选者被置换，然后计算它的生存时间(即当前实际时间 - 上次使用时间),然后与T(工作集指的是过去T秒内实际运行时间中它所访问的页面集合)做比较，如果生存时间大于T，则用新的页面置换它，否则仍保留在工作集中，然后继续扫描更新剩余的表项。如果扫描了一遍都小于T，则淘汰生存时间最长的页面（即上次使用时间最小的值），最坏的情况R位都为1，则随机选择一个干净的页面淘汰。</p>
<h4 id="工作集时钟页面置换算法"><a href="#工作集时钟页面置换算法" class="headerlink" title="工作集时钟页面置换算法"></a>工作集时钟页面置换算法</h4><p>​     和时钟算法一样，所需的数据结构是一个以页框为元素的循环表，最初这个表是空的，然后不断装入页面知道形成一个环。每个页表项包含来自基本工作集的上次使用时间，以及R位和M位(未标出来）。</p>
<img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/workset_clock.png" class="" title="时钟工作集示例">

<p>只是变成了一个环，扫描的时候移动的是指针，本质和工作集算法是一致的，淘汰机制也是一样。</p>
<br/>

<h3 id="页面置换算法总结"><a href="#页面置换算法总结" class="headerlink" title="页面置换算法总结"></a>页面置换算法总结</h3><img src="/2021/03/14/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/summary.png" class="" title="总结">]]></content>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>来聊聊TCP传输控制</title>
    <url>/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<p>​           每次我看看TCP为了保证可靠传输，涉及到的滑动窗口、流量控制、拥塞控制，总是看得事前迷迷糊糊，事后转身就忘，这怎么能行呢？我谢某人怎么能做摸鱼怪呢 ！！今天必须整明白，理解透。</p>
<span id="more"></span> 

<h3 id="基础预备："><a href="#基础预备：" class="headerlink" title="基础预备："></a>基础预备：</h3><p>TCP可以保证可靠传输，那就应该有数据的发送方和数据的接收方，两个端点吧。</p>
<p>RTT: 发送方发送一个数据包到 收到 接收方回复的ACK确认信息，这样一个来回所花费的实际时间</p>
<p>RTO: 重传时间间隔，假如发送方发的数据在网络中丢了，是不是要定时计算一下，一定时间内没收到ACK信号就重传呀。</p>
<h3 id="让你来保证可靠传输，你怎么弄？"><a href="#让你来保证可靠传输，你怎么弄？" class="headerlink" title="让你来保证可靠传输，你怎么弄？"></a>让你来保证可靠传输，你怎么弄？</h3><p>按照正常的思维，我发一个东西给你，你收到了就回复收到了，没收到就不回复，过段时间我还没有听到你说收到，我就再发。</p>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/transport.jpg" class="" title="可靠传输">

<p>通过图片我们可以了解到，要发送的数据被切分成了多个数据包，然后进行编码发送。</p>
<p>每发送一个数据包，接收方就要回复一次我收到了的 ACK确认标志。</p>
<p>当某一个数据包在网络中丢失时，那么客户端会进行重传，因为它没有收到接收端的ACK信号。</p>
<p><strong>注意：</strong> 每个数据包都会有一个对应的 计时器。计时时间略大于平均的RTT一次往返的时间。这样当大于指定时间了该数据包还未收到确认信息时，就会超时重传。</p>
<h3 id="上面的这份做法有什么缺点？"><a href="#上面的这份做法有什么缺点？" class="headerlink" title="上面的这份做法有什么缺点？"></a>上面的这份做法有什么缺点？</h3><p>我们发现发送方每次发送一份数据包就得等待接收端的ACK信号，这种做法是不是太低效了点！！！</p>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/RTT.jpg" class="" title="RTT">

<h4 id="怎么说？"><a href="#怎么说？" class="headerlink" title="怎么说？"></a>怎么说？</h4><p>就好像你淘宝在一个商店买了一大堆东西，结果它是打算一天发一件给你，你必须回复收到它才再发一件，这样玩，几个月才可以发完，是不是想吐血。</p>
<p>有什么好办法呢？</p>
<br/>

<h3 id="流水线的发送方式"><a href="#流水线的发送方式" class="headerlink" title="流水线的发送方式"></a>流水线的发送方式</h3><img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/water_RTT.jpg" class="" title="流水线RTT">

<p>假设接收方可以无限接收，那么发送方可以不停的发送数据包给接收方，接收方收到了就发送ACK确认序号，某个数据包没收到ACK的话发送方重发就好了。</p>
<h4 id="这样做的好处是什么？"><a href="#这样做的好处是什么？" class="headerlink" title="这样做的好处是什么？"></a>这样做的好处是什么？</h4><p>信道利用率大幅提升了，我们不需要每发送一次数据包就傻傻的等着对应的ACK确认信号，可以不停的发送。</p>
<h4 id="这样做的缺点呢？"><a href="#这样做的缺点呢？" class="headerlink" title="这样做的缺点呢？"></a>这样做的缺点呢？</h4><p>前面我说明，是假设接收方可以无限接收的前提下，才能做到流水线的传输方式，可问题来了，接收方能力有限呀，发送方有自己的发送缓存，接收方有自己的接受缓存。通常来说发送方的发送速度是要看接收方的当时处理能力来的。</p>
<br/>

<h4 id="怎么说？-1"><a href="#怎么说？-1" class="headerlink" title="怎么说？"></a>怎么说？</h4><p>​         这种莽夫式的发法比最初的发送方式（发一个接受一个）的这种要好许多，信道利用率高，发送频率高，但是这种方法太莽夫了，你要考虑一下接收方大爷的胃口呀，不能接收方装不下了你发送方还塞个不停，为了双方的性能考虑，就出来了一个流量控制和接收缓存以及发送缓存。</p>
<br/>

<h3 id="举个栗子"><a href="#举个栗子" class="headerlink" title="举个栗子"></a>举个栗子</h3><p>比如接收方现在说来吧，我现在可以接受1000字节的数据包，发过来吧。</p>
<p>这时候发送方就按照接收方的要求发送了一个1000字节的数据包给它，好，接收方收到数据包后，接受缓存直接快满了，这个时候怎么办，是不是要消化一下，把数据写入到磁盘，再清理自己的<strong>接受缓存</strong>从而接收新的数据吧。</p>
<p>这个时候接收方可能就说不行了，发送方老哥，你之前发送的数据包我收到了，但是你要悠着点，我有点顶不住了，我现在只能接受2个字节的数据包了。</p>
<p>发送方收到后，说好的，把之前<strong>发送缓存</strong>中存的1000字节的数据包删除了，填入新的数据，再给接收方发送了一个2个字节的数据包。</p>
<p>就这样不停的来回的发送和接受，直到把整个数据发送完毕。</p>
<br/>

<p>现实的tcp通信更贴近于上面说的这个小例子，而不是莽夫一样的直接全部把数据甩给接收方。</p>
<p>这就涉及到了两个概念 <strong>TCP的流量控制和滑动窗口</strong>了。</p>
<p>这个时候相信大家可以大概理解流量控制，控制的是啥？</p>
<p>无非就是接收方告诉 我现在的接受量（也就是滑动窗口的窗口大小）是多少，发送方按照接收方的当前窗口大小来调整对应的发送数据的大小。</p>
<br/>

<h3 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h3><img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/window.jpg" class="" title="滑动窗口">

<p>这里的滑动窗口是指的我们的发送方有一个发送窗口，接收方有一个接受窗口</p>
<p><strong>注意： 发送窗口是根据 接受窗口的大小来调整自身大小的</strong></p>
<p>这里假设当前接收方B的接受窗口大小是20</p>
<p>目前 发送方A已经发送序号1-30的字节数据了，并且接收方B已经成功接受了1-30序号的数据，已经从接受缓存删除，存入到磁盘作为一个临时文件了。</p>
<p>这个时候发送方A的滑动窗口就是存放了序号从 31-50总共20个字节的数据，其中序号31-41已经发送到接收方B了，但是还未确认。</p>
<p><strong>注意</strong>： 这里发送窗口中数据都可以发送，也就是即使 序号31-41的数据还未确认，但是序号42-50的数据仍然可以接着发送。</p>
<p>但是如果窗口中的数据发送完了，就必须要等待接收方的确认，如果没有及时收到确认就要超时重发了。</p>
<p>这个时候 假设接收方B的接受窗口中，只接受到了31，以及34-41，怎么办？</p>
<p>那么接收方B会回复一个 ack &#x3D; 32这样一个确认信号给接收方A。</p>
<p><strong>什么意思？</strong></p>
<p>​         为啥是回复一个确认序号ack &#x3D;32呢？ 简单来说就是告诉发送方A说，31和之前的连续数据我已经收到了，你发送从32开始的数据给我吧。</p>
<p>​         这个时候发送A收到确认信号 ack &#x3D;32后，就明白，哎呀，我之前发送的32-41的数据，这家伙只收到了31，算了，接着重新发 32以后的数据。</p>
<p>​          你可以理解成 接收方是绝对的大爷，它想怎么样就怎么样，发送方必须按照它的胃口来发送数据。</p>
<br/>

<h4 id="但是我们不是说的是滑动窗口吗，你倒是滑动呀！"><a href="#但是我们不是说的是滑动窗口吗，你倒是滑动呀！" class="headerlink" title="但是我们不是说的是滑动窗口吗，你倒是滑动呀！"></a>但是我们不是说的是滑动窗口吗，你倒是滑动呀！</h4><p>别急，这里假设 接收方B收到了序号32-41的数据了，它会干什么？ 它会回复一个 ack&#x3D;42的信号，告诉发送方可以发送序号42以后的数据了,前面的我已经收到了。</p>
<p>然后 接收方B会把接收窗口滑动准备接收序号 42-61的数据，并将收到了序号31-41的字节数据写入磁盘的临时文件中同时将接收缓存中的序号31-41的接受数据删除。</p>
<p>而发送方收到接收方发来了确认信号 ack &#x3D; 42后，它也会滑动发送窗口，对应准备发送 序号42-61的数据，并且将之前发送缓存中的31-41的数据删除，进而可以放入新的数据。</p>
<br/>

<h3 id="流量控制"><a href="#流量控制" class="headerlink" title="流量控制"></a>流量控制</h3><p>前面说了流量控制，那么现实中是如何实现流量控制的呢？</p>
<p>刚才举的例子，大家理解的话，其实这里也是一样的。那就接收方按照自己处理数据的速度，动态调整接收窗口大小，并且发送信号告诉发送方自己的接收窗口大小，要求发送方按照接收窗口的大小来调整发送窗口的大小。</p>
<br/>

<h3 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h3><p>在某段时间，若对网络中某一资源的需求超过了该资源所能提供的可用部分，网络性能就要变坏。这种情况就叫做拥塞（congestion）。</p>
<p>​    在计算机网络中的链路容量（即带宽）、交换结点中的缓存和处理机等，都是网络的资源。</p>
<p>若出现拥塞而不进行控制，整个网络的吞吐量将随输入负荷的增大而下降。</p>
<h4 id="怎么说，我来翻译一波："><a href="#怎么说，我来翻译一波：" class="headerlink" title="怎么说，我来翻译一波："></a>怎么说，我来翻译一波：</h4><p>​         就是有时候，接收方需求大，有钱了，要更多的货了，但是通往接收方的马路（网络带宽）只能同时走两辆马车，中途的驿站只能装下几吨货物，你这个时候说要几百吨马上送来，这不是步子迈太大，扯着裆吗？到时候一群人和货物全部堵死在马路上，这就凉凉了。</p>
<p>前面流量控制说过，发送方要按照接收方大爷的要求来，接收方大爷今天想要多少，发送方就按照今天的要求发多少。</p>
<p>但是可惜的是，发送方不仅仅只有一个大爷，它还有一个二大爷-网络，网络二大爷今天心情好，马路好走，就尽量按照两位大爷的要求来，二大爷心情不好，有马匪，发送的货物数据被人抢了不少，丢包了，肯定不能傻傻的送货了呀，咋办？接着往下看！</p>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/block.jpg" class="" title="拥塞与非拥塞">

<br/>

<p>既然网络时好时坏，那就要得想出解决办法来。</p>
<h4 id="Tcp的四种拥塞控制算法"><a href="#Tcp的四种拥塞控制算法" class="headerlink" title="Tcp的四种拥塞控制算法"></a>Tcp的四种拥塞控制算法</h4><ul>
<li>慢开始（slow-start）</li>
<li>拥塞避免（congestion avoidance）</li>
<li>快重传（fast retransmit）</li>
<li>快恢复（fast recovery）</li>
</ul>
<p>下面来介绍这四种策略的基本原理，这里为了方便介绍拥塞控制，假设如下条件</p>
<p>1、数据是单方面传送，而另一个方向只传送确认。</p>
<p>2、接收方总是有足够大的缓存空间，因而发送方窗口的大小由网络的拥塞程度来决定。</p>
<p>3、以TCP报文段的个数作为讨论问题的单位，而不是以字节为单位。</p>
<p>你可以简单理解为，接收方大爷是个无底洞，发送方只需要考虑如何根据 网络二大爷的心情，最大速度的发送数据并保证货物的完整。</p>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/cwnd.jpg" class="" title="端点拥塞">

<br/>

<p>发送方维护一个叫做 <strong>拥塞窗口cwnd</strong> 的状态变量，其值<strong>取决于网络的拥塞程度</strong>，并且<strong>动态变化</strong>。</p>
<ul>
<li>​    拥塞窗口<strong>cwnd的维护原则</strong>： 只要网络没有出现拥塞，拥塞窗口就再增大一些，但是一旦网络出现拥塞，拥塞窗口就减少一些。</li>
<li>判断出现网络拥塞的依据： 没有按照收到应当到达的确认报文（即发生超时重传）。</li>
</ul>
<p>发送方将拥塞窗口作为发送窗口，即swnd &#x3D; cwnd</p>
<p>维护一个慢开始门限 <strong>ssthresh</strong> 状态变量：</p>
<ul>
<li>当cwnd  &lt; ssthresh 时，使用慢开始算法；</li>
<li>当 cwnd &gt; ssthresh时，停止使用慢开始算法而改用拥塞避免算法；</li>
<li>当cwnd &#x3D; ssthresh时，即可使用慢开始算法，也可使用 拥塞避免算法。</li>
</ul>
<br/>

<h4 id="怎么说，再翻译一波："><a href="#怎么说，再翻译一波：" class="headerlink" title="怎么说，再翻译一波："></a>怎么说，再翻译一波：</h4><p>​         首先， 把要送的货物总批数 取个名字，叫 总批数cwnd，刚开始2的指数增长，但是这个指数增长的玩法不能超过 保险数量 ssthresh，我刚开始不怕死，为了赚钱，满足接收方大爷的要求，每次翻倍的发货，从第一天发1批，第二天发2批，第三天发4批，第4天发8批，到第5天的时候你发了16批 到了保险数量 ssthresh，这个时候我有点怂了，万一我今天的货被马匪劫了，那我不是亏大了！！这个时候拥塞避免算法，我怕货被劫但是我又想多赚点，怎么搞？ 算了，每天多加一批，这就很舒服。</p>
<p>​        突然有一天，马匪来了，觉得你这只羊养肥了可以割一下韭菜了，你的货损失惨重。怎么办？没办法，生意还是要做，大爷还是要供着。只是我怂一点，重新开始，从第一天发1批货这样来指数增长，但是保险数量得改一改，ssthresh 等于 你被马匪截的那天总批数的一半。 这样到时候只长肥一半的羊估计马匪应该还想再养一会，我可以趁着和平日子多赚点钱。</p>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/slow_start.jpg" class="" title="慢开始">

<ul>
<li>“慢开始” 是指一开始向网络注入的报文段少，并不是指拥塞窗口 cwnd增长速度慢；</li>
<li>“拥塞避免” 并非指完全能够避免拥塞，而是指在拥塞避免阶段将拥塞窗口控制为按线性规律增长，使网络比较不容易出现拥塞；</li>
</ul>
<br/>

<p>慢开始和拥塞避免算法是1988年提出的TCP拥塞控制算法（TCP Tahoe版本）</p>
<p>1990年又增加了两个新的拥塞控制算法（改进TCP的性能），这就是快重传和快恢复（TCP Reno版本）</p>
<p>有时候，个别报文段会在网络中丢失，但实际上网络并发生拥塞。</p>
<p>这将导致发送超时重传，并误以为网络发生了拥塞；</p>
<p>发送方错误启动慢开始算法，并把拥塞窗口cwnd又设置为最小值1，因为降低了传输效率</p>
<br/>

<h4 id="快重传算法"><a href="#快重传算法" class="headerlink" title="快重传算法"></a>快重传算法</h4><p>采用快重传算法可以让发送方尽早知道了个别报文段的丢失。</p>
<p>所谓快重传，就是使发送方尽快进行重传，而不是等超时重传计时器超时再重传。</p>
<ul>
<li>要求接收方不要等待自己发送数据时才进行捎带确认，而是要<strong>立即发送确认</strong>；</li>
<li>即使收到了失序的报文段也要立即发出对已收到的报文段的重复确认。</li>
<li>发送方一旦收到<strong>3个连续的重复确认</strong>，就将相应的报文段<strong>立即重传</strong>，而不是等该报文段的超时重传计时器超时再重传。</li>
<li>对于个别丢失的报文段，发送方不会出现超时重传，也就不会误认为出现了拥塞（进而降低拥塞窗口cwnd为1）。使用快重传可以使整个网络的吞吐量提供约20%。</li>
</ul>
<img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/quick_return.jpg" class="" title="快重传">

<br/>

<h4 id="怎么说？翻译一波"><a href="#怎么说？翻译一波" class="headerlink" title="怎么说？翻译一波"></a>怎么说？翻译一波</h4><p>​        有一天我，湖南小霸王，听我手下说我的货又又又一次被马匪抢劫了，我还是按照打算按照套路  慢开始，从1开始，再拥塞避免，猥琐的发送货物。但是接收方大爷跟我说你的货不是被抢了，是你手下删库跑路了！ 你不要怕，赶紧发货，记得按顺序来，哪一批货没了，我就重复告诉你3次，3次过后你不发过来，我就把我的意大利炮给你看看。</p>
<p>​       我，湖南小霸王，没得办法，面对威胁只能服气，老老实实的把今天的货批数cwnd 等于被抢之前的一半，按照编好的顺序，每天多发送一批，遇到发送方大爷的狗腿子告诉我3次哪批货少了，我就得赶紧重新把对应的货补上重发，直到全部发完为止。</p>
<h4 id="综合图如下："><a href="#综合图如下：" class="headerlink" title="综合图如下："></a>综合图如下：</h4><img src="/2020/03/30/%E6%9D%A5%E8%81%8A%E8%81%8ATCP%E4%BC%A0%E8%BE%93%E6%8E%A7%E5%88%B6/all_transport.jpg" class="" title="全部的拥塞重传">



<br/>

<h3 id="结语："><a href="#结语：" class="headerlink" title="结语："></a>结语：</h3><p>​        相信到这里，大家对TCP的滑动窗口、流量控制、拥塞控制都要一定的认知和理解了，我也发现网络这块知识，如果整体去理解某个协议的发展过程，比单一的去理解某个知识点效果要好很多。</p>
<h4 id="参考："><a href="#参考：" class="headerlink" title="参考："></a>参考：</h4><p>这里的很多知识来源于 B站中的 湖科大教书匠和 韩立刚老师的网络教程。复习计算机网络这块知识，推荐看这两个up主的视频。</p>
]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
      <tags>
        <tag>TCP</tag>
      </tags>
  </entry>
  <entry>
    <title>消息中间件-ActiveMQ</title>
    <url>/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/</url>
    <content><![CDATA[<h1 id="消息中间件-ActiveMQ"><a href="#消息中间件-ActiveMQ" class="headerlink" title="消息中间件-ActiveMQ"></a>消息中间件-ActiveMQ</h1><h2 id="ActiveMQ-安装"><a href="#ActiveMQ-安装" class="headerlink" title="ActiveMQ 安装"></a>ActiveMQ 安装</h2><p>和tomcat差不多，都需要Java环境</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/install.jpg" class="" title="mq安装">

<span id="more"></span>

<p>这里在公司的虚拟机上</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/start.jpg" class="">

<p><strong>grep -v grep</strong></p>
<p>反转查找，即 查找不包含grep的进程</p>
<h4 id="查看端口"><a href="#查看端口" class="headerlink" title="查看端口"></a><strong>查看端口</strong></h4><ul>
<li><strong>netstat -anp | grep 61616</strong></li>
<li><strong>lsof -i:61616</strong></li>
</ul>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/netstat.jpg" class="">



<h4 id="带日志启动"><a href="#带日志启动" class="headerlink" title="带日志启动"></a><strong>带日志启动</strong></h4><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/startwithlog.jpg" class="">



<h4 id="访问："><a href="#访问：" class="headerlink" title="访问："></a>访问：</h4><p>后台端口 61616</p>
<p>前端访问端口 8161</p>
<h3 id="一个电商系统的模块调用图"><a href="#一个电商系统的模块调用图" class="headerlink" title="一个电商系统的模块调用图"></a><strong>一个电商系统的模块调用图</strong></h3><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/shopmodule.jpg" class="">

<h2 id="JMS开发步骤"><a href="#JMS开发步骤" class="headerlink" title="JMS开发步骤"></a>JMS开发步骤</h2><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/jmsStep.jpg" class="">

<h3 id="个人总结JMS基础开发步骤"><a href="#个人总结JMS基础开发步骤" class="headerlink" title="个人总结JMS基础开发步骤"></a>个人总结JMS基础开发步骤</h3><p>1、 创建一个 ConnectFactory ,带 url</p>
<p>2、 通过factory创建 Connection,并启动 connection</p>
<p>3、 创建Session</p>
<p>4、 通过session创建 Destination对象，Queue、Topic</p>
<p>5、 创建消费者 Consumer或者注册一个 JMS message listenter 或者 生产者 Producer,并设置 Destination</p>
<p>6、 发送或者接收 JMS message</p>
<p>7、 关闭JMS资源</p>
<p>当一个服务提供者提供6条信息，2条消费者监听该服务<strong>队列</strong>，则一人一半，还是按照顺序分配的</p>
<h2 id="两种Destination模式"><a href="#两种Destination模式" class="headerlink" title="两种Destination模式"></a>两种Destination模式</h2><p>队列： 1 ： 1，1对1，当一个生产者队列对应了多个消费者时，消息采用轮播的方式，但是一条消息只能给一个消费者</p>
<p>主题： 类似微信公众号 1 ： N, 当订阅者的数量N为0时，主题生产者发送的消息会当作废消息处理。</p>
<h2 id="JMS-Message的组成："><a href="#JMS-Message的组成：" class="headerlink" title="JMS Message的组成："></a>JMS Message的组成：</h2><h3 id="消息头"><a href="#消息头" class="headerlink" title="消息头"></a>消息头</h3><p><strong>核心的五个属性</strong></p>
<ul>
<li>JMSdesination ：消息发送的目的地，主要是指Queue与Topic</li>
<li>JMSDeliveryMode：持久与非持久模式</li>
</ul>
<p>​               持久模式下，一条消息发送中如果出现故障会再次发送，非持久模式下最多发送一次，出现故障消息会永久丢失</p>
<ul>
<li>JMSExpiration： 消息过期时间，默认永不过期</li>
<li>JMSPriority： 消息优先级，要求加急消息要先于普通消息，0-4是普通消息，5-9是加急消息，默认是4级，同类型的消息如 9 不一定比7快，但一定比4快。</li>
<li>JMSMessageID ： 唯一识别每个消息的表示，由MQ产生</li>
</ul>
<h3 id="消息体"><a href="#消息体" class="headerlink" title="消息体"></a>消息体</h3><p><strong>封装具体消息的数据</strong></p>
<p><strong>发送和接受的消息体类型必须是一致对应的</strong></p>
<p><strong>主要是 TextMessage,MapMessage</strong></p>
<ul>
<li>TextMessage: 普通的字符串消息，包含一个string</li>
<li>MapMessage: 一个map类型的消息，key为stirng类型，而value 为java的基本类型</li>
<li>BytesMessage: 二进制数组消息，包含一个byte[]</li>
<li>StreamMessage: java数据流消息，用标准流操作来顺序的填充或读取</li>
<li>ObjectMessage: 对象消息，包含一个可序列化的java对象</li>
</ul>
<h3 id="消息属性"><a href="#消息属性" class="headerlink" title="消息属性"></a>消息属性</h3><p>如果需要除消息头字段以外的值，那么可以使用消息属性</p>
<p>识别&#x2F;去重&#x2F;重点标注等操作很有用</p>
<p>setProperty 类型 如 setStringProperty</p>
<h2 id="JMS的可靠性："><a href="#JMS的可靠性：" class="headerlink" title="JMS的可靠性："></a>JMS的可靠性：</h2><h3 id="持久（Persistent"><a href="#持久（Persistent" class="headerlink" title="持久（Persistent):"></a>持久（Persistent):</h3><p><strong>默认为持久化</strong></p>
<ul>
<li>非持久化：messageProducer.setDeliveryMode(DeliVeryMode.NON_PERSISTENT         特点： 当服务器挂掉的时候，消息丢失。</li>
<li>持久化：服务器挂掉的时候，未消费的消息仍然存在，可以继续消费。</li>
</ul>
<p>其中主体topic持久化要想接到消息的前提是，必须在消息发送之前订阅过该主题，就跟微信公众号一样。订阅后不管消费者关闭后再次启动，关机期间服务端主题发送的消息都会再启动后接收到</p>
<h3 id="事务："><a href="#事务：" class="headerlink" title="事务："></a>事务：</h3><p> 一旦开启事务，消息发送和接受，session需要commit才生效</p>
<h3 id="签收："><a href="#签收：" class="headerlink" title="签收："></a>签收：</h3><ul>
<li>非事务： 默认自动签收、可设置手动签收 Session.CLIENT_ACKONWLEDGE，重复消息</li>
<li>事务： 当设置为事务时，只要session.commit()不管签收的模式是什么，可当做自动签收。当未session.commit()时，手动签收模式是否确认签收，都无效，总会收到重复消息，因为事务优先级最高。</li>
</ul>
<h2 id="ActiveMQ的Broker"><a href="#ActiveMQ的Broker" class="headerlink" title="ActiveMQ的Broker"></a>ActiveMQ的Broker</h2><p>一个具体的服务器MQ实例，可以被JAVA创建一个简易的MQ内核开启关闭，同时支持外部访问，不过一般还是用linux上的MQ</p>
<h2 id="ActiveMQ的传输协议"><a href="#ActiveMQ的传输协议" class="headerlink" title="ActiveMQ的传输协议"></a>ActiveMQ的传输协议</h2><p><strong>官网介绍：</strong></p>
<p><a href="http://activemq.apache.org/configuring-transports.html">http://activemq.apache.org/configuring-transports.html</a></p>
<p>ActiveMQ的核心协议在下面</p>
<p>如果不特别指定ActiveMQ的网络监听端口，那么这些端口都将使用BIO网络IO模型（openWire,STOMP,AMQP..)</p>
<p>所以要提升单节点的传输性能，我们需要底层配置协议</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/transports.jpg" class="">

<p>而 ActiveMQ自身的配置文件 activemq.xml中 端口配置情况</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/xml-port.jpg" class="">

<p><strong>看到没有？ 不同的协议设置了不同的网络监听端口，默认是一个端口一个协议</strong></p>
<p>URL描述信息的头部都是采用协议名称，例如：</p>
<p>描述 amqp协议的监听端口时，采用的URL描述格式为 “amap:&#x2F;&#x2F;….”;</p>
<p>但唯独描述 openwire协议描述时，URL头却采用 “tcp:&#x2F;&#x2F;….”</p>
<p>为什么？</p>
<p><strong>因为 ActiveMQ的默认消息协议就是 openwire 协议。</strong></p>
<h3 id="ActiveMQ与NIO-网络通信模型："><a href="#ActiveMQ与NIO-网络通信模型：" class="headerlink" title="ActiveMQ与NIO 网络通信模型："></a>ActiveMQ与NIO 网络通信模型：</h3><p>NIO协议与TCP协议类似，但NIO更加侧重底层的访问操作。它允许开发人员对同一资源的更多Client调用和服务端有更多负载。</p>
<p>从上述的描述中我们可以明白 NIO相比 TCP 在底层资源这块处理更佳</p>
<h4 id="适用场景："><a href="#适用场景：" class="headerlink" title="适用场景："></a>适用场景：</h4><ul>
<li>​      可能存在 大量的Client连接到Broker上，一般情况下，大量的Client去连接Broker是会被操作系统限制线程数的，然而 NIO 的实现相比 TCP 可以做到 更少的线程去运行。</li>
<li>​      可能对于Broker有一个很迟钝的网络传输，NIO比TCP提供更好的性能。</li>
</ul>
<h3 id="如何让一个端口可以支持-相关网络模型-以及多协议呢？"><a href="#如何让一个端口可以支持-相关网络模型-以及多协议呢？" class="headerlink" title="如何让一个端口可以支持 相关网络模型 以及多协议呢？"></a>如何让一个端口可以支持 相关网络模型 以及多协议呢？</h3><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/port-more.jpg" class="">

<p>从5.13版本开始，auto 关键字支持4种协议之间的互通，这里auto类似java中这个4个协议统一实现的接口一样。</p>
<p>官网AUTO介绍：<a href="http://activemq.apache.org/auto">http://activemq.apache.org/auto</a></p>
<h4 id="例如：-让-5671-端口同时支持NIO网络模型和多协议"><a href="#例如：-让-5671-端口同时支持NIO网络模型和多协议" class="headerlink" title="例如： 让 5671 端口同时支持NIO网络模型和多协议"></a>例如： 让 5671 端口同时支持NIO网络模型和多协议</h4><p>localhost设置为 0.0.0.0</p>
<p>在activemq.xml中配置</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">transportConnector</span> <span class="attr">name</span>=<span class="string">&quot;auto+nio&quot;</span> <span class="attr">uri</span>=<span class="string">&quot;auto+nio://localhost:5671&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p><em>重启动服务器后</em></p>
<ul>
<li><em>我们想使用自带的4种协议的时候，不用写具体详细名，可以直接使用  tcp:&#x2F;&#x2F;服务器地址:5671，它会自动根据代码去适配这四种协议中的一种。</em></li>
<li><em>如果我们想使用 NIO进行单节点数据传输协议，我们就 nio:&#x2F;&#x2F;服务器地址:5671</em></li>
</ul>
<p><em>当然生产环境可不能这么简单的配置，好歹也要设置最大连接数量，最大消息的大小设置,NIO的线程池的最大工作线程数量。</em></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">transportConnector</span> <span class="attr">name</span>=<span class="string">&quot;auto+nio&quot;</span> <span class="attr">uri</span>=<span class="string">&quot;auto+nio://0.0.0.0:61608?maximumConnections=1000&amp;wireFormat.maxFrameSize=104857600&amp;org.apache.activemq.transport.nio.SelectorManager.corePoolSize=20&amp;org.apache.activemq.transport.nio.SelectorManamaximumConnections=1000&amp;wireFormat.maxFrameSize=104857600&amp;org.apache.activemq.transport.nio.SelectorManager.corePoolSize=20&amp;org.apache.activemq.transport.nio.SelectorManager.maximumPmPoolSize=50&quot;</span> /&gt;</span> </span><br></pre></td></tr></table></figure>

<p>官网上根据URL参数类型的作用，在不同网页介绍，没个统一参数表让我看的很迷糊。</p>
<p>具体URL参数信息请看官网</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/configure-uri.jpg" class="">



<h2 id="ActiveMQ-的持久化机制："><a href="#ActiveMQ-的持久化机制：" class="headerlink" title="ActiveMQ 的持久化机制："></a>ActiveMQ 的持久化机制：</h2><p>​       为了避免意外宕机后丢失数据，消息系统一般都会采用持久化机制来保证重启后可以恢复消息队列。</p>
<p>​       ActiveMQ的持久化机制有JDBC,AMQ,KahaDB和LevelDB,无论使用哪种持久化方式，消息的存储逻辑都是一样的。</p>
<h3 id="AMQ-Message-Store"><a href="#AMQ-Message-Store" class="headerlink" title="AMQ Message Store:"></a>AMQ Message Store:</h3><p>一种的文件存储形式，具有写入速度快和容易恢复的特点。消息存储在一个个文件中，文件默认大小为32M，如果文件中的消息全部被消费，则该文件会被标记可删除，下一个清除阶段会被删除，<strong>适用于MQ5.3以前的版本。</strong></p>
<h3 id="KahaDB消息存储："><a href="#KahaDB消息存储：" class="headerlink" title="KahaDB消息存储："></a>KahaDB消息存储：</h3><p>基于日志文件，从 ActiveMQ5.4版本默认使用的持久化插件。</p>
<p>即使是最新的5.15版本，我自己装的就是5.15.9，配置文件中仍是kahaDB</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/persistence-kahadb.jpg" class="">

<p>它与其他持久化机制相比，文件组成十分简洁，仅有4类文件和一个lock文件锁：</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/kahadb-component.jpg" class=""> 

<p><strong>介绍：</strong></p>
<h4 id="1、-db-log"><a href="#1、-db-log" class="headerlink" title="1、 db-.log"></a>1、 db-<NUMBER>.log</h4><p>KahaDB存储消息到预定义大小的数据记录文件中，文件命名为 db-<Number>.log。当数据文件已满时，文件会随之创建，number数值也会递增，它随之消息数量的增多，如每32M一个文件，文件名按照数字进行编号，如 db-1.log,db-2.log ,db-3.log,当不再有引用到数据文件中的任何消息时，文件会被删除或归档。</p>
<h4 id="2、-db-data"><a href="#2、-db-data" class="headerlink" title="2、 db.data"></a>2、 db.data</h4><p>该文件包含了持久化的B-Tree索引，索引了消息数据记录中的消息，它是消息的索引文件，本质是B-Tree,B-Tree作为索引指向db-<Number>.log里面存储的信息。</p>
<h4 id="3、db-free"><a href="#3、db-free" class="headerlink" title="3、db.free"></a>3、db.free</h4><p>当前db.data文件里哪些页面是空闲的，文件具体内容是所有空闲页的ID</p>
<h4 id="4、db-redo"><a href="#4、db-redo" class="headerlink" title="4、db.redo"></a>4、db.redo</h4><p>作为消息恢复，如果KahaDB消息存储在强制退出后启动，用于恢复B-Tree的索引</p>
<h4 id="5、lock文件锁"><a href="#5、lock文件锁" class="headerlink" title="5、lock文件锁"></a>5、lock文件锁</h4><p>表示当前获取KahaDB读取权限的broker</p>
<h3 id="JDBC消息存储："><a href="#JDBC消息存储：" class="headerlink" title="JDBC消息存储："></a>JDBC消息存储：</h3><p>也就是说直接存储进mysql</p>
<h4 id="jdbc配置："><a href="#jdbc配置：" class="headerlink" title="jdbc配置："></a>jdbc配置：</h4><p>第一步： 将 mysql连接包传入 activemq服务器lib目录下，我这里是 mysql-connector-java-5.1.38.jar</p>
<p>第二步：在配置文件activemq.xml中将持久化机制设置为 jdbc，这个数据库名称mysql-ds是自己取的,注意要加一个 # ，我这里使用的是默认的名称</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">persistenceAdapter</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">jdbcPersistenceAdapter</span> <span class="attr">dataSource</span>=<span class="string">&quot;#mysql-ds&quot;</span>/&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">persistenceAdapter</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>第三步：在配置文件activemq.xml中配置mysql数据库的信息,单独作为一个标签</p>
<p>注意： activemq自带的是连接池jar包为 dbcp2，如果是想改成 C3PO连接池或者阿里的，请记得导入相应的jar包到 activemq服务器lib目录下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- MySql DataSource Sample Setup --&gt;</span> </span><br><span class="line"> <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;mysql-ds&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.apache.commons.dbcp2.BasicDataSource&quot;</span> <span class="attr">destroy-method</span>=<span class="string">&quot;close&quot;</span>&gt;</span> </span><br><span class="line">   <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;driverClassName&quot;</span> <span class="attr">value</span>=<span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>/&gt;</span> </span><br><span class="line">   <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;url&quot;</span> <span class="attr">value</span>=<span class="string">&quot;jdbc:mysql://localhost/activemq?relaxAutoCommit=true&quot;</span>/&gt;</span> </span><br><span class="line">   <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;username&quot;</span> <span class="attr">value</span>=<span class="string">&quot;activemq&quot;</span>/&gt;</span> </span><br><span class="line">   <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;password&quot;</span> <span class="attr">value</span>=<span class="string">&quot;activemq&quot;</span>/&gt;</span> </span><br><span class="line">   <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;poolPreparedStatements&quot;</span> <span class="attr">value</span>=<span class="string">&quot;true&quot;</span>/&gt;</span> </span><br><span class="line"> <span class="tag">&lt;/<span class="name">bean</span>&gt;</span> </span><br></pre></td></tr></table></figure>

<p>当我配置好，重启时发现无进程运行，又不报错</p>
<p>然后发现了许多问题，如果有重启不成功的，可以查看我的一篇activemq的jdbc持久化问题解决的文章。</p>
<p><strong>队列 Queue:</strong></p>
<p>由于默认是持久化的，当我们队列点对点的发送消息，mysql的activemq_msgs表中有相关数据记录，这是因为持久化，消息保存在文件或者数据库中，当有任意消费者消费信息后会删除相应的信息。</p>
<p>而当我们设置为非持久化时，消息是保存到内存中。</p>
<p><strong>主题 Topic:</strong></p>
<p>主题的发布者会将发布消息持久化到activemq_msgs表，同时订阅者记录在activemq_acks表中，消费后不删除消息记录。</p>
<p><strong>activemq_acks</strong></p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/activemq_acks.jpg" class="">

<p><strong>activemq_msgs</strong></p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/activemq_msgs.jpg" class="">



<h3 id="ActiveMQ-Journel"><a href="#ActiveMQ-Journel" class="headerlink" title="ActiveMQ Journel"></a>ActiveMQ Journel</h3><p>每次消息的创建和消费，都需要去读写库。</p>
<p>ActiveMQ journal 使用高速缓存写入技术，大大提高了性能。</p>
<p><strong>当消费者的消费速度能够及时跟得上生产者消息的生产速度时，journal文件能够大大减少需要写入到DB的消息。</strong></p>
<p><strong>这句话是什么意思呢？举个栗子</strong></p>
<p>生产者生产了1000条消息，这1000条消息存入 journal文件中，如果消费者消费速度很快，消费了9000条，那么这个时候就只要同步剩下的1000条到数据库中，如果消费者消费速度慢，journal文件就会批量处理的方式写入到DB中。</p>
<p>也就是说，消息消费快的时候， 大部分逻辑都在journal的高速内存中处理了，有点像redis哈。</p>
<h4 id="如何配置-Journal"><a href="#如何配置-Journal" class="headerlink" title="如何配置 Journal"></a>如何配置 Journal</h4><p>注意 dataSource的名称</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">persistenceFactory</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">journalPersistenceAdapterFactory</span> </span></span><br><span class="line"><span class="tag">     <span class="attr">journalLogFiles</span>=<span class="string">&quot;5&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">journalLogFileSize</span>=<span class="string">&quot;32768&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">useQuickJournal</span> = <span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">dataDirectory</span>=<span class="string">&quot;activemq-data&quot;</span> </span></span><br><span class="line"><span class="tag">     <span class="attr">dataSource</span>=<span class="string">&quot;#mysql-ds&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">persistenceFactory</span>&gt;</span> </span><br></pre></td></tr></table></figure>

<p><strong>修改 activemq.xml配置文件</strong></p>
<p>以前是</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/persistence_jdbc.jpg" class="">

<p>现在改成</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/persistence_journal.jpg" class="">

<p><strong>测试：</strong></p>
<p>当我们用生产者产生队列消息，刷新几秒都不会存入到mysql数据库，因为这个时候队列消息是存在journal高速缓存中。而消费者可以消费到产生的消息。当我们几分钟都不消费，才会写入到mysql数据库中。</p>
<h2 id="ActiveMQ多节点集群："><a href="#ActiveMQ多节点集群：" class="headerlink" title="ActiveMQ多节点集群："></a>ActiveMQ多节点集群：</h2><h3 id="如何保证MQ高可用？"><a href="#如何保证MQ高可用？" class="headerlink" title="如何保证MQ高可用？"></a><strong>如何保证MQ高可用？</strong></h3><p>集群就完事了</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/zookeeper.jpg" class="">

<p>activemq5.9版本以后，基于zookeeper和leveldb搭建activemq集群 集群仅提供主备方式的高可用集群功能，避免单点故障</p>
<p>zookeeper+replicated-leveldb-store的主从集群</p>
<h3 id="zookeeper与activemq原理"><a href="#zookeeper与activemq原理" class="headerlink" title="zookeeper与activemq原理"></a>zookeeper与activemq原理</h3><p>使用ZooKeeper实现的Master-Slave实现方式，是对ActiveMQ进行高可用的一种有效的解决方案，高可用的原理：使用ZooKeeper（集群）注册所有的ActiveMQ Broker。只有其中的一个Broker可以对外提供服务（也就是Master节点），其他的Broker处于待机状态，被视为Slave。如果Master因故障而不能提供服务，则利用ZooKeeper的内部选举机制会从Slave中选举出一个Broker充当Master节点，继续对外提供服务。</p>
<p>由于zookeeper没学，暂时这里不写了</p>
<h3 id="配置步骤："><a href="#配置步骤：" class="headerlink" title="配置步骤："></a>配置步骤：</h3><p><strong>1、配置的话基本就是将activemq复制3份，然后配置相关端口</strong></p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/zk_port.jpg" class="">



<p><strong>2、 修改管理控制台端口：</strong></p>
<p>配置各个节点的端口，在jetty.xml 配置文件中 </p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/zk_jetty.jpg" class="">



<p><strong>3、修改节点服务器的hostname，统一名称</strong></p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/zk_hostname.jpg" class=""> 



<p><strong>4、在3个节点上的activemq.xml 配置持久化设置 ： zookeeper集群</strong></p>
<p>注意这个bind端口的设置，3台机器对应不同的bind端口</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">persistenceAdapter</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">replicatedLevelDB</span></span></span><br><span class="line"><span class="tag">           <span class="attr">directory</span>=<span class="string">&quot;$&#123;activemq.data&#125;/leveldb&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">replicas</span>=<span class="string">&quot;3&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">bind</span>=<span class="string">&quot;tcp://0.0.0.0:63632&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">zkAddress</span>=<span class="string">&quot;localhost:2191,localhost:2192,localhost:2193&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">hostname</span>=<span class="string">&quot;duanyuefeng-server&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">sync</span>=<span class="string">&quot;local_disk&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">zkPath</span>=<span class="string">&quot;/activemq/leveldb-stores&quot;</span></span></span><br><span class="line"><span class="tag">           /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">persistenceAdapter</span>&gt;</span></span><br></pre></td></tr></table></figure>



<p><strong>5.按照顺序启动3个ActiveMQ节点， 到这步前题是zk集群已经成功启动运行</strong></p>
<p><strong>测试可靠性，kill掉当前master的机器，看zookeeper是否从其他的slave中选择一台作为master</strong></p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/zk_master.jpg" class=""> 



<h2 id="ActiveMQ-高级特性："><a href="#ActiveMQ-高级特性：" class="headerlink" title="ActiveMQ 高级特性："></a>ActiveMQ 高级特性：</h2><h3 id="1、引入消息队列之后该如何保证其高可用性"><a href="#1、引入消息队列之后该如何保证其高可用性" class="headerlink" title="1、引入消息队列之后该如何保证其高可用性"></a>1、引入消息队列之后该如何保证其高可用性</h3><p>zookeeper和leveldb搭建activemq集群</p>
<h3 id="2、异步投递Async-Sends"><a href="#2、异步投递Async-Sends" class="headerlink" title="2、异步投递Async Sends"></a>2、异步投递Async Sends</h3><p>ActiveMQ支持同步、异步两种发送的模式将消息发送到broker，模式的选择对发送延迟有巨大的影响。produce能达到怎样的产出率</p>
<p>（产出率&#x3D;发送数据总量&#x2F;时间）主要受发送延时的影响，使用异步发送可以显著的提高发送的性能。</p>
<p><strong>ActiveMQ默认使用异步发送的模式</strong></p>
<p><strong>同步发送的情景：</strong></p>
<ul>
<li>明确指定使用同步发送的方式</li>
<li>未使用事务的前提下发送持久化的信息</li>
</ul>
<p>如果你没有使用事务且发送的是持久化信息，每一次发送都是同步发送的且会堵塞producer 直到broker返回一个确认，表示消息已经被安全的持久化到磁盘。</p>
<p>确认机制提供了消息安全的保障，但同时会阻塞客户端带来很大的延时。</p>
<p>很多高性能的应用，<strong>允许在失败的情况下有少量的数据丢失</strong>。如果你的应用满足这个特点，你可以使用异步发送来提高生产率，即使发送的是持久化的消息。</p>
<p><strong>异步发送</strong></p>
<p>它可以最大化producer端的发送效率，我们通常在发送消息量比较密集的情况下使用异步发送，可以很大的提升 producer性能。</p>
<p>不过这也带来了额外的问题</p>
<p>就是需要消耗较多的Client端内存同时也会导致broker端性能消耗增加；</p>
<p>此外它不能有效的确保消息的发送成功。在useAsyncSend&#x3D;true的情况下，客户端需要容忍消息丢失的可能。</p>
<p>设置方式</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/async_setting.jpg" class=""> 



<h3 id="3、-那如何确定异步发送成功呢？"><a href="#3、-那如何确定异步发送成功呢？" class="headerlink" title="3、 那如何确定异步发送成功呢？"></a>3、 那如何确定异步发送成功呢？</h3><h4 id="异步发送丢失消息的场景是："><a href="#异步发送丢失消息的场景是：" class="headerlink" title="异步发送丢失消息的场景是："></a><strong>异步发送丢失消息的场景是：</strong></h4><p>生产者设置 UseAsyncSend&#x3D;true,使用producer.send(msg)持续发送消息。</p>
<p>由于消息不阻塞，生产者会认为所有send消息均被成功发送至MQ</p>
<p>如果MQ突然宕机，此时生产者端内存中尚未被发送至MQ的消息 会丢失。</p>
<p>所以，正确的异步发送方法是需要接受回调的。</p>
<h4 id="同步发送和异步发送的区别："><a href="#同步发送和异步发送的区别：" class="headerlink" title="同步发送和异步发送的区别："></a><strong>同步发送和异步发送的区别：</strong></h4><p>同步发送等send不阻塞了就表示一定发送成功了。</p>
<p>异步发送需要接受回执并由客户端再判断一次是否发送成功。</p>
<p>需要写一个消息回调函数进行判断</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">message = session.createTextMessage(<span class="string">&quot;async msg---&quot;</span> + i);</span><br><span class="line">message.setJMSMessageID(UUID.randomUUID().toString() + <span class="string">&quot;--order &quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">msgID</span> <span class="operator">=</span> message.getJMSMessageID();</span><br><span class="line">activeMQMessageProducer.send(message, <span class="keyword">new</span> <span class="title class_">AsyncCallback</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onSuccess</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(msgID + <span class="string">&quot;has been send success!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onException</span><span class="params">(JMSException e)</span> &#123;</span><br><span class="line">        System.out.println(msgID + <span class="string">&quot;fail to end!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>



<h2 id="3、-延时投递和定时投递"><a href="#3、-延时投递和定时投递" class="headerlink" title="3、 延时投递和定时投递"></a>3、 延时投递和定时投递</h2><p> 需开启 activemq.xml中 broker标签的一个属性 schedulerSupport</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/delay_setting.jpg" class="">

<p>示例：</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/delay_example.jpg" class="">

<h3 id="四大属性"><a href="#四大属性" class="headerlink" title="四大属性"></a>四大属性</h3><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/delay_property.jpg" class="">

<h3 id="官网设置的代码"><a href="#官网设置的代码" class="headerlink" title="官网设置的代码"></a>官网设置的代码</h3><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/delay_setting2.jpg" class="">

<p>示例：设置消息延迟3秒发送，重复发送5次，每次隔4秒</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">long</span> <span class="variable">delay</span> <span class="operator">=</span> <span class="number">3</span> * <span class="number">1000</span>;</span><br><span class="line"><span class="type">long</span> <span class="variable">period</span> <span class="operator">=</span> <span class="number">4</span> * <span class="number">1000</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">repeat</span> <span class="operator">=</span> <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">TextMessage</span> <span class="variable">message</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt;= <span class="number">3</span>; i++) &#123;</span><br><span class="line">    message = session.createTextMessage(<span class="string">&quot;delay msg---&quot;</span> + i);</span><br><span class="line">    message.setLongProperty(ScheduledMessage.AMQ_SCHEDULED_DELAY,delay);</span><br><span class="line">    message.setLongProperty(ScheduledMessage.AMQ_SCHEDULED_PERIOD,period);</span><br><span class="line">    message.setIntProperty(ScheduledMessage.AMQ_SCHEDULED_REPEAT,repeat);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<hr>
<h2 id="ActiveMQ的消息重试机制："><a href="#ActiveMQ的消息重试机制：" class="headerlink" title="ActiveMQ的消息重试机制："></a>ActiveMQ的消息重试机制：</h2><h3 id="哪些情况会导致消息重发？"><a href="#哪些情况会导致消息重发？" class="headerlink" title="哪些情况会导致消息重发？"></a>哪些情况会导致消息重发？</h3><ol>
<li>Client用了 transactions 且 在session中调用了 rollback()</li>
<li>Client用了transactions 且在调用commit() 之前关闭或者没有commit</li>
<li>Client在CLIENT_ACKNOWLEDGE的传递模式下，在session中调用了recover()</li>
</ol>
<h3 id="消息时间重发间隔与重发次数？"><a href="#消息时间重发间隔与重发次数？" class="headerlink" title="消息时间重发间隔与重发次数？"></a>消息时间重发间隔与重发次数？</h3><p>间隔： 1</p>
<p>次数： 6</p>
<h3 id="有毒消息-Posion-ACK"><a href="#有毒消息-Posion-ACK" class="headerlink" title="有毒消息 Posion ACK"></a>有毒消息 Posion ACK</h3><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/posion_ack.jpg" class="">

<p>一个消息被redelivedred 超过默认的最大重发次数（6次），消费端会给MQ发送一个 “poison ack”表示这个消息有毒，告诉broker不要再发了，这个时候broker会把这个消息放到DLQ（死信队列）</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/posion_property.jpg" class="">

<p>测试： 消费者开启事务，session不commit，消费7次后（第一次失败+6次重发）</p>
<p>标记为死信队列</p>
<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/activemq_DLQ.jpg" class="">

<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/mysql_DLQ.jpg" class="">

<p>可以自己设置相关属性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ActiveMQConnectionFactory</span> <span class="variable">activeMQConnectionFactory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ActiveMQConnectionFactory</span>(ACTIVEMQ_URL);</span><br><span class="line"></span><br><span class="line"><span class="type">RedeliveryPolicy</span> <span class="variable">redeliveryPolicy</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RedeliveryPolicy</span>();</span><br><span class="line"><span class="comment">// 设置最大重发次数为3次</span></span><br><span class="line">redeliveryPolicy.setMaximumRedeliveries(<span class="number">2</span>);</span><br><span class="line">activeMQConnectionFactory.setRedeliveryPolicy(redeliveryPolicy);</span><br><span class="line"><span class="type">Connection</span> <span class="variable">connection</span> <span class="operator">=</span> activeMQConnectionFactory.createConnection();</span><br><span class="line">connection.start();</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="死信队列："><a href="#死信队列：" class="headerlink" title="死信队列："></a>死信队列：</h2><img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/DLQ_example.jpg" class="">

<img src="/2019/08/25/%E6%B6%88%E6%81%AF%E4%B8%AD%E9%97%B4%E4%BB%B6-ActiveMQ/module_DLQ.jpg" class="">





<h2 id="如何保证消息不被重复消费呢？谈谈幂等性问题。"><a href="#如何保证消息不被重复消费呢？谈谈幂等性问题。" class="headerlink" title="如何保证消息不被重复消费呢？谈谈幂等性问题。"></a>如何保证消息不被重复消费呢？谈谈幂等性问题。</h2><p>给消息一个唯一主键，如果数据库已有该主键，证明重复消费了。</p>
<p>或者是用redis 记录&lt;ID,message&gt;</p>
<p>redis,set(id,message)</p>
<p>set(ID: xx-324234-4234234-1:1:!:!,UUID.randomUUID());</p>
<p>做数据重复检测</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>ActiveMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解ThreadLocal</title>
    <url>/2020/08/30/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3ThreadLocal/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>​         ThreadLocal 不管是在系统的深入使用和一些性能考虑，或者是 线程安全方便都因为线程局部变量的安全隔离特性而被广泛使用。我们可以从很多优秀的框架中看到 ThreadLocal的身影，例如 Spring、Flink、Redis等等。 因为我对其感了兴趣，收集了一些资料，在此做一下总结。</p>
<p>我会从以下几个方面来阐述 ThreadLocal。</p>
<ul>
<li>什么是ThreadLocal？</li>
<li>ThreadLocal的基础知识</li>
<li>ThreadLocal可以用来做什么？</li>
<li>ThreadLocal是如何设计的？</li>
<li>常见的ThreadLocal问题</li>
</ul>
<span id="more"></span> 

<p>申明： 基础知识部分来自 并发编程网 <a href="http://ifeve.com/java-threadlocal%E7%9A%84%E4%BD%BF%E7%94%A8/">http://ifeve.com/java-threadlocal%E7%9A%84%E4%BD%BF%E7%94%A8/</a></p>
<p>常见问题来自 中华石衫博文 <a href="https://mp.weixin.qq.com/s/ZqMkRXKeZ6QU-Imy3maYuQ">https://mp.weixin.qq.com/s/ZqMkRXKeZ6QU-Imy3maYuQ</a></p>
<br/>

<h2 id="什么是ThreadLocal？"><a href="#什么是ThreadLocal？" class="headerlink" title="什么是ThreadLocal？"></a><strong>什么是ThreadLocal？</strong></h2><p>​        在Java中，ThreadLocal是一个类，此类提供线程局部变量，每个线程都有自己的独立的、初始化的变量副本，对于这些局部变量的访问是通过 get&#x2F;set方法来获取。</p>
<p>简单点说，我们平时创建的成员变量是线程不安全的，然而，只要是 ThreadLocal变量，它是属于线程私有的，在多线程环境中，即使多个线程同时访问此变量，其内部也做了隔离,现在可以大致的理解为其内部是一个 HashMap，key为 线程ID，value为 我们所存放的值。 有点类似我们去超市的时候，将自己一些东西放到储物柜里，每个人都有自己专属的条形码和柜子。</p>
<br/>

<h2 id="ThreadLocal的基础知识"><a href="#ThreadLocal的基础知识" class="headerlink" title="ThreadLocal的基础知识"></a><strong>ThreadLocal的基础知识</strong></h2><p><strong>如何创建ThreadLocal变量</strong></p>
<p>以下代码展示了如何创建一个ThreadLocal变量：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="type">ThreadLocal</span> <span class="variable">myThreadLocal</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>(); </span><br></pre></td></tr></table></figure>

<p>我们可以看到，通过这段代码实例化了一个ThreadLocal对象。我们只需要实例化对象一次，并且也不需要知道它是被哪个线程实例化。</p>
<p><strong>虽然所有的线程都能访问到这个ThreadLocal实例，但是每个线程却只能访问到自己通过调用ThreadLocal的set()方法设置的值</strong>。</p>
<p>即使是两个不同的线程在同一个ThreadLocal对象上设置了不同的值，他们仍然无法访问到对方的值。</p>
<br/>



<p><strong>如何访问ThreadLocal变量</strong></p>
<p>一旦创建了一个ThreadLocal变量，你可以通过如下代码设置某个需要保存的值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">myThreadLocal.set(<span class="string">&quot;A thread local value”); </span></span><br></pre></td></tr></table></figure>

<p>可以通过下面方法读取保存在ThreadLocal变量中的值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">threadLocalValue</span> <span class="operator">=</span> (String) myThreadLocal.get(); </span><br></pre></td></tr></table></figure>

<p>get()方法返回一个Object对象，set()对象需要传入一个Object类型的参数。</p>
<br/>

<p><strong>为ThreadLocal指定泛型类型</strong></p>
<p>我们可以创建一个指定泛型类型的ThreadLocal对象，这样我们就不需要每次对使用get()方法返回的值作强制类型转换了。</p>
<p>下面展示了指定泛型类型的ThreadLocal例子：</p>
<p>private ThreadLocal myThreadLocal &#x3D; new ThreadLocal<String>(); </p>
<p>现在我们只能往ThreadLocal对象中存入String类型的值了。</p>
<p>并且我们从ThreadLocal中获取值的时候也不需要强制类型转换了。</p>
<br/>

<p><strong>ThreadLocal有什么特性，可以用来做什么？</strong></p>
<p>​           我们用一个工具来干活，肯定是因为它具备某些特性，能够匹配特定的场景。那么ThreadLocal它的特性有哪些？</p>
<br/>

<p><strong>ThreadLocal只初始化一次变量</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * This method will be invoked the first</span></span><br><span class="line"><span class="comment"> * time a thread accesses the variable with the &#123;<span class="doctag">@link</span> #get&#125;</span></span><br><span class="line"><span class="comment"> * method, unless the thread previously invoked the &#123;<span class="doctag">@link</span> #set&#125;</span></span><br><span class="line"><span class="comment"> * method, in which case the &#123;<span class="doctag">@code</span> initialValue&#125; method will not</span></span><br><span class="line"><span class="comment"> * be invoked for the thread.  Normally, this method is invoked at</span></span><br><span class="line"><span class="comment"> * most once per thread</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;This implementation simply returns &#123;<span class="doctag">@code</span> null&#125;; if the</span></span><br><span class="line"><span class="comment"> * programmer desires thread-local variables to have an initial</span></span><br><span class="line"><span class="comment"> * value other than &#123;<span class="doctag">@code</span> null&#125;, &#123;<span class="doctag">@code</span> ThreadLocal&#125; must be</span></span><br><span class="line"><span class="comment"> * subclassed, and this method overridden.  Typically, an</span></span><br><span class="line"><span class="comment"> * anonymous inner class will be used.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> the initial value for this thread-local</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> T <span class="title function_">initialValue</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>从该</strong>i<strong>nitialValue方法注释中，我们可以得知  正常来说，在一个线程中，这个 initialValue 方法最多只会在第一次调用get方法时执行一次，后续不会再重复执行，并且通常以匿名内部类的方式覆盖实现</strong></p>
<p><strong>这就体现出它的作用，如果 一个线程在处理多处代码逻辑，需要经常创建一些常用辅助对象时，完全可以在 initialValue 方法中 提前初始化，从而避免重复创建对象。</strong></p>
<br/>

<p><strong>举个栗子</strong></p>
<p>​        我们平时系统开发中，如果某块业务的处理逻辑涉及到多个方法的日期处理，那么我们完全可以在 initialValue中创建 SimpleDateFormat 对象，这样我们就不用在每个方法中都创建一个 SimpleDateFormat 对象了，它不香吗？</p>
<p>​       当然SimpleDateFormat 属于小对象，你如果在这个线程的生命周期中都没有涉及大量创建，那确实没有特别明显的优势，如果是一个大对象呢？那么存在线程中只创建一次，还是懒加载，它一定会比 从缓存中或者数据库中去频繁的读取的方式要高效，节省性能开销。</p>
<p>​       又或者是一些框架设计时，如 jdbc连接池，一些持久层框架 如 Hibernate 它就是将 connection存放在ThreadLocal 中，那可真是一次创建，长期使用。</p>
<br/>

<p>我们总结了第一个用处</p>
<p>用处1：对象可以在initialValue只初始化创建一次，适用于业务处理中涉及经常需要使用辅助对象的场景</p>
<br/>

<p><strong>来一个Demo</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span> EiletXie</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Since</span> 2020/8/29 18:06</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadLocalDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String <span class="title function_">dateToStr</span><span class="params">(<span class="type">int</span> millisSeconds)</span> &#123;</span><br><span class="line">        <span class="type">Date</span> <span class="variable">date</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Date</span>(millisSeconds);</span><br><span class="line">        <span class="type">SimpleDateFormat</span> <span class="variable">simpleDateFormat</span> <span class="operator">=</span> ThreadSafeFormatter.dateFormatThreadLocal.get();</span><br><span class="line">        System.out.println(Thread.currentThread().getName());</span><br><span class="line">        <span class="keyword">return</span> simpleDateFormat.format(date);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">ExecutorService</span> <span class="variable">executorService</span> <span class="operator">=</span> Executors.newFixedThreadPool(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">50</span>; i++) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> i;</span><br><span class="line">            executorService.execute(() -&gt; &#123;</span><br><span class="line">                <span class="type">String</span> <span class="variable">date</span> <span class="operator">=</span> dateToStr(j * <span class="number">1000</span>);</span><br><span class="line">                System.out.println(date);</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">        executorService.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ThreadSafeFormatter</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ThreadLocal&lt; SimpleDateFormat &gt; dateFormatThreadLocal = <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">protected</span> SimpleDateFormat <span class="title function_">initialValue</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;yyyy-MM-dd hh:mm:ss&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h3 id="思考一下"><a href="#思考一下" class="headerlink" title="思考一下"></a>思考一下</h3><p>这里demo中，我们使用 newFixedThreadPool 线程池，用 10个线程处理 50个 在阻塞队列中的任务，那么SimpleDateFormat对象最多创建多少个呢？</p>
<img src="/2020/08/30/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3ThreadLocal/%E5%9B%BE1-1.gif" class="" title="图1-1">



<p>没错！ 最多创建10个，因为该线程池最多同时存在10个线程，单个线程处理多个在阻塞队列中的任务，也就意味着 initialValue 最多被调用10次。</p>
<br/>

<p><strong>ThreadLocal最大的特性是什么？</strong></p>
<p>线程私有的局部变量呀，至于怎么做到的，在下一个章节 关于 ThreadLocal的设计原理说明，这里先说说这个特性意味着什么，匹配哪些场景。</p>
<br/>

<p>我在知乎上看到了这样一段话：</p>
<p>当你在一个类中使用 static 成员变量时，一定要问自己这 个 static 成员变量需要考虑“线程安全吗？”（也就是说多个线程需要自己独立的 static 成员变量吗？）如果需要那就需要使用ThreadLocal</p>
<p>其实说的就是 如何将 线程共享的static变量变成 线程私有的场景。因为static修饰的变量是存储在方法区，方法区是线程共享的。</p>
<p>那么在实际生活的系统场景下，用户登录时的session管理，其实也是可以用  ThreadLocal</p>
<p>在当前线程中存储用户信息，处理完当前的部分业务逻辑，再get取出来传递。</p>
<p>这样做的好处就在于减少了将用户信息在不同的场景下传递次数，一定程度降低了代码的耦合性。</p>
<p>（当然复杂的业务逻辑不可能只用一个线程就解决了，所以只能说一定程度。。）</p>
<br/>

<h3 id="总结一下特性"><a href="#总结一下特性" class="headerlink" title="总结一下特性"></a><strong>总结一下特性</strong></h3><p>1、只初始化一次变量，懒加载，减少对象的创建开销</p>
<p>2、线程私有的局部变量，保证线程安全的同时，减少了信息的传递开销，一定程度可以降低代码的耦合性</p>
<p>接下来是重头戏</p>
<br/>

<h2 id="ThreadLocal的设计原理"><a href="#ThreadLocal的设计原理" class="headerlink" title="ThreadLocal的设计原理"></a><strong>ThreadLocal的设计原理</strong></h2><p>要了解其设计思路，我们首先需要观察其内部组成</p>
<p>ThreadLocal中有一个重要的静态内部类 ThreadLocalMap </p>
<p>从源码中我们可以看出 我们的数据是存储在 ThreadLocalMap，Key为 ThreadLocal的hashcode，value为我们存储的对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadLocal</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">ThreadLocalMap</span> &#123;</span><br><span class="line">        <span class="comment">// 注意这里一个 Entry 数组，先思考为什么，下面会给出答案</span></span><br><span class="line">        <span class="keyword">private</span> Entry[] table;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// ThreadLocalMap内部有Entry类，Entry的key是ThreadLocal本身，value是泛型值</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Entry</span> <span class="keyword">extends</span> <span class="title class_">WeakReference</span>&lt;ThreadLocal&lt;?&gt;&gt; &#123;</span><br><span class="line">            Object value;</span><br><span class="line">            Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">                <span class="built_in">super</span>(k);</span><br><span class="line">                value = v;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置 ThreadLocal的hashcode为key，值为我们所存储的对象，默认是 Object类型，除非我们指定类型</span></span><br><span class="line">        ThreadLocalMap(ThreadLocal&lt;?&gt; firstKey, Object firstValue) &#123;</span><br><span class="line">            table = <span class="keyword">new</span> <span class="title class_">Entry</span>[INITIAL_CAPACITY];</span><br><span class="line">            <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> firstKey.threadLocalHashCode &amp; (INITIAL_CAPACITY - <span class="number">1</span>);</span><br><span class="line">            table[i] = <span class="keyword">new</span> <span class="title class_">Entry</span>(firstKey, firstValue);</span><br><span class="line">            size = <span class="number">1</span>;</span><br><span class="line">            setThreshold(INITIAL_CAPACITY);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>那么它是怎么存的，又是如何取的？</p>
<p>先看一下 Thread类下与 ThreadLocalMap 、ThreadLocal的关联</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Thread</span> <span class="keyword">implements</span> <span class="title class_">Runnable</span> &#123;</span><br><span class="line">      ThreadLocal.<span class="type">ThreadLocalMap</span> <span class="variable">threadLocals</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Thread类中存储了一个 ThreadLocal的 ThreadLocalMap变量 </p>
<p>我们来看一下 ThreadLocal的get方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> T <span class="title function_">get</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="literal">null</span>) &#123;</span><br><span class="line">        ThreadLocalMap.<span class="type">Entry</span> <span class="variable">e</span> <span class="operator">=</span> map.getEntry(<span class="built_in">this</span>);</span><br><span class="line">        <span class="keyword">if</span> (e != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">            <span class="type">T</span> <span class="variable">result</span> <span class="operator">=</span> (T)e.value;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> setInitialValue();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ThreadLocalMap <span class="title function_">getMap</span><span class="params">(Thread t)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> t.threadLocals;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//判断1、 设置初始化</span></span><br><span class="line"><span class="keyword">private</span> T <span class="title function_">setInitialValue</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">T</span> <span class="variable">value</span> <span class="operator">=</span> initialValue();</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="literal">null</span>)</span><br><span class="line">        map.set(<span class="built_in">this</span>, value);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        createMap(t, value);</span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 判断2、获取线程 hashcode对应存储的 数据对象</span></span><br><span class="line"><span class="keyword">private</span> Entry <span class="title function_">getEntry</span><span class="params">(ThreadLocal&lt;?&gt; key)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> key.threadLocalHashCode &amp; (table.length - <span class="number">1</span>);</span><br><span class="line">    <span class="type">Entry</span> <span class="variable">e</span> <span class="operator">=</span> table[i];</span><br><span class="line">    <span class="keyword">if</span> (e != <span class="literal">null</span> &amp;&amp; e.get() == key)</span><br><span class="line">        <span class="keyword">return</span> e;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> getEntryAfterMiss(key, i, e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p><strong>get方法步骤总结：</strong></p>
<p>1、获取当前调用 get方法的 线程，这里简称为A</p>
<p>2、通过线程A获取其局部变量 Thread类下的ThreadLocal.ThreadLocalMap map，这个时候做判断</p>
<p>​      如果map为 空，代表着是第一次调用，需要初始化，调用 initialValue() 方法</p>
<p>​     如果map不为空，则 查询 ThreadLocalMap </p>
<p>3、获取线程A的hashcode，来匹配 Entry数组下的index，从而获取对应的ThreadLocal对象，通过ThreadLocal对象获取存储的数据对象</p>
<br/>

<h3 id="思考一下-1"><a href="#思考一下-1" class="headerlink" title="思考一下"></a><strong>思考一下</strong></h3><p><strong>这里抛出一个问题，为什么 ThreadLocalMap 存储的是一个 Entry数组，而不是一个 简单的Entry对象？</strong></p>
<img src="/2020/08/30/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3ThreadLocal/%E5%9B%BE1-1.gif" class="" title="图1-1">



<p>​      因为你业务代码能new好多个ThreadLocal对象，各司其职。但是在一次请求里，也就是一个线程里，ThreadLocalMap是同一个，而不是多个，不管你new几次ThreadLocal。</p>
<p>​      <strong>ThreadLocalMap在一个线程里就一个，因为ThreadLocalMap的引用是在Thread里的</strong>，所以它里面的Entry数组存放的是一个线程里你new出来的多个ThreadLocal对象。</p>
<br/>

<p>至于ThreadLocal的set、remove方法比较简单，这里就不再赘述，大家看一下下面的源码就一目了然</p>
<p><strong>set方法逻辑</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(T value)</span> &#123;</span><br><span class="line">    <span class="type">Thread</span> <span class="variable">t</span> <span class="operator">=</span> Thread.currentThread();</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">map</span> <span class="operator">=</span> getMap(t);</span><br><span class="line">    <span class="keyword">if</span> (map != <span class="literal">null</span>)</span><br><span class="line">        map.set(<span class="built_in">this</span>, value);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        createMap(t, value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">createMap</span><span class="params">(Thread t, T firstValue)</span> &#123;</span><br><span class="line">    t.threadLocals = <span class="keyword">new</span> <span class="title class_">ThreadLocalMap</span>(<span class="built_in">this</span>, firstValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p><strong>remove方法逻辑</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">remove</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">ThreadLocalMap</span> <span class="variable">m</span> <span class="operator">=</span> getMap(Thread.currentThread());</span><br><span class="line">    <span class="keyword">if</span> (m != <span class="literal">null</span>)</span><br><span class="line">        m.remove(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">remove</span><span class="params">(ThreadLocal&lt;?&gt; key)</span> &#123;</span><br><span class="line">    Entry[] tab = table;</span><br><span class="line">    <span class="type">int</span> <span class="variable">len</span> <span class="operator">=</span> tab.length;</span><br><span class="line">    <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> key.threadLocalHashCode &amp; (len-<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">Entry</span> <span class="variable">e</span> <span class="operator">=</span> tab[i];</span><br><span class="line">         e != <span class="literal">null</span>;</span><br><span class="line">         e = tab[i = nextIndex(i, len)]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (e.get() == key) &#123;</span><br><span class="line">            e.clear();</span><br><span class="line">            expungeStaleEntry(i);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再来看一下Thread、ThreadLocal、ThreadLocalMap、Entry之间的相互关联</p>
<img src="/2020/08/30/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3ThreadLocal/%E5%9B%BE1-2.png" class="" title="ThreadLocal组成">

<br/>

<h2 id="其他常见的ThreadLocal问题"><a href="#其他常见的ThreadLocal问题" class="headerlink" title="其他常见的ThreadLocal问题"></a>其他常见的ThreadLocal问题</h2><p>这一部分有点超出我的能力范围了，该部分来自 中华石衫博文 <a href="https://mp.weixin.qq.com/s/ZqMkRXKeZ6QU-Imy3maYuQ">https://mp.weixin.qq.com/s/ZqMkRXKeZ6QU-Imy3maYuQ</a></p>
<p><strong>1、和Synchronized的区别</strong></p>
<p>问：他和线程同步机制（如：Synchronized）提供一样的功能，这个很吊啊。</p>
<p>答：否！同步机制保证的是多线程同时操作共享变量并且能正确的输出结果。ThreadLocal不行啊，他把共享变量变成线程私有了，每个线程都有独立的一个变量。举个通俗易懂的案例：网站计数器，你给变量count++的时候带上synchronized即可解决。ThreadLocal的话做不到啊，他没发统计，他只能说能统计每个线程登录了多少次。</p>
<br/>

<p><strong>2、存储在jvm的哪个区域</strong></p>
<p>问：线程私有，那么就是说ThreadLocal的实例和他的值是放到栈上咯？</p>
<p>答：不是。还是在堆的。ThreadLocal对象也是对象，对象就在堆。只是JVM通过一些技巧将其可见性变成了线程可见。</p>
<br/>

<p><strong>3、真的只是当前线程可见吗</strong></p>
<p>问：真的只是当前线程可见吗？</p>
<p>答：貌似不是，貌似通过InheritableThreadLocal类可以实现多个线程访问ThreadLocal的值，但是我没研究过，知道这码事就行了</p>
<br/>

<p><strong>4、会导致内存泄漏么</strong></p>
<p>问：会导致内存泄漏么？</p>
<p>答：分析一下：</p>
<ul>
<li>1、ThreadLocalMap.Entry的key会内存泄漏吗？</li>
<li>2、ThreadLocalMap.Entry的value会内存泄漏吗？</li>
</ul>
<p>先看下key-value的核心源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Entry</span> <span class="keyword">extends</span> <span class="title class_">WeakReference</span>&lt;ThreadLocal&lt;?&gt;&gt; &#123;</span><br><span class="line">    Object value;</span><br><span class="line">    Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">        <span class="built_in">super</span>(k);</span><br><span class="line">        value = v;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>先看继承关系，发现是继承了弱引用，而且key直接是交给了父类处理super(key)，父类是个弱引用，所以key完全不存在内存泄漏问题，因为他不是强引用，它可以被GC回收的。</p>
<p>弱引用的特点：如果这个对象只被弱引用关联，没有任何强引用关联，那么这个对象就可以被GC回收掉。弱引用不会阻止GC回收。这是jvm知识。</p>
<p>再看value，发现value是个强引用，但是想了下也没问题的呀，因为线程终止了，我管你强引用还是弱引用，都会被GC掉的，因为引用链断了（jvm用的可达性分析法，线程终止了，根节点就断了，下面的都会被回收）。</p>
<p>这么分析一点毛病都没有，但是忘了一个主要的角色，那就是线程池，线程池的存在核心线程是不会销毁的，只要创建出来他会反复利用，生命周期不会结束掉，但是key是弱引用会被GC回收掉，value强引用不会回收，所以形成了如下场面：</p>
<p>Thread-&gt;ThreadLocalMap-&gt;Entry(key为null)-&gt;value</p>
<p>由于value和Thread还存在链路关系，还是可达的，所以不会被回收，这样越来越多的垃圾对象产生却无法回收，造成内存泄漏，时间久了必定OOM。</p>
<br/>

<p>解决方案: ThreadLocal已经为我们想好了，提供了remove()方法，这个方法是将value移出去的。所以用完后记得remove()。</p>
<br/>

<p><strong>5、你学习的开源框架哪些用到了ThreadLocal</strong></p>
<p>Spring框架。</p>
<p>DateTimeContextHolder</p>
<p>RequestContextHolder</p>
<br/>

<p><strong>6、ThreadLocal里的对象一定是线程安全的吗</strong></p>
<p>未必，如果在每个线程中ThreadLocal.set()进去的东西本来就是多线程共享的同一个对象，比如static对象，那么多个线程的ThreadLocal.get()获取的还是这个共享对象本身，还是有并发访问线程不安全问题。</p>
<br/>

<h3 id="笔试题"><a href="#笔试题" class="headerlink" title="笔试题"></a>笔试题</h3><p>最后再来一道笔试题，看看你对 ThreadLocal 了解了没有？</p>
<p>问：下面这段程序会输出什么？为什么？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestThreadLocalNpe</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ThreadLocal&lt;Long&gt; threadLocal = <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">()</span> &#123;</span><br><span class="line">        threadLocal.set(<span class="number">1L</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">long</span> <span class="title function_">get</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> threadLocal.get();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">            set();</span><br><span class="line">            System.out.println(get());</span><br><span class="line">        &#125;).start();</span><br><span class="line">        <span class="comment">// 目的就是为了让子线程先运行完</span></span><br><span class="line">        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">        System.out.println(get());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>答：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> </span><br><span class="line">Exception in thread <span class="string">&quot;main&quot;</span> java.lang.NullPointerException  at com.eiletxie.study.thread.TestThreadLocalNpe.get(TestThreadLocalNpe.java:<span class="number">16</span>)  at com.eiletxie.study.thread.TestThreadLocalNpe.main(TestThreadLocalNpe.java:<span class="number">26</span>) </span><br></pre></td></tr></table></figure>



<br/>

<p>为什么？</p>
<p>为什么输出个1，然后空指针了？</p>
<p>首先输出1是没任何问题的，其次主线程空指针是为什么？</p>
<p>如果你这里回答</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> </span><br><span class="line"><span class="number">1</span> </span><br></pre></td></tr></table></figure>

<p>那我恭喜你，你连ThreadLocal都不知道是啥，这明显两个线程，子线程和主线程。子线程设置1，主线程肯定拿不到啊，ThreadLocal和线程是嘻嘻相关的。这个不多费口舌。</p>
<h4 id="说说为什么是空指针？"><a href="#说说为什么是空指针？" class="headerlink" title="说说为什么是空指针？"></a>说说为什么是空指针？</h4><p>因为你get方法用的long而不是Long，那也应该返回null啊，大哥，long是基本类型，默认值是0，没有null这一说法。ThreadLocal里的泛型是Long，get却是基本类型，这需要拆箱操作的，也就是会执行null.longValue()的操作，这绝逼空指针了。</p>
<br/>

<h3 id="ThreadLocalUtil-工具类"><a href="#ThreadLocalUtil-工具类" class="headerlink" title="ThreadLocalUtil 工具类"></a><strong>ThreadLocalUtil 工具类</strong></h3><p>再提供一个工具类，如果项目中有需要使用ThreadLocal的场景，可以直接使用下面的工具类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.google.common.collect.Maps;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.NamedThreadLocal;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadLocalUtil</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;Map&lt;String, Object&gt;&gt; threadLocal = <span class="keyword">new</span> <span class="title class_">NamedThreadLocal</span>(<span class="string">&quot;xxx-threadlocal&quot;</span>) &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">protected</span> Map&lt;String, Object&gt; <span class="title function_">initialValue</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> Maps.newHashMap();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Map&lt;String, Object&gt; <span class="title function_">getThreadLocal</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> threadLocal.get();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; T <span class="title function_">get</span><span class="params">(String key)</span> &#123;</span><br><span class="line">        <span class="type">Map</span> <span class="variable">map</span> <span class="operator">=</span> threadLocal.get();</span><br><span class="line">        <span class="comment">// todo:copy a new one</span></span><br><span class="line">        <span class="keyword">return</span> (T)map.get(key);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; T <span class="title function_">get</span><span class="params">(String key,T defaultValue)</span> &#123;</span><br><span class="line">        <span class="type">Map</span> <span class="variable">map</span> <span class="operator">=</span> threadLocal.get();</span><br><span class="line">        <span class="keyword">return</span> (T)map.get(key) == <span class="literal">null</span> ? defaultValue : (T)map.get(key);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(String key, Object value)</span> &#123;</span><br><span class="line">        <span class="type">Map</span> <span class="variable">map</span> <span class="operator">=</span> threadLocal.get();</span><br><span class="line">        map.put(key, value);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(Map&lt;String, Object&gt; keyValueMap)</span> &#123;</span><br><span class="line">        <span class="type">Map</span> <span class="variable">map</span> <span class="operator">=</span> threadLocal.get();</span><br><span class="line">        map.putAll(keyValueMap);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">remove</span><span class="params">()</span> &#123;</span><br><span class="line">        threadLocal.remove();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>ThreadLocal</tag>
      </tags>
  </entry>
  <entry>
    <title>聊聊HashMap中的设计</title>
    <url>/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h2 id="聊聊HashMap中的设计"><a href="#聊聊HashMap中的设计" class="headerlink" title="聊聊HashMap中的设计"></a>聊聊HashMap中的设计</h2><p>​             刚开始了解HashMap的时候，知道它底层是 数组 + 链表 + 红黑树的数据结构。最近重新看源码做复习的时候，发现很多写法我可以借鉴，例如变量判断中赋值，取比某个整数大的最小2次幂等等。</p>
<p>所以我这里主要介绍以下几个方面的内容。</p>
<p>1、 HashMap 的底层组成原理，为什么这样设计？</p>
<p>2、HashMap 扩容逻辑和PUT SET方法</p>
<p>3、 如何取一个不小于给定整数的最小二次幂</p>
<span id="more"></span> <br/>

<h2 id="HashMap初步介绍："><a href="#HashMap初步介绍：" class="headerlink" title="HashMap初步介绍："></a>HashMap初步介绍：</h2><p>HashMap 位于java.util包下。继承关系如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HashMap</span>&lt;K,V&gt; <span class="keyword">extends</span> <span class="title class_">AbstractMap</span>&lt;K,V&gt;</span><br><span class="line">    <span class="keyword">implements</span> <span class="title class_">Map</span>&lt;K,V&gt;, Cloneable, Serializable</span><br></pre></td></tr></table></figure>

<p>基于哈希表的实现了Map<code>接口，并允许</code>null的<code>值和</code>null键。 </p>
<br/>

<p>我们需要了解两个重要的参数</p>
<p><strong>initialCapacity</strong> 初始容量 和 <strong>loadFactor</strong> 负载因子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// new HashMap() 时默认初始容量 16</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEFAULT_INITIAL_CAPACITY</span> <span class="operator">=</span> <span class="number">1</span> &lt;&lt; <span class="number">4</span>; <span class="comment">// aka 16</span></span><br><span class="line"><span class="comment">// 负载因子 默认为0.75 当数组中的个数 &gt; 数组大小 * 负载因子 时就会扩容 ,这个 阀门阈值 为 loadFatoar</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">float</span> <span class="variable">DEFAULT_LOAD_FACTOR</span> <span class="operator">=</span> <span class="number">0.75f</span>;</span><br></pre></td></tr></table></figure>



<p>可以看出我们初始化时 HashMap&lt;K,V&gt; map  &#x3D; new HashMap&lt;&gt;()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public HashMap() &#123;</span><br><span class="line">       this.loadFactor = DEFAULT_LOAD_FACTOR; </span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>​         像我们平常使用的就是这种方式，这种方式实际上是不好的，如果在业务场景中hashmap中要插入大量的key-value的话，需要不断的从初始容量扩容多次，这里的原理我聊到  put方法的时候再解释。</p>
<br/>

<h2 id="HashMap的底层组成"><a href="#HashMap的底层组成" class="headerlink" title="HashMap的底层组成"></a>HashMap的底层组成</h2><p>​         HashMap在 JDK 1.7 之前 是 数组 + 链表的组成结构，JDK1.8开始 转变为 数组 + 链表 + 红黑树。</p>
<p>​        这里我列出部分核心的结构代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="comment">// 数组 table 存储了链表头结点</span></span><br><span class="line"><span class="keyword">transient</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  链表节点 Node ,单向链表，保留节点的hash和 key-value</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;K,V&gt; <span class="keyword">implements</span> <span class="title class_">Map</span>.Entry&lt;K,V&gt; &#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="type">int</span> hash;</span><br><span class="line">        <span class="keyword">final</span> K key;</span><br><span class="line">        V value;</span><br><span class="line">        Node&lt;K,V&gt; next;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 红黑树 TreeNode, 注意 这里 TreeNode 和 Node 都继承了 Map接口</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">TreeNode</span>&lt;K,V&gt; <span class="keyword">extends</span> <span class="title class_">LinkedHashMap</span>.Entry&lt;K,V&gt; &#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; parent;  <span class="comment">// red-black tree links</span></span><br><span class="line">        TreeNode&lt;K,V&gt; left;</span><br><span class="line">        TreeNode&lt;K,V&gt; right;</span><br><span class="line">        TreeNode&lt;K,V&gt; prev;    <span class="comment">// needed to unlink next upon deletion</span></span><br><span class="line">        <span class="type">boolean</span> red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>底层组成图:</p>
<img src="/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/%E5%BA%95%E5%B1%82%E7%BB%84%E6%88%90.jpg" class="" title="底层组成">

<br/>

<h3 id="为什么要选择这种底层组成呢？"><a href="#为什么要选择这种底层组成呢？" class="headerlink" title="为什么要选择这种底层组成呢？"></a>为什么要选择这种底层组成呢？</h3><p>​         首先为什么初始选择链表而不是AVL树?</p>
<p>​         这是内存占用与存储桶内查找复杂性之间的权衡。请记住，大多数哈希函数将产生非常少的冲突，因此为大小为3或4的桶维护树将是非常昂贵的，没有充分的理由。</p>
<p>​      作为参考，这是一个HashMap的Java 8 impl（它实际上有一个很好的解释，整个事情如何工作，以及为什么他们选择8和6，作为“TREEIFY”和“UNTREEIFY”阈值）</p>
<p>​     其实这里还涉及一个 泊松分布的知识点，感兴趣的同学可以自行了解一下。        </p>
<p>参考： <a href="https://stackoverflow.com/questions/42422469/why-does-a-hashmap-contain-a-linkedlist-instead-of-an-avl-tree">https://stackoverflow.com/questions/42422469/why-does-a-hashmap-contain-a-linkedlist-instead-of-an-avl-tree</a></p>
<br/>

<p>当链表长度达到8时，转换的是红黑树</p>
<h4 id="为什么选择红黑树而不是-AVL树"><a href="#为什么选择红黑树而不是-AVL树" class="headerlink" title="为什么选择红黑树而不是 AVL树?"></a>为什么选择红黑树而不是 AVL树?</h4><p>RB-Tree和AVL树作为BBST (balance binary search tree 自平衡二叉搜索树)，其实现的算法时间复杂度相同，AVL作为最先提出的BBST，貌似RB-tree实现的功能都可以用AVL树是代替，那么为什么还需要引入RB-Tree呢？</p>
<p>红黑树不追求”完全平衡”，即不像AVL那样要求节点的 <code>|balFact| &lt;= 1</code>，它只要求部分达到平衡，但是提出了为节点增加颜色，红黑是用非严格的平衡来换取增删节点时候旋转次数的降低，任何不平衡都会在三次旋转之内解决，而AVL是严格平衡树，因此在增加或者删除节点的时候，根据不同情况，旋转的次数比红黑树要多。</p>
<ol>
<li><p>就插入节点导致树失衡的情况，AVL和RB-Tree都是最多两次树旋转来实现复衡rebalance，旋转的量级是O(1)<br> 删除节点导致失衡，AVL需要维护从被删除节点到根节点root这条路径上所有节点的平衡，旋转的量级为O(logN)，而RB-Tree最多只需要旋转3次实现复衡，只需O(1)，所以说RB-Tree删除节点的rebalance的效率更高，开销更小！</p>
</li>
<li><p>AVL的结构相较于RB-Tree更为平衡，插入和删除引起失衡，如2所述，RB-Tree复衡效率更高；当然，由于AVL高度平衡，因此AVL的Search效率更高啦。</p>
</li>
<li><p>针对插入和删除节点导致失衡后的rebalance操作，红黑树能够提供一个比较”便宜”的解决方案，降低开销，是对search，insert ，以及delete效率的折衷，总体来说，RB-Tree的统计性能高于AVL.</p>
</li>
<li><p>故引入RB-Tree是<strong>功能、性能、空间开销的折中结果</strong>。<br> 5.1 AVL更平衡，结构上更加直观，时间效能针对读取而言更高；维护稍慢，空间开销较大。<br> 5.2  红黑树，读取略逊于AVL，维护强于AVL，空间开销与AVL类似，内容极多时略优于AVL，维护优于AVL。<br> 基本上主要的几种平衡树看来，<strong>红黑树有着良好的稳定性和完整的功能，性能表现也很不错，综合实力强</strong>，在诸如STL的场景中需要稳定表现。</p>
</li>
</ol>
<img src="/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/RB%E5%92%8CAVL%E6%AF%94%E8%BE%83.jpg" class="" title="RB和AVL比较">

<p>参考: <a href="https://www.jianshu.com/p/37436ed14cc6">https://www.jianshu.com/p/37436ed14cc6</a></p>
<br/>

<h2 id="HashMap的PUT方法"><a href="#HashMap的PUT方法" class="headerlink" title="HashMap的PUT方法"></a>HashMap的PUT方法</h2><p>put方法调用的是putVal 方法，我们分析一下源码</p>
<p>这里要注意，变量在判断的时候都被赋值了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> V <span class="title function_">put</span><span class="params">(K key, V value)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> putVal(hash(key), key, value, <span class="literal">false</span>, <span class="literal">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> V <span class="title function_">putVal</span><span class="params">(<span class="type">int</span> hash, K key, V value, <span class="type">boolean</span> onlyIfAbsent,</span></span><br><span class="line"><span class="params">               <span class="type">boolean</span> evict)</span> &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="type">int</span> n, i;</span><br><span class="line">    <span class="comment">//1、 若 HashMap 未被初始化，则进行初始化操作。</span></span><br><span class="line">    <span class="keyword">if</span> ((tab = table) == <span class="literal">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">        n = (tab = resize()).length;</span><br><span class="line">    <span class="comment">//2、 当对应数组下标为空时，填入节点 hash重新优化使得范围在 0~n-1 (上面判断使得 n = tab.length） </span></span><br><span class="line">    <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="literal">null</span>)</span><br><span class="line">        tab[i] = newNode(hash, key, value, <span class="literal">null</span>);</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 当对应数组下标有链表节点时</span></span><br><span class="line">        Node&lt;K,V&gt; e; K k;</span><br><span class="line">        <span class="comment">// 在第二步判断中，p 被赋值为 hash后对应的下标首节点，这里当首节点与插入的节点的 hash 和 key都一致时，首节点赋值给插入节点 e = p</span></span><br><span class="line">        <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">            ((k = p.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">            e = p;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 当 首节点为红黑树时，检测返回一个新建的树节点赋值给待插入的节点e</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">            e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="built_in">this</span>, tab, hash, key, value);</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 否则 此时朝着链表向下遍历，在尾节点创建新节点 传入要插入的相关值</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">binCount</span> <span class="operator">=</span> <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">                <span class="comment">// 当遍历到链表的尾节点时，注意每次判断都在重新赋值 e = p.next</span></span><br><span class="line">                <span class="keyword">if</span> ((e = p.next) == <span class="literal">null</span>) &#123;</span><br><span class="line">                    p.next = newNode(hash, key, value, <span class="literal">null</span>);</span><br><span class="line">                    <span class="comment">// 判断当前节点的链表长度是否大于 TREEIFY_THRESHOLD 树的临界值 8,大于时进行是否转为红黑树判断</span></span><br><span class="line">                    <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// -1 for 1st</span></span><br><span class="line">                    <span class="comment">//  treeifyBin 会判断tab总节点数如果小于 MIN_TREEIFY_CAPACITY 64时，重新排列resize节点，大于时则将该数组下标的链表转换成红黑树</span></span><br><span class="line">                        treeifyBin(tab, hash);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 这里是判断链表节点是否有一样的 key 和hash值，如果有无需重复插入，直接跳出</span></span><br><span class="line">                <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                    ((k = e.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                p = e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 判断 待插入的节点e 存在于 map时，替换 oldValue (上面的遍历中 如果e的key不存在则创建了节点，存在则指向已知节点)</span></span><br><span class="line">        <span class="keyword">if</span> (e != <span class="literal">null</span>) &#123; <span class="comment">// existing mapping for key</span></span><br><span class="line">            <span class="type">V</span> <span class="variable">oldValue</span> <span class="operator">=</span> e.value;</span><br><span class="line">            <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="literal">null</span>)</span><br><span class="line">                e.value = value;</span><br><span class="line">            afterNodeAccess(e);</span><br><span class="line">            <span class="comment">// 如果是已存在的节点，替换值后直接返回了被替换的新值</span></span><br><span class="line">            <span class="keyword">return</span> oldValue;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ++modCount;</span><br><span class="line">    <span class="comment">// 值得注意的是 到这里来的时候，证明 插入的是新的节点，都会size++进行 一次 threshold 阈值（扩容临界值）的判断</span></span><br><span class="line">    <span class="keyword">if</span> (++size &gt; threshold)</span><br><span class="line">        resize();</span><br><span class="line">    afterNodeInsertion(evict);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h3 id="PUT方法的流程图："><a href="#PUT方法的流程图：" class="headerlink" title="PUT方法的流程图："></a>PUT方法的流程图：</h3><img src="/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/put.jpg" class="" title="put">

<br/>

<h2 id="HashMap的GET方法"><a href="#HashMap的GET方法" class="headerlink" title="HashMap的GET方法"></a>HashMap的GET方法</h2><p>GET 方法相对而言要简单不少，毕竟不涉及数据的存储结构的变化。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title function_">getNode</span><span class="params">(<span class="type">int</span> hash, Object key)</span> &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="type">int</span> n; K k;</span><br><span class="line">    <span class="comment">// 判断 数组是否为空或 对应的下标有无头节点，否则证明无对应key返回null</span></span><br><span class="line">    <span class="keyword">if</span> ((tab = table) != <span class="literal">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">        (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 检查头节点是否和给定节点匹配</span></span><br><span class="line">        <span class="keyword">if</span> (first.hash == hash &amp;&amp; <span class="comment">// always check first node</span></span><br><span class="line">            ((k = first.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">            <span class="keyword">return</span> first;</span><br><span class="line">        <span class="keyword">if</span> ((e = first.next) != <span class="literal">null</span>) &#123;</span><br><span class="line">             <span class="comment">// 当头结点为红黑树类型，进行树的查找</span></span><br><span class="line">            <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line">             <span class="comment">// 否则为 链表节点，直接遍历后续节点直到为空即可</span></span><br><span class="line">            <span class="keyword">do</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                    ((k = e.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                    <span class="keyword">return</span> e;</span><br><span class="line">            &#125; <span class="keyword">while</span> ((e = e.next) != <span class="literal">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h2 id="HashMap的扩容逻辑"><a href="#HashMap的扩容逻辑" class="headerlink" title="HashMap的扩容逻辑"></a>HashMap的扩容逻辑</h2><p>​       当hashmap未赋值时，或者节点达到一定数量时都会在put方法中出发resize扩容方法。我们来分析一下其源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">    <span class="comment">//  注意 这两个参数 oldCap 旧数组的容量大小  oldThr 旧的扩容阀值 = 负载因子 * 旧数组容量大小</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">oldCap</span> <span class="operator">=</span> (oldTab == <span class="literal">null</span>) ? <span class="number">0</span> : oldTab.length;</span><br><span class="line">    <span class="type">int</span> <span class="variable">oldThr</span> <span class="operator">=</span> threshold;</span><br><span class="line">    <span class="type">int</span> newCap, newThr = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (oldCap &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">            threshold = Integer.MAX_VALUE;</span><br><span class="line">            <span class="keyword">return</span> oldTab;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//1、 设定 newCap 新数组大小为 旧数组大小的两倍 （这里发现 当新容量大于 2的30次方时，这里未做处理 在下方的 2步骤进行重新赋值newThr）</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp;</span><br><span class="line">                 oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">            newThr = oldThr &lt;&lt; <span class="number">1</span>; <span class="comment">// double threshold</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (oldThr &gt; <span class="number">0</span>) <span class="comment">// 初始容量置于阈值</span></span><br><span class="line">        newCap = oldThr;</span><br><span class="line">    <span class="keyword">else</span> &#123;              </span><br><span class="line">        <span class="comment">// 旧数组的容量和 扩容的阈值都为0  代表是 new HashMap() 这种情况开始赋值给newCap和newThr,可以回看 put方法 判断tab是否为空时调用的就是 resize给tab赋值</span></span><br><span class="line">        newCap = DEFAULT_INITIAL_CAPACITY;</span><br><span class="line">        newThr = (<span class="type">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (newThr == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 2、 发生newThr为空, 可能是 newCap大于限定最大值 MAXIMUM_CAPACITY ,这里赋值 newThr为  Integer.MAX_VALUE</span></span><br><span class="line">        <span class="type">float</span> <span class="variable">ft</span> <span class="operator">=</span> (<span class="type">float</span>)newCap * loadFactor;</span><br><span class="line">        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="type">float</span>)MAXIMUM_CAPACITY ?</span><br><span class="line">                  (<span class="type">int</span>)ft : Integer.MAX_VALUE);</span><br><span class="line">    &#125;</span><br><span class="line">    threshold = newThr;</span><br><span class="line">    <span class="meta">@SuppressWarnings(&#123;&quot;rawtypes&quot;,&quot;unchecked&quot;&#125;)</span></span><br><span class="line">        Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="keyword">new</span> <span class="title class_">Node</span>[newCap];</span><br><span class="line">    table = newTab;</span><br><span class="line">    <span class="keyword">if</span> (oldTab != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 遍历 旧数组</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; oldCap; ++j) &#123;</span><br><span class="line">            Node&lt;K,V&gt; e;</span><br><span class="line">            <span class="comment">// 当旧数组对应下标的头节点不为空时</span></span><br><span class="line">            <span class="keyword">if</span> ((e = oldTab[j]) != <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="comment">// 将 oldTab[j]置为 null，加快JVM回收</span></span><br><span class="line">                oldTab[j] = <span class="literal">null</span>;</span><br><span class="line">                <span class="comment">// 如果头节点无下一节点时 直接重新 计算下标  e.hash &amp; (newCap - 1) 进行赋值</span></span><br><span class="line">                <span class="keyword">if</span> (e.next == <span class="literal">null</span>)</span><br><span class="line">                    newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">                <span class="comment">// 注意 这时候的节点 为红黑树 并且 e.next不为空时，我们拆分头节点，并进行自平衡，由于篇幅关系，刚兴趣的同志可以看看split方法中红黑树的remove节点逻辑</span></span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                    ((TreeNode&lt;K,V&gt;)e).split(<span class="built_in">this</span>, newTab, j, oldCap);</span><br><span class="line">                <span class="keyword">else</span> &#123; <span class="comment">// preserve order</span></span><br><span class="line">                <span class="comment">// 只剩最后一种情况了，是链表，并且 e.next不为空 这里涉及一个 下标重排的巧妙优化，我会在下面讲解</span></span><br><span class="line">                    Node&lt;K,V&gt; loHead = <span class="literal">null</span>, loTail = <span class="literal">null</span>;</span><br><span class="line">                    Node&lt;K,V&gt; hiHead = <span class="literal">null</span>, hiTail = <span class="literal">null</span>;</span><br><span class="line">                    Node&lt;K,V&gt; next;</span><br><span class="line">                    <span class="keyword">do</span> &#123;</span><br><span class="line">                        next = e.next;</span><br><span class="line">                        <span class="comment">// 这句话是关键，下面会详细解释这个公式</span></span><br><span class="line">                        <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">                            <span class="keyword">if</span> (loTail == <span class="literal">null</span>)</span><br><span class="line">                                loHead = e;</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                loTail.next = e;</span><br><span class="line">                            loTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">else</span> &#123;</span><br><span class="line">                            <span class="keyword">if</span> (hiTail == <span class="literal">null</span>)</span><br><span class="line">                                hiHead = e;</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                hiTail.next = e;</span><br><span class="line">                            hiTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">while</span> ((e = next) != <span class="literal">null</span>);</span><br><span class="line">                    <span class="keyword">if</span> (loTail != <span class="literal">null</span>) &#123;</span><br><span class="line">                        loTail.next = <span class="literal">null</span>;</span><br><span class="line">                        newTab[j] = loHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (hiTail != <span class="literal">null</span>) &#123;</span><br><span class="line">                        hiTail.next = <span class="literal">null</span>;</span><br><span class="line">                        newTab[j + oldCap] = hiHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> newTab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>扩容时涉及链表节点的下标分配的问题，旧数组的下标和新数组的下标有什么关联吗？ 这个需要从下面这个公式讲起。</p>
<br/>

<h3 id="理解-e-hash-oldCap-0"><a href="#理解-e-hash-oldCap-0" class="headerlink" title="理解 (e.hash &amp; oldCap) &#x3D;&#x3D; 0"></a>理解 (e.hash &amp; oldCap) &#x3D;&#x3D; 0</h3><p>在讲解这个公式之前，我们需要重温下面这几个知识点</p>
<ul>
<li>旧数组的容量大小是 2的 m次方, 这里我假设是 4次方 ，16大小，后面会以这个大小示例</li>
<li>新数组是旧数组的容量大小的两倍</li>
<li>put方法中 e.hash &amp; (n-1) 实际上求的是 hash的后m位,即对于的旧数组下标</li>
</ul>
<br/>

<p>值得注意的是，我们可以确定 在同一个旧数组下标下的所有节点，它们的hash后m位是一致（数组容量是 2的m次方） 这里 方便解释指定 m&#x3D;4。</p>
<p>为什么？ 如果不一致 那么 e.hash &amp; (n-1) 就不可能在同一个下标下。</p>
<p>这里假设它们在该下标下的 hash后4位为 xxxx</p>
<p>这里我们可以看下图推演</p>
<img src="/2019/12/08/%E8%81%8A%E8%81%8AHashMap%E4%B8%AD%E7%9A%84%E8%AE%BE%E8%AE%A1/hash.jpg" class="" title="hash">

<br/>

<p>那在旧数组的指定下标下的这些节点在 新数组下对应的下标计算就是 </p>
<p>e.hash &amp; (2n - 1)  </p>
<p>它与 旧数组下标计算 hash &amp; (n - 1) 有什么区别呢？</p>
<p>它多保留了一位，它是保留了节点hash值后5位了，所以出现了两种情况,这里hash长度总共32位，我简写只保留后8位，前面都因为在 &amp; 数组容量 操作中都为0了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">0000</span> xxxx</span><br><span class="line"><span class="number">0001</span> xxxx</span><br></pre></td></tr></table></figure>

<p>注意 二进制 x不是为0,就是为1</p>
<p>所以发现没有，旧数组某个下标下的所有节点Node 在分配到新数组，重新计算下标时,出现了两种情况。</p>
<p>一种是 0000 xxxx 这种情况 和旧数组的下标  xxxx是一致的，我们用链表A保存起来</p>
<p>另外一种 0001 xxxx 这种情况我们需要推演一下</p>
<p>注意： 我们这里假设旧数组的容量是 2的4次方 16</p>
<p>1 xxxx &#x3D; xxxx + 10000 &#x3D; 旧数组下标 + 旧数组容量 oldCap</p>
<p>所以第二种情况 在新数组的下标 &#x3D; 旧数组的下标 + 旧数组的容量 oldCap</p>
<br/>

<p>那我们如何判断在 旧数组某下标下的所有节点， 它们是属于哪种情况呢？</p>
<p>我们只需要计算 倒数第5位是 0,还是1不就行了吗？</p>
<p> hash &amp; 1 0000 &#x3D;&gt;  hash &amp; oldCap &#x3D;&#x3D; 0 ?</p>
<p>所以现在我们能理解这个公式了吗？</p>
<p><strong>(e.hash &amp; oldCap) &#x3D;&#x3D; 0</strong></p>
<br/>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>当 (e.hash &amp; oldCap) &#x3D;&#x3D; 0 时，证明节点Node 在 新数组下标 和旧数组的下标一致，我们用链表A保存</p>
<p>否则 证明 节点Node在 新数组的下标 &#x3D; 旧数组下标 + oldCap一样。</p>
<p>所以现在我们可以很轻松的明白 resize方法中的最后一个判断了吧。</p>
<p>不得不说 写这段代码的大佬太牛了，短短一句话包含这么多逻辑。</p>
<br/>

<h2 id="HashMap如何取不小于给定整数的最小二次幂"><a href="#HashMap如何取不小于给定整数的最小二次幂" class="headerlink" title="HashMap如何取不小于给定整数的最小二次幂"></a>HashMap如何取不小于给定整数的最小二次幂</h2><p>   这就要聊聊 HashMap中的 <strong>tableSizeFor</strong> 方法了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Returns a power of two size for the given target capacity.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">tableSizeFor</span><span class="params">(<span class="type">int</span> cap)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> cap - <span class="number">1</span>;</span><br><span class="line">    n |= n &gt;&gt;&gt; <span class="number">1</span>;</span><br><span class="line">    n |= n &gt;&gt;&gt; <span class="number">2</span>;</span><br><span class="line">    n |= n &gt;&gt;&gt; <span class="number">4</span>;</span><br><span class="line">    n |= n &gt;&gt;&gt; <span class="number">8</span>;</span><br><span class="line">    n |= n &gt;&gt;&gt; <span class="number">16</span>;</span><br><span class="line">    <span class="keyword">return</span> (n &lt; <span class="number">0</span>) ? <span class="number">1</span> : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该方法做到了，返回一个不小于给定整数的最小二次幂</p>
<br/>

<p>先来分析有关n位操作部分：先来假设n的二进制为01xxx…xxx。接着</p>
<p>对n右移1位：001xx…xxx，再位或 | ：011xx…xxx</p>
<p>对n右移2为：00011…xxx，再位或 | ：01111…xxx</p>
<p>此时前面已经有四个1了，再右移4位且位或可得8个1</p>
<p>同理，有8个1，右移8位肯定会让后八位也为1。</p>
<p><strong>综上可得，该算法让最高位的1后面的位全变为1。</strong></p>
<br/>

<p>最后再让结果n+1，即得到了2的整数次幂的值了。</p>
<p>现在回来看看第一条语句：</p>
<p>int n &#x3D; cap - 1; </p>
<p>　　让cap-1再赋值给n的目的是另找到的目标值大于或等于原值。例如二进制1000，十进制数值为8。如果不对它减1而直接操作，将得到答案10000，即16。显然不是结果。减1后二进制为111，再进行操作则会得到原来的数值1000，即8。</p>
<br/>

<h2 id="个人体会："><a href="#个人体会：" class="headerlink" title="个人体会："></a>个人体会：</h2><p>​         HashMap等Java源码在判断和数组扩容等场景下，大量使用了位运算来保证代码整洁和执行的高效性，我们在操作数组的部分业务场景中完全可以学习这种思路，包括判断中赋值，取得最小二次幂等等，还是得多刷点位运算的算法题来提升自己这种简洁代码的能力。</p>
]]></content>
      <tags>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解之Hystrix</title>
    <url>/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/</url>
    <content><![CDATA[<p>Hystrix是 SpringCloud中的一个组件，它能起到了断路器的作用。</p>
<h2 id="Hystrix的基本介绍"><a href="#Hystrix的基本介绍" class="headerlink" title="Hystrix的基本介绍"></a>Hystrix的基本介绍</h2><h3 id="它解决了什么问题？"><a href="#它解决了什么问题？" class="headerlink" title="它解决了什么问题？"></a>它解决了什么问题？</h3><p>​       当服务之间调用可能因为某个服务的堵塞或服务器问题使得其他需要消费该服务的其他服务请求处于等待，由于服务之间是相互调用的，会导致大量请求进程挂起进而导致整个系统CPU飙升进而崩溃的局面。</p>
<p>​       而断路器是作用是做到 当一个服务单元发生故障，通过故障监控，向调用方返回一个错误响应，而不是长时间等待，使得线程不会因故障长时间不释放，避免故障在服务间蔓延。</p>
<br/>
<span id="more"></span>

<h3 id="hystrix基本配置"><a href="#hystrix基本配置" class="headerlink" title="hystrix基本配置"></a>hystrix基本配置</h3><p>hystrix默认超时时间 2000毫秒</p>
<p>原生组件 Hystrix</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.netflix.hystrix<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hystrix-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.8<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>



<p>当然 SpringCloud 也集成了Hystrix</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span>     <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span>     <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-netflix-hystrix<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span> </span><br></pre></td></tr></table></figure>



<br/>

<p>在服务消费者的主类OrderApplication上使用 @EnableCircuitBreaker注解开启断路器功能</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableDiscoveryClient</span></span><br><span class="line"><span class="meta">@EnableCircuitBreaker</span></span><br><span class="line"><span class="meta">@ComponentScan(basePackages = &quot;com.eiletxie&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">      SpringApplication.run(OrderApplication.class, args);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>其实可以直接使用 @SpringCloudApplication 注解来修饰主类，因为它包含上面的这3个注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target(&#123;ElementType.TYPE&#125;)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Inherited</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableDiscoveryClient</span></span><br><span class="line"><span class="meta">@EnableCircuitBreaker</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SpringCloudApplication &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<br/>

<h3 id="设置函数降级策略"><a href="#设置函数降级策略" class="headerlink" title="设置函数降级策略"></a>设置函数降级策略</h3><p><strong>@HystrixCommand(fallbackMethod &#x3D; “降级函数名”)</strong></p>
<p>我们可以在 controller层或者service层对方法增加 @HystrixCommand 注解来指定回调方法，也就是常说的降级策略</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@HystrixCommand(fallbackMethod = &quot;fallback&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/getProductInfoList&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">getProductInfoList</span><span class="params">(<span class="meta">@RequestParam(&quot;number&quot;)</span> Integer number)</span> &#123;</span><br><span class="line"></span><br><span class="line">   <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RestTemplate</span>();</span><br><span class="line">   <span class="keyword">return</span> restTemplate.postForObject(<span class="string">&quot;http://127.0.0.1:8005/product/listForOrder&quot;</span>,</span><br><span class="line">         Arrays.asList(<span class="string">&quot;157875196366160022&quot;</span>),</span><br><span class="line">         String.class);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> String <span class="title function_">fallback</span><span class="params">()</span> &#123;</span><br><span class="line">   <span class="keyword">return</span> <span class="string">&quot;太拥挤了, 请稍后再试~~&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="服务熔断参数设置"><a href="#服务熔断参数设置" class="headerlink" title="服务熔断参数设置"></a>服务熔断参数设置</h3><p>当然你也可以对函数调用的具体相关参数做熔断的条件判断</p>
<p>Hystrix默认超时时间为2000毫秒</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@HystrixCommand(commandProperties = &#123;</span></span><br><span class="line"><span class="meta">      @HystrixProperty(name = &quot;circuitBreaker.enabled&quot;, value = &quot;true&quot;),              //设置熔断</span></span><br><span class="line"><span class="meta">      @HystrixProperty(name = &quot;circuitBreaker.requestVolumeThreshold&quot;, value = &quot;10&quot;),    //请求数达到后才计算</span></span><br><span class="line"><span class="meta">      @HystrixProperty(name = &quot;circuitBreaker.sleepWindowInMilliseconds&quot;, value = &quot;10000&quot;), //休眠时间窗</span></span><br><span class="line"><span class="meta">      @HystrixProperty(name = &quot;circuitBreaker.errorThresholdPercentage&quot;, value = &quot;60&quot;),  //错误率</span></span><br><span class="line"><span class="meta">&#125;)</span></span><br></pre></td></tr></table></figure>

<br/>

<p>接下来就是重头戏了</p>
<p>我们通过官方给的流程图来了解 请求调用其他服务时 Hystrix如何工作的。</p>
<h2 id="Hystrix工作流程"><a href="#Hystrix工作流程" class="headerlink" title="Hystrix工作流程"></a>Hystrix工作流程</h2><img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/hystrix_flow.jpg" class="" title="Hystrix流程图">

<p>我们根据流程图来分析每一个节点</p>
<h3 id="1、-创建-HystrixCommand-或-HystrixObservableCommand-对象"><a href="#1、-创建-HystrixCommand-或-HystrixObservableCommand-对象" class="headerlink" title="1、 创建 HystrixCommand 或 HystrixObservableCommand 对象"></a>1、 创建 HystrixCommand 或 HystrixObservableCommand 对象</h3><p>这两个对象是用来干嘛的呢？ </p>
<p>​       用来 封装请求（对其他服务调用的请求），它包含了所有需要传递的参数。这两个Command对象分别针对不同的应用场景，都使用到了设计模式中的命令模式。</p>
<blockquote>
<ul>
<li>HystrixCommand ： 用从依赖的服务返回单个操作结果的时候。</li>
<li>HystrixObservableCommand: 用在依赖的服务返回多个操作结果的时候。</li>
</ul>
</blockquote>
<br/>

<p>observable可观察的</p>
<p>这里可以先看菜鸟教程的命令模式简介 <a href="https://www.runoob.com/design-pattern/command-pattern.html">https://www.runoob.com/design-pattern/command-pattern.html</a></p>
<h4 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h4><p>命令模式（Command Pattern）是一种数据驱动的设计模式，它属于行为型模式。</p>
<p>将一个请求封装为一个对象，从而使你可用不同的请求对客户端进行参数化，对请求排队或记录请求日志，以及支持可撤销的操作。</p>
<p>简单来说： 命令模式将来自客户端的请求封装成一个对象，从而让你使用不同的请求对客户端进行参数化，它的最大好处是 行为请求者 与 行为实现者 之间 的解耦。</p>
<p><strong>个人写了一篇对于命令模式的描述,感兴趣的朋友可以看看 -&gt;</strong>   <a href="http://www.eiletxie.cn/2019/12/25/%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/"><strong>命令模式</strong></a></p>
<br/>

<p><strong>为什么这里用到了命令模式呢？</strong></p>
<p>个人理解：  我们思考一下，在controller或service层中，服务消费者调用了 服务提供者的相关接口，同时我们使用了hystrix，也就是类似 @HystrixCommand(fallbackMethod &#x3D; “fallback”) 这种注解来对请求做了一定的限定，要求 这个请求（请求服务提供方的API）的调用过程要符合在我们给定的参数范围内，例如： 请求时间不能超过3秒，失败后重复请求次数不能超过5次、以及失败后执行哪个回调函数。 我们通过命令模式这种设计思想，将每个服务调用的请求封装成命令。当服务消费端 执行该命令，command命令底层再调用相关的服务提供方，让服务之间实现了解耦。</p>
<p>​         并且因为将请求封装成命令，那么它可以做到记录日志（将命令执行次数与我们设定的参数做对比）、撤销操作，以及高并发时用队列存放起来排队执行。</p>
<br/>

<h3 id="2、命令执行"><a href="#2、命令执行" class="headerlink" title="2、命令执行"></a>2、命令执行</h3><p>从上图中，我们可以看到一共存在 4种命令的执行方式，Hystrix在执行时会根据创建的Command对象和具体情况来选择一个执行。</p>
<ul>
<li>HystrixCommand实现的两种执行方式</li>
</ul>
<blockquote>
<p>execute()： 同步执行，从依赖的服务返回一个单一的结果对象，或是在发生错误的时候抛出异常。<br>queue(): 异步执行，直接返回一个Future对象，其中包含了服务执行结束时要返回的单一结果对象。<br>R value &#x3D; command.execute();<br>Future fValue &#x3D; command.queue();</p>
</blockquote>
<ul>
<li>HystrixObservableCommand的两种执行方式</li>
</ul>
<blockquote>
<p>observe(): 返回 Observable 对象，它代表操作的多个结果，它是一个 Hot Observale<br>toObserve(): 同样返回Observable 对象，它也代表操作的多个结果，但是它返回的是一个 Cold Observable</p>
</blockquote>
<p>值得一提的是，Hystrix的底层实现大量使用了 RXJava，为了后续的讲解，这里大概讲一下 RXJava主要用到的 观察者-订阅者模式</p>
<p> 观察者-订阅者模式： 一看到观察-订阅，就让我们想到了 消息中间件的Topic订阅。</p>
<br/>

<h4 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h4><p>在软件设计中是一个对象，维护一个依赖列表，当任何状态发生改变自动通知它们。</p>
<p>例如： B站中你关注了几个up主，当关注的up主更新时，会第一时间通知所有关注了他的人。</p>
<p>这里的 up主是 Subject，而作为观众的你是 Observe</p>
<p><strong>思考： 这个状态变化的监测和通知的过程是怎么实现的？</strong></p>
<p>​      其实不难，在一些教程中，它们的实现思路就是 订阅的主题 subject中有一个 List<Observer>列表来存放所有的观察者Observer的实现类，而Observer实现了一个统一的接口，该接口规定要实现一个 update方法。</p>
<p>​        所以当subject主题状态更新，只需要遍历观察者列表，执行update接口方法来调用具体的实现类的状态通知。</p>
<br/>

<h4 id="发布-订阅设计模式"><a href="#发布-订阅设计模式" class="headerlink" title="发布-订阅设计模式"></a>发布-订阅设计模式</h4><p>与观察者模式基本差不多，区别在哪呢？ 中间多了一层中间消息件一样的东西</p>
<p>例如： 你订阅了腾讯新闻的财经频道，然后你可以收到很多作者写的新闻，但是作者是将新闻发布到财经频道，你是在频道中看到新闻，你和作者之间没有关联。</p>
<p>在发布-订阅模式，消息的发送方，叫做<strong>发布者（publishers）</strong>，消息不会直接发送给特定的接收者，叫做<strong>订阅者</strong>。</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/pub_sub.jpg" class="" title="发布-订阅">

<br/>

<p><strong>总结一下两模式的区别</strong></p>
<ul>
<li><strong>观察者模式</strong>大多数时候是<strong>同步</strong>的，比如当事件触发，Subject就会去调用观察者的方法。而<strong>发布-订阅</strong>模式大多数时候是<strong>异步的</strong>（使用消息队列）。</li>
<li>在<strong>观察者</strong>模式中，观察者是知道Subject的，Subject一直保持对观察者进行记录。然而，在<strong>发布订阅</strong>模式中，发布者和订阅者<strong>不知道对方的存在</strong>。它们只有通过消息代理进行通信。</li>
<li>在<strong>发布订阅</strong>模式中，组件是松散耦合的，正好和观察者模式相反</li>
</ul>
<p>代入hystrix来看，使用了hystrix的服务消费方开启了一个观察监控，监控请求对应的服务提供方的响应，如果正常响应则直接获取响应数据来处理消费方的业务逻辑。如果失败就根据对应用户设置的请求参数判断是否开启断路器。</p>
<p><strong>这时候来看看 Hystrix中 Hot</strong> <strong>Observable  和 Cold Observable  这两个概念</strong></p>
<ul>
<li>Hot Observable 是无论 不管“事件源” 是否有 订阅者，都会创建后对事件进行发布，所以它应该对应的是 发布订阅模式,它只能看到部分过程。</li>
<li>Cold Observable  则是在 没有“订阅者”的时候并不会发布事件，而是进行等待，直到有“订阅者”之后才发布事件，这样它可以查看到整个过程，它这种同步的方式对应的是 观察者模式。</li>
</ul>
<br/>

<h3 id="3、结果是否被缓存"><a href="#3、结果是否被缓存" class="headerlink" title="3、结果是否被缓存"></a>3、结果是否被缓存</h3><p> 若当前命令的请求缓存功能是否被开启的，并且该命令缓存命中，那么缓存的结果会立即以Observable 对象的形式返回。</p>
<p>这一步是比较好理解的，由于可能发生网络中断或者用户重复点击等请求数据重复获取的情况，将命令对象存放在 线程池中，先检验是否缓存命中，再发起网络请求，效率会有一定提升，但是由于请求的参数和时间差异,应用面较小。</p>
<h3 id="4、断路器是否打开"><a href="#4、断路器是否打开" class="headerlink" title="4、断路器是否打开"></a>4、断路器是否打开</h3><p>   在命令结果没有缓存命中的时候，Hystrix在执行命令前需要检查断路器是否为打开状态：</p>
<ul>
<li>如果断路器是打开的，那么Hystrix不会执行命令，而是转接到fallback处理逻辑（防止服务之间请求堆积）</li>
<li>如果断路器是关闭的，那么Hystrix会跳到第5步，检查是否有可用资源来执行命令。</li>
</ul>
<br/>

<h3 id="5、线程池-请求队列-信号量是否占满"><a href="#5、线程池-请求队列-信号量是否占满" class="headerlink" title="5、线程池&#x2F;请求队列&#x2F;信号量是否占满"></a>5、线程池&#x2F;请求队列&#x2F;信号量是否占满</h3><p>​       当与命令相关的线程池和请求队列，或者信号量（不使用线程池的时候）已经被占满，那么 Hystrix 也不会执行命令，而是转接到 fallback处理逻辑。</p>
<p>值得一提的是，这里的线程池不是我们认为的一个大的服务消费者的线程池，而是针对每个请求对应的依赖服务创建的专有线程池。 并且 Hystrix为了保证不会因为某个依赖服务的问题影响其他依赖服务而采用了 <strong>“舱壁模式”</strong> 来隔离每个依赖的服务。</p>
<br/>

<h3 id="6、HystrixObservableCommand-construct-或-HystrixCommand-run"><a href="#6、HystrixObservableCommand-construct-或-HystrixCommand-run" class="headerlink" title="6、HystrixObservableCommand.construct() 或 HystrixCommand.run()"></a>6、HystrixObservableCommand.construct() 或 HystrixCommand.run()</h3><p>Hystrix 会根据我们编写的方法来决定采用什么的方式去请求依赖服务。</p>
<ul>
<li>HystrixCommand.run()：返回单一的结果，或者抛出异常。</li>
<li>HystrixObservableCommand.construct() ： 返回一个Observable对象来发射多个结果，或通过 onError发送错误通知。</li>
</ul>
<p>​     如果run()或 construct() 方法执行时间超过了 命令设置的超时阈值，当前处理的线程将抛出一个 TimeoutException。在这种情况下，Hystrix会转接到fallback处理逻辑。</p>
<p>​    如果命令正常执行返回了结果，那么Hystrix会记录一些相关的日志并采集监控报告之后将该结果返回。这就是我们看到了那个 Hystrix-DashBoard，也称为仪表盘，豪猪监控界面了 。</p>
<br/>

<h3 id="7、计算断路器的健康度"><a href="#7、计算断路器的健康度" class="headerlink" title="7、计算断路器的健康度"></a>7、计算断路器的健康度</h3><p>​     Hystrix会将“成功”、“失败”、“拒绝”、“超时”等信息报告给断路器，而断路器会维护一组计数器来统计这些数据。</p>
<p>​     断路器会使用这些统计数据来决定是否将断路器打开，来对某个依赖服务的请求进行 ”熔断&#x2F;短路“，直到恢复期结束。若在恢复期结束后，根据统计数据判断如果还未到健康指标，就会再次”熔断&#x2F;短路“</p>
<br/>

<h3 id="8、fallback处理"><a href="#8、fallback处理" class="headerlink" title="8、fallback处理"></a>8、fallback处理</h3><p>   当命令执行失败时，Hystrix会进入 fallback尝试回调处理，该操作被称为 “服务降级”，引起服务降级处理的情况有下面几种</p>
<ul>
<li>第4步： 当前命令处于 “熔断&#x2F;短路”状态，并且断路器是打开的状态。</li>
<li>第5步： 当前命令的线程池、请求队列或者信号量被占满的时候。</li>
<li>第6步：HystrixObservableCommand.construct() 或 HystrixCommand.run() 抛出 异常的时候</li>
</ul>
<p> 需要说明的是：在服务降级的逻辑中，我们需要一个通用的响应结果，并且这个结果的处理逻辑应该是从缓存或一些静态逻辑中获取，大部分情况指的就是服务自身的方法，而不是通过网络请求来获取，如果是通过网络请求，那么该请求也必须被 HystrixCommand或HystrixObservableCommand申明，形成一个级联的 降级策略，但最终的降级逻辑一定不是通过网络请求的处理，而是一个能稳定返回结果的处理逻辑。</p>
<p>​    当我们没有为命令实现降级逻辑或者降级处理逻辑中抛出了异常，Hystrix依然会返回一个 Observable 对象，但是它不会产生任何结果，而是通过onError方法通知命令立即中断请求，并通过onError方法将引发命令失败的异常发送给调用者。</p>
<p>​    当降级执行发现失败的时候，Hystrix会根据不同的执行方法做出不同的处理（注意： 是<strong>降级处理的过程中发生异常</strong>）。</p>
<ul>
<li>execute(): 抛出异常</li>
<li>queue(): 正常返回Future对象，但是当调用 get() 来获取结果的时候会抛出异常。</li>
<li>observe(): 正常返回 Observable对象，当订阅它的时候，将立即通过调用 onError方法来通知终止请求。</li>
<li>toObservable(): 正常返回 Observable对象，当订阅它的时候，将立即通过调用 onError方法来通知终止请求。</li>
</ul>
<br/>

<h3 id="9、返回成功的响应"><a href="#9、返回成功的响应" class="headerlink" title="9、返回成功的响应"></a>9、返回成功的响应</h3><p>​    当Hystrix命令执行成功之后，它会将处理结果直接返回或是以Observable的形式返回。而具体以哪种方式返回取决于之前第2步中我们所提到的对命令的4种不同执行方式，下图总结了这4种调用方式之间的依赖关系。</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/success_return.jpg" class="" title="成功的响应">

<ul>
<li>toObservable(): 返回最原始的 Observable() ,必须通过订阅它才会真正触发命令的执行流程。</li>
<li>observe(): 在 toObservable() 产生原始 Observable 之后立即订阅它，让命令能够马上开始异步执行，并返回一个Observable对象，当调用它的subscribe时，将重新产生结果和通知给订阅者。</li>
<li>queue(): 将toObservable() 产生的原始Observable通过 toBlocking()方法转换成 BlockingObservable对象，并调用它的toFuture()方法返回异步的Future对象</li>
<li>execute(): 在queue()产生异步结果 Future对象之后，通过调用 get() 方法阻塞并等待结果的返回。</li>
</ul>
<p>也就是说 返回的是一个 Observable形式的对象，然后根据我们最初命令执行的方法（queue、execut、observe）来进行返回格式的转换。</p>
<p>说完了Hystrix的工作流程，是时候聊聊能断水断电，牛逼的断路器了。</p>
<br/>

<h2 id="断路器原理"><a href="#断路器原理" class="headerlink" title="断路器原理"></a>断路器原理</h2><p>断路器那么厉害，它是如何决策熔断和记录信息的呢？</p>
<h3 id="HystrixCircuitBreaker-接口分析"><a href="#HystrixCircuitBreaker-接口分析" class="headerlink" title="HystrixCircuitBreaker 接口分析"></a>HystrixCircuitBreaker 接口分析</h3><p>我们查看一下 HystrixCircuitBreaker 接口的代码 （这是 Springcloud 2.0.6.RELEASE版，与其他版本差不多）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">HystrixCircuitBreaker</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">allowRequest</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">isOpen</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">markSuccess</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">markNonSuccess</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">attemptExecution</span><span class="params">()</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">NoOpCircuitBreaker</span> <span class="keyword">implements</span> <span class="title class_">HystrixCircuitBreaker</span>&#123;...&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">HystrixCircuitBreakerImpl</span> <span class="keyword">implements</span> <span class="title class_">HystrixCircuitBreaker</span> &#123;...&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Factory</span> &#123;...&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看出接口定义较为简单，比较核心的三个抽象方法</p>
<ul>
<li>allowRequest(): 每个Hystrix命令的请求都通过它判断是否被执行</li>
<li>isOpen(): 返回当前断路器是否打开</li>
<li>markSuccess(): 代表请求执行成功，用来关闭断路器</li>
</ul>
<p>另外还有三个静态类</p>
<p><strong>Factory</strong>：</p>
<p>​       维护了一个Hystrix命令与HystrixCircuitBreaker的关系集合：ConcurrentHashMap&lt;String, HystrixCircuitBreaker&gt; circuitBreakersByCommand，其中String类型的key通过 HystrixCommandKey定义。每一个Hystrix命令需要有一个key来标识，同时一个Hystrix命令也会在该集合中找到它对应的断路器 HystrixCircuitBreaker实例。</p>
<p><strong>NoOpCircuitBreaker</strong> <strong>：</strong></p>
<p>   定义了一个什么都不做的断路器实现，它允许所有请求，并且断路器状态始终闭合。</p>
<p><strong>HystrixCircuitBreakerImpl</strong> <strong>：</strong></p>
<p>   断路器接口 HystrixCircuitBreaker 的实现类，在该类中定义了断路器的几个核心对象</p>
<ul>
<li>HystrixCommandProperties properties: 断路器对应 HystrixCommand实例的属性对象</li>
<li>HystrixCommandMetrics metrics： 用来让 HystrixCommand记录各类度量指标的对象</li>
<li>AtomicLong circuitOpened: 断路器是否打开的标志</li>
<li>spring cloud微服务实战书中说 1.3x版本有 AtomicLong circuitOpenedorLastTestedTime: 断路器打开或是上一次测试的时间</li>
</ul>
<p>而2.0版本修改成了下面两个属性</p>
<ul>
<li>AtomicReference<Status> status： 断路器状态标识</li>
<li>AtomicReference<Subscription> activeSubscription ： Subscription中包含了断路器开启的时间，以及对断路器的状态记录</li>
</ul>
<p>下面我们来分析<strong>HystrixCircuitBreakerImpl</strong> 来查看下面3个核心方法的实现</p>
<ul>
<li>isOpen(): 判断断路器的打开&#x2F;关闭状态，详细逻辑如下：</li>
<li>allowRequest(): 判断请求是否被允许</li>
</ul>
<p>可以看到断路器直接通过this.properties判断对应强制打开或关闭属性是否被设置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isOpen</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> ((Boolean)<span class="built_in">this</span>.properties.circuitBreakerForceOpen().get()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((Boolean)<span class="built_in">this</span>.properties.circuitBreakerForceClosed().get()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.circuitOpened.get() &gt;= <span class="number">0L</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">allowRequest</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> ((Boolean)<span class="built_in">this</span>.properties.circuitBreakerForceOpen().get()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((Boolean)<span class="built_in">this</span>.properties.circuitBreakerForceClosed().get()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">this</span>.circuitOpened.get() == -<span class="number">1L</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> ((HystrixCircuitBreaker.HystrixCircuitBreakerImpl.Status)<span class="built_in">this</span>.status.get()).equals(HystrixCircuitBreaker.HystrixCircuitBreakerImpl.Status.HALF_OPEN) ? <span class="literal">false</span> : <span class="built_in">this</span>.isAfterSleepWindow();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>markSuccess():从if判断可以得知，markSuccess在 断路器“半开路”或“关闭”状态下判断</li>
</ul>
<p>​              当Hystrix请求重新调用成功时，它取消了上次请求的数据记录，并重置当前的度量指标对象和关闭断路器,这里我们可以推断出 markSuccess方法应该是 请求失败后再次重连成功后的断路器状态处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">markSuccess</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.status.compareAndSet(HystrixCircuitBreaker.HystrixCircuitBreakerImpl.Status.HALF_OPEN, HystrixCircuitBreaker.HystrixCircuitBreakerImpl.Status.CLOSED)) &#123;</span><br><span class="line">        <span class="built_in">this</span>.metrics.resetStream();</span><br><span class="line">        <span class="type">Subscription</span> <span class="variable">previousSubscription</span> <span class="operator">=</span> (Subscription)<span class="built_in">this</span>.activeSubscription.get();</span><br><span class="line">        <span class="keyword">if</span> (previousSubscription != <span class="literal">null</span>) &#123;</span><br><span class="line">            previousSubscription.unsubscribe();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">Subscription</span> <span class="variable">newSubscription</span> <span class="operator">=</span> <span class="built_in">this</span>.subscribeToStream();</span><br><span class="line">        <span class="built_in">this</span>.activeSubscription.set(newSubscription);</span><br><span class="line">        <span class="built_in">this</span>.circuitOpened.set(-<span class="number">1L</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再看一下 Netfix Hystrix官方文档中对于断路器的详细执行逻辑图</p>
<h3 id="断路器的详细执行逻辑"><a href="#断路器的详细执行逻辑" class="headerlink" title="断路器的详细执行逻辑"></a>断路器的详细执行逻辑</h3><img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/circuit_flow.jpg" class="" title="断路器流程">

<p>参考博文： <a href="https://segmentfault.com/a/1190000012439580">https://segmentfault.com/a/1190000012439580</a></p>
<p>断路器打开和关闭有如下几种情况：</p>
<ul>
<li>假设断路中的请求满足了一定的阈值（HystrixCommandProperties.circuitBreakerRequestVolumeThreshold()）</li>
<li>假设错误发生的百分比超过了设定的错误发生的阈值HystrixCommandProperties.circuitBreakerErrorThresholdPercentage()</li>
<li>断路器状态由CLOSE变换成OPEN</li>
<li>如果断路器打开，所有的请求都会被断路器所熔断。</li>
<li>一定时间之后HystrixCommandProperties.circuitBreakerSleepWindowInMilliseconds()，下一个的请求会被通过（处于半打开状态），如果该请求执行失败，断路器会在睡眠窗口期间返回OPEN，如果请求成功，断路器会被置为关闭状态，重新开启1步骤的逻辑。</li>
</ul>
<br/>



<h2 id="舱壁模式"><a href="#舱壁模式" class="headerlink" title="舱壁模式"></a>舱壁模式</h2><p>​     如果了解过 Docker的小伙伴，肯定听过“舱壁模式”，它实现了进程的隔离，使得容器之间不会互相收到影响。Hystrix采用舱壁模式来隔离相互之间的依赖关系，它会为每个依赖服务创建一个独立的线程池，这样就算某个依赖服务出现延迟高的情况，也不会影响到其他的依赖服务。</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/container_divorse.jpg" class="" title="舱壁模式">

<h3 id="线程和线程池"><a href="#线程和线程池" class="headerlink" title="线程和线程池"></a>线程和线程池</h3><p>客户端（第三方包、网络调用等）会在单独的线程执行，会与调用的该任务的线程进行隔离，以此来防止调用者调用依赖所消耗的时间过长而阻塞调用者的线程。</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/thread_pool.jpg" class="" title="线程池调用服务">

<p>您可以在不使用线程池的情况下防止出现故障，但是这要求客户端必须能够做到快速失败（网络连接&#x2F;读取超时和重试配置），并始终保持良好的执行状态。</p>
<p>Netflix设计Hystrix，并且选择使用线程和线程池来实现隔离机制，有以下几个原因：</p>
<ul>
<li><p>很多应用会调用多个不同的后端服务作为依赖。</p>
</li>
<li><p>每个服务会提供自己的客户端库包。</p>
</li>
<li><p>每个客户端的库包都会不断的处于变更状态。</p>
</li>
<li><p>每个客户端库包都可能包含重试、数据解析、缓存等等其他逻辑。</p>
</li>
<li><p>对用户来说，客户端库往往是“黑盒”的，对于实现细节、网络访问模式。默认配置等都是不透明的。</p>
</li>
<li><p>大部分的网络访问是同步执行的。</p>
</li>
<li><p>客户端代码中也可能出现失败和延迟，而不仅仅是在网络调用中。</p>
</li>
</ul>
<br/>

<ul>
<li><strong>使用线程池的好处</strong></li>
</ul>
<p>通过线程在自己的线程池中隔离的好处是：</p>
<ul>
<li><ul>
<li>该应用程序完全可以不受失控的客户端库的威胁。即使某一个依赖的线程池已满也不会影响其他依赖的调用。</li>
<li>应用程序可以低风险的接受新的客户端库的数据，如果发生问题，它会与出问题的客户端库所隔离，不会影响其他依赖的任何内容。</li>
<li>当失败的客户端服务恢复时，线程池将会被清除，应用程序也会恢复，而不至于使得我们整个Tomcat容器出现故障。</li>
<li>如果一个客户端库的配置错误，线程池可以很快的感知这一错误（通过增加错误比例，延迟，超时，拒绝等），并可以在不影响应用程序的功能情况下来处理这些问题（可以通过动态配置来进行实时的改变）。</li>
<li>如果一个客户端服务的性能变差，可以通过改变线程池的指标（错误、延迟、超时、拒绝）来进行属性的调整，并且这些调整可以不影响其他的客户端请求。</li>
<li>除了隔离的优势之外，拥有专用的线程池可以提供内置的请求任务的并发性，可以在同步客户端上构建异步门面。</li>
</ul>
</li>
</ul>
<p>简而言之，由线程池提供的隔离功能可以使客户端库和子系统性能特性的不断变化和动态组合得到优雅的处理，而不会造成中断，保证了应用的健壮性</p>
<p>当然线程池隔离的方案好处多多，但是也多了额外的开销和延迟，但Netfix官方对于线程做了相关测试，以结果打消我们的顾忌。</p>
<br/>

<ul>
<li><strong>线程池的缺点</strong></li>
</ul>
<p>线程池最主要的缺点就是增加了CPU的计算开销，每个命令都会在单独的线程池上执行，这样的执行方式会涉及到命令的排队、调度和上下文切换。</p>
<p>Netflix在设计这个系统时，决定接受这个开销的代价，来换取它所提供的好处，并且认为这个开销是足够小的，不会有重大的成本或者是性能影响。</p>
<br/>

<ul>
<li><strong>线程成本</strong></li>
</ul>
<p>Hystrix在子线程执行construct()方法和run()方法时会计算延迟，以及计算父线程从端到端的执行总时间。所以，你可以看到Hystrix开销成本包括（线程、度量，日志，断路器等）。</p>
<h3 id="使用线程池的性能测试"><a href="#使用线程池的性能测试" class="headerlink" title="使用线程池的性能测试"></a>使用线程池的性能测试</h3><p>下图显示了一个HystrixCommand在单个API实例上每秒执行60个请求（每个服务器每秒执行大约350个线程执行总数）：</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/compare.jpg" class="" title="性能比较测试">

<p>注意： 这个图在怎么看呢？ 主要是看下方对于每条线的颜色注解，它统计了服务器处理的3个峰值，每种颜色的线有两根，分别对应使用线程池 User开头，与未使用线程池做了对比</p>
<p>我们可以看到只有当服务提供方的处理达到顶点时，两种使用方式之间才有9ms的延迟差，几乎可以忽略不计。</p>
<table>
<thead>
<tr>
<th>服务器峰值</th>
<th>未使用线程池</th>
<th>使用线程池</th>
<th>延迟差</th>
</tr>
</thead>
<tbody><tr>
<td>中位数</td>
<td>2ms</td>
<td>2ms</td>
<td>0ms</td>
</tr>
<tr>
<td>90%</td>
<td>5ms</td>
<td>8ms</td>
<td>3ms</td>
</tr>
<tr>
<td>99%</td>
<td>28ms</td>
<td>37ms</td>
<td>9ms</td>
</tr>
</tbody></table>
<br/>

<h2 id="请求合并"><a href="#请求合并" class="headerlink" title="请求合并"></a>请求合并</h2><p>​      微服务架构中的依赖通常是通过远程调用实现的，而远程调用中最常见的问题就是通信消耗与连接数占用。因为通信次数的增加，总的通信时间消耗将会不太理想。同时hystrix创建的依赖服务的线程池资源有限，将会出现排队等待与响应延迟的情况。为了优化这两个问题，Hystrix推出了 <strong>HystrixCollapser</strong> 来实现 请求的合并，以减少消耗和线程数的占用。</p>
<p>​      HstrixCollapser 实现了 在 HystixCommand 之前放置一个合并处理器，将处于一个很短的时间窗（默认10毫秒）内的针对同一个依赖服务的多个请求进行整合并批量方式发起请求的功能（当然服务提供方也需要有相应的批量实现接口）。</p>
<p>简单点说： 假设消费方（单用户与多用户）短时间内对服务提供方UserService 进行了大量的请求调用，那么消费方会将这些 HystrixCommand打包在一块，一次发送，一次获取数据，而不是每次发送一次请求获取一次数据，大大节约了通信开销。</p>
<p>如下图：</p>
<img src="/2020/03/09/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E4%B9%8BHystrix/hystrix_collasper.jpg" class="" title="请求合并">

<br/>

<h3 id="为什么使用请求合并"><a href="#为什么使用请求合并" class="headerlink" title="为什么使用请求合并"></a>为什么使用请求合并</h3><p>事情请求合并来减少执行并发HystrixCommand请求所需要的线程数和网络连接数。请求合并以自动方式执行的，不需要代码层面上进行批处理请求的编码。</p>
<ul>
<li>全局上下文（所有的tomcat线程）</li>
</ul>
<p>理想的合并方式是在全局应用程序级别来完成的，以便来自任何用户的任何Tomcat线程的请求都可以一起合并。</p>
<p>例如，如果将HystrixCommand配置为支持任何用户请求获取影片评级的依赖项的批处理，那么当同一个JVM中的任何用户线程发出这样的请求时，Hystrix会将该请求与其他请求一起合并添加到同一个JVM中的网络调用。</p>
<p>请注意，合并器会将一个HystrixRequestContext对象传递给合并的网络调用，为了使其成为一个有效选项，下游系统必须处理这种情况。</p>
<ul>
<li>用户请求上下文（单个tomcat线程）</li>
</ul>
<p>如果将HystrixCommand配置为仅处理单个用户的批处理请求，则Hystrix仅仅会合并单个Tomcat线程的请求。</p>
<p>例如，如果一个用户想要加载300个影片的标签，Hystrix能够把这300次网络调用合并成一次调用。</p>
<ul>
<li>对象建模和代码的复杂性</li>
</ul>
<p>有时候，当你创建一个对象模型对消费的对象而言是具有逻辑意义的，这与对象的生产者的有效资源利用率不匹配。</p>
<p>例如，给你300个视频对象，遍历他们，并且调用他们的getSomeAttribute()方法，但是如果简单的调用，可能会导致300次网络调用（可能很快会占满资源）。</p>
<p>有一些手动的方法可以解决这个问题，比如在用户调用getSomeAttribute()方法之前，要求用户声明他们想要获取哪些视频对象的属性，以便他们都可以被预取。</p>
<p>或者，您可以分割对象模型，以便用户必须从一个位置获取视频列表，然后从其他位置请求该视频列表的属性。</p>
<br/>

<h3 id="请求合并之HystrixCollapser"><a href="#请求合并之HystrixCollapser" class="headerlink" title="请求合并之HystrixCollapser"></a>请求合并之HystrixCollapser</h3><p>让我们看一下 HystrixCollapser 类的实现,除了一些getset，比较核心的是下面这3个方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">HystrixCollapser</span>&lt;BatchReturnType, ResponseType, RequestArgumentType&gt; </span><br><span class="line">    <span class="keyword">implements</span> <span class="title class_">HystrixExecutable</span>&lt;ResponseType&gt;, HystrixObservable&lt;ResponseType&gt; &#123;</span><br><span class="line">        ...</span><br><span class="line">     <span class="keyword">public</span> <span class="keyword">abstract</span> RequestArgumentType <span class="title function_">getRequestArgument</span><span class="params">()</span>;</span><br><span class="line">      </span><br><span class="line">      <span class="keyword">protected</span> <span class="keyword">abstract</span> HystrixCommand&lt;BatchReturnType&gt; <span class="title function_">createCommand</span><span class="params">(Collection&lt;HystrixCollapser.CollapsedRequest&lt;ResponseType, RequestArgumentType&gt;&gt; var1)</span>;</span><br><span class="line">      </span><br><span class="line">      <span class="keyword">protected</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">mapResponseToRequests</span><span class="params">(BatchReturnType var1, Collection&lt;HystrixCollapser.CollapsedRequest&lt;ResponseType, RequestArgumentType&gt;&gt; var2)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从HystrixCollapser抽象类的定义中可以看到，它指定了三个不同的参数类型</p>
<ul>
<li>BatchReturnType: 合并后批量请求的返回类型。</li>
<li>ResponseType： 单个请求返回的类型</li>
<li>RequestArgumentType: 请求参数类型</li>
</ul>
<p>这3个参数类型，我们可以从这些调用方法中查看到 是我们指定了传入的参数类型，和批量返回的参数类型是什么。</p>
<br/>

<p>那么请求合并具体怎么使用呢?</p>
<p><strong>请求合并的实现类</strong></p>
<p>请求合并的实现包含了两个主要实现类，一个是合并请求类，一个是批量处理类，合并请求类的作用是收集一定时间内的请求，将他们传递的参数汇总，然后调用批量处理类，通过向服务调用者发送请求获取批量处理的结果数据，最后在对应的request方法体中依次封装获取的结果，并返回回去</p>
<p>实战代码，待补充。。。</p>
<p>这里推荐一个 多个请求合并请求 用户服务的例子</p>
<p><a href="https://www.jianshu.com/p/8f5d1fd6d601">https://www.jianshu.com/p/8f5d1fd6d601</a></p>
]]></content>
      <categories>
        <category>springcloud</category>
      </categories>
      <tags>
        <tag>hystrix</tag>
      </tags>
  </entry>
  <entry>
    <title>聊聊Semaphore的使用</title>
    <url>/2020/06/06/%E8%81%8A%E8%81%8ASemaphore%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>​          对于Semaphore，我对其印象和使用都是比较模糊的，今天在lc中碰到了一道题目很适合用 Semaphore信号量来处理，同时在查看他人代码的过程中遇到了疑惑的地方，进行源代码的查阅和Debug后，对于Semaphore的使用和理解上面有了不少的提升，这里特意写一篇博客记录。</p>
<span id="more"></span>

<h3 id="事前说明："><a href="#事前说明：" class="headerlink" title="事前说明："></a>事前说明：</h3><p>源码的解析流程是比较长且繁琐的，建议大家可以根据这篇文章，打好相应的断点进行debug调试，调试的代码和具体的方法我在下文中会说明！</p>
<p>这里首先抛出几个问题。</p>
<ol>
<li>Semaphore是什么？</li>
<li>Semaphore的适用场景？</li>
<li>Semaphore是如何实现的？</li>
</ol>
<br/>

<h2 id="Semaphore是什么？"><a href="#Semaphore是什么？" class="headerlink" title="Semaphore是什么？"></a>Semaphore是什么？</h2><p>​            Semaphore是一个 计数信号量，类路径为 java.util.concurrent.Semaphore，你可以把Semaphore理解成 公司的共享雨伞（指定容量State），员工可以拿伞（获取资源acquire），可以归还伞（释放资源release），但是有一个前提 那就是 公司雨伞（容量state）是有限的。如果下雨天，有五百个员工，100把伞的话，当伞都被借走时，如果有员工想要借伞就必须要等别人归还伞，否则只能一直等待。</p>
<br/>



<h2 id="Semaphore的适用场景？"><a href="#Semaphore的适用场景？" class="headerlink" title="Semaphore的适用场景？"></a>Semaphore的适用场景？</h2><p>​     通过上面的描述，我想大家能明白Semaphore的常见用途。</p>
<p>​    1、 Semaphore可以用来 资源限流。例如： 大量人员购票，车站售票窗口有限。 又或者是具体应用场景 数据库连接池中同时连接的线程不能超过指定的连接数量</p>
<p>​    2、Semaphore可以用来进行状态切换，例如： 红绿灯、开关，但是要理解Semaphore其实只适合少量的状态变化，你看完下文理解了就明白为啥不适合大量的状态切换了。</p>
<br/>

<p>​        这里说明： 我之所以Semaphore学的比较模糊是因为我没有在具体的场景中将其合理的运用过，这导致我对于Semaphore的理解很浅，semaphore进行资源限流，我想大家都应该比较了解，无非就是指定 Semaphore容量，进行acquire和release的过程。</p>
<h3 id="资源限流的场景"><a href="#资源限流的场景" class="headerlink" title="资源限流的场景"></a>资源限流的场景</h3><p>例如：</p>
<p>春节人民取票，火车站只有6个售票窗口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Semaphore</span> <span class="variable">windows</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">6</span>);</span><br><span class="line"><span class="comment">// 春节无数人进行取票</span></span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>)&#123; <span class="comment">// 这里图省事就直接无限循环的，现实中往往是一个 for(0 -&gt; n) 的循环</span></span><br><span class="line">    windows.acquire(); <span class="comment">// 占用一个窗口取票，没有窗口就会堵塞等待</span></span><br><span class="line">    .... 执行取票逻辑</span><br><span class="line">    windows.release(); <span class="comment">// 离开该窗口</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是如何灵活的使用 Semaphore进行状态切换，执行每个状态下该有的逻辑，我想大家应该是比较懵的，这里推荐一道应用场景题，从该题目来入手</p>
<br/>

<h3 id="状态切换的场景"><a href="#状态切换的场景" class="headerlink" title="状态切换的场景"></a>状态切换的场景</h3><p><a href="https://leetcode-cn.com/problems/print-foobar-alternately/"><strong>1115. 交替打印FooBar</strong></a></p>
<p>两个不同的线程将会共用一个 FooBar 实例。其中一个线程将会调用 foo() 方法打印 foo，另一个线程将会调用 bar() 方法打印 bar。</p>
<p>请设计修改程序，以确保 “foobar” 被输出 n 次。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">FooBar</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> n;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FooBar</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.n = n;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">foo</span><span class="params">(Runnable printFoo)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// printFoo.run() 打印 foo</span></span><br><span class="line">            printFoo.run();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bar</span><span class="params">(Runnable printBar)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// printBar.run() 打印 bar</span></span><br><span class="line">            printBar.run();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>当然，这个题目可以用 Lock来做、也可以用Semaphore，甚至可以用 volatile变量进行自旋锁判断，只是性能较差，不推荐。</p>
<p>请大家思考一下，用 semaphore怎么做？</p>
 <img src="/2020/06/06/%E8%81%8A%E8%81%8ASemaphore%E7%9A%84%E4%BD%BF%E7%94%A8/think.jpg" class="" title="思考表情图">

<p>这里我们先来公布一下答案：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">FooBar</span> &#123;</span><br><span class="line">   <span class="keyword">private</span> <span class="type">int</span> n;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FooBar</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.n = n;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">Semaphore</span> <span class="variable">foo</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="type">Semaphore</span> <span class="variable">bar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">foo</span><span class="params">(Runnable printFoo)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            foo.acquire();</span><br><span class="line">            printFoo.run();</span><br><span class="line">            bar.release();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bar</span><span class="params">(Runnable printBar)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            bar.acquire();</span><br><span class="line">            printBar.run();</span><br><span class="line">            foo.release();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 测试代码</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">            <span class="type">FooBar</span> <span class="variable">fb</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FooBar</span>(<span class="number">2</span>);</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    fb.foo(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                        <span class="meta">@Override</span></span><br><span class="line">                        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                            System.out.print(<span class="string">&quot;foo&quot;</span>);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        </span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    fb.bar(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                        <span class="meta">@Override</span></span><br><span class="line">                        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                            System.out.print(<span class="string">&quot;bar&quot;</span>);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<p>这里我想大家对这段代码会有疑问 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">foo</span><span class="params">(Runnable printFoo)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            foo.acquire();</span><br><span class="line">            printFoo.run();</span><br><span class="line">            bar.release();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>为啥，bar.release(); 可以这么玩？bar的信号量不是为0吗？还能接着 release()？  Semaphore bar &#x3D; new Semaphore(0); 并且在 bar方法中还可以获取信号量？</p>
 <img src="/2020/06/06/%E8%81%8A%E8%81%8ASemaphore%E7%9A%84%E4%BD%BF%E7%94%A8/what.jpg" class="" title="不可思议图">

<br/>

<h4 id="release-方法分析"><a href="#release-方法分析" class="headerlink" title="release()方法分析"></a>release()方法分析</h4><p>别急! 这个时候得看源码分析一下操作了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Semaphore</span> <span class="keyword">implements</span> <span class="title class_">java</span>.io.Serializable &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">release</span><span class="params">()</span> &#123;</span><br><span class="line">        sync.releaseShared(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Sync sync;</span><br><span class="line">    <span class="comment">// 使用AQS实现的同步器,公平版（指锁竞争机制公平，排队先到先得)</span></span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Sync</span> <span class="keyword">extends</span> <span class="title class_">AbstractQueuedSynchronizer</span> &#123;</span><br><span class="line">        <span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryReleaseShared</span><span class="params">(<span class="type">int</span> releases)</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">current</span> <span class="operator">=</span> getState();</span><br><span class="line">                <span class="type">int</span> <span class="variable">next</span> <span class="operator">=</span> current + releases;</span><br><span class="line">                <span class="keyword">if</span> (next &lt; current) <span class="comment">// overflow</span></span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Maximum permit count exceeded&quot;</span>);</span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(current, next))</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//  NonFair version 非公平版</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">NonfairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们可以看到 Semaphore的release方法调用的 是  AQS类的releaseShared(1);</p>
<p>AbstractQueuedSynchronizer.releaseShared()方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">releaseShared</span><span class="params">(<span class="type">int</span> arg)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (tryReleaseShared(arg)) &#123;</span><br><span class="line">        doReleaseShared();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重头戏来了，我们来重点分析这两个方法！！</p>
<p><strong>分析步骤1：  if (tryReleaseShared(1))</strong></p>
<p><strong>分析步骤2：   doReleaseShared();</strong></p>
<p>tryReleaseShared(int arg)方法，由于子类中有实现，会实现子类的方法，注意看上面Semaphore 对应的 Sync内部静态类中的该方法（这里强烈建议自己debug一下）</p>
<p>注意： 我们讨论的是 下面这代码为什么信号量为0了，还可以直接释放信号量，并且还可以获得信号量的操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Semaphore</span> <span class="variable">bar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">0</span>);</span><br><span class="line">bar.release();</span><br><span class="line">bar.acquire();</span><br></pre></td></tr></table></figure>

<br/>

<p>接着看Semaphore 源码 tryReleaseShared(1)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">tryReleaseShared</span><span class="params">(<span class="type">int</span> releases)</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">//  getState()为0，尝试释放的releases为1</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">current</span> <span class="operator">=</span> getState();</span><br><span class="line">                <span class="type">int</span> <span class="variable">next</span> <span class="operator">=</span> current + releases;</span><br><span class="line">                <span class="comment">// (next = 1) &gt; (current = 0)</span></span><br><span class="line">                <span class="keyword">if</span> (next &lt; current) <span class="comment">// overflow</span></span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Maximum permit count exceeded&quot;</span>);</span><br><span class="line">                    <span class="comment">// 注意这里， 这里直接用CAS 把 current设置为 next，也就是把信号量从0变成了1！！！</span></span><br><span class="line">                    <span class="comment">// 无限循环，自旋CAS来确认增加了当前信号量 = 原来的信号量 current  + 要释放的信号量 releases</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(current, next))</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>由于  if (tryReleaseShared(1)) 返回的是true，信号量++了，此时再看 doReleaseShared();</p>
<p>这里 doReleaseShared() 方法是 AQS类中的，子类 Semaphore中Sync 同步器并未实现该方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doReleaseShared</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="literal">null</span> &amp;&amp; h != tail) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> h.waitStatus;</span><br><span class="line">            <span class="keyword">if</span> (ws == Node.SIGNAL) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!compareAndSetWaitStatus(h, Node.SIGNAL, <span class="number">0</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;            <span class="comment">// loop to recheck cases</span></span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (ws == <span class="number">0</span> &amp;&amp;</span><br><span class="line">                     !compareAndSetWaitStatus(h, <span class="number">0</span>, Node.PROPAGATE))</span><br><span class="line">                <span class="keyword">continue</span>;                <span class="comment">// loop on failed CAS</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (h == head)                   <span class="comment">// loop if head changed</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这一段代码，如果没研究过 AQS的话会看得很迷糊，我这里为了篇幅起见就简短的解释一下。</p>
<p>首先大部分我们使用的同步类都是通过 继承 AQS来实现的。</p>
<p>因为AQS 将 线程安全中大部分的逻辑都已经封装好了， 继承的子类只需要调用它的方法来做 信号量的修改，以及决定是 共享锁的方式还是 独占锁的方式来表现子类的特性就行。</p>
<p>AQS中有个 CLH 等待队列,结构是一个 双向链表，它的用途就是 当一个资源被A线程占有时，其他申请资源的线程都会进入 CLH等待队列中排队等待 A线程释放资源。</p>
<p><strong>上面 doReleaseShared方法，就是释放资源信号量后，使得CLH队列 head节点资源，并且将head节点 &#x3D; head.next的这样一个过程。</strong></p>
<p>这样一来， bar.acquire();就能够说得通了,因为 bar.release()方法 是将原本 为零的信号量， 通过CAS 变成了 state &#x3D; 1</p>
<h4 id="release-方法总结"><a href="#release-方法总结" class="headerlink" title="release()方法总结"></a><strong>release()方法总结</strong></h4><p>Semaphore的 release()方法，不会考虑原本的信号量的数量问题，而是通过CAS实现 当前信号量加一的操作，从而实现了信号量从无到有的这个过程。</p>
<br/>

<h4 id="acquire-方法分析"><a href="#acquire-方法分析" class="headerlink" title="acquire() 方法分析"></a>acquire() 方法分析</h4><p>那 acquire() 方法呢,这个肯定是会要求当前信号量大于0的，否则就会堵塞请求的线程，为什么呢？</p>
<p>因为不这样，那还叫啥 线程安全的同步器呀，都没有堵塞机制了！！</p>
<p>不信，咋们看代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">acquire</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">    sync.acquireSharedInterruptibly(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用了 AQS类的acquireSharedInterruptibly方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">acquireSharedInterruptibly</span><span class="params">(<span class="type">int</span> arg)</span></span><br><span class="line">           <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">         <span class="comment">// 判断线程是否中断，已经中断的话直接抛异常</span></span><br><span class="line">       <span class="keyword">if</span> (Thread.interrupted())</span><br><span class="line">           <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InterruptedException</span>();</span><br><span class="line">          </span><br><span class="line">       <span class="keyword">if</span> (tryAcquireShared(arg) &lt; <span class="number">0</span>)</span><br><span class="line">           doAcquireSharedInterruptibly(arg);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>由于子类对应的 Semaphore的同步器 实现了  tryAcquireShared(arg)方法，必然会调用到具体实现子类 Semaphore 的静态内部类 非公平同步类 NonfairSync</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">NonfairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">tryAcquireShared</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> nonfairTryAcquireShared(acquires);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们可以查看 Semaphore 的静态内部类 Sync实现的 nonfairTryAcquireShared(1)方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Semaphore</span> <span class="keyword">implements</span> <span class="title class_">java</span>.io.Serializable &#123;</span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Sync</span> <span class="keyword">extends</span> <span class="title class_">AbstractQueuedSynchronizer</span> &#123;</span><br><span class="line">         <span class="keyword">final</span> <span class="type">int</span> <span class="title function_">nonfairTryAcquireShared</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">// 假设此时信号量getState()为0</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">available</span> <span class="operator">=</span> getState();</span><br><span class="line">                <span class="comment">// 那么 remaining = -1</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">remaining</span> <span class="operator">=</span> available - acquires;</span><br><span class="line">                <span class="comment">// 注意： 如果此时 信号量大于0，那么这里会直接通过 CAS 设置 信号量-1，并且返回最新的信号量值，这就意味着获取成功了</span></span><br><span class="line">                <span class="keyword">if</span> (remaining &lt; <span class="number">0</span> ||</span><br><span class="line">                    compareAndSetState(available, remaining))</span><br><span class="line">                    <span class="keyword">return</span> remaining;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于if (tryAcquireShared(arg) &lt; 0)的条件中 tryAcquireShared(1) &#x3D; -1，所以执行if下的逻辑，调用   doAcquireSharedInterruptibly(arg);</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (tryAcquireShared(arg) &lt; <span class="number">0</span>)</span><br><span class="line">           doAcquireSharedInterruptibly(arg);</span><br></pre></td></tr></table></figure>

<p>AQS类中的doAcquireSharedInterruptibly方法</p>
<p>注意： 此时是获取</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doAcquireSharedInterruptibly</span><span class="params">(<span class="type">int</span> arg)</span></span><br><span class="line">    <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 在CLH等待队列中添加一个节点</span></span><br><span class="line">    <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> addWaiter(Node.SHARED);</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">failed</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="comment">// node.predecessor() ：返回node的上一个节点</span></span><br><span class="line">            <span class="keyword">final</span> <span class="type">Node</span> <span class="variable">p</span> <span class="operator">=</span> node.predecessor();</span><br><span class="line">            <span class="keyword">if</span> (p == head) &#123;</span><br><span class="line">                <span class="comment">// 当上一个节点是头节点是，再次调用tryAcquireShared(arg)方法，这个方法是 Semaphore的内部类实现的，具体看下面</span></span><br><span class="line">                <span class="comment">// 当资源还没有被release释放时， 即信号量还是为0时，tryAcquireShared(arg)方法肯定返回的还是 -1</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">r</span> <span class="operator">=</span> tryAcquireShared(arg);</span><br><span class="line">                <span class="comment">// 如果 getState() = 0 ,那么 r为-1，接着循环执行，直到其他线程释放信号量了，那么 tryAcquireShared(arg)成功，r &gt;= 0了</span></span><br><span class="line">                <span class="keyword">if</span> (r &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// 信号量大于0了，这就意味着头节点p可以获取信号量并移除该头节点，那么设置当前 线程节点为 CLH队列的头节点，然后跳出循环</span></span><br><span class="line">                    setHeadAndPropagate(node, r);</span><br><span class="line">                    p.next = <span class="literal">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    failed = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                parkAndCheckInterrupt())</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InterruptedException</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (failed)</span><br><span class="line">            cancelAcquire(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里贴上添加等待节点的方法addWaiter来让大家了解 CLH等待队列</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Node <span class="title function_">addWaiter</span><span class="params">(Node mode)</span> &#123;</span><br><span class="line">    <span class="type">Node</span> <span class="variable">node</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>(Thread.currentThread(), mode);</span><br><span class="line">    <span class="comment">// Try the fast path of enq; backup to full enq on failure</span></span><br><span class="line">    <span class="type">Node</span> <span class="variable">pred</span> <span class="operator">=</span> tail;</span><br><span class="line">    <span class="keyword">if</span> (pred != <span class="literal">null</span>) &#123;</span><br><span class="line">        node.prev = pred;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123;</span><br><span class="line">            pred.next = node;</span><br><span class="line">            <span class="keyword">return</span> node;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    enq(node);</span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Semaphore也可以定义 公平锁与非公平锁，默认是 非公平锁，我们这里为了篇幅起见，只介绍非公平锁，因为逻辑基本差不多</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">NonfairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">tryAcquireShared</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">        <span class="comment">// 由于是非公平锁，所以会直接取抢信号量，不需要排队</span></span><br><span class="line">        <span class="comment">// 再次执行 nonfairTryAcquireShared(acquires)，假设此时资源还没释放，信号量为零，那么这里肯定返回的是 -1，具体逻辑上面代码已经分析了</span></span><br><span class="line">        <span class="keyword">return</span> nonfairTryAcquireShared(acquires);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">FairSync</span> <span class="keyword">extends</span> <span class="title class_">Sync</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> <span class="type">int</span> <span class="title function_">tryAcquireShared</span><span class="params">(<span class="type">int</span> acquires)</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">if</span> (hasQueuedPredecessors())</span><br><span class="line">                <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">            <span class="type">int</span> <span class="variable">available</span> <span class="operator">=</span> getState();</span><br><span class="line">            <span class="type">int</span> <span class="variable">remaining</span> <span class="operator">=</span> available - acquires;</span><br><span class="line">            <span class="keyword">if</span> (remaining &lt; <span class="number">0</span> ||</span><br><span class="line">                compareAndSetState(available, remaining))</span><br><span class="line">                <span class="keyword">return</span> remaining;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h4 id="acquire方法总结"><a href="#acquire方法总结" class="headerlink" title="acquire方法总结"></a>acquire方法总结</h4><p>1、 当执行 tryAquire方法时，它会先通过 sync.nonfairTryAcquireShared(1)  &gt;&#x3D; 0 来判断，当前信号量是否大于0</p>
<p>2、如果大于0，则nonfairTryAcquireShared()方法中，直接通过CAS将 信号量减一并返回true</p>
<p>3、如果小于0，先将当前线程封装成一个 node节点添加到 CLH等待队列中，然后不停的循环判断，是否node的上一个节点是否为头节点</p>
<p>4、循环判断，当node.prev &#x3D; head时，通过 tryAcquireShared(arg)方法判断 信号量是否大于0，大于0，将node设置为 CLH队列的头节点</p>
<br/>

<p>这里细心的同学会有疑问，虽然说当信号量为0时，线程acquire无限等待CLH队列的头节点的出列，直到这个被封装的线程等待节点 waiterNode成为了CLH等待队列的头节点，但是！！</p>
<p>它终究没有获取到信号量啊！！！怎么就跳出方法了呀，这就意味着还没获取资源就跳出循环，这不没有起到堵塞的效果吗？？？</p>
 <img src="/2020/06/06/%E8%81%8A%E8%81%8ASemaphore%E7%9A%84%E4%BD%BF%E7%94%A8/success.jpg" class="" title="很好你成功了图">

<p>good！不要急，唯一有机会继续搞幺儿子的就是   setHeadAndPropagate(node, r);方法了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">setHeadAndPropagate</span><span class="params">(Node node, <span class="type">int</span> propagate)</span> &#123;</span><br><span class="line">    <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head; <span class="comment">// Record old head for check below</span></span><br><span class="line">    setHead(node);</span><br><span class="line">   <span class="comment">// 因为node为新的头节点了，那么 h肯定为null了，因为已经旧的头节点被释放了</span></span><br><span class="line">    <span class="keyword">if</span> (propagate &gt; <span class="number">0</span> || h == <span class="literal">null</span> || h.waitStatus &lt; <span class="number">0</span> ||</span><br><span class="line">        (h = head) == <span class="literal">null</span> || h.waitStatus &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">s</span> <span class="operator">=</span> node.next;</span><br><span class="line">        <span class="comment">// 这里忽略if,正常情况都会执行下面的语句</span></span><br><span class="line">        <span class="keyword">if</span> (s == <span class="literal">null</span> || s.isShared())</span><br><span class="line">            doReleaseShared();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这个nextWaiter和SHARED，就很复杂了，留到下一篇文章分析AQS再说吧</span></span><br><span class="line"><span class="keyword">final</span> <span class="type">boolean</span> <span class="title function_">isShared</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> nextWaiter == SHARED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> doReleaseShared();方法会无限循环判断，直到头节点改变了，证明 node已经获取到信号量了，此时退出循环，正式结束整个acquire方法的逻辑！！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doReleaseShared</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">h</span> <span class="operator">=</span> head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="literal">null</span> &amp;&amp; h != tail) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">ws</span> <span class="operator">=</span> h.waitStatus;</span><br><span class="line">            <span class="comment">// 这里对于节点状态的CAS替换判断，涉及CAS，留到下一篇</span></span><br><span class="line">            <span class="comment">// 但是大家应该可以通过官方的注释和无限循环来理解doReleaseShared的作用</span></span><br><span class="line">            <span class="keyword">if</span> (ws == Node.SIGNAL) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!compareAndSetWaitStatus(h, Node.SIGNAL, <span class="number">0</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;            <span class="comment">// loop to recheck cases</span></span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (ws == <span class="number">0</span> &amp;&amp;</span><br><span class="line">                     !compareAndSetWaitStatus(h, <span class="number">0</span>, Node.PROPAGATE))</span><br><span class="line">                <span class="keyword">continue</span>;                <span class="comment">// loop on failed CAS</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (h == head)                   <span class="comment">// loop if head changed</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br/>

<h4 id="acquire方法再次总结"><a href="#acquire方法再次总结" class="headerlink" title="acquire方法再次总结"></a>acquire方法再次总结</h4><p>1、 当执行 tryAquire方法时，它会先通过 sync.nonfairTryAcquireShared(1)  &gt;&#x3D; 0 来判断，当前信号量是否大于0</p>
<p>2、如果大于0，则nonfairTryAcquireShared()方法中，直接通过CAS将 信号量减一并返回true</p>
<p>3、如果小于0，先将当前线程封装成一个 node节点添加到 CLH等待队列中，然后不停的循环判断，是否node的上一个节点是否为头节点</p>
<p>4、循环判断，当node.prev &#x3D; head时，通过 tryAcquireShared(arg)方法判断 信号量是否大于0，大于0，将node设置为 CLH队列的头节点</p>
<p>5、在设置node为CLH队列的头节点的这个方法中doReleaseShared会无限循环判断当前头节点node是否已经改变了，改变了说明已经获取到信号量了，acquire方法结束！</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title>记录LangChain对主流视频网站的进行视频总结</title>
    <url>/2026/02/25/%E8%AE%B0%E5%BD%95LangChain%E5%AF%B9%E4%B8%BB%E6%B5%81%E8%A7%86%E9%A2%91%E7%BD%91%E7%AB%99%E7%9A%84%E8%BF%9B%E8%A1%8C%E8%A7%86%E9%A2%91%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h2 id="简要阐述："><a href="#简要阐述：" class="headerlink" title="简要阐述："></a>简要阐述：</h2><p>&#x20; 出于一些收费视频广告问题，以及对喜欢的某些博主的视频语速过快，没有字幕等情况。导致我想要将B站和YouTube两个网站的视频做一个流媒体视频的文本收集和视频总结的功能。</p>
<p>&#x20; 然后因为学过LangChain了，所以利用LangChain来爬取视频，然后转换音频，音频转换成文本。最后用大模型进行文本识别和语义修复，输出想要的格式文件。</p>
<span id="more"></span>

<h2 id="版本说明："><a href="#版本说明：" class="headerlink" title="版本说明："></a>版本说明：</h2><p>&#x20;   在实际操作的过程中,发现不同的插件版本的代码写法，以及对whisper的支持适配都不同。所以需要提前跟读者约定好当前成功的版本。</p>
<p>&#x20;   出于学习大模型的目的，基本使用的都是最新版，可能与很多网上视频和帖子的版本不同，因为大模型更新迭代太快，老帖子基本还是用的LangChain的0.2以下的版本。</p>
<p>&#x20;   所以如果读者熟悉老版本的话，建议直接让大模型将文中的代码转换成老版本，就是怕与第三方插件可能版本不适配，还是以实操为主。</p>
<p>&#x20;   主要关注 langchain和python版本即可，其他的插件，pip基本会自动安装适配的插件版本，这里是如果不可以，用户再来关注可用的插件版本</p>
<ul>
<li>langchain 1.2.6</li>
<li>python 3.11.14</li>
<li>openai 2.15.0</li>
<li>yt-dlp                    2026.1.29</li>
<li>pytube                    15.0.0</li>
</ul>
<h2 id="视频总结："><a href="#视频总结：" class="headerlink" title="视频总结："></a>视频总结：</h2><p>1、whisper插件使用base模型进行音频转文本即可，使用large模型转换效果会更好，但是速度太慢，后面完全可以用大模型进行文本语义修复，速度会快很多。</p>
<p>2、使用yt-dlp来下载视频，yt-dlp 的名字里有 “YouTube”，但它绝不仅仅支持 YouTube —— 它实际上是目前最强大、最通用的命令行视频下载工具之一，支持 1000+ 个网站，包括 Bilibili（B站）、Twitter、Instagram、TikTok、Vimeo、优酷、腾讯视频等。</p>
<p>3、为了省钱，我是用本地的Ollama模型跑的，用openai的要收费，跑多了烧钱。。。。 其次本地8B的模型能跑大部分业务了。</p>
<h3 id="抓取B站进行视频总结："><a href="#抓取B站进行视频总结：" class="headerlink" title="抓取B站进行视频总结："></a>抓取B站进行视频总结：</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> yt_dlp</span><br><span class="line"><span class="keyword">import</span> dotenv</span><br><span class="line">dotenv.load_dotenv()</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">import</span> tempfile</span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">from</span> urllib.parse <span class="keyword">import</span> urlparse, parse_qs</span><br><span class="line"><span class="keyword">from</span> langchain_openai <span class="keyword">import</span> ChatOpenAI</span><br><span class="line"><span class="keyword">from</span> langchain_core.prompts <span class="keyword">import</span> ChatPromptTemplate</span><br><span class="line"><span class="keyword">from</span> langchain_core.output_parsers <span class="keyword">import</span> StrOutputParser</span><br><span class="line">os.environ[<span class="string">&#x27;OPENAI_API_KEY&#x27;</span>] = os.getenv(<span class="string">&quot;OPENAI_API_KEY1&quot;</span>)</span><br><span class="line">os.environ[<span class="string">&#x27;OPENAI_BASE_URL&#x27;</span>] = os.getenv(<span class="string">&quot;OPENAI_BASE_URL&quot;</span>)</span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 配置区（按需修改）</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line">HEADERS = &#123;</span><br><span class="line">    <span class="string">&quot;User-Agent&quot;</span>: <span class="string">&quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Referer&quot;</span>: <span class="string">&quot;https://www.bilibili.com/&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">WHISPER_MODEL = <span class="string">&quot;base&quot;</span>  <span class="comment"># 可选: base, small, medium（越大越准，越慢）</span></span><br><span class="line">USE_YOU_GET = <span class="literal">True</span>      <span class="comment"># 设为 False 改用 yt-dlp（需额外安装）</span></span><br><span class="line">LLM_MODEL = <span class="string">&quot;gpt-4o&quot;</span>            <span class="comment"># 或 &quot;qwen-plus&quot;, &quot;claude-3-5-sonnet&quot;, &quot;gpt-3.5-turbo&quot;</span></span><br><span class="line">USE_LOCAL_LLM = <span class="literal">True</span>           <span class="comment"># 设为 True 可改用 Ollama（见下方注释）</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 工具函数</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">extract_bvid_from_url</span>(<span class="params">url: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;从 URL 提取 BV 号&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;bilibili.com&quot;</span> <span class="keyword">not</span> <span class="keyword">in</span> url:</span><br><span class="line">        <span class="keyword">return</span> url</span><br><span class="line">    parsed = urlparse(url)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;bvid=&quot;</span> <span class="keyword">in</span> parsed.query:</span><br><span class="line">        <span class="keyword">return</span> parse_qs(parsed.query)[<span class="string">&quot;bvid&quot;</span>][<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">match</span> = re.search(<span class="string">r&quot;BV\w+&quot;</span>, parsed.path)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">match</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">match</span>.group(<span class="number">0</span>)</span><br><span class="line">    <span class="keyword">raise</span> ValueError(<span class="string">&quot;无法提取 BV 号&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">try_get_subtitle_via_api</span>(<span class="params">bvid: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span> | <span class="literal">None</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;尝试通过 API 获取有效中文字幕&quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;🔍 尝试通过 B站 API 获取字幕...&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取 cid</span></span><br><span class="line">    view_resp = requests.get(</span><br><span class="line">        <span class="string">f&quot;https://api.bilibili.com/x/web-interface/view?bvid=<span class="subst">&#123;bvid&#125;</span>&quot;</span>,</span><br><span class="line">        headers=HEADERS,</span><br><span class="line">        timeout=<span class="number">10</span></span><br><span class="line">    )</span><br><span class="line">    view_data = view_resp.json()</span><br><span class="line">    <span class="keyword">if</span> view_data[<span class="string">&quot;code&quot;</span>] != <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">    cid = view_data[<span class="string">&quot;data&quot;</span>][<span class="string">&quot;pages&quot;</span>][<span class="number">0</span>][<span class="string">&quot;cid&quot;</span>]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取字幕列表</span></span><br><span class="line">    sub_resp = requests.get(</span><br><span class="line">        <span class="string">f&quot;https://api.bilibili.com/x/player/v2?bvid=<span class="subst">&#123;bvid&#125;</span>&amp;cid=<span class="subst">&#123;cid&#125;</span>&quot;</span>,</span><br><span class="line">        headers=HEADERS,</span><br><span class="line">        timeout=<span class="number">10</span></span><br><span class="line">    )</span><br><span class="line">    sub_data = sub_resp.json()</span><br><span class="line">    <span class="keyword">if</span> sub_data[<span class="string">&quot;code&quot;</span>] != <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    subtitles = sub_data[<span class="string">&quot;data&quot;</span>][<span class="string">&quot;subtitle&quot;</span>][<span class="string">&quot;subtitles&quot;</span>]</span><br><span class="line">    candidates = []</span><br><span class="line">    <span class="keyword">for</span> sub <span class="keyword">in</span> subtitles:</span><br><span class="line">        lang = sub[<span class="string">&quot;lan&quot;</span>]</span><br><span class="line">        <span class="keyword">if</span> lang == <span class="string">&quot;zh-CN&quot;</span>:   <span class="comment"># 人工字幕，优先</span></span><br><span class="line">            candidates.insert(<span class="number">0</span>, sub)</span><br><span class="line">        <span class="keyword">elif</span> lang == <span class="string">&quot;ai-Zh&quot;</span>: <span class="comment"># AI 字幕，次之</span></span><br><span class="line">            candidates.append(sub)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 尝试下载每个候选字幕</span></span><br><span class="line">    <span class="keyword">for</span> sub <span class="keyword">in</span> candidates:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            url = <span class="string">&quot;https:&quot;</span> + sub[<span class="string">&quot;subtitle_url&quot;</span>]</span><br><span class="line">            content_resp = requests.get(url, headers=HEADERS, timeout=<span class="number">10</span>)</span><br><span class="line">            body = content_resp.json()[<span class="string">&quot;body&quot;</span>]</span><br><span class="line">            text = <span class="string">&quot; &quot;</span>.join(item[<span class="string">&quot;content&quot;</span>] <span class="keyword">for</span> item <span class="keyword">in</span> body).strip()</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 过滤无效内容</span></span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(text) &lt; <span class="number">50</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">any</span>(kw <span class="keyword">in</span> text <span class="keyword">for</span> kw <span class="keyword">in</span> [<span class="string">&quot;付费&quot;</span>, <span class="string">&quot;购买&quot;</span>, <span class="string">&quot;试看&quot;</span>, <span class="string">&quot;完整视频&quot;</span>, <span class="string">&quot;解锁&quot;</span>]):</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">            <span class="built_in">print</span>(<span class="string">&quot;✅ API 字幕获取成功！&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> text</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;⚠️ 字幕下载失败: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">download_audio_by_yt_dlp_api</span>(<span class="params">bvid: <span class="built_in">str</span>, output_dir: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    url = <span class="string">f&quot;https://www.bilibili.com/video/<span class="subst">&#123;bvid&#125;</span>&quot;</span></span><br><span class="line">    output_template = os.path.join(output_dir, <span class="string">f&quot;<span class="subst">&#123;bvid&#125;</span>.%(ext)s&quot;</span>)</span><br><span class="line"></span><br><span class="line">    ydl_opts = &#123;</span><br><span class="line">        <span class="string">&#x27;format&#x27;</span>: <span class="string">&#x27;bestaudio[ext=m4a]/bestaudio&#x27;</span>,  <span class="comment"># 优先 m4a</span></span><br><span class="line">        <span class="string">&#x27;postprocessors&#x27;</span>: [&#123;</span><br><span class="line">            <span class="string">&#x27;key&#x27;</span>: <span class="string">&#x27;FFmpegExtractAudio&#x27;</span>,</span><br><span class="line">            <span class="string">&#x27;preferredcodec&#x27;</span>: <span class="string">&#x27;m4a&#x27;</span>,</span><br><span class="line">            <span class="string">&#x27;preferredquality&#x27;</span>: <span class="string">&#x27;192&#x27;</span>,</span><br><span class="line">        &#125;],</span><br><span class="line">        <span class="string">&#x27;outtmpl&#x27;</span>: output_template,</span><br><span class="line">        <span class="string">&#x27;quiet&#x27;</span>: <span class="literal">False</span>,          <span class="comment"># 显示进度（可设为 True 静默）</span></span><br><span class="line">        <span class="string">&#x27;no_warnings&#x27;</span>: <span class="literal">True</span>,</span><br><span class="line">        <span class="string">&#x27;noprogress&#x27;</span>: <span class="literal">False</span>,     <span class="comment"># 显示进度条</span></span><br><span class="line">        <span class="string">&#x27;socket_timeout&#x27;</span>: <span class="number">20</span>,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;📥 正在下载音频: <span class="subst">&#123;url&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="keyword">with</span> yt_dlp.YoutubeDL(ydl_opts) <span class="keyword">as</span> ydl:</span><br><span class="line">        ydl.download([url])</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找生成的 .m4a 文件</span></span><br><span class="line">    <span class="keyword">for</span> filename <span class="keyword">in</span> os.listdir(output_dir):</span><br><span class="line">        <span class="keyword">if</span> filename.startswith(bvid) <span class="keyword">and</span> filename.endswith(<span class="string">&#x27;.m4a&#x27;</span>):</span><br><span class="line">            <span class="keyword">return</span> os.path.join(output_dir, filename)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">raise</span> FileNotFoundError(<span class="string">&quot;未找到下载的 .m4a 文件&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">transcribe_audio_with_whisper</span>(<span class="params">audio_path: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;使用 Whisper 转写音频&quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;🎙️ 使用 Whisper 转写音频（首次加载模型较慢）...&quot;</span>)</span><br><span class="line">    <span class="keyword">import</span> whisper</span><br><span class="line">    model = whisper.load_model(WHISPER_MODEL)</span><br><span class="line">    result = model.transcribe(audio_path, fp16=<span class="literal">False</span>, language=<span class="string">&quot;zh&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> result[<span class="string">&quot;text&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_video_text</span>(<span class="params">bvid: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;主逻辑：先 API，再音频转写&quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># Step 1: 尝试 API</span></span><br><span class="line">    text = try_get_subtitle_via_api(bvid)</span><br><span class="line">    <span class="keyword">if</span> text:</span><br><span class="line">        <span class="keyword">return</span> text</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Step 2: fallback 到音频转写</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;⚠️ API 未获取到有效字幕，正在下载音频转写...&quot;</span>)</span><br><span class="line">    <span class="keyword">with</span> tempfile.TemporaryDirectory() <span class="keyword">as</span> tmpdir:</span><br><span class="line">        audio_path = download_audio_by_yt_dlp_api(bvid, tmpdir)</span><br><span class="line">        text = transcribe_audio_with_whisper(audio_path)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(text) &lt; <span class="number">50</span>:</span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">&quot;转写结果过短，可能失败&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;✅ 音频转写成功！&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> text</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">summarize_text</span>(<span class="params">text: <span class="built_in">str</span>, model_name: <span class="built_in">str</span> = <span class="string">&quot;gpt-3.5-turbo&quot;</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;用 LangChain 总结文本&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">if</span> USE_LOCAL_LLM:</span><br><span class="line">        <span class="comment"># 示例：使用 Ollama（需提前运行 ollama run qwen:7b）</span></span><br><span class="line">        <span class="keyword">from</span> langchain_community.chat_models <span class="keyword">import</span> ChatOllama</span><br><span class="line">        llm = ChatOllama(model=<span class="string">&quot;deepseek-r1:8b&quot;</span>, temperature=<span class="number">0.3</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># 默认使用 OpenAI（需设置 OPENAI_API_KEY）</span></span><br><span class="line">        llm = ChatOpenAI(model=LLM_MODEL, temperature=<span class="number">0.3</span>)</span><br><span class="line">    prompt = ChatPromptTemplate.from_template(</span><br><span class="line">        <span class="string">&quot;请修正以下语音转写文本中的错别字，保持原意不变，只输出修正后的文本,并且用中文总结以下视频内容（500字以内）：\n&#123;text&#125;&quot;</span></span><br><span class="line">    )</span><br><span class="line">    chain = prompt | llm | StrOutputParser()</span><br><span class="line">    <span class="keyword">return</span> chain.invoke(&#123;<span class="string">&quot;text&quot;</span>: text&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 主函数</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">analyze_bilibili_video</span>(<span class="params">url: <span class="built_in">str</span></span>) -&gt; <span class="built_in">tuple</span>[<span class="built_in">str</span>, <span class="built_in">str</span>]:</span><br><span class="line">    bvid = extract_bvid_from_url(url)</span><br><span class="line">    text = get_video_text(bvid)</span><br><span class="line">    summary = summarize_text(text)</span><br><span class="line">    <span class="keyword">return</span> text, summary</span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 使用示例</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># 替换为你想分析的 B站 视频</span></span><br><span class="line">    URL = <span class="string">&quot;https://www.bilibili.com/video/BV16fcczRETs&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        text, summary = analyze_bilibili_video(URL)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;\n&quot;</span> + <span class="string">&quot;=&quot;</span>*<span class="number">60</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;🎯 视频总结：&quot;</span>)</span><br><span class="line">        <span class="built_in">print</span>(summary)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;=&quot;</span>*<span class="number">60</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;\nℹ️  原文长度: <span class="subst">&#123;<span class="built_in">len</span>(text)&#125;</span> 字&quot;</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;📄 预览: <span class="subst">&#123;text&#125;</span>...&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;❌ 处理失败: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="跑完效果如下："><a href="#跑完效果如下：" class="headerlink" title="跑完效果如下："></a>跑完效果如下：</h3><p>🔍 尝试通过 B站 API 获取字幕…<br>⚠️ API 未获取到有效字幕，正在下载音频转写…<br>📥 正在下载音频: <a href="https://www.bilibili.com/video/BV16fcczRETs">https://www.bilibili.com/video/BV16fcczRETs</a><br>[BiliBili] Extracting URL: <a href="https://www.bilibili.com/video/BV16fcczRETs">https://www.bilibili.com/video/BV16fcczRETs</a><br>[BiliBili] 16fcczRETs: Downloading webpage<br>[BiliBili] BV16fcczRETs: Extracting videos in anthology<br>[BiliBili] BV16fcczRETs: Downloading wbi sign<br>[BiliBili] BV16fcczRETs: Downloading video formats for cid 35915826698<br>[BiliBili] Format(s) 1080P 高码率 are missing; you have to become a premium member to download them. Use –cookies-from-browser or –cookies for the authentication. See  <a href="https://github.com/yt-dlp/yt-dlp/wiki/FAQ#how-do-i-pass-cookies-to-yt-dlp">https://github.com/yt-dlp/yt-dlp/wiki/FAQ#how-do-i-pass-cookies-to-yt-dlp</a>  for how to manually pass cookies<br>[BiliBili] 116035336281401: Extracting chapters<br>[info] BV16fcczRETs: Downloading 1 format(s): 30280<br>[download] Destination: C:\Users\EiletXie\AppData\Local\Temp\tmp7c2lvfm5\BV16fcczRETs.m4a<br>[download] 100% of    1.99MiB in 00:00:00 at 5.45MiB&#x2F;s<br>[ExtractAudio] Not converting audio C:\Users\EiletXie\AppData\Local\Temp\tmp7c2lvfm5\BV16fcczRETs.m4a; file is already in target format m4a<br>🎙️ 使用 Whisper 转写音频（首次加载模型较慢）…<br>✅ 音频转写成功！</p>
<p>=&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>🎯 视频总结：</p>
<p><strong>修正后的文本：</strong></p>
<p>你能把你想象器地带到所有的物体、所有的人物、所有的空间、所有的场景中，而不仅仅局限于现在是不是复杂？是不是多元素的问题？多元素、复杂、这些关系、这些联系，它的背后是视觉逻辑。那视觉逻辑一定要服务于我们自己的能量场域，让他们进入我自己的场域，然后给我服务，而不一定我要走出我的这个场域去找他们，还去故意去刻意地去找他们。我的画面可以复杂，我的元素可以越来越多，都没有关系。它实施中是在我自己的场域本身之内去酝酿，而不是我去出去住，我出去啦，我自己家不管了，我去别人家去，这个多吗？也不少，但是它达成了相对统一。因为你就记住，多元素它一定是统一的。这个统一从心里上来讲，就是在你那里去统一。然后从视觉上、物理层面来讲，它总有一种的相似性，比如说这个状态的形态相似性，色调的相似性，色彩的相似，对，它能成一片的感觉。人物，然后我觉得你对状态本身的理解也特别好，有点自己的理解。对状态的理解，你看这个人物，它就是这个状态，板缓的扭头，对吧？稍微的扬起，风刮过来看相远处，好像有心事，画面有静谧，然后同时人物后面又是这种燃烧疯乱的这些树，它就有一个口在那里。人就在那里，你的这种能量是什么呀？它是很硬朗的能量，它不需要那么直接。为什么呀？能达到这种是因为你内心的丰富度到底还是感觉，就是我能感觉到那一片叶子，那一片叶子，一块石头它的情绪，对吧？我可能跟它建立某种微妙的联系，那一片云，那一颗树，对吧，那一个人物的微小的微妙的一种状态，我是能感觉到的。顺便还是感觉，只有我们的感觉更敏锐、更丰富、更细腻的时候，照片才能呈现那个细节，那个细节才会足够多度，这才是最关键的能量所在。</p>
<p><strong>中文总结（500字以内）：</strong></p>
<p>这段视频探讨了视觉创作中多元素与统一性的关系，强调视觉逻辑应服务于创作者自身的能量场域，引导所有元素（物体、人物、空间、场景）统一于“我”的内部世界，而非四处搜寻素材。核心观点是，多元素的复杂性并非问题，关键在于其内在统一性。这种统一源于创作者内心的丰富感知和细腻体验，表现为形态、色调、色彩等方面的相似性，形成和谐统一的画面。创作者需深入观察，捕捉万物微妙的情绪和状态，提升感知敏锐度，才能在作品中呈现丰富的细节，实现能量的统一与表达。视频鼓励创作者构建自己的视觉场域，让元素自然融合，而非强行搜集，最终达到视觉与内心的统一。</p>
<p>ℹ️  原文长度: 615 字</p>
<h2 id="Youtube视频下载"><a href="#Youtube视频下载" class="headerlink" title="Youtube视频下载"></a>Youtube视频下载</h2><p>&#x20; youtube视频下载可以完全复用上面这段代码，只是要注意url的更换，以及提取bv号时注意将bvid改成v即可。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url = <span class="string">f&quot;https://www.youtube.com/watch?v=<span class="subst">&#123;bvid&#125;</span>&quot;</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">extract_bvid_from_url</span>(<span class="params">url: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;从 URL 提取 BV 号&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;youtube.com&quot;</span> <span class="keyword">not</span> <span class="keyword">in</span> url:</span><br><span class="line">        <span class="keyword">return</span> url</span><br><span class="line">    parsed = urlparse(url)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;v=&quot;</span> <span class="keyword">in</span> parsed.query:</span><br><span class="line">        <span class="keyword">return</span> parse_qs(parsed.query)[<span class="string">&quot;v&quot;</span>][<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">match</span> = re.search(<span class="string">r&quot;BV\w+&quot;</span>, parsed.path)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">match</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">match</span>.group(<span class="number">0</span>)</span><br><span class="line">    <span class="keyword">raise</span> ValueError(<span class="string">&quot;无法提取 BV 号&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="音频输出标准格式化语义的文本"><a href="#音频输出标准格式化语义的文本" class="headerlink" title="音频输出标准格式化语义的文本"></a>音频输出标准格式化语义的文本</h2><p>如果你有直接音频，可以直接进行翻译，注意prompt的提示词即可</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> langchain_community.chat_models <span class="keyword">import</span> ChatOllama</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> dotenv</span><br><span class="line"><span class="keyword">from</span> pathlib <span class="keyword">import</span> Path</span><br><span class="line"><span class="keyword">from</span> langchain_core.prompts <span class="keyword">import</span> ChatPromptTemplate</span><br><span class="line"><span class="keyword">from</span> langchain_core.output_parsers <span class="keyword">import</span> StrOutputParser</span><br><span class="line"><span class="keyword">from</span> langchain_openai <span class="keyword">import</span> ChatOpenAI  <span class="comment"># 也支持 Ollama、Claude 等</span></span><br><span class="line"></span><br><span class="line">dotenv.load_dotenv()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用在线模型</span></span><br><span class="line"><span class="comment"># os.environ[&#x27;OPENAI_API_KEY&#x27;] = os.getenv(&quot;OPENAI_API_KEY1&quot;)</span></span><br><span class="line"><span class="comment"># os.environ[&#x27;OPENAI_BASE_URL&#x27;] = os.getenv(&quot;OPENAI_BASE_URL&quot;)</span></span><br><span class="line"><span class="comment"># chat_model = ChatOpenAI(</span></span><br><span class="line"><span class="comment">#     model=&quot;gpt-4o-mini&quot;</span></span><br><span class="line"><span class="comment"># )</span></span><br><span class="line"></span><br><span class="line"> <span class="comment"># 使用 Ollama 本地模型（默认连接 http://localhost:11434）</span></span><br><span class="line">llm = ChatOllama(</span><br><span class="line">    model=<span class="string">&quot;deepseek-r1:8b&quot;</span>,      <span class="comment"># ← 必须和你 pull 的模型名一致</span></span><br><span class="line">    temperature=<span class="number">0.3</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 配置区（按需修改）</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line">WHISPER_MODEL = <span class="string">&quot;base&quot;</span>        <span class="comment"># base, small, medium, large-v3（越大越准）</span></span><br><span class="line">LLM_MODEL = <span class="string">&quot;gpt-4o&quot;</span>            <span class="comment"># 或 &quot;qwen-plus&quot;, &quot;claude-3-5-sonnet&quot;, &quot;gpt-3.5-turbo&quot;</span></span><br><span class="line">USE_LOCAL_LLM = <span class="literal">False</span>           <span class="comment"># 设为 True 可改用 Ollama（见下方注释）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 步骤 1：语音转文字（Whisper）</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">transcribe_audio</span>(<span class="params">audio_path: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;使用 Whisper 将音频转为原始文本&quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;🎙️ 正在使用 Whisper 转写音频...&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 懒加载 whisper（避免未安装时报错）</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">import</span> whisper</span><br><span class="line">    <span class="keyword">except</span> ImportError:</span><br><span class="line">        <span class="keyword">raise</span> ImportError(<span class="string">&quot;请先安装 whisper: pip install openai-whisper&quot;</span>)</span><br><span class="line"></span><br><span class="line">    model = whisper.load_model(WHISPER_MODEL)</span><br><span class="line">    result = model.transcribe(</span><br><span class="line">        audio_path,</span><br><span class="line">        language=<span class="string">&quot;zh&quot;</span>,      <span class="comment"># 锁定中文，大幅提升准确率</span></span><br><span class="line">        fp16=<span class="literal">False</span>,         <span class="comment"># CPU 用户设为 False</span></span><br><span class="line">        verbose=<span class="literal">False</span>       <span class="comment"># 关闭进度条</span></span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">return</span> result[<span class="string">&quot;text&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 用大模型一次性：纠错 + 加标点 + 分段</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">polish_transcript</span>(<span class="params">raw_text: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;用大模型对原始转写文本进行专业编辑&quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;✨ 正在用大模型优化文本（纠错 + 分段 + 加标点）...&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> USE_LOCAL_LLM:</span><br><span class="line">        <span class="comment"># 示例：使用 Ollama（需提前运行 ollama run qwen:7b）</span></span><br><span class="line">        <span class="keyword">from</span> langchain_community.chat_models <span class="keyword">import</span> ChatOllama</span><br><span class="line">        llm = ChatOllama(model=<span class="string">&quot;qwen:7b&quot;</span>, temperature=<span class="number">0.3</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># 默认使用 OpenAI（需设置 OPENAI_API_KEY）</span></span><br><span class="line">        llm = ChatOpenAI(model=LLM_MODEL, temperature=<span class="number">0.3</span>)</span><br><span class="line"></span><br><span class="line">    prompt = ChatPromptTemplate.from_template(</span><br><span class="line">        <span class="string">&quot;&quot;&quot;你是一位专业的视频字幕编辑，请对以下语音转写文本进行以下处理：</span></span><br><span class="line"><span class="string">        1. 修正同音错别字（如“视平”→“视频”，“魔性”→“模型”）</span></span><br><span class="line"><span class="string">        2. 添加合适的中文标点符号（，。？！等）</span></span><br><span class="line"><span class="string">        3. 按语义逻辑分成自然段落，每段聚焦一个子主题</span></span><br><span class="line"><span class="string">        4. 段落之间用两个换行符（\\n\\n）分隔</span></span><br><span class="line"><span class="string">        5. 保持原文所有信息，不要删减或添加内容</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        待处理文本：</span></span><br><span class="line"><span class="string">        &#123;text&#125;&quot;&quot;&quot;</span></span><br><span class="line">            )</span><br><span class="line"></span><br><span class="line">    chain = prompt | llm | StrOutputParser()</span><br><span class="line">    <span class="keyword">return</span> chain.invoke(&#123;<span class="string">&quot;text&quot;</span>: raw_text&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 主函数：一键解析音频</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">parse_audio_to_text</span>(<span class="params">audio_path: <span class="built_in">str</span>, output_path: <span class="built_in">str</span> = <span class="literal">None</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    解析音频文件，返回优化后的文本</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">        audio_path (str): 音频文件路径（支持 .mp3, .m4a, .wav 等）</span></span><br><span class="line"><span class="string">        output_path (str, optional): 保存结果的 txt 路径</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">        str: 优化后的分段文本</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    audio_path = Path(audio_path)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> audio_path.exists():</span><br><span class="line">        <span class="keyword">raise</span> FileNotFoundError(<span class="string">f&quot;音频文件不存在: <span class="subst">&#123;audio_path&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Step 1: Whisper 转写</span></span><br><span class="line">    raw_text = transcribe_audio(<span class="built_in">str</span>(audio_path))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;📝 原始转写完成（<span class="subst">&#123;<span class="built_in">len</span>(raw_text)&#125;</span> 字）&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Step 2: 大模型优化</span></span><br><span class="line">    polished_text = polish_transcript(raw_text)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Step 3: 保存（可选）</span></span><br><span class="line">    <span class="keyword">if</span> output_path:</span><br><span class="line">        Path(output_path).write_text(polished_text, encoding=<span class="string">&quot;utf-8&quot;</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;💾 结果已保存至: <span class="subst">&#123;output_path&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> polished_text</span><br><span class="line"></span><br><span class="line"><span class="comment"># 直接解析某个文本进行分段</span></span><br><span class="line"><span class="comment"># with open(&quot;睡前消息.txt&quot;, &quot;r&quot;, encoding=&quot;utf-8&quot;) as f:</span></span><br><span class="line"><span class="comment">#     text = f.read()</span></span><br><span class="line"><span class="comment"># polished_text = polish_transcript(text)</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="comment"># 使用示例</span></span><br><span class="line"><span class="comment"># ======================</span></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># 替换为你的音频文件路径</span></span><br><span class="line">    AUDIO_FILE = <span class="string">&quot;temp_audio.mp3&quot;</span></span><br><span class="line">    OUTPUT_FILE = <span class="string">&quot;transcript.txt&quot;</span></span><br><span class="line">p</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        text = parse_audio_to_text(AUDIO_FILE, OUTPUT_FILE)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;\n&quot;</span> + <span class="string">&quot;=&quot;</span>*<span class="number">60</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;🎯 解析结果预览：&quot;</span>)</span><br><span class="line">        <span class="built_in">print</span>(text[:<span class="number">500</span>] + <span class="string">&quot;...&quot;</span> <span class="keyword">if</span> <span class="built_in">len</span>(text) &gt; <span class="number">500</span> <span class="keyword">else</span> text)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;=&quot;</span>*<span class="number">60</span>)</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;❌ 处理失败: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>agent</tag>
      </tags>
  </entry>
  <entry>
    <title>openclaw实践</title>
    <url>/2026/03/01/openclaw%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="OpenClaw-实践"><a href="#OpenClaw-实践" class="headerlink" title="OpenClaw 实践"></a>OpenClaw 实践</h1><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>最近 OpenClaw 特别火，刷到几个短视频都在阐述这个类似 AGI 的工具，于是打算本地部署测试一下。</p>
<p>个人测试下来感觉适合有点说不出来的中庸，比较适合有点钱的普通人来远程处理电脑资料、进行一些基础总结等。</p>
<p>为什么这么说？你需要经常切换 session，不然加上上下文的传递，我 3、4 次对话就花了 20 万的 Tokens，特别费 token，并且还只是一些简短的任务要求。对模型要求高。如果是做产品，还是用 Cursor 好点。</p>
<h2 id="测试流程"><a href="#测试流程" class="headerlink" title="测试流程"></a>测试流程</h2><p>整理测试流程如下：</p>
<ol>
<li>本地部署 OpenClaw</li>
<li>对接第三方，这里使用飞书</li>
<li>进行本地电脑 OpenClaw 测试，和第三方对接测试。</li>
</ol>
<h2 id="1-首先是部署-OpenClaw"><a href="#1-首先是部署-OpenClaw" class="headerlink" title="1. 首先是部署 OpenClaw"></a>1. 首先是部署 OpenClaw</h2><p>我这边装的是中文社区版的，跟海外版几乎一致，只是做了一些配置的中文转换而已，方便安装使用。</p>
<blockquote>
<p>安装地址：<a href="https://clawd.org.cn/#install">https://clawd.org.cn/#install</a></p>
</blockquote>
<p>我本地是 Windows 10 系统，所以按照要求，在 PowerShell 中执行该命令即可。注意 Node.js 的版本要求是 22 以上。</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在 PowerShell 中运行</span></span><br><span class="line"><span class="built_in">iwr</span> <span class="literal">-useb</span> https://clawd.org.cn/install.ps1 | <span class="built_in">iex</span></span><br></pre></td></tr></table></figure>

<p>等几秒即可安装成功。</p>



<p>接下来就是配置模型、第三方这些。我建议是你第一次玩这个，模型暂时就用 Qwen 的免费模型即可，根据给的链接去注册一下千问的账号就 auth 成功了；其他对接第三方等配置的先跳过，直接一路 enter 启动就行。</p>
<h3 id="启动问题"><a href="#启动问题" class="headerlink" title="启动问题"></a>启动问题</h3><p>我本地默认安装 OpenClaw 启动失败了，报 1006，查看日志也没具体描述，就是 gateway status 是 error。可能是 Windows 的 bug 或者防火墙的问题，我重新执行 openclaw gateway –port 18789 就可以正常启动。</p>
<h3 id="openclaw常用命令"><a href="#openclaw常用命令" class="headerlink" title="openclaw常用命令"></a>openclaw常用命令</h3><img src="/2026/03/01/openclaw%E5%AE%9E%E8%B7%B5/%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4.jpg" class="">

<h2 id="2-对接第三方（飞书）"><a href="#2-对接第三方（飞书）" class="headerlink" title="2. 对接第三方（飞书）"></a>2. 对接第三方（飞书）</h2><p>参考 OpenClaw 对接飞书文章： <a href="https://cloud.tencent.com/developer/article/2626160">https://cloud.tencent.com/developer/article/2626160</a></p>
<p>飞书对接的目的是通过配置一个飞书 App 的机器人，实现信息传递，本质只是远程调用小龙虾助理干活，将信息返回。</p>
<p>你将飞书的 App ID 和 Secret Key 配置给 OpenClaw（可以直接在小龙虾的聊天窗口把信息给他，他会自动配置）。</p>
<p>可以参考上述文章配置飞书机器人，权限可以全部开启作为测试。</p>
<h2 id="3-测试对接"><a href="#3-测试对接" class="headerlink" title="3. 测试对接"></a>3. 测试对接</h2><p>使用飞书发送消息，要求小龙虾给我搜最新的伊朗新闻，然后截图发送给我。期间我还提示它，由于没开 VPN，不要用 Google 来搜，要用必应。它也会理解处理。</p>
<img src="/2026/03/01/openclaw%E5%AE%9E%E8%B7%B5/%E9%A3%9E%E4%B9%A6%E6%88%AA%E5%9B%BE1.jpg" class="">]]></content>
      <tags>
        <tag>agent</tag>
      </tags>
  </entry>
</search>
